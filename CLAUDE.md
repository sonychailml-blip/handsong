# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Air Synth 3 is a gesture-controlled synthesizer and interactive scales/tunings tutorial that runs entirely in the browser. It uses the webcam + MediaPipe hand tracking to play notes/chords/bass/drums/effects with pinch gestures, synthesizing all sound with the Web Audio API. On top of live play it has a **beat-clock looper** (record intent, overdub layers, undo per layer) and an **arrangement** system (harmony/rhythm/bass dropped in as frozen layers). The UI text and code comments are in **Russian** — keep new user-facing strings and comments in Russian to match.

**The app is split into ES modules under `src/`, loaded via `<script type="module" src="src/main.js">`.** There is no build system, no package.json, no dependencies to install, and no tests — the modules load directly in the browser. `index.html` holds only markup; `style.css` holds all CSS; the only external dependency is `@mediapipe/tasks-vision`, loaded from a CDN via an ES module `import` in `vision.js`.

**One control model (PC mode is gone).** The screen is a single vertical layout: **one role fills the whole screen** (`phoneInstr`, cycled by the role button). In **landscape** an optional **split** (`splitOn`) puts **two roles side by side** — the screen halves, each half is one instrument. **Role is decided by ZONE** (which half a hand pinches in — either hand plays whichever half it lands in), and **work inside a half is by POSITION** (the chord palette is position-only), with **one hand-keyed exception**: effects stay bound to the left hand, and only inside the solo half. There is no more `uiMode`, no `pc`/`phone` toggle, no `drawPC` — the old three-column PC layout and everything unique to it were deleted; the split covers PC's one real capability (two roles at once) and does strictly more.

### Module map

- `index.html` — markup only: start screen, top bar, two settings panels (`#panelScale` overlay, `#panelLoop` working), help overlay, loop transport strip, `<video>`/`<canvas>`.
- `style.css` — all CSS.
- `src/config.js` — gesture thresholds, scheduler timings, voice-pool sizes, FX-bar geometry, the chord-palette geometry (`CH_PAL_W`, `CH_PAL_PAD`, `CH_PAL_GAP`, `CH_PAL_HEAD_H`, `PAL_HYST_X`, `PAL_HYST_Y`, `palColX`/`palRowY`), **`palSplitX(x0,x1)=x0+CH_PAL_W*(x1-x0)`** (the SINGLE palette|notes split formula, shared by gestures' hit-test and draw — used to be computed twice from `W`), the rect-grid band geometry `rectBandY(i,H,nRect)` (single-source pixel band, bottom band = index 0), and the **camera-frame margin `CAM_MARGIN=0.12` + `remapAxis(v)=(v-M)/(1-2M)`** (the play area = the inner ~76% of the frame; see "Camera frame margin"). Leaf: imports nothing. (`RECT_OCT` was retired — the octave is a playable rectangle, register in `state.js`. `FXW`/`ZB` — the old PC-column boundaries — were deleted with PC mode.)
- `src/state.js` — cross-cutting musical + layout state (`scaleIdx, tonic, aRef, seventh, leadIdx, chIdx, bassIdx, drumKitIdx, fx, revDisp, latchDeg, latchTy, chordFam, chordVar, phoneInstr, handFn, splitOn`) and its setters. `chordFam`/`chordVar` = palette column/row, selected by position. **`aRef`** = the adjustable A4 reference pitch (default 440, clamped 380–480 in UI, via `setARef`) — read by `scales.baseF`/`cFix` so it moves every scale together (see "Reference pitch"). **`handFn`** = the per-role/per-hand function table `{ld:{L,R}, bs:{L,R}}` (`'fx'`/`'note'`/`'hold'`/`'therm'`), written via `setHandFn(role,hand,fn)`; helpers `handSide(key)`, `handFnOf(key,role)` (null for `ch`/`dr` — they have no entry), `roleHasTherm(role)`, `roleHasFx(role)`. **This replaced the removed `swapHands` and global `theremin` toggle** (see "Hand functions"). **Split (`splitOn`) + `SPLIT_ROLES` (mutable `[left,right]` pair, written via `setSplitRole(i,role)`) + `phoneHalves(W)`** — the single source of half geometry+roles (`[{role,rx0,rx1}]`, `W/2` boundary lives ONLY here). Plus the rect-model registers `octReg`/`bassOctReg`/`chordOctReg` (per-role sticky octave) — all live bindings with setters. **The role→register resolver `rectOctReg(role)`/`setRectOctReg(role,v)` lives here** — keyed on an **explicit role argument** (`ch`→`chordOctReg`, `bs`→`bassOctReg`, else `octReg`), NOT the global `phoneInstr` (in split, `phoneInstr` no longer identifies the pinching hand's role); no `role==='…'` register ternary elsewhere. **Camera-mirroring single source:** `camFacing` (`'user'`/`'environment'`, via `setCamFacing`), `mirrored()=camFacing==='user'`, `flipX(nx)=mirrored()?1-nx:nx` (the ONE flip formula — no scattered `1-x`), and the **screen-coordinate helpers `sx(nx,W)=remapAxis(flipX(nx))*W` / `sy(ny,H)=remapAxis(ny)*H`** (compose flip + frame-margin remap; used by BOTH gestures hit-test and draw). Also `videoRec` (clip-recording flag, via `setVideoRec`, read by draw to hide chrome). `uiMode`/`setUiMode`, `swapHands`/`setSwapHands`, `theremin`/`setTheremin` are gone.
- `src/hooks.js` — the `hooks` object: nullable callbacks (`leadInstr, bassInstr, drumKit, rec, loop`) lower layers use to reach the DOM.
- `src/scales.js` — the teaching layer: `TRADITIONS` (**7**, incl. `nonoct` and `indian`), `SCALES` (**75 entries, indices 0..74** — the tail adds the three well temperaments), `scalesOfTrad`/`tradOfScale`, **the reference-pitch anchor** `a3()=aRef/2` feeding both **`baseF()`** (tonic pitch) and **`cFix()`** (the fixed-C anchor for `fixedKey` scales) — one source, so world and historical scales re-tune together (see "Reference pitch"); **`tonicFreq(s)`** (fixed keynote for `fixedKey`, else `baseF()`, used by the drone), **`periodOf(s)=s.period||2`** (interval of equivalence — octave by default, tritave for BP, fifth for Carlos), `leadFreq`/`bassFreq`/`chordFreqs`/`centsOf` (all generalized from a hardcoded `2` to `P=periodOf`, and all with a `fixedKey` branch — see "fixedKey"), `chordSteps`, chord/row labels, **`regWord`** (register noun by period: 2→`окт`, 3→`тритава`, else `рег.`), **`swaraLbl`** (Indian sargam label: `SWARA_OF`+`SHRUTI_OF` cents tables → «свара · имя-шрути» for the full 22-shruti grid, swara-only for ragas; gated by the `swaraNames`/`swaraFull` properties), and the typed-chord tables (`CHORD_FAM_SETS`, `chordFams`, `rootName`). `chordFreqs` has **four branches in a fixed gate order** (see "Chord pricing" below). Predicates that gate behaviour off scale **properties**: `supportsProgressions`, `supportsChords` (`noChords`), `typedChords`, `rectGrid`, **`fixedKey`**; plus the rect helpers `rectRows` (**note-count `+1` is period-gated**)/`rectRowsFull` and the theremin partition/pitch helpers `thereminSpan`/`thereminHz` (the shared y→note single source, log/cents interpolation; drives solo AND bass theremin — bass Hz = `thereminHz/4`). The source of truth for pitch.
- `src/audio.js` — the Web Audio engine (see "Audio engine" — **three synthesis paths**: subtractive, FM `buildFMBank`, Karplus–Strong `buildKSBank`). Lead banks are built in `buildLeadBanks(preBus, ksOk)`; the KS tail (`KS_BANKS`, indices 16..22) needs the worklet, and if `addModule` failed `buildKSFallback` fills those slots with subtractive plucks so `banks[]` never desyncs from `LEAD_INSTR`. Drum kits (`DRUM_KITS`: Стандарт/Дарбука/Табла/Гамелан/Тайко) are per-hit synths (gamelan = inline FM `gmlFM`), same 6 rows/indices. `metroClick(t,level)` takes a 4-value accent level (2 bar-head / 1 group-head / 0 beat / −1 khali). Also **`createRecordingTap()`** — a parallel `MediaStreamDestination` off `limiter` (records exactly what's heard, live output untouched; `dispose()` removes the edge), consumed by `clip.js`.
- `src/ks-worklet.js` — the Karplus–Strong `AudioWorkletProcessor` (`registerProcessor('ks-string')`), loaded by `initAudio` via `addModule` **inside** the start click. A worklet is **mandatory, not a choice** (see "Audio engine"): a native `DelayNode` feedback loop is floored at one render quantum (128 samples ≈ 344 Hz) so higher notes are unreachable, and sample-quantised delay detunes the rest — disqualifying for a microtonal instrument. The delay line is a `Float32Array` read with a **first-order allpass fractional delay** plus loop-filter phase compensation → tuning error under 1 cent across the whole range. `freq`/`pluck` are a-rate params (continuous glissando, sample-accurate pluck edge).
- `src/arrange.js` — arrangement data + pure generation (`PROGRESSIONS`, `HARMONIES`, `RHYTHMS`, `BASS_MODES`, `buildArrangement`). No side effects.
- `src/recorder.js` — record + looper (`events[]`, `loop`, the `W*`/`ENG` indirection, beat-clock scheduler, transport, `softAllOff`, `loadArrangement`). Freezes per-event `sc`/`sev`; chord type rides as `a.ty`. **Variable metre:** `loop.metre` (seeded by `config.BEATS_PER_BAR`, set via `setLoopMetre`, clamped to `METRES=[2,3,4,5,6,7,8,9,10,12,16]`, empty-loop only) replaced the old constant; **no denominator by design** (the engine counts beats at BPM, so 7/8 vs 7/4 = tempo only — honest for talas/Balkan). **Accent grouping:** `METRE_GROUPS` (7=3+2+2, 9=2+2+2+3, 16=4×4) + `METRE_KHALI` (16→vibhag 3) drive `beatLevel(metre,b)` → the 4-value metronome accent AND the `drawLooper` tick grid; khali is a **weakened** head, not silence. **Look-ahead scheduler** (see "Scheduler" + hard rule): `scheduleLayers` fires prepared layers at their EXACT times (`loop.t0+(rep*lb+ev.t)*spb`) within `SCHED_AHEAD`(≈300 ms); live `W*` calls pass no `when` → `AC.currentTime`. **Jam:** `loadJam=sel=>loadArrangement(sel,true)` stamps each event with **`ev.jam=true`**; `clearJam()` removes exactly the `jam`-flagged events (player recordings never carry the flag → they're safe by construction — the flag lives in the event, not a layer number that shifts).
- `src/vision.js` — camera + MediaPipe (CDN import, `video`/`canvas`/`ctx`, `roundRect` polyfill, `resize`, `landmarker`, `initVision`). **Camera switching:** `openCamera(facing)` (stops old tracks first, then `getUserMedia({facingMode})` onto the SAME `<video>`) and `switchCamera(facing)` — the landmarker is **never recreated**; on failure the caller (ui) rolls back and toasts.
- `src/clip.js` — **clip/audio recorder** (one engine, two kinds): `startClip('video'|'audio')`/`stopClip`, `activeKind()`, `onClipChange(fn)`. Video = `canvas.captureStream(30)` video track + the audio tap combined into ONE `MediaStream` → one WebM with picture+sound (HTML chrome is a separate DOM layer, excluded from the canvas capture); audio = the tap only. `recKind` is the single source of truth; `fireChange` notifies ui after every real transition (start / real `onstop` / error / external track-end) so the buttons can't stick. Sets `videoRec` only for video, only once recording truly starts.
- `src/demo.js` — the start-screen "путешествие по строям" demo (six tunings, no camera; own mini-voices, self-contained). Side-effect module; wires the (now removed-from-UI) demo button defensively (`if(db)`).
- `src/gestures.js` — the gesture state machine: `HANDS`, `leadOwner`/`chOwner`/`bassOwner`, `processHands`, `handRole`, `degHyst`/`degRaw`, and the palette hit-test (`axHyst` + `cellHyst`) used by the `chFam` zone. **`handRole(key)` is now fixed** (`Left→'fx'`, `Right→'notes'`, no `swapHands`) and used ONLY for the chords/drums palette hand — the **chord palette hand is fixed to the left** (accepted; swapping is done via hand functions for solo/bass, see "Hand functions"). **At pinch capture the hand's role + X-range are FROZEN on `S` (`S.role`/`S.rx0`/`S.rx1`, beside `S.zone`), plus `S.fn`** (the hand's note-function for its role, from `handFnOf` — solo/bass only): with `splitOn` the half (and thus the role driving `handFnOf`) is chosen from the pinch `px` via `phoneHalves`; else it's the single role over `[0,W]`. So play and draw use exactly the range the capture used. The play block branches on `S.fn`: **`'therm'`** (continuous Hz via `thereminHz`, live-only override — **solo AND bass**, bass Hz `/4`), **`'hold'`** (freezes `S.deg`/`S.oct` from the first frame; volume keeps following X; the finger→octave change is gated off), else continuous notes. **Mono hand-over:** `leadOwner`/`bassOwner` = last pinch; `endPinch` hands the voice BACK to a still-pinched hand (`otherPinched`) instead of cutting it, so a still-held hand resumes with no stuck note. Also the **rect model** (`rectPlay` band→note mapping via `rectRowsFull`/`degHyst`) and the **`'oct'` zone** (position-captured octave rectangle → `setRectOctReg(role,…)` with the half's role; SPLIT-gated in chords). `zoneAt`/`zoneX` (PC column hit-test) are gone.
- `src/draw.js` — canvas rendering: `drawVideoBackground` (**crops the video to the inner play area** with `CAM_MARGIN` per axis, mirrored via `mirrored()` for the front camera only — matches the landmark remap so picture and hand stay aligned), `drawOverlays` (**just calls `drawPhone`**), **`drawRole(instr,rx0,rx1,playH)`** (renders ONE role into its X-range — called once single-role, twice under split), **`drawPhone`** (single-role, or the split branch: two halves via `phoneHalves` + a mid divider + FX bars under the solo half; hides the on-canvas looper box while `videoRec`), **`drawHandsPhone`** (resolves role/range **per hand** — frozen `S` when pinched, else the live half under `lm[8]`; all landmark→screen via `sx`/`sy`), grid/tag/looper helpers, `drawGrid`, `drawRectGrid` (rect note rectangles; takes `rx0` separately from the grid's `x0` so the FX legend follows the role), `drawRectOctBand`, **`drawThereminLines`** (theremin is now an **additive centre-line overlay** drawn on TOP of the normal grid — not a replacement — gated on `roleHasTherm(instr)`; the field/oct-band/borders come from the grid), **`drawChordReadout`**/`latchChordFreqs` (the live chord Hz readout — see "Chord readout"), `drawChordPalette`, `drawDrumGrid(rx0,rx1,playH)` (now range-aware — was hardcoded full-width), the FX bars (drawn only when `roleHasFx('ld')` — hidden when no hand is assigned effects), and **`drawStatus`** — for a `cents` scale it prints **«центовый строй · N ступеней»**, NOT a fabricated «N-TET · шаг X» (the equal step was a lie for gamelan/Partch/temperaments/ragas). **`gridNoteLbl`** — the non-rect row label: `swaraLbl` for Indian (`swaraNames`) scales, **`rowLabel` (real note names) for `fixedKey` scales** (the grid is absolute, so the names are true again), ordinal `Т,2,3…` for other **non-octave (`period≠2`) OR cents scales**, `rowLabel` for plain octave scales; **`rectNoteLbl`** — the rect legend ordinal. The solo/bass tag prints cents on **every** scale now (the old `edo!==12` gate hid them exactly on the historical `edo:12` temperaments). `drawPC`/`drawBar`/`drawThereminGrid` are gone.
- `src/ui.js` — menu/buttons: `$`, `buildUI`, `fillScales` (groups the scale dropdown **by `grp` key**), every handler; the **single role button `instrBtn`** (shown iff `!splitOn`) and **two per-half role buttons `instrBtnL`/`instrBtnR`** (shown iff `splitOn`; `cycleHalf(i)` advances that half through `INSTR_SEQ` **skipping the other half's role**); the `◨` split toggle (`splitBtn`, `applySplit`), the `🔄` camera toggle (`toggleCamera`), the **`⛶` fullscreen toggle** (`fsBtn` in the bar + `fsBtnStart` on the start card — one state, synced via `fullscreenchange`, both hidden where the Fullscreen API is unavailable; `requestFullscreen` only from the click), the `🎥`/`🎙` clip+audio buttons (`applyRec`, driven by `onClipChange` — mutually exclusive), and the `🎵` **jam** button (`jamVariants`/`applyJam`/`jamTo`, property-keyed variant cycling; see "Jam"). **The `〰` theremin toggle and `⇄` swap control are GONE** — replaced by the **«Функции рук»** panel section (`renderHandFn`: a select per hand per note-role in play — single-role shows the one role, split shows each `ld`/`bs` half; bass omits `fx`; see "Hand functions"). Also the scale panel's **reference-pitch control** (`aRefSel` presets + clamped `aRefInput`, 380–480) and the **`loopMetre`** dropdown (`refreshMetreCtl` — disabled on a non-empty/playing loop). **`canSplit()=innerWidth>innerHeight`** (split is landscape-only) and ui's own `onResize` listener (auto-off split on rotation to portrait). **Auto-hiding top bar:** `#bar` collapses to `.min` after `BAR_HIDE_MS`(3500 ms); `revealBar()` shows it, `armBarHide()`/`panelOpen()`/`pointerDown` guard against hiding under the user's hand; a mouse-move threshold `MOUSE_EPS`(8 px) prevents flicker. Collapsed, only the role indicator(s) + the three record buttons stay — **the same DOM elements**, not a mirror. Also the **start card** wiring (`learnBtn` "скоро" note, footer `buildStartLinks` with the JS-assembled e-mail). Button visibility is expressed purely via `splitOn`/`canSplit()`/`SPLIT_ROLES`/`phoneInstr` — no CSS mode class. Side-effect module; **exports `$` and `revealBar`**. `modeBtn`/`applyMode`, `applyTheremin`/`thereminBtn`, `applySwap`/`swapBtn` are gone.
- `src/main.js` — composition root: `loop()`, `lastTs`/`latest`, the `#startBtn` handler (calls `revealBar()` after showing the bar); imports `./ui.js` and `./demo.js` for side effects.

> There is **no `backing.js`**, and typed chords no longer use per-row sectors. Stale names to ignore if seen: `backing.js`, `playRec`, `toggleRec`, `stopRec`, `recStart`, `drawChordSectors`, `sectHyst`, `S.sect`, `TYPED_CH_VOL`; **and (PC-removal) `uiMode`/`setUiMode`, `drawPC`, `drawBar`, `zoneAt`, `zoneX`, `FXW`/`ZB`, `modeBtn`, `applyMode`, the `.phone` body class**; **and `#modeHint`, `#arrHint`, the panel help/textbook button (`#helpBtn`)**; **and (hand-functions/reference-pitch/metre rounds) `swapHands`/`setSwapHands`, the global `theremin`/`setTheremin` toggle, `thereminBtn`/`applyTheremin`, `swapBtn`/`applySwap`, `drawThereminGrid`, and `BEATS_PER_BAR` as a hard constant (now just the seed for the live `loop.metre`)** — all removed/replaced.

## Running / developing

- Serve over **HTTPS or `localhost`** — camera + wakeLock need a secure context; `file://` fails. Team default: VS Code Live Server on `http://127.0.0.1:5500`.
- Phone testing: `npx cloudflared tunnel --url http://localhost:5500` (fresh short-lived HTTPS URL; bust cache with `?v=N`).
- MediaPipe model/WASM/library are fetched from CDNs at runtime — internet required even when serving locally. Needs a webcam + WebGL. Audio starts only on the "▶ Запустить" click.

## Architecture

The render loop lives in `main.js`. Each frame: draw the video → MediaPipe detects hands → `processHands()` updates the gesture state machine and drives audio → `drawOverlays()` renders. The recorder thins the ~60/sec engine calls into a few intent events per note.

### Screen model — one layout, optional split
- **Single role (default):** one role fills the screen (`phoneInstr` = `'ld'/'ch'/'bs'/'dr'`, cycled by the role button `instrBtn`). What each hand does in a **solo/bass** role comes from the **hand-function table `handFn`** (see "Hand functions"): the default `ld:{L:'fx',R:'note'}` reproduces the old "left=effects, right=notes" without a `swapHands` flag. `handRole()` (fixed Left→fx / Right→notes) survives only for the **chords/drums palette hand** (fixed to the left).
- **Split (`splitOn`, landscape only):** the screen halves — **left half = `SPLIT_ROLES[0]`, right half = `SPLIT_ROLES[1]`** (each cycled by its own button, `instrBtnL`/`instrBtnR`). **ZONE = INSTRUMENT:** either hand plays whichever half it pinched in. Inside a half everything is by **POSITION** — the chord palette is position-only (thumb+index left of the half's `palSplitX`). **Hand functions apply PER HALF, keyed by that half's role** (`handFnOf(key, h.role)`) — a `ld`/`bs` half behaves exactly like single-role (effects/hold/theremin all available); a `ch`/`dr` half ignores `handFn`. There is **no hardcoded "effects on the left hand in the solo half" special case anymore** — the default `handFn.ld.L='fx'` *derives* it (see hard rule). Each half has its **own** octave/register rectangle writing ITS role's register (`setRectOctReg(S.role,…)`).
- **Frozen at capture:** at pinch the hand's role + X-range are frozen onto `S` (`S.role`/`S.rx0`/`S.rx1`) from the half `px` lands in; play and draw then read that frozen range, so hit-test and render never drift. **Single-source geometry:** halves from `phoneHalves(W)`, the internal palette split from `palSplitX(rx0,rx1)`, bands from `rectBandY`/`rectRows` — each computed **once** and shared by gestures and draw.
- **Skip-in-cycle invariant:** `cycleHalf` never sets a half to the role the other half holds, so the pair is **always two different roles** → duplicate halves are impossible and the mono-voice chains (**solo and bass are single-voice**) can never appear twice. Two solo (or two bass) halves would fight over one voice; the invariant makes that unreachable by construction.
- **Landscape gate:** `canSplit()=innerWidth>innerHeight`. `splitBtn` shows only in landscape; rotating to portrait with split on **auto-disables** it (`softAllOff`, nothing left ringing) and does not auto-re-enable.

### Gesture model
- **Pinch** = thumb (4) near a fingertip (8/12/16/20), normalized by hand size. The pinched finger (`S.oct`) selects the **octave register** (index=I … pinky=IV), switchable mid-pinch to glide — **except in the rect model** (below), where it selects the note-in-rectangle and the octave comes from the octave rectangle.
- **Y** = scale degree (`degHyst`); **X** = volume, mapped over the hand's frozen range — `[palSplitX(rx0,rx1), rx1]` in a typed-chord role, else `[rx0, rx1]`; **Z** = reverb send, solo only.

### Camera frame margin — two coordinate roles (RAW vs REMAPPED)
`CAM_MARGIN=0.12`: we **display and play the inner ~76%** of the camera frame, while MediaPipe keeps receiving the **FULL** frame (`detectForVideo(video)` is never cropped — only the canvas render and the hit-test are). This keeps the hand tracked when it reaches the outermost rectangles: the hand travels off the visible edge while the camera still sees it. **Two coordinate roles are kept strictly separate (hard rule):**
- **RAW full-frame coords** (untouched `lm.x`/`lm.y`, 0..1) drive **all gesture math** — pinch distance (`pinchRatios`), hand size (`dist(lm[0],lm[9])`), the Z-reverb depth, the FX vertical drag, every threshold — so a pinch still registers when the hand is partly off-screen.
- **REMAPPED coords** — `sx(nx,W)`/`sy(ny,H)` = `remapAxis(flipX(nx))·W` / `remapAxis(ny)·H` (state.js, single source for gestures hit-test AND draw) — drive **only** on-screen position and hit-testing (which half/rectangle/zone, the volume X, the drawn hand). `remapAxis` is symmetric about 0.5 so it commutes with `flipX`.
- **Coordinates and rendering are never clamped** — a hand in the margin runs off the visible edge (that's the point). Only the resulting **selection** saturates (there is no rectangle beyond the outermost; a margin pinch's half is clamped to the nearest edge so it can't fall into the wrong half). The video background crop uses the same per-axis `CAM_MARGIN`, so picture and hand stay aligned; it does not introduce new stretch (the fill was already a stretch).

### Camera switching & mirroring
`camFacing` + `mirrored()` + `flipX` (state.js) are the **single source** of "do we mirror". Front camera (`'user'`) is mirrored (you see yourself as in a mirror); back camera (`'environment'`) is not. Both the video draw (`drawVideoBackground`) and every landmark→screen conversion read `flipX`/`sx`, so picture and hit-test flip together. `🔄` re-acquires the stream on the SAME `<video>` (landmarker not recreated); a failure rolls back to the previous camera and shows a Russian toast.

### Reverb aim under split
The Z-reverb preview (`revDisp`, shown before the pinch) follows the **note hand of the SOLO half** — `soloNoteHand()` is the single rule for both preview sites. Single-role: just `phoneInstr==='ld'`. Split: the hand's half is its frozen `S.role` when pinched, else the live half under `lm[8]` via `phoneHalves`. When no half is solo (`'ld'` not in `SPLIT_ROLES`), nobody aims and `revDisp` resets to 0 (the FX bars aren't drawn either, so nothing stale lingers).

### Rect model — "4 notes per rectangle" (shipped: SOLO, BASS, CHORDS)
For scales with a **`rectGrid`** property (today: 19-TET, 31-TET, Partch, **Carlos gamma**), the phone note grid is not N narrow rows but **fat rectangles**: `rectRows()` note rectangles + **1 octave/register rectangle** at the bottom = `rectRowsFull()=rectRows()+1` bands. Band pixel geometry is single-sourced in `rectBandY` (config) and the count in `rectRows`/`rectRowsFull` (scales); gestures' hit-test (`degHyst` on `rectRowsFull()`) and draw partition H the same way, so finger and screen never drift. **Y selects the rectangle** (band); the pinched **finger I–IV = 1 of 4 notes/roots** inside it (`deg = (band−1)*4 + finger`, clamped). **`rectRows()` is now period-gated: `(iv.length + (periodOf===2?1:0))/4`** — an octave scale appends a top-tonic (`+1`) to close the period in the grid, but a **non-octave scale has no equivalence interval**, so its "note a period up" arrives via the register rectangle, not an extra row. So `rectGrid` is legal iff **`(iv.length + (periodOf===2?1:0)) % 4 === 0`** (19→20, 31→32, Partch→44, **gamma 20→20**; but not BP 13, alpha 9, beta 11).
- **Octave/register rectangle** (band 0, bottom): pinching it — by **position**, so ANY hand captures it (`'oct'` zone, overrides role; in chords it's SPLIT-gated to the right half so the palette hand isn't captured) — sets a **per-role sticky register** via `setRectOctReg()`: solo→`octReg`, bass→`bassOctReg`, chords→`chordOctReg`. This solved the old "finger-busy-so-octave-is-lost" blocker; the register survives the hand leaving the band/frame. The `'oct'` hand is silent (no note, no owner). The register shifts by the scale's **period** (via `leadFreq`'s `P^oct`) — an octave for octave scales, a **perfect fifth** for gamma; the band label reads `ОКТАВА` for `period===2`, else `regWord().toUpperCase()`.
- **Chords**: rect applies to the ROOT rows only, in the right part `[palSplitX,rx1]` of the role's range; the left palette is unchanged; the type still rides the sticky palette; the latch logic (same-off/re-attack/glide) is unchanged — only the root degree + octave source changed.
- Non-`rectGrid` scales keep the narrow-row model byte-for-byte.

### Hand functions (replaced the theremin toggle + the swap-hands control)
Each hand is assigned a **function per role**, held in `state.handFn` (`{ld:{L,R}, bs:{L,R}}`, defaults `ld:{fx,note}`/`bs:{note,note}` — reproducing today). The four functions:
- **`'fx'` (Эффекты)** — effects hand; **solo only** (bass has no `fx` option in the UI). The fx bars are drawn only when `roleHasFx('ld')` — assign both solo hands to notes and the bars disappear.
- **`'note'` (Ноты непрерывно)** — normal play: degree follows the hand while pinched.
- **`'hold'` (Ноты с удержанием)** — the note captured on the first pinch frame is **frozen** (`S.deg`/`S.oct`, and the finger→octave change is gated off) while the hand roams; **volume keeps following X** (swell); it stops only on finger-release. It records as an ordinary long note — no new event format.
- **`'therm'` (Терменвокс)** — continuous pitch, **solo AND bass**. `thereminSpan`/`thereminHz` (scales) map y→a fractional note position over the SAME partition draw uses, then interpolate **linear-in-cents** (`log2` of the neighbours' `leadFreq`); bass Hz = `thereminHz/4`. Drawn as an **additive centre-line overlay** (`drawThereminLines`) on top of the normal grid, shown only when a hand of that role is `'therm'` (`roleHasTherm`). Live-only: the continuous Hz rides the **live-only 3rd arg of `ENG.leadOn`/`ENG.bassOn`** (never recorded); the looper stores the nearest step (`S.deg`, event format unchanged).

**UI:** the «Функции рук» section (`renderHandFn`) shows a select per hand for each note-role in play — labels group by role (🎸 Соло / 🎚 Бас) over `Левая рука`/`Правая рука` rows, so a split with both roles makes clear the assignment is per **hand within a role**. Solo/bass keep **separate** assignments (persisted across role switches). In split it's consulted **per half by the half's role**.

**Mono hand-over (last-pinch-wins + handback):** the lead/bass voice is mono; `leadOwner`/`bassOwner` = the most recent pinch, so a newer pinch takes the voice. On owner release, `endPinch` transfers ownership to a still-pinched hand in the same zone (`otherPinched`) instead of cutting the voice — the still-held hand resumes, no stuck note (works via the watchdog too).

### Typed chords — the palette
Scales with a `typedChords` property split the chord role: **left = a palette of chord types** (columns = families, rows = variants; counts from `chordFams()`/`types.length`, geometry in `palColX`/`palRowY`), **right = note rows**. The palette selects a cell by POSITION on a thumb+**index** pinch inside it (`S.oct===0 && S.x<palSplitX`); sticky, default first family/type. **Single-role:** the left (fx) hand is the palette hand; the right hand plays. **Split:** the palette is **position-only** — either hand picks a type by being left of the half's split with thumb+index; the other hand plays roots. Play: Y=root, X=volume, and the octave — **on non-rect typed scales (chrom12, BP, Pythagorean, both Naturals, meantone)** the finger picks it, **on the rect typed scales (19-TET/31-TET/Partch)** the finger picks the root-in-rectangle and the octave comes from the octave rectangle (`chordOctReg`). `ty` is a live reference into `CHORD_FAM_SETS`; latch identity = (degree + type). Two-axis hysteresis `cellHyst`.

`typedChords` keys → sets in `CHORD_FAM_SETS` (**8 keys**):
- `'chrom12'`, `'edo19'`, `'edo31'` — **integer steps** in that scale's own edo (priced by the integer `chordSteps` fallback).
- `'partch'` — 11-limit **ratios** (4 columns × 6: О otonal / У utonal / Диез 7-11 / Станд.); priced by the pure-ratio cents branch.
- `'bp'` — Bohlen–Pierce 3.5.7-subgroup **ratios** (15 chords: 3:5:7 major / 5:7:9 minor / characteristic); priced by the **period branch**.
- `'pyth'`, `'nat'` — the same 24-type vocabulary as chrom12 but as **pure ratios** (Pythagorean 3-limit / Natural 5-limit); priced by the pure-ratio cents branch (root × ratio, so every chord is pure from its own root).
- `'natfix'` — the same 24 types as **integer semitone offsets**, but used by the **fixed-grid** scales (Natural-fixed, meantone) whose `gridChords:true` reads each note out of the frozen cents grid → some chords pure, others wolf.

### Behaviour properties (prefer these over `tag`/index checks)
- `noChords` → chord role builds nothing (`supportsChords()` false). Maqams; the **gamelan** cents-scales and **pelog patets** (monophonic by choice); the **Carlos** scales (stage 1). Partch/Pythagorean/Natural are **not** `noChords` — they have typed chords.
- `typedChords: '<key>'` → palette mode; keys a set in `CHORD_FAM_SETS`. Keys: `'chrom12'`, `'edo19'`, `'edo31'`, `'partch'`, `'bp'`, `'pyth'`, `'nat'`, `'natfix'`.
- `cents: [...]` → non-equal tuning; pitch from `2^(cents[i]/1200)` instead of the equal step (see "Music theory & pitch").
- `period: <n>` → **interval of equivalence ≠ octave** (BP `3` = tritave, Carlos `3/2` = fifth). `periodOf()=s.period||2`. Absent ⇒ `2` ⇒ every octave scale byte-for-byte. Drives register/step (`P^oct`, `P^(step/edo)`), the `centsOf` period (`pc=1200*log2(P)`), `regWord`, `gridNoteLbl`, and the `rectRows` `+1` gate. **Never hardcode `2` on any pitch/label path** — use `periodOf`.
- `gridChords: true` → (cents scale only) typed chords read their notes **out of the fixed cents grid** by integer semitone offset (fixed-keyboard model), instead of pure ratios from the root. Selects the first `chordFreqs` branch. Today: Natural-fixed, meantone, and the three well temperaments. **Distinct from `fixedKey`** (chord CONSTRUCTION vs the tonic being a key) — memberships overlap but differ.
- `fixedKey: true` → the tonic is a KEY within a grid nailed to absolute C, not a multiplier (see "fixedKey"). The six historical fixed tunings. Drives the `fixedKey` branch of `leadFreq`/`bassFreq`/`chordFreqs`/`centsOf` and real-note-name row labels. **NOT** on mobile Natural or any world/EDO/BP/Carlos/Partch/raga/maqam scale.
- `rectGrid: true` → the "4 notes per rectangle" layout for solo/bass/chords. Legal iff **`(iv.length + (periodOf===2?1:0)) % 4 === 0`** (19→20, 31→32, Partch 44, gamma 20). Drives `rectRows`/`rectRowsFull`.
- `supportsProgressions()` = 7 degrees. Note: a 7-note `noChords` scale (maqams, pelog) is still gated out of progressions because `refreshProgAvail` uses `supportsProgressions() && supportsChords()`. (Partch/12-note tunings/BP/Carlos aren't 7-note, so progressions stay gated regardless.)

### Music theory & pitch
`SCALES` is the source of truth: each has `edo`, `iv` (degrees in those steps), `tag`, `trad` (tradition, for the menu), optional `grp` (submenu), optional behaviour properties. **75 scales (indices 0..74).** Traditions (`TRADITIONS`, 7): Ладовая (`modal`), Арабская (`arab`, 24-TET maqamat), Микротональная (`micro`, 19/31-TET), Хроматика (`chrom`), Мировые строи (`world`, non-equal cents tunings: gamelan Слендро/Пелог + pelog patets + Partch), **Неоктавные (`nonoct`, period≠2: Bohlen–Pirce + Carlos α/β/γ)**, and **Индийская (`indian`)**. The menu shelf `grp:'Исторические темперации'` inside `modal` collects the historical 12-note tunings (Pythagorean, both Naturals, meantone, and the three well temperaments Веркмайстер III / Валлотти / Кирнбергер III) — all `fixedKey` except mobile Natural.

**Indian tradition (`indian`):** a 22-shruti just-intonation grid (`cents:[0,90,112,182,204,…,1110]`, min step 22¢, the syntonic comma — same cents mechanism as gamelan/Partch, octave, no `period`) plus **10 ragas** that each pick 7 swaras out of that grid. All are `noChords` (Indian classical is melodic — a deliberate model choice, not a limitation). Labelling uses the `swaraNames`/`swaraFull` properties → `swaraLbl`: the full grid shows **«свара · имя-шрути»** (`SWARA_OF`+`SHRUTI_OF`, both keyed on real cents so they can't drift), ragas show swaras only (Са Ре Га Ма Па Дха Ни). The swara sits on the **LAST shruti of its group** (Sangita-Ratnakara: Са=Чхандовати, Па=Алапини), so the tonic reads «Са · Чхандовати», not «Са · Тивра»; the modern positional convention (Са=Тивра) is documented in the source as the rejected alternative. Honesty notes in the code: a raga is **more than a scale** (aroha/avaroha, vadi/samvadi, pakad, time-of-day — none modelled); Кафи and Мальхар share the same scale by design (they differ in movement, which we don't model).

**Tail index map:** 50 = Bohlen–Pierce · 51/52/53 = Carlos alpha/beta/gamma · 54/55/56 = Pelog patets Lima/Nem/Barang · 57 = Pythagorean · 58 = Natural (mobile/choir) · 59 = Natural (fixed/harpsichord) · 60 = quarter-comma meantone · 61 = 22-shruti grid · 62..71 = ragas (Бхайрав, Йаман, Кафи, Бхайрави, Тоди, Кхамадж, Асавари, Мальхар, Пурви, Мармари) · **72 = Веркмайстер III (1691) · 73 = Валлотти (1754) · 74 = Кирнбергер III (1779)** (the well temperaments — see "fixedKey"). **75 scales, 0..74.**

**Three ways to define pitch — equal steps, cents overlay, or non-octave period:**
- **Equal (default):** within-period ratio = `P^(iv[i]/edo)` where `P=periodOf(s)` (`2` unless `period` set). All EDO scales (12/19/24/31-TET) and the non-octave equal scales (BP `P=3`, Carlos `P=3/2`).
- **Cents overlay:** an optional `cents:[...]` array (`length === iv.length`) gives each degree's exact cents above the tonic — for **non-equal** tunings (gamelan, Partch, the historical temperaments). When present, `leadFreq`/`bassFreq`/`centsOf` use `2^(cents[i]/1200)` with the appended top = 1200¢. `edo`/`iv` stay as **nominal structure** (degree count, row layout, hysteresis). Cents scales are octave (`period` absent).
- **Non-octave period:** `period:<n>` replaces the hardcoded `2` in register (`P^oct`), equal step (`P^(step/edo)`), and the cents period (`pc=1200*log2(P)`). Register shifts by the period (BP tritave, Carlos fifth), not the octave. `period` and `cents` are mutually exclusive in practice (BP/Carlos are equal; the temperaments are cents+octave).

**Chord pricing — `chordFreqs` has four branches, checked in this exact gate order** (order decides which scale gets which — do not reorder):
1. **`s.cents && ty && s.gridChords`** → **fixed-grid**: `ty` is integer semitone offsets; each note = grid pitch `корень+off` read from `s.cents` (octave carry for off≥12). Natural-fixed, meantone. Produces the pure/wolf spread of a fixed keyboard.
2. **`s.cents && ty`** → **pure-ratio cents**: `ty` is ratios; note = `baseF()/2·2^(o)·2^(rootCents/1200)·ratio` — root from the cents overlay, each interval a pure ratio. Partch, Pythagorean, Natural-mobile. Every chord pure from its own root.
3. **`periodOf(s)!==2 && ty`** → **period-equal**: `ty` is ratios; root by `P^(iv/edo)` (period register `P^o`), note = root·ratio. Bohlen–Pierce.
4. **fallback** → `chordSteps()`: integer path. Typed `ty` adds its step array to the root; else 7-note diatonic/ethnic/maqam stack thirds, pentatonic/blues/chromatic power chords, pure-EDO (`tag:'edo'`) by interval ratios. chrom12/edo19/edo31 and every non-typed scale. **Byte-for-byte unchanged** — a scale reaches here only when the three gates above all fail.
- `CHORD_FAM_SETS` `iv` are **integer steps** for `chrom12`/`edo19`/`edo31`/`natfix`, and **plain-number ratios** for `partch`/`bp`/`pyth`/`nat`. Never give a step-set to a scale with a different `edo`, and never give a ratio-set to a non-cents/non-period scale.

### Reference pitch (`aRef`) — ONE source
`aRef` (A4 in Hz, default 440, adjustable 380–480 in the scale panel) lives in `state.js` beside the tonic. `scales.js` derives **both** `baseF()` (the mobile tonic pitch) **and** `cFix()` (the fixed-C anchor for `fixedKey` scales) from the single `a3()=aRef/2`. There must be **exactly one** reference — a second copy would move world scales to 415 while the historical fixed tunings stayed at 440, surfacing much later as a "detuned instrument". Changing `aRef` re-tunes everything live (solo/bass/chords via `baseF`, drone via `tonicFreq()`, theremin, and every loop layer on its next replay); the UI clamps and falls back to the last valid value on bad input.

### fixedKey — the tonic is a KEY, not a multiplier (historical fixed tunings)
Scales with the **`fixedKey`** property (the six: Пифагоров, Натуральный фиксированный, Мезотон, Веркмайстер, Валлотти, Кирнбергер) nail their grid to **absolute pitches anchored at C** (`cFix()`); the tonic then selects **which key** you play within that fixed grid, instead of transposing the whole tuning. That is why Веркмайстер's major third reads **≈390¢ in C and ≈408¢ in F#** — the whole lesson. `leadFreq`/`bassFreq`/`chordFreqs`/`centsOf` each have a `fixedKey` branch (via `fixedSlot`); row labels show **real note names** (`rowLabel`) because the grid is absolute, so the names are true again — all other cents scales keep ordinals. **`fixedKey` is a SEPARATE property from `gridChords`** — the memberships differ: «Натуральный (подвижный, хор)» deliberately stays **mobile** (the pair's contrast is the lesson), and Пифагоров is `fixedKey` but keeps **pure-ratio chords** (not `gridChords`). The three well temperaments (Веркмайстер III / Валлотти / Кирнбергер III) sit on the `Исторические темперации` shelf, `natfix`+`gridChords`+`fixedKey`, each with its own comma recipe; A/B them to hear near vs far keys.

### Scheduler — look-ahead is a hard invariant
Prepared loop layers (`isLayer`: chords/bass/drums) are scheduled at their **exact** times `loop.t0 + (rep*lb+ev.t)*spb` inside a `SCHED_AHEAD`(≈300 ms) window (`scheduleLayers` → `ENG[fn](a, ev, when)`), while **live** `W*` calls pass no `when` → `AC.currentTime`. Every voice function takes an optional trailing `when` defaulting to now — **do NOT "simplify" that away.** Why it exists: layers used to fire "now" at whatever instant the poll ran, and a blocked main thread (synchronous MediaPipe + three synthesis paths on one thread) bunched them; the metronome was already scheduled ahead, and that asymmetry was the audible drift. The window **masks** blocking, it does not remove it (root cause = vision on the main thread — see BACKLOG). Known bounded costs (accepted): a drum tail up to one window on stop (one-shot oscillators can't be cancelled) and a ≤window dropout for surviving layers after undo/`clearJam` (rewinding `loop.sched` would double-fire drums).

### Metre & accent grouping (variable time signatures)
`loop.metre` (default from `config.BEATS_PER_BAR`, set via `setLoopMetre`, clamped to `METRES=[2,3,4,5,6,7,8,9,10,12,16]`, changeable **only on an empty loop**) replaced the old `BEATS_PER_BAR` constant. **No denominator by design:** the engine counts beats at BPM, so 7/8 and 7/4 differ only by tempo — which fits talas (counted in matras) and Balkan pulses honestly; quantisation was already beat-relative. **Accent grouping:** `METRE_GROUPS` (`7=3+2+2`, `9=2+2+2+3`, `16=4×4`) + `METRE_KHALI` (`16→[2]`, the empty third vibhag) feed `beatLevel(metre,b)` → four metronome accent strengths (2 bar-head / 1 group-head / 0 beat / −1 khali) AND the `drawLooper` tick grid, so grouping is seen and heard; **khali is a weakened head, not silence** (a tala's structural feature). `arrange.js` patterns declare their own `beats` (`Вальс`/`Дадра`/`Балканский 7`/`Кехерва`/`Балканский 9`/`Тинтал`, …); a pattern whose `beats ≠ loop.metre` is skipped. Count-in is always ONE bar (`loop.metre` beats) at any metre.

### Chord readout — live Hz from the sounding voices
While a chord SOUNDS, its highlighted root rectangle lists each note's **Hz** with the interval **above the root** in cents (`drawChordReadout` in draw.js). The numbers are read from the **sounding voices** (`chordHold['latch']` → `latchChordFreqs`), **not recomputed** — so the display can never disagree with what's heard, and it works for all four `chordFreqs` branches for free. It shows only **live latched** chords; loop-played chords (owner `'loop:N'`) keep the plain highlight.

### Audio engine (`initAudio`) — five chains into one master
`master → limiter → destination`. Shared `ConvolverNode` reverb (only the solo send is Z-controlled). Chains: (1) solo — banks → drive → envelope → volume → tremolo → delay/reverb; (2) chord pool (`CHORD_POOL_N` always-on 2-osc voices, gain-gated); (3) bass pool (`BASS_POOL_N` mono, timbre baked on attack); (4) drums (synth per hit, kit from `a.kit`); (5) drone (detuned saw pair, LFO-swept LP, follows `tonicFreq()`). Plus the metronome.

**Three synthesis paths for the lead banks (not one) — a bank is just `{gain, setFreq, cancel, strike?}`, so all three ride the SAME solo chain and effects:**
1. **Subtractive** (most banks): oscillators via `mkOsc` (which wires `vibGain`+`humDetune` into `.detune`) → optional filter → gate. Velocity→brightness (`fstrike`/`strike`) fires at ATTACK, not per-frame (a per-frame cutoff would stomp the filter envelope; `setFreq` deliberately never touches the filter). Humanisation (`HUM_CENTS=4`, in `humDetune`) is fresh at each attack and **never recorded** — a replayed loop gets new variation. **Pads and organs are pitch-clean** (`hum:0`, `det:0`): the beating you hear is the TUNING, not the timbre. **Bass is not pitch-humanised** (low-frequency detune muddies).
2. **FM** (`buildFMBank({ratio,peak,sus,tau})`): carrier via `mkOsc` (so it flows through the shared chain), modulator is a bare osc → `modGain` → `carrier.frequency` only (not the output). The modulation-index envelope (`strike`) decays FASTER than amplitude (`rel` > `tau`) — that struck-metal read. **Non-integer `ratio` gives inharmonic spectra the subtractive path physically cannot produce.** Banks: «Колокол (глубокий)» (ratio 1.41), «Металлофон» (3.51).
3. **Karplus–Strong** (`buildKSBank`, wraps the `ks-string` worklet): `node → [body resonator] → gain → chain`; `setFreq` is the a-rate `freq` param (same `setTargetAtTime` as every bank → glissando/bend/theremin free), `strike` fires the `pluck` param edge. `KS_BANKS` params: `fb` (decay), `damp` (loop filter), `exBase`/`exSpan` (pluck brightness), `nl` (loop nonlinearity = **jawari** on Ситар / metal edge on Сантур), `symp` (bandpass resonators following `baseF()` = **tarab** on Ситар, retuned like the drone), `body`. KS has no `oscillator.detune` (`hum:0`). Banks: «Струна», «Ситар», «Уд», «Кото», «Сантур», «Гитара», «Пиццикато». If the worklet fails to load, `buildKSFallback` fills those slots so indices never desync.

Drum kits are per-hit synths (`drumHit(row,vol,kit,when)`): Стандарт, Дарбука (`dDum`/`dTek`), Табла (`tabBaya` with the signature downward pitch bend + `tabNa`), Гамелан (inline FM `gmlFM` → inharmonic bronze), Тайко (`taikoDrum`/`taikoKa`) — same 6 rows/indices across kits.

### Looper & polymodality
An event is `{t, layer, fn, a, sc, sev}`: `t` in beats (within a `loop.bars`×`loop.metre` loop — see "Metre & accent grouping"), pitch as degree+octave, frozen scale `sc`, `sev`, plus `a.ty`/`a.inst`/`a.kit`. Frequency is derived at play time via `leadFreq/chordFreqs/bassFreq(..., frozenSc)`, so the loop re-tunes to the live tonic while each layer keeps its frozen scale (**and its `cents` tuning**, for free). Replay via `ENG` isn't re-recorded. This is polymodality — don't "simplify" replay to the current scale. Layers are scheduled ahead (see "Scheduler"). **Jam layers additionally carry `ev.jam=true`** (see recorder.js) — an intrinsic per-event flag, so `clearJam` removes exactly the jam and never the player's own recordings; a layer-number scheme would break as numbers shift.

### Clip / audio recording
One engine (`clip.js`), two kinds. **Video** = `canvas.captureStream(30)` (the game canvas only — HTML chrome is a separate DOM layer, so buttons/panels are excluded) + a parallel audio tap (`createRecordingTap`, off `limiter`) combined into ONE `MediaStream` → one WebM file with picture AND sound. **Audio** = the tap only. The two are **mutually exclusive** (`activeKind()` gates the buttons + an `if(rec)return` guard). Button `.act`/`disabled` are driven by real recorder transitions (`onClipChange`), so they can't stick. Format is honest: WebM/Opus (Safari may give mp4) — no MP3/WAV/MP4 promise. While a video clip records, `videoRec` hides the on-canvas looper box from the frame.

### Jam
The `🎵` button lays a culture-appropriate backing in one tap; each tap switches to the NEXT variant (previous stops — variants never layer), and the cycle ends with OFF. It's a **shortcut, not a mode**: the variants are ordinary looper layers, so the ⚙ panel, undo, and record all work as usual. Variants are keyed on **scale properties** (`supportsProgressions`/`supportsChords`/`typedChords`, `edo===24` for maqam rhythm), selecting only from existing `HARMONIES`/`RHYTHMS`/`BASS_MODES` — jam only chooses. Its layers carry `ev.jam` so `clearJam` can't touch player recordings.

### UI shell (start card, auto-hiding bar, light panels)
- **Start screen** is a light card over a light backdrop with two buttons: «▶ Играть» (the unchanged start flow — AudioContext on click → camera → wakeLock) and «Обучение» (coming soon — shows a "скоро" note, not disabled). A small footer: first-version note, a feedback link, a donate slot hidden while its URL is empty, and an e-mail **assembled in JS** (not a plain `mailto:` in the HTML source). The long instructional text moved into `#helpOv`, which is **currently unreachable from the UI** until the tutorial ships.
- **Top bar auto-hides** (`.min`) after ~3.5 s and reveals on tap / mouse-move (8 px threshold); collapsed it keeps ONLY the role indicator(s) and the three record buttons — **the same DOM elements** (no mirror strip that could disagree). It never hides while a panel is open or a pointer is down. The `⛶` fullscreen button lives in the bar (and on the start card); it is not part of the collapsed strip.
- **Panels are light and opaque** (they sit over a bright camera image — translucency would wash out the text). The scale panel holds the scale/tonic controls, the **reference-pitch** control (A4 presets + clamped number), and the **«Функции рук»** section (replacing the old `⇄` swap); the looper panel adds the **metre** dropdown next to bars/BPM/quantise. Both moved to the top, full height, no scroll in the normal case.

## Conventions

- Terse, comment-heavy, single-letter helpers, compact lines. Match it.
- Add a feature in the module that owns it:
  - a **scale** = one entry in `SCALES`, **appended at the end** (index is `scaleIdx`; `state` and `sameDegrees` depend on it). Menu position is set by `trad`/`grp`, not array position — `fillScales` buckets by `grp` key.
  - a **cents (non-equal) scale** = same, plus a `cents:[...]` array (`length === iv.length`); keep a valid `edo`/`iv` as nominal structure. Home tradition: `world` (or `modal`/`Исторические темперации` for the historical 12-note tunings). It may be `noChords` (gamelan/pelog) OR carry `typedChords` with a **ratio** family set (Partch/Pythagorean/Natural — priced by the pure-ratio cents branch) OR `gridChords:true` + an integer-step set (Natural-fixed/meantone/well temperaments — priced by the fixed-grid branch). A **historical fixed tuning** additionally gets **`fixedKey:true`** (tonic = key, absolute-C anchor, real-note-name labels — see "fixedKey"); the C-anchored `cents:[...]` array is the tuning's recipe from C.
  - a **non-octave scale** = a `period:<n>` (`3` tritave, `3/2` fifth…), `edo`/`iv` equal within that period, tradition `nonoct`. `periodOf` carries it through pitch/labels/register — never add a `2` literal. `noChords`, or a **ratio** family set priced by the period branch (BP `'bp'`). `rectGrid` only if `(iv.length)%4===0` (period≠2 drops the `+1`).
  - a **typed-chord family set** = one entry in `CHORD_FAM_SETS` keyed by a scale's `typedChords` value; **integer steps** for an edo/`gridChords` scale, **or plain-number ratios** for a cents/period ratio scale.
  - a **timbre/kit** = an entry in `LEAD_INSTR`/`CHORD_INSTR`/`BASS_INSTR`/`DRUM_KITS`.
  - a **gesture/timing threshold** or palette geometry = `config.js`.
  - a **behaviour gate** = a scale property + a predicate in `scales.js`, never a `tag`/index check.

## Hard rules — do not break these

1. **AudioContext is created only inside the `#startBtn` click handler.**
2. **Never hardcode frequencies, note tables, or the octave `2`.** Pitch derives from `baseF()` and the current (or frozen) scale — `P^(iv/edo)` with `P=periodOf(s)`, or the `cents` overlay `2^(cents/1200)`. A literal `2` in a register/step/period path breaks the non-octave tunings (BP, Carlos) — use `periodOf`.
3. **Pool oscillators start once and never stop** (chord/bass gated by GainNodes; per-hit drums/metronome/drone LFO are the exception).
4. **The master limiter stays.**
5. **Lower layers never touch the DOM** — `audio.js`, `arrange.js`, `recorder.js` reach the UI only via `hooks`. `draw.js` is exempt.
6. **Live bindings — write state only through its setter**, never a module-level `const`. The looper re-reads `baseF()`/`CUR()`/`chordSteps()` every step.
7. **Per-event scale freeze is the polymodality mechanism** (`sc`/`sev`/`a.ty`, and `cents` rides on the frozen `sc` — including for **chords**: a frozen Partch chord layer replays in just intonation via the `chordFreqs` cents branch on `ev.sc`).
8. **The video draw and `detectForVideo` read the same camera frame** (`drawVideoBackground()` before detect, `drawOverlays()` after, one synchronous tick).
9. **There is ONE layout; never reintroduce a mode gate.** PC/`uiMode` are deleted. New behaviour keys off scale properties and `splitOn`, never a `uiMode`-style global branch. **Hit-test and render must derive geometry from the SAME frozen range** — the hand's `S.role`/`S.rx0`/`S.rx1` (frozen at capture) and the single-source helpers (`phoneHalves` for halves, `palSplitX` for the internal split, `rectBandY`/`rectRows` for bands). Two independent copies of a boundary will drift and the finger takes a different cell than it sees.
10. **The split's role pair stays two distinct roles, at most one mono role.** `SPLIT_ROLES` is mutated only via `cycleHalf`, which skips the other half's role — keeping solo+solo and bass+bass (the single-voice chains) unreachable. Don't add a path that sets both halves to the same role.
11. **Continuous-pitch overrides ride a live-only channel and are NEVER recorded.** The theremin Hz is a live-only trailing arg of `ENG.leadOn` **and `ENG.bassOn`** (passed only by the live `WleadOn`/`WbassOn`); replay derives pitch from the frozen `deg/oct`. Do not fold live Hz into the event payload — for solo or bass.
12. **RAW vs REMAPPED coordinates stay separate; the detector always gets the full frame.** `detectForVideo` reads the uncropped `<video>`. Gesture math (pinch/hand-size/Z-reverb/FX-drag/thresholds) uses RAW `lm.x`/`lm.y`; on-screen position and hit-testing use `sx`/`sy` (the frame-margin remap, single source for gestures AND draw). Never clamp coordinates or rendering — only the resulting selection saturates. A new `1-x`, a second copy of the remap, or feeding cropped pixels to the detector all break this.
13. **Mirroring has one source.** `camFacing`/`mirrored()`/`flipX` decide it; the video draw and every landmark→screen conversion read them. Never scatter a `1-x`.
14. **`clearJam` deletes only `ev.jam`-flagged events.** The flag is intrinsic to the event so player recordings (never flagged) are safe. Don't switch jam-clearing to layer numbers.
15. **Layers are scheduled AHEAD at explicit times; the `when` arg stays.** `scheduleLayers` fires layer events at `loop.t0+(rep*lb+ev.t)*spb` within `SCHED_AHEAD`; live calls default to `AC.currentTime`. Never collapse voice functions back to "fire now" — that reintroduces the main-thread-blocking drift the window fixes (see "Scheduler").
16. **`fixedKey` scales anchor to absolute C; the tonic is a KEY, not a multiplier.** Their `leadFreq`/`bassFreq`/`chordFreqs`/`centsOf` branches read `cFix()`+`fixedSlot`, and labels use real note names. Don't route them through the mobile `baseF()·2^(cents)` path — it would erase the per-key character (C gentle, F# tense) that is the entire lesson. `fixedKey` is a **separate** property from `gridChords`.
17. **The A4 reference has exactly ONE source.** `aRef` (state) feeds `a3()` → both `baseF()` and `cFix()` in scales. Never add a second copy or a per-family reference — mobile and fixed scales must move together, or a `415 vs 440` mismatch surfaces as a "detuned instrument".
18. **In split, hand functions are consulted PER HALF by that half's role; the old fx-hand hardcode is GONE.** `handFnOf(key, h.role)` decides `fx`/`note`/`hold`/`therm`; the default `handFn.ld.L='fx'` *derives* "effects on the left hand in the solo half". Do not reintroduce a hardcoded special case — one source of truth (`handFn`).

## How to verify a change

No tests. Verification is **manual, in the browser** — do not run node, a server, headless Chrome, or any automated test/git command; that is the user's job.

1. Serve, "▶ Играть", allow camera (light start card, two buttons; «Обучение» shows a "скоро" note). The app **starts in single-role phone** (SOLO full-width, one role button). The top bar auto-hides after ~3.5 s to the role indicator + three record buttons; a tap/mouse-move reveals it; it won't hide with a panel open.
2. Solo/chords/bass/drums roles play; looper records/overdubs/undoes; arrangement drops in. Panels are light/opaque; the scale panel carries the reference-pitch control and the «Функции рук» section, the loop panel opens in the scale panel's spot without scrolling.
3. Typed chords (Хроматика, 31-TET, 19-TET): left index pinch selects a palette cell; right hand plays; 19-TET pure minor beats less than 12-TET minor; 31-TET `дом7` = 4:5:6:7.
4. Cents scales (Мировые строи → Слендро/Пелог): sound UNEVEN (gamelan), octave a clean 2:1, play-tag cents read the real values; gamelan chords show the no-chords hint.
5. Menu: each tradition's `grp` subgroups render once, in order (Ладовая has Диатоника/Лады(моды)/Этнические/Пентатоника-блюз/Симметричные/Экзотические/Мировые пентатоники/Японские; Арабская has one Макамы group). Мировые строи lists Слендро/Пелог/Партч.
6. **Split (landscape, `◨`):** the button appears only in landscape; enabling it halves the screen (left/right roles from `SPLIT_ROLES`) with a mid divider. Either hand plays whichever half it pinches; a chord latched in one half keeps ringing while the other half plays solo (two roles sounding at once). Each half sets its **own** register in its own octave band. The two role buttons each cycle соло/аккорды/бас/ударные **skipping the other half's role** (never two-the-same, never two solo / two bass). A **bass** or **drums** half renders and plays inside its half only (drum rows span the half, not the screen). With no solo half the FX bars vanish. Hand functions apply **per half** (a solo/bass half consults its role's `handFn`). Rotating to **portrait** auto-disables split (nothing left ringing) and hides `◨`.
7. Rect model (19-TET/31-TET/Partch, solo/bass/chords): fat rectangles + a bottom «ОКТАВА» band; Y picks the rectangle, finger I–IV picks 1 of 4 notes/roots; pinching the octave band sets a **per-role** sticky register (solo/bass/chords don't share it). Chords: left palette unchanged, rect roots in the right part.
8. Partch (Мировые строи): 11-limit JI; solo/bass glide as 11 rectangles; chord role shows the О/У/Диез/Станд. palette; an О7 is a beat-free 4:5:6:7; a recorded Partch chord layer replays in JI.
9. Theremin (now a HAND FUNCTION, «Функции рук» → Терменвокс, solo AND bass): the fine centre-lines appear as an ADDITIVE overlay ON TOP of the normal grid, only when a hand of that role is assigned Терменвокс; pitch glides continuously; the looper still records the nearest step; bass theremin plays an octave-family lower; other functions/roles unchanged.
10. Non-octave (Неоктавные): Bohlen–Pierce solo/bass play in a tritave (register a `3:1` apart), labels read `Т,2,3…` and the register word is `тритава`; BP chords show the 3:5:7/5:7:9/characteristic palette (a 3:5:7 is beat-free). Carlos α/β/γ divide the fifth (register a `3:2` apart, register word `рег.`); **gamma is rect** (5 fat rectangles + register band), alpha/beta are the narrow grid; all three are `noChords`.
11. Historical temperaments (Ладовая → Исторические темперации): Pythagorean chords have a sharp third (81/64); Natural-mobile chords are pure from every root; Natural-fixed / meantone / the well temperaments read chords from the fixed grid → some pure, some wolf (meantone concentrates the wolf into one fifth). A/B the same chord across the shelf. These `fixedKey` scales now show **real note names** (the grid is absolute); mobile cents scales (gamelan/ragas) still show ordinal `Т,2,3…`.
12. Indian (Индийская): the 22-shruti grid sounds UNEVEN (JI, min step 22¢), rows read «свара · имя-шрути» (tonic = «Са · Чхандовати»); ragas read swaras only; all show the no-chords hint in the chord role. `drawStatus` prints «центовый строй · N ступеней», not a fake «N-TET».
13. Camera switch (`🔄`): front camera is mirrored, back is not; the picture and the hand flip together; no second camera → Russian toast, no break.
14. Frame margin: at the outermost rectangles the hand runs off the visible edge but tracking survives (the detector still sees the full frame); the on-screen hand and the cell it grabs stay aligned.
15. Clip/audio (`🎥`/`🎙`): one records canvas+sound, the other sound only; they're mutually exclusive (the other button disables); the file has picture+sound (video) and the buttons return to rest when recording really stops.
16. Jam (`🎵`): tap lays a culture-appropriate backing; each tap switches variant (previous stops, never layers), cycle ends OFF; a jam that's cleared/switched never deletes the player's own recorded layers.
17. Hand functions («Функции рук» in the scale panel): solo and bass keep separate assignments (switch role and back — they persist); Ноты (с удержанием) holds the note while the hand roams and stops on release, with volume still following X; assigning both solo hands to notes hands the mono voice over cleanly (no stuck note) and hides the FX bars; chords/drums are untouched (palette by position, fixed to the left).
18. Reference pitch (scale panel, A4): a preset (415 «барочный» … 444 «оркестровый») or free entry clamped 380–480 (nonsense reverts). Choosing 415 lowers everything ~a semitone and **both mobile scales (гамелан, раги) and fixed (Веркмайстер) move together** — intervals in cents stay, Hz drop; the drone follows; 440 is byte-for-byte as before.
19. fixedKey character: on Веркмайстер the solo/bass tag reads the major third ≈390¢ in key C and ≈408¢ in key F# (same instrument, different key); Натуральный (подвижный) still transposes with no character change. Cents show tenths on every scale now, including plain 12-TET.
20. Chord readout: while a live chord sounds, its highlighted rectangle lists each note's Hz + cents-above-root; the numbers match the tuning (Партч О7 → 386/702/969); it disappears when the chord stops; a loop-played chord keeps the plain highlight.
21. Variable metre (loop panel «Размер», empty loop only): pick 7 → the metronome accents 3+2+2 and the tick grid groups the same; 16 → four vibhags with a weakened (not silent) third; count-in is one bar at any metre; a drum pattern of a different metre is skipped.
22. Timbres: FM banks «Колокол (глубокий)»/«Металлофон» ring inharmonic with a bright strike over a longer tail; KS banks «Струна»/«Ситар»/… play in tune across the whole range (worklet); if the worklet fails, those slots fall back to plucks with no index shift. Fullscreen (`⛶` on the start card and in the bar): works before playing, both toggles reflect the state, Esc updates them, hidden where the API is unavailable.

Never report a change as done without stating which steps you could not verify.

## Language

- Code comments, and `SCALES`/`CHORD_FAM_SETS` names: **Russian**. Never translate or "clean up" these.
- **User-facing UI strings: multilingual, with ENGLISH as the default** (plus ru/es/de). The old "UI strings stay Russian, never translate" rule is **retired** for user-facing strings — see BACKLOG §3.18 (Локализация/i18n), which supersedes §3.2.
  - ⚠️ **The i18n work itself is NOT started and MUST wait for the UI rework** (BACKLOG §3.19/§3.21/§3.22). **Do not start translating or rewriting UI strings into a `{en,ru,es,de}` dictionary yet** — those strings are about to be rewritten by the UI rework, so translating first wastes the effort. Until i18n lands, **keep adding new user-facing strings in Russian** (consistent with the existing code), so there is a single language to migrate cleanly later.
- Chat responses to the user: **English** (the Windows terminal mangles Cyrillic).
