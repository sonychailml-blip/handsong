# BACKLOG — Air Synth 3

Общий список задач. Обновляется по ходу работы.

**Статусы:**
`🔵` — в работе · `⬜` — готово к работе, спецификация есть · `❓` — нужна спецификация · `✅` — сделано

---

## 0. Сейчас в работе

### 🔵 Рефакторинг: разбиение на модули
Полный план — `PLAN.md`, 14 шагов. Правило: один шаг = одна проверка в браузере = один коммит.

| Шаг | Что | Статус |
|---|---|---|
| 1 | `style.css` | ✅ |
| 2 | Весь скрипт → `src/main.js` одним куском | ✅ |
| 3 | `src/config.js` — константы, имена магическим числам | ✅ |
| 4 | `src/state.js` + сеттеры | ✅ |
| 5 | `src/scales.js` | ✅ |
| 6 | `src/hooks.js` + разрезание DOM-записей | ✅ |
| 7 | `src/audio.js` | ✅ |
| 8 | `src/backing.js` | ✅ |
| 9 | `src/recorder.js` | ✅ |
| 10 | `src/vision.js` | ✅ |
| 11 | `src/gestures.js` | ✅ |
| 12 | `src/draw.js` | ✅ |
| 13 | `src/ui.js` + финальный `main.js` | 🔵 следующий |
| 14 | Обновить `CLAUDE.md` под новую структуру | ⬜ |

**Ничего из списков ниже не делаем, пока идут шаги 13–14.**

---

## 1. Фундамент (сразу после шага 14, до фич)

### ⬜ localStorage для настроек
Сейчас каждый запуск — минорная пентатоника от A.
Сохранять: `scaleIdx`, `tonic`, `seventh`, `leadIdx`, `chIdx`, `back.styleSel`, `back.bpm`, громкость подложки.

### ⬜ Service Worker: кэш модели
Модель + WASM качаются с CDN при каждом запуске. Плохой интернет = приложение не стартует.
Даёт офлайн и быстрый старт.

---

## 2. Микротоника: аккорды и сетка

Самый недооценённый пласт проекта. Три отдельные задачи — **не путать их между собой**.

### ⬜ 2.1. Аккорды в `tag:'edo'` — сейчас теряется главное

**Что сейчас.** `19-TET` и `31-TET` заданы как `iv:range(19)` / `iv:range(31)`, `tag:'edo'` → не проходят `isTert` → `chordSteps` выдаёт пауэр-аккорд:

```js
const r = s.iv[deg%n] + s.edo*Math.floor(deg/n);
return [r, r+fifthStep(s.edo), r+s.edo];   // корень, квинта, октава
```

**Почему автор так сделал — причина уважительная.** При `iv=range(31)` наслоение по индексу `i, i+2, i+4` даёт `0, 77, 155 центов`. Это кластер, а не аккорд. Пауэр-аккорд был защитой от каши, а не ленью.

**Что теряется.** 19 и 31 — мезотонические строи, и созвучия у них лучше, чем в 12-TET:

| Интервал | Шагов | Центов | Чистый | Ошибка |
|---|---|---|---|---|
| **31-TET** больш. терция | 10 | 387.1 | 386.3 | **+0.8** |
| **31-TET** натур. септима | 25 | 967.7 | 968.8 | **−1.1** |
| 31-TET квинта | 18 | 696.8 | 702.0 | −5.2 |
| 31-TET нейтр. терция | 9 | 348.4 | — | (≈ Раст) |
| **19-TET** мал. терция | 5 | 315.8 | 315.6 | **+0.2** |
| 19-TET больш. терция | 6 | 378.9 | 386.3 | −7.4 |
| 19-TET квинта | 11 | 694.7 | 702.0 | −7.2 |
| — 12-TET больш. терция | 4 | 400 | 386.3 | +13.7 |
| — 12-TET мал. терция | 3 | 300 | 315.6 | −15.6 |

**Главное в этой таблице:**
- **31-TET — строй для септаккордов.** `[0, 10, 18, 25]` = отношение **4:5:6:7**, натуральный септаккорд. В 12-TET он физически невозможен: там септима 1000¢ вместо 969, промах 31 цент — отсюда напряжение доминанты и потребность в разрешении. В 31-TET он просто консонанс. Тот самый «барбершопный» аккорд, ради которого 31-тоновые инструменты и строят.
- **19-TET — строй для минора.** Малая терция промахивается на 0.2 цента. Чище нет нигде. Септима наоборот плохая (−21.5¢), доминанты там строить не стоит.

**Решение — строить аккорд по интервалу, а не по индексу:**

```js
const stepFor = (edo, ratio) => Math.round(edo * Math.log2(ratio));
// 31: 5/4→10, 3/2→18, 7/4→25, 6/5→8
// 19: 6/5→5,  3/2→11, 5/4→6
```

**Все 31 нота остаются палитрой.** На любой строится настоящее трезвучие или натуральный септаккорд.

**Открытый вопрос:** в хроматической палитре нет ладовой функции — какое трезвучие строить по умолчанию? Мажорное? Минорное (для 19 логичнее)? Нейтральное (31 умеет, 9 шагов)? Вариант: превратить тумблер «Трезвучия/Септаккорды» в селектор качества для `tag:'edo'`.

### ⬜ 2.2. Диатонические подмножества — это про СЕТКУ, а не про аккорды

Отдельная задача. Мезотонический строй — **палитра, а не гамма**: исторически из 31 ноты выбирали 7 или 12 (цепочку квинт) и играли в них. Все 31 как ступени лада не использовал никто.

```js
{name:'31-TET мажор', grp:'Микротоника', edo:31, iv:[0,5,10,13,18,23,28], tag:'dia'},
{name:'31-TET минор', grp:'Микротоника', edo:31, iv:[0,5,8,13,18,21,26],  tag:'dia'},
{name:'19-TET мажор', grp:'Микротоника', edo:19, iv:[0,3,6,8,11,14,17],   tag:'dia'},
{name:'19-TET минор', grp:'Микротоника', edo:19, iv:[0,3,5,8,11,13,16],   tag:'dia'},
```

Канонические мезотонические гаммы. С `tag:'dia'` и длиной 7 существующий `chordSteps` наслоит терции по индексу — **ни строчки в нём менять не надо**. На I ступени 31-TET выйдет `0, 10, 18` = почти чистое мажорное трезвучие.

Полные `range(19)` / `range(31)` **остаются**. Два режима: палитра для исследования, гамма для игры.

⚠️ `chordLabel` / `qual` рассчитаны на 12-TET и на новых ладах скорее всего свалятся в запасной вариант с номерами ступеней. Доделать.

### ⬜ 2.3. Гистерезис уходит под уровень шума

Не «мелко и неудобно» — гистерезис **перестаёт работать**.

`degHyst` использует `seg*ROW_HYST`, где `seg = H/rows`. На телефоне в альбомной ориентации (H ≈ 390 px):

| Лад | Рядов | Ряд | Гистерезис |
|---|---|---|---|
| Диатоника | 7 | 56 px | 9 px |
| 31-TET полный | 31 | 13 px | **2 px** |

Шум трекинга MediaPipe примерно постоянен в пикселях и составляет несколько. В 31-TET защита оказывается ниже шума.

**Решение:** пиксельный пол — `Math.max(seg*ROW_HYST, HYST_MIN_PX)`, константу в `config.js`. Ценой того, что смена ступени потребует осознанного движения — в 31-TET это честно.

### ℹ️ Макам: бага нет

Раст `edo:24, iv:[0,4,7,10,14,18,21]` → аккорд на I = ступени `0, 7, 14` = **0, 350, 700 центов**. 350¢ — нейтральная терция (мажорная 400, минорная 300). Это определяющий признак джинса Раст, без неё это не Раст. Математика верна.

Странность подлинная и не техническая: **арабская музыка не строит трезвучий**, она монодийная и гетерофонная. Гармонизация макамов аккордами — практика XX века (египетское кино, эстрада) и до сих пор спорная. Наслоение терций на лад, для этого не предназначенный, даёт звук, которого в европейской музыке нет.

Чинить нечего. Возможный ход — упомянуть это в учебном оверлее.

---

## 3. Фичи

### ⬜ 3.1. Защёлка аккордов (LATCH) — спецификация готова, §6
Убирает главную боль жестового интерфейса: рука перестаёт висеть в воздухе.

### ❓ 3.2. Английский интерфейс
**Требует решения до начала: заменить русский или добавить переключатель языка?**
- **Замена** — проще, но убивает учебный слой.
- **i18n** — строки в словарь `{ru:{...}, en:{...}}`, работы вдвое больше, обе версии живут.

Объём: названия ладов в `SCALES`, кнопки, панель, строка статуса, **оверлей-учебник** (самый большой текст в проекте), подписи зон на canvas, стартовый экран.

⚠️ До шага 14 правило `CLAUDE.md` «UI strings stay Russian» остаётся в силе.

### ❓ 3.3. Переделать подложки
**Запрошено, не специфицировано.** Что именно не так:
- бедные паттерны, мало вариаций?
- звук барабанов (всё синтезируется на лету, сэмплов нет)?
- мало стилей (сейчас 4: lofi / synthwave / ethnic / ambient)?
- плохо ложится на микротональные лады?
- не хватает управления (свой рисунок, отключение инструментов)?

### ✅ 3.4. Запись → события намерения вместо кадров
Было: `WleadOn` каждый кадр (~60/с) → лог кадров с абсолютными частотами. Нельзя лупить, транспонировать, квантовать.

Сделано:
- **Схема события `{t, layer, fn, a}`.** `t` — в ДОЛЯХ такта (при `back.bpm`), не в секундах → переживает смену BPM на сетке. `a` — payload со **ступенью+октавой** (`deg/oct`), не частотой. `layer` — задел под отмену овердаба (§3.5).
- **Частота выводится на воспроизведении** (`ENG.leadOn/chOn` зовут `leadFreq/chordFreqs`) → запись перестраивается под текущий строй. Это дефолт и это и есть §3.7. «Заморозить герцы» — возможный режим, НЕ дефолт, не сделан (не нужен ни 3.4, ни 3.5, ни 3.7).
- **Запись по изменению:** дискретное (deg/oct/inst) — при смене, непрерывное (vol/rev) — за мёртвой зоной (`REC_VOL_EPS/REC_REV_EPS`). Соло: `leadOn`(атака)→`leadSet`(без переатаки)→`leadOff`. Кадровый лог схлопнулся в горстку точек на ноту. `gestures.js` по-прежнему шлёт `WleadOn` каждый кадр — прореживается только запись, живой звук гладкий (правило 7).
- **Аккорды почти не трогались:** защёлка (§6) уже сделала их разреженными (`WchOn/WchSet/WchOff` в музыкальные моменты). Кадровой проблемы у них не было — только у соло.
- **Воспроизведение — один насос по часам AudioContext** вместо тысячи `setTimeout` (снят дрейф таймеров, фазовая когерентность с фоном). Планировщик `backing.js` НЕ переиспользован как есть: он привязан к такту и сетке 16-х, а свободное соло-воспроизведение ни к чему не привязано — переиспользован ПАТТЕРН (насос по AC-часам), не сетка.
- Живой ввод по-прежнему через `W*`, воспроизведение зовёт `ENG` НАПРЯМУЮ (мимо `W*`) → переигранное не пишется заново без участия `inPB()`. `inPB()` теперь гейтит ровно одно — живой ввод во время плейбэка; это тот гейт, что §3.5 снимет для овердаба.

**Что §3.5 берёт поверх:** длина петли + заворот (modulo + `softAllOff` зависших голосов на границе); count-in (одна доля метронома через планировщик фона); мягкая квантизация аккордов к долям (соло — никогда); снятие живого гейта `inPB()` + арбитраж голосов (живой овердаб vs переигранный слой за один envelope/`'latch'` — настоящая сложность овердаба, НЕ проблема гейта записи); инкремент `layer` + отмена последнего слоя.

### ✅ 3.5. Лупер
Записали круг аккордов → играет по кольцу → поверх ведёте соло. Инструмент самодостаточен.

Сделано (`recorder.js` — транспорт петли, `audio.js` — `metroClick`, UI — 4 кнопки + степпер):
- **Петля фиксированной длины** `loop.bars` тактов (степпер 1–8 в панели, меняется только на пустой петле). Время события — доли ВНУТРИ петли `t ∈ [0, loopBeats)`; заворот по `((now−t0)·bpm/60) mod loopBeats`.
- **Один насос по AC-часам** крутит петлю: на завороте гасит зависшие голоса (`softAllOff`) и переигрывает события с начала; `fireWindow(a,b]` шлёт события окна в `ENG` напрямую (мимо `W*` → сами себя не пишут).
- **● запись:** пусто → отсчёт 1 такт (метроном) → пишет слой 0 за круг → авто-луп и разоружение. Играет петля → тумблер **овердаба** (новый слой поверх).
- **Овердаб без перезаписи плейбэка:** живой ввод идёт через `W*` (звук сразу + запись), переигровка — через `ENG`. Живой гейт `inPB()` снят (это и была засада §3.4): «не переписывать плейбэк» держится структурно (насос зовёт `ENG`, не `W*`), а не гейтом. Слой, что пишется ПРЯМО СЕЙЧАС, `fireWindow` НЕ переигрывает (`recording && layer===loop.layer`) — иначе двойной триггер.
- **Разделение каналов = основной сценарий:** аккорды (`'latch'`) слоем 0, соло (lead) слоем 1 — не конфликтуют. Соло-поверх-соло / аккорд-поверх-аккорда делят один голос: побеждает последний (ограничение движка, не бага).
- **⤺ отмена** последнего слоя (по `layer`), **✕ очистка**, **⟳ луп** играть/пауза.
- **Count-in** через тот же насос (щелчки с опережением по AC-часам); отсчётный такт ничего не пишет (`push` при `e<0` выходит).
- **Мягкая квантизация аккордов** к доле (снап, если ближе 10% доли); соло — никогда.
- Время в долях ⇒ петля переживает смену BPM на сетке (позиция считается из живого `back.bpm`). Известное: смена BPM ПОСРЕДИ круга даёт разовый скачок позиции (t0 не переякоривается) — терпимо.

Открытое на потом: полифонический овердаб одного канала (сейчас 1 голос lead + 1 'latch'); переякорь t0 при смене BPM во время игры.

### ⬜ 3.6. Режим урока
Подсветить следующую ступень, задание «сыграй I–IV–V–I», проверка попадания.

### ⬜ 3.7. Сравнение строёв
«Та же фигура в другом строе»: сыграть записанное в 12-TET, потом в 31-TET. Момент «ого», ради которого приложение существует. Зависит от 3.4.

### ⬜ 3.8. Показ отклонения в центах
`centsOf` уже написан, нужен вывод. Хорошо сочетается с §2 — таблица ошибок становится слышимой.

### ⬜ 3.9. Заменить реверб
IR генерируется из случайного шума (`makeIR`) — самое слабое место звука.

---

## 4. Баги

### ✅ Стартовый экран не исчезал
`#start{display:block}` (1-0-0) перебивал `.ov.on` (0-2-0). **Исправлено.**

### ⬜ Подсветка при игре двумя руками в аккордах
В `draw()` переменная `chAct` скалярная, цикл по рукам перезаписывает — побеждает последняя. Звучат два аккорда, подсвечен один.
**Отдельно не чинить: закрывается защёлкой (§6).** Там аккорд ровно один, `chAct = latchDeg`, скаляр становится верным по построению.
Аргумент против двух аккордов сразу: лестница подписана `I`, `IV`, `Cmaj7` — два аккорда делают подпись ложью, назвать `I+V` одним именем нельзя. Учебный слой ломается. А защёлка освобождает вторую руку и делает вопрос беспредметным.

### ⬜ `revDisp` не сбрасывается при отпускании соло-щипка
REV-бар замирает на последнем значении.

### ✅ `clrBtn` / очистка во время записи — БОЛЬШЕ НЕ АКТУАЛЬНО
Пункт описывал модель записи ДО лупера (`recStart` и ручной отсчёт). Модель переписана
на лупер (`recorder.js`): `recStart` в коде НЕТ, а `clearRec()` уже зовёт
`setRecording(false)` (плюс `clearPump`, `softAllOff`, `droneOff`). Чистить нечего —
пункт снят, чтобы не сбивать с толку. (Имя `recStart` — в списке устаревших, см. CLAUDE.md.)

### ⬜ `selTonic.onchange` не вызывает `softAllOff`
Смена тоники не глушит звучащий аккорд — остаётся в старой тональности. `selScale.onchange` вызывает, `selTonic` — нет. **С защёлкой станет постоянным.** Чинить вместе с §6.

### ⬜ `qTriad`/`qSev` не вызывают `softAllOff`
То же: переключили на септаккорды, звучащее трезвучие осталось трезвучием. **С защёлкой станет заметным.**

### ⬜ Идентификация рук: `key+=i`
При двух руках одной handedness ключ зависит от порядка в массиве результатов и может меняться между кадрами → редкая повисшая нота до watchdog.

### ⬜ `refreshStyle(back.styleSel!=='auto')` затирает пользовательский BPM
При ручном выборе стиля BPM сбрасывается в дефолт стиля.

### ⬜ `chordGlide` при смене длины аккорда
Голосов было 3 (трезвучие), стало 4 (септаккорд) — четвёртый не зазвучит до переатаки. Защищён `if(fr==null)return;`, то есть молча.

---

## 5. Решено НЕ делать

- **Новые лады и тембры** ради количества. Их уже с избытком. (§2.2 — исключение: там лады решают конкретную задачу.)
- **MIDI-экспорт как ранняя фича.** Микротональность в обычный MIDI не влезает (только MPE).
- **React / Vue.** Рендерить нечего, всё на canvas.
- **Tone.js.** Движок написан и работает. Замена = месяц регрессий ради того же.
- **Сборщик.** Зависимостей нет, собирать нечего.

---

## 6. Спецификация: защёлка аккордов (LATCH)

**Область: только колонка АККОРДОВ.** Соло и эффекты не трогаем.

### Поведение

| Событие | Что происходит |
|---|---|
| Соединил пальцы на ступени, ничего не защёлкнуто | Аккорд звучит. Рука ведёт: Y = ступень (глиссандо), X = громкость |
| Разъединил пальцы | **Аккорд продолжает звучать.** Ступень и громкость замирают |
| Соединил пальцы на **той же** ступени | **Аккорд выключается.** Рука инертна до размыкания |
| Соединил пальцы на **другой** ступени | Плавный переход (`chordGlide`, без переатаки). Старый снят |

**Защёлкнутый аккорд ровно один.** Любая рука им управляет; кто последним сделал щипок — тот и рулит.

### Ключевое решение: инертность

III защёлкнут, делаете стоп-щипок на III → звук выключился, но **пальцы всё ещё соединены**. Рука дрожит, палец уезжает на IV, аккорд следует за рукой (`chordGlide` каждый кадр) → IV включился. Выключить невозможно в принципе.

Поэтому: **после стоп-щипка рука ничего не играет, пока пальцы не разомкнуты.**

Глиссандо для обычного щипка сохраняется: соединил на III → звучит III → ведёшь к V не размыкая → плавно переходит → разомкнул → V защёлкнут.

### Состояние

- `latchDeg` — номер защёлкнутой ступени, `-1` если тишина
- `chOwner` — ключ руки, которая рулит (или `null`)
- `S.inert` — флаг на руке, ставится при стоп-щипке, снимается при размыкании
- **Владелец голосов — постоянный ключ `'latch'`**, не ключ руки

Последнее бесплатно решает watchdog: рука ушла из кадра → `endPinch` → голоса принадлежат `'latch'`, аккорд жив.

### Правки по файлам

| Файл | Что |
|---|---|
| `gestures.js` | Логика захвата: сравнение `deg` с `latchDeg`, ветка стоп / ветка переключения. Флаг `S.inert`. `endPinch` для зоны `'ch'` больше **не** зовёт `WchOff` |
| `audio.js` | Ничего. `chordOn/chordGlide/chordOff` уже принимают произвольный ключ |
| `draw.js` | `chAct` из `latchDeg`, а не из состояния рук. Подсветка горит всегда |
| `ui.js` | `selTonic.onchange`, `qTriad`/`qSev` → `softAllOff()` + сброс `latchDeg` |
| `recorder.js` | `softAllOff` обходит `Object.keys(chordHold)` → `'latch'` попадёт сам. Добавить `latchDeg = -1`. То же в `panic` |

### Проверка

1. Щипок → отпустил → **аккорд продолжает звучать**.
2. Щипок на той же ступени → **выключился**.
3. Щипок на другой → плавно переключился, наложения нет.
4. Стоп-щипок → не размыкая, поводить рукой → **тишина**.
5. Аккорд защёлкнут → **убрать руку из кадра** → продолжает звучать.
6. Защёлкнут → сменить тонику → **заглох** (не остался в старой тональности).
7. Защёлкнут → сменить лад → заглох.
8. Защёлкнут → panic → заглох, подсветка погасла.
9. Подсветка горит на защёлкнутом аккорде и без щипка.
10. Рука A защёлкнула III, рука B щипает V → V звучит, III снят.

### Известный риск

**Дребезг на границе при стоп-щипке.** III защёлкнут, щипаете у границы III/IV, шум даёт IV → вместо выключения переключились. `degHyst` при захвате не работает: там `S.deg = -1`, возвращается сырое значение.

Если промахи частые — применить `degHyst` при захвате с `prev = latchDeg` (уклон в пользу защёлкнутой ступени). Заранее не делать. **Связано с §2.3** — пиксельный пол гистерезиса поможет и здесь.
### ❓ Переработать меню — перегружено

Панель настроек выросла до ~15 контролов в одном плоском списке: лад, тоника,
септаккорды, три тембра, фон (стиль/BPM/громкость), режим ПК/Смартфон, инструмент
телефона, смена рук, такты лупа, квантизация, три селектора аранжировки.

Найти нужное невозможно, а на телефоне список не помещается на экран.

Нужны разделы или вкладки. Черновая группировка:
- **Музыка**: лад, тоника, септаккорды
- **Звук**: тембры соло / аккордов / баса
- **Луп**: такты, квантизация, аранжировка (гармония / ритм / бас)
- **Фон**: стиль, BPM, громкость  ← вероятно исчезнет вместе с генеративным фоном
- **Устройство**: ПК/Смартфон, инструмент телефона, смена рук

Делать ПОСЛЕ решения по генеративному фону (§3.3) — оно уберёт целый раздел.
### ⬜ 3.10. Микрохроматика: богатая палитра аккордов (часть задачи о типах)

Это НЕ отдельная задача — это расширение «типов аккордов в хроматике».
Полный 19/31-TET = микротональная хроматика, и ей нужен тот же выбор типа рукой,
но список типов БОГАЧЕ, чем в 12-TET.

Сейчас в полных 19/31 аккорды строятся правильно (по интервалам, §2.1), но
ФИКСИРОВАННЫМ типом на всех ступенях: 31 → всегда мажор [1,5/4,3/2], 19 → всегда
минор [1,6/5,3/2]. Пользователь слышал это как «на всех ступенях одинаковый аккорд» —
он прав. В палитре нет ступеней, которым «положен» минор, поэтому единственно верное
решение — выбор типа рукой, а не зашитый тип.

Каталог типов, доступных в 31-TET и НЕВОЗМОЖНЫХ в 12-TET (готовый список для дизайна):
| Тип | Шаги (31-TET) | Центы | Чем уникален |
|---|---|---|---|
| Мажор чистый | 0·10·18 | 0·387·697 | терция +0.8¢, без биений |
| Минор чистый | 0·8·18 | 0·310·697 | |
| **Нейтральный** | 0·9·18 | 0·348·697 | ни мажор ни минор — звук вне оппозиции |
| **Домин.7 (4:5:6:7)** | 0·10·18·25 | 0·387·697·968 | натур. септима −1¢, НЕ тянет разрешаться |
| Мажор-maj7 | 0·10·18·28 | 0·387·697·1084 | |
| Минор7 чистый | 0·8·18·26 | 0·310·697·1006 | |
| **Субминор (7/6)** | 0·7·18·25 | 0·271·697·968 | терция между минором и «ещё меньше» |
| Увеличенный чистый | 0·10·20 | 0·387·774 | |

19-TET: чистейший минор (терция +0.2¢, 0·316·695) — лучший минор среди равномерных строёв.

Смысл: типы аккордов — это не просто джаз в хроматике, это ЕДИНСТВЕННЫЙ способ
услышать то, ради чего микротональные строи придуманы. Поднимает приоритет дизайна
жеста (палец выбирает семейство типа). Список типов зависит от строя: 12-TET —
привычные, 31-TET — плюс нейтральные/натуральные-септимы/субминор.

Зависит от: решения по жесту выбора типа (см. UI-MAP, роль «Аккорды» в хроматике).

### ⬜ 3.11. Запись видео и звука — поделиться музыкой

Дать пользователю снять то, что он играет, и поделиться.

⚠️ Подводный камень, знать заранее: экран (canvas + камера) и звук (Web Audio) —
ДВА РАЗНЫХ потока, их надо СВЕСТИ в один файл. Технически:
- звук: MediaStreamDestination из AudioContext (уже есть контекст — подключить к нему)
- видео: canvas.captureStream()
- свести оба в MediaRecorder → webm
Три развилки, решить при подходе к задаче:
1. Только звук (проще, аудио-клип) или звук+видео (нагляднее, но тяжелее)?
2. Видео с камерой (видно руки — красиво для соцсетей) или только canvas (чисто)?
3. Запись в реальном времени (пишем пока играешь) или рендер лупа в файл
   (чище, но сложнее — надо «проиграть» события в буфер)?
На слабом телефоне (196мс инференс) одновременная запись видео может добить
производительность — проверить, не роняет ли захват экрана и без того низкий fps.

Формат: webm везде кроме iOS Safari (там нужен mp4/H.264 — отдельная головная боль).

### ⬜ 3.12. Больше строёв и больше тембров инструментов

Расширить, когда фундамент устоится. НЕ раньше перестройки меню в традиции (UI-MAP),
иначе новые строи некуда класть по-человечески.
- Строи: другие макамы, индийские раги (что требует своей микротоники),
  исторические темпераменты (среднетоновый, Веркмейстер), другие EDO (17, 22, 53).
- Тембры: расширить LEAD_INSTR / CHORD_INSTR / BASS_INSTR / DRUM_KITS.

⚠️ Из раздела «решено НЕ делать»: добавлять строи и тембры РАДИ КОЛИЧЕСТВА —
самая приятная и бесполезная работа. Каждый новый — только если закрывает
конкретную музыкальную потребность, а не «пусть будет больше». Добавлять по одному
и слушать, а не списком из головы.
### ⬜ 3.13. Микшер слоёв лупера — громкость на канал

Отдельная громкость для каждого инструмента лупа: аккорды / бас / барабаны / дрон.

⚠️ ДЕЛАТЬ ТОЛЬКО ПОСЛЕ гейнстейджинга (правки уровней шин под лимитер).
Причина: жалоба «новый слой глушит старые» — это НЕ баланс, это лимитер прижимает
весь микс при перегрузе (bassBus=1.0 толкает сумму за threshold −6, ratio 20).
Сначала чиним лимитер уровнями шин — возможно, после этого слои перестанут душить
друг друга и микшер станет нужен только для художественного баланса, а не для
починки. Каким он должен быть — решать ПОСЛЕ, играя с исправленным звуком:
- может хватить одного «громкость лупа» (луп vs живое соло)
- может понадобиться полный микшер 4 канала
- зависит от того, что почувствуешь после гейнстейджинга

Дизайн (когда дойдём): ползунки в меню лупера, значения в state, применяются к
loopBus/шинам после initAudio (узлы существуют только после старта — hard rule).
Учесть перегруз меню — 4 ползунка это много, может вкладка «микс».