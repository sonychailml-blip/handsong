# BACKLOG — Handsong (бывш. Air Synth 3)

Общий список задач. Обновляется по ходу работы.

> **Ближайшее (после публикации, обучения и локализации):** §3.31 промо-ролик · §3.32 замер задержки
> входа · испанский/немецкий (§3.18, ждём проверяющего терминологию).
>
> ⛔ **ПРЕДПОСЫЛКА, блокирующая две фичи:** «СТАБИЛЬНЫЙ ИДЕНТИФИКАТОР ЛАДА» (§1). Без него нельзя ни
> сохранять петли, ни делать конструктор строёв (§3.34) — у ладов нет `id`, а слой держит ССЫЛКУ на
> объект лада, которую не сериализовать.
>
> ⚠️ **Статусы сверены с кодом.** ✅ у пункта = проверено в `src/`, а не «кажется, сделали».

**Статусы:**
`🔵` — в работе · `⬜` — готово к работе, спецификация есть · `❓` — нужна спецификация · `✅` — сделано

---

## 0. Сейчас в работе

### ✅ Рефакторинг: разбиение на модули — ЗАВЕРШЁН ДАВНО (таблица ниже — история)

⚠️ **Самый устаревший пункт файла, и самый вредный: он объявлял СТОП на все остальные работы
(«ничего из списков ниже не делаем, пока идут шаги 13–14»). Этот стоп СНЯТ — все 14 шагов пройдены,
и с тех пор выпущены десятки фич.** В `src/` семнадцать модулей, включая `ui.js` и финальный `main.js`
(шаг 13), а `CLAUDE.md` (шаг 14) с тех пор переписывался многократно и описывает текущий код.

⚠️ Строка «8 · `src/backing.js` ✅» — историческая: модуль был выделен, а ПОЗЖЕ УДАЛЁН целиком
(генеративный фон заменён аранжировкой-слоями; см. §3.3). Не искать его в коде.
Модули, появившиеся ПОСЛЕ этого плана и в таблице отсутствующие: `arrange.js`, `clip.js`, `demo.js`,
`i18n.js`, `ks-worklet.js`, `tutor.js`.

Полный план — `PLAN.md` (АРХИВ), 14 шагов. Правило было: один шаг = одна проверка в браузере = один коммит.

| Шаг | Что | Статус |
|---|---|---|
| 1 | `style.css` | ✅ |
| 2 | Весь скрипт → `src/main.js` одним куском | ✅ |
| 3 | `src/config.js` — константы, имена магическим числам | ✅ |
| 4 | `src/state.js` + сеттеры | ✅ |
| 5 | `src/scales.js` | ✅ |
| 6 | `src/hooks.js` + разрезание DOM-записей | ✅ |
| 7 | `src/audio.js` | ✅ |
| 8 | `src/backing.js` | ✅ |
| 9 | `src/recorder.js` | ✅ |
| 10 | `src/vision.js` | ✅ |
| 11 | `src/gestures.js` | ✅ |
| 12 | `src/draw.js` | ✅ |
| 13 | `src/ui.js` + финальный `main.js` | 🔵 следующий |
| 14 | Обновить `CLAUDE.md` под новую структуру | ⬜ |

~~**Ничего из списков ниже не делаем, пока идут шаги 13–14.**~~ — снято, шаги пройдены.

---

## 1. Фундамент

_(Заголовок раньше читался «сразу после шага 14, до фич» — рефакторинг давно закончен, а фичи давно
идут. Здесь лежит то, что ПОДПИРАЕТ другие работы: хранилище настроек, идентификатор лада, офлайн.)_

### ⬜ localStorage для настроек
> **Механизм УЖЕ есть и работает:** `i18n.store` — обёртка над localStorage с try/catch и падением в
> ПАМЯТЬ (приватный режим и `file://` не должны ронять старт). Через неё уже персистятся **язык**
> (`handsong.lang`) и **прогресс уроков** (`handsong.lesson.<id>`). Настройки ниже — просто ещё ключи
> в том же хранилище; писать новую обвязку не нужно.

Сейчас каждый запуск — минорная пентатоника от A, и все накопившиеся настройки СБРАСЫВАЮТСЯ.
Сохранять: `scaleIdx`, `tonic`, `seventh`, `leadIdx`/`chIdx`/`bassIdx`/`drumKitIdx`, `loop.bpm`.
**Добавились сессионные настройки, которые тоже стоит персистить:** `aRef` (эталон A4 — сейчас сброс на
440), `handFn` (функции рук соло/бас), `loop.metre` (размер такта). Хорошо ложится к публикации (§3.25/
HANDOFF): человек вернётся к своему строю/эталону/раскладке рук.

### ⬜ СТАБИЛЬНЫЙ ИДЕНТИФИКАТОР ЛАДА — ПРЕДПОСЫЛКА, блокирует две фичи

**Факт (проверено по коду).** У ладов **НЕТ идентификатора вообще**: в `SCALES` 75 записей, ни у одной
нет поля `id`. Лад адресуется ПОЗИЦИЕЙ в массиве (`scaleIdx`) — отсюда и правило «дописывать только
в конец». А записанное событие лупера морозит **`sc:CUR()`, то есть ССЫЛКУ НА САМ ОБЪЕКТ лада**
(`recorder.push`), а не индекс и не имя.

**Что из этого следует — ровно две вещи, и обе важны:**
- **В ПАМЯТИ всё хорошо.** Слой держит объект, поэтому переигровка переживёт и перестановку массива,
  и переименование лада, и локализацию имён. Полимодальность (hard rule 7) на этом и стоит.
- **НА ДИСКЕ ничего не работает.** Ссылку на объект нельзя сериализовать. Значит **сохранить петлю
  невозможно** — не «трудно», а не на что записать, каким ладом играл слой.

⚠️ **ЭТО БЛОКИРУЕТ ДВЕ ФИЧИ, и ни за одну нельзя браться раньше:**
1. **Сохранение петель** (в localStorage, в файл, в ссылку — любое). Без id слой не восстановить.
2. **§3.34 Конструктор собственного строя.** Пользовательский лад по определению не может жить по
   индексу в append-only массиве: его создают, редактируют и УДАЛЯЮТ. Что происходит со слоем, который
   прямо сейчас играет удалённый строй? Пока нет id — ответа нет.

**Что делать (эскиз, не спецификация).** Дать каждому ладу стабильный строковый `id` (встроенным —
осмысленный постоянный ключ, пользовательским — сгенерированный и неизменяемый), адресовать лад по нему
всюду, где адрес переживает сессию, и оставить `scaleIdx` тем, чем он и является — позицией в текущем
списке для меню и состояния. `sc:CUR()` в событии при этом можно НЕ трогать: в памяти он и так верен;
id нужен на границе сериализации. Отдельно решить судьбу слоя, чей строй удалили (запретить удаление
используемого / подставить копию строя в петлю / отвязать слой).

⚠️ Это ФУНДАМЕНТ, а не фича: он ничего не добавляет пользователю сам по себе, но без него две фичи выше
делать нельзя. Оценивать и планировать его отдельно от них.

### ⬜ Service Worker: кэш модели
> **SW УЖЕ зарегистрирован — и НАМЕРЕННО пустой** (`sw.js`, пустой `fetch`-обработчик). Его единственная
> задача — устанавливаемость: без зарегистрированного SW с fetch-обработчиком Chromium не присылает
> `beforeinstallprompt`, и кнопка установки не появляется вовсе (см. «Публикация» в CLAUDE.md). Кэша нет
> СОЗНАТЕЛЬНО: ничто не может застрять устаревшим, камера/звук/ворклет и относительные пути под подпапкой
> GitHub Pages работают ровно как без SW. **Этот пункт = добавить кэш В НЕГО**, а не заводить новый SW.

Модель + WASM качаются с CDN при каждом запуске. Плохой интернет = приложение не стартует.
Даёт офлайн и быстрый старт. ⚠️ Взвесить цену: кэш модели — это ещё и риск застрявшей версии, ради которого
кэш и не заводили сразу.

---

## 2. Микротоника: аккорды и сетка

Самый недооценённый пласт проекта. Три отдельные задачи — **не путать их между собой**.

### ✅ 2.1. Аккорды в `tag:'edo'` — СДЕЛАНО (решение оказалось шире, чем предлагалось)

**СДЕЛАНО, причём дважды.** Предлагавшееся здесь «строить аккорд по ИНТЕРВАЛУ, а не по индексу»
реализовано прямо в записи лада: у 19-TET стоит `chord:[1, 6/5, 3/2]`, `chord7:[1, 6/5, 3/2, 9/5]`,
у 31-TET — `chord:[1, 5/4, 3/2]`, `chord7:[1, 5/4, 3/2, 7/4]`, то есть тот самый **4:5:6:7**, ради
которого пункт и писался. Пауэр-аккорд для них больше не строится.
Сверх того оба получили **полную палитру типов** (`typedChords:'edo19'`/`'edo31'`), что закрыло и
«открытый вопрос» ниже: качество аккорда больше не выбирается по умолчанию — его выбирает РУКА
(см. §3.10, тоже сделан). Тумблер «Трезвучия/Септаккорды» переделывать не понадобилось.
Ниже — исходный разбор, он ценен как обоснование (таблица ошибок строёв) и остаётся как история.

<details><summary>Исходный разбор (история)</summary>

**Что было.** `19-TET` и `31-TET` заданы как `iv:range(19)` / `iv:range(31)`, `tag:'edo'` → не проходили `isTert` → `chordSteps` выдавал пауэр-аккорд:

```js
const r = s.iv[deg%n] + s.edo*Math.floor(deg/n);
return [r, r+fifthStep(s.edo), r+s.edo];   // корень, квинта, октава
```

**Почему автор так сделал — причина уважительная.** При `iv=range(31)` наслоение по индексу `i, i+2, i+4` даёт `0, 77, 155 центов`. Это кластер, а не аккорд. Пауэр-аккорд был защитой от каши, а не ленью.

**Что теряется.** 19 и 31 — мезотонические строи, и созвучия у них лучше, чем в 12-TET:

| Интервал | Шагов | Центов | Чистый | Ошибка |
|---|---|---|---|---|
| **31-TET** больш. терция | 10 | 387.1 | 386.3 | **+0.8** |
| **31-TET** натур. септима | 25 | 967.7 | 968.8 | **−1.1** |
| 31-TET квинта | 18 | 696.8 | 702.0 | −5.2 |
| 31-TET нейтр. терция | 9 | 348.4 | — | (≈ Раст) |
| **19-TET** мал. терция | 5 | 315.8 | 315.6 | **+0.2** |
| 19-TET больш. терция | 6 | 378.9 | 386.3 | −7.4 |
| 19-TET квинта | 11 | 694.7 | 702.0 | −7.2 |
| — 12-TET больш. терция | 4 | 400 | 386.3 | +13.7 |
| — 12-TET мал. терция | 3 | 300 | 315.6 | −15.6 |

**Главное в этой таблице:**
- **31-TET — строй для септаккордов.** `[0, 10, 18, 25]` = отношение **4:5:6:7**, натуральный септаккорд. В 12-TET он физически невозможен: там септима 1000¢ вместо 969, промах 31 цент — отсюда напряжение доминанты и потребность в разрешении. В 31-TET он просто консонанс. Тот самый «барбершопный» аккорд, ради которого 31-тоновые инструменты и строят.
- **19-TET — строй для минора.** Малая терция промахивается на 0.2 цента. Чище нет нигде. Септима наоборот плохая (−21.5¢), доминанты там строить не стоит.

**Решение — строить аккорд по интервалу, а не по индексу:**

```js
const stepFor = (edo, ratio) => Math.round(edo * Math.log2(ratio));
// 31: 5/4→10, 3/2→18, 7/4→25, 6/5→8
// 19: 6/5→5,  3/2→11, 5/4→6
```

**Все 31 нота остаются палитрой.** На любой строится настоящее трезвучие или натуральный септаккорд.

**Открытый вопрос (ЗАКРЫТ палитрой типов, см. выше):** в хроматической палитре нет ладовой функции — какое трезвучие строить по умолчанию? Мажорное? Минорное (для 19 логичнее)? Нейтральное (31 умеет, 9 шагов)? Вариант: превратить тумблер «Трезвучия/Септаккорды» в селектор качества для `tag:'edo'`.

</details>

### ⬜ 2.2. Диатонические подмножества — это про СЕТКУ, а не про аккорды

Отдельная задача. Мезотонический строй — **палитра, а не гамма**: исторически из 31 ноты выбирали 7 или 12 (цепочку квинт) и играли в них. Все 31 как ступени лада не использовал никто.

```js
{name:'31-TET мажор', grp:'Микротоника', edo:31, iv:[0,5,10,13,18,23,28], tag:'dia'},
{name:'31-TET минор', grp:'Микротоника', edo:31, iv:[0,5,8,13,18,21,26],  tag:'dia'},
{name:'19-TET мажор', grp:'Микротоника', edo:19, iv:[0,3,6,8,11,14,17],   tag:'dia'},
{name:'19-TET минор', grp:'Микротоника', edo:19, iv:[0,3,5,8,11,13,16],   tag:'dia'},
```

Канонические мезотонические гаммы. С `tag:'dia'` и длиной 7 существующий `chordSteps` наслоит терции по индексу — **ни строчки в нём менять не надо**. На I ступени 31-TET выйдет `0, 10, 18` = почти чистое мажорное трезвучие.

Полные `range(19)` / `range(31)` **остаются**. Два режима: палитра для исследования, гамма для игры.

⚠️ `chordLabel` / `qual` рассчитаны на 12-TET и на новых ладах скорее всего свалятся в запасной вариант с номерами ступеней. Доделать.

### 🔵 2.3. Гистерезис уходит под уровень шума — ЗАКРЫТ ТАМ, ГДЕ БОЛЬНЕЕ ВСЕГО; остался один случай

**Закрыто rect-моделью** («4 ноты в прямоугольнике»): у ладов с большим числом
ступеней Y выбирает не узкий ряд, а ЖИРНЫЙ прямоугольник, и палец I–IV добирает ноту внутри него.
31-TET вместо 31 ряда по 13 px имеет 8 нотных прямоугольников + октавный — гистерезис снова
измеряется десятками пикселей, а не двумя. Так же закрыты 19-TET, Партч (43 ступени!) и Карлос гамма.
С §3.38 раскладка стала ПЕРЕКЛЮЧАЕМОЙ (`state.rectPref`) и доступна ВСЕМ ладам без исключения — но
по умолчанию обычные лады остаются в узких рядах, и таблица ниже про них в силе.
⚠️ Обратная сторона ЖИРНЫХ полос («липкая» граница: `ROW_HYST` — ДОЛЯ полосы, у 7-нотного лада на
трёх полосах выходило ~50px против ~10px у Партча) ушла сама: со вторым заходом §3.38 полос стало
8–12, и зона гистерезиса вернулась к ~14/~9px — как у узких рядов. Взамен выросла ЦЕНА промаха: шаг
по Y теперь k нот, а не одна.

✅ **ПОСЛЕДНИЙ НЕРАЗЛОЖИМЫЙ СЛУЧАЙ ЗАКРЫТ: сетка 22 шрути.** Прежде прямоугольники требовали кратности
4, а у шрути не делились ни 22, ни 23 — единственный такой лад среди 75. С ПЕРЕМЕННЫМ k (4/3/2) она
раскладывается при **k=2: 11 прямоугольников по две ноты** (полоса ~64px вместо ряда ~32px), играют
указательный и средний, безымянный и мизинец холостые (нарисованы тусклым прочерком). Раги (по 7
ступеней) идут при k=4 — 2 прямоугольника на октаву, четыре октавы сразу. Идея ниже (пиксельный ПОЛ
гистерезиса) остаётся полезной для узких рядов как таковых — их никто не отменял:
пиксельный ПОЛ гистерезиса (не доля ряда, а минимум px —
помогает и стоп-щипку защёлки, см. «Известный риск» в §6). Ниже — исходный разбор.

`degHyst` использует `seg*ROW_HYST`, где `seg = H/rows`. На телефоне в альбомной ориентации (H ≈ 390 px):

| Лад | Рядов | Ряд | Гистерезис |
|---|---|---|---|
| Диатоника | 7 | 56 px | 9 px |
| 31-TET полный | 31 | 13 px | **2 px** |

Шум трекинга MediaPipe примерно постоянен в пикселях и составляет несколько. В 31-TET защита оказывается ниже шума.

**Решение:** пиксельный пол — `Math.max(seg*ROW_HYST, HYST_MIN_PX)`, константу в `config.js`. Ценой того, что смена ступени потребует осознанного движения — в 31-TET это честно.

### ℹ️ Макам: бага нет

Раст `edo:24, iv:[0,4,7,10,14,18,21]` → аккорд на I = ступени `0, 7, 14` = **0, 350, 700 центов**. 350¢ — нейтральная терция (мажорная 400, минорная 300). Это определяющий признак джинса Раст, без неё это не Раст. Математика верна.

Странность подлинная и не техническая: **арабская музыка не строит трезвучий**, она монодийная и гетерофонная. Гармонизация макамов аккордами — практика XX века (египетское кино, эстрада) и до сих пор спорная. Наслоение терций на лад, для этого не предназначенный, даёт звук, которого в европейской музыке нет.

Чинить нечего. Возможный ход — упомянуть это в учебном оверлее.

---

## 3. Фичи

### ✅ 3.1. Защёлка аккордов (LATCH) — СДЕЛАНО
Убирает главную боль жестового интерфейса: рука перестаёт висеть в воздухе.
**Выпущено по спецификации §6** (она ниже, как история): владелец голосов — постоянный ключ `'latch'`,
`latchDeg`/`latchTy`, инертность руки после стоп-щипка. Сверх спецификации выросло: тождество защёлки
в типизированных ладах = **(ступень + ТИП)**, тип морозится на атаке (`S.ty`), и защёлка стала одной из
ДВУХ функций аккордовой руки — «Аккорды (защёлка)» (дефолт) и «Аккорды (с удержанием)» (`handFn.ch`).
⚠️ Живой защёлкнутый аккорд теперь ПЕРЕЖИВАЕТ заворот петли (hard rule 20 в CLAUDE.md) — это правило
появилось позже спецификации и её отменяет в части «на завороте гасим всё».

### ✅ 3.2. Английский интерфейс — ПОГЛОЩЁН §3.18, СДЕЛАНО
Вопрос «замена или переключатель?» решён в пользу **i18n с английским дефолтом** (не замена — учебный слой
цел), и это уже выпущено: см. **§3.18** (механизм, три формы имён музыкальных данных, опасность «имя как
идентификатор») и **§3.27** (переключатель на старте). Оговорка «до шага 14 UI остаётся русским» СНЯТА.
Ниже — исходный текст, только как история.

<details><summary>Исходная постановка (история)</summary>

**Требовало решения до начала: заменить русский или добавить переключатель языка?**
- **Замена** — проще, но убивает учебный слой.
- **i18n** — строки в словарь `{ru:{...}, en:{...}}`, работы вдвое больше, обе версии живут.

Объём: названия ладов в `SCALES`, кнопки, панель, строка статуса, **оверлей-учебник** (самый большой текст в проекте), подписи зон на canvas, стартовый экран.

⚠️ До шага 14 правило `CLAUDE.md` «UI strings stay Russian» остаётся в силе.

</details>

### ✅ 3.3. Переделать подложки — СНЯТ: подложки, о которых он писал, УДАЛЕНЫ

⚠️ **Пункт описывал ГЕНЕРАТИВНЫЙ ФОН из `backing.js`** («4 стиля: lofi / synthwave / ethnic / ambient»).
Этого модуля в проекте НЕТ — он удалён, а из него спасены только дрон и голоса дарбуки (см. комментарии
в `audio.js`). Стилей, BPM фона и его громкости больше не существует, поэтому чинить нечего.
Их заменила ДРУГАЯ модель: **аранжировка как слои лупера** (`arrange.js`: `HARMONIES`/`RHYTHMS`/
`BASS_MODES`) и **кнопка «Подложка»** (джем целиком / только ударные). Вопросы «бедные паттерны»,
«мало вариантов», «плохо ложится на микротональные лады» переехали живыми в **§3.16** (наполнение
джема) и **§3.33** (ритмы), где и решаются. Здесь ничего не осталось.

### ✅ 3.4. Запись → события намерения вместо кадров
Было: `WleadOn` каждый кадр (~60/с) → лог кадров с абсолютными частотами. Нельзя лупить, транспонировать, квантовать.

Сделано:
- **Схема события `{t, layer, fn, a}`.** `t` — в ДОЛЯХ такта (при `back.bpm`), не в секундах → переживает смену BPM на сетке. `a` — payload со **ступенью+октавой** (`deg/oct`), не частотой. `layer` — задел под отмену овердаба (§3.5).
- **Частота выводится на воспроизведении** (`ENG.leadOn/chOn` зовут `leadFreq/chordFreqs`) → запись перестраивается под текущий строй. Это дефолт и это и есть §3.7. «Заморозить герцы» — возможный режим, НЕ дефолт, не сделан (не нужен ни 3.4, ни 3.5, ни 3.7).
- **Запись по изменению:** дискретное (deg/oct/inst) — при смене, непрерывное (vol/rev) — за мёртвой зоной (`REC_VOL_EPS/REC_REV_EPS`). Соло: `leadOn`(атака)→`leadSet`(без переатаки)→`leadOff`. Кадровый лог схлопнулся в горстку точек на ноту. `gestures.js` по-прежнему шлёт `WleadOn` каждый кадр — прореживается только запись, живой звук гладкий (правило 7).
- **Аккорды почти не трогались:** защёлка (§6) уже сделала их разреженными (`WchOn/WchSet/WchOff` в музыкальные моменты). Кадровой проблемы у них не было — только у соло.
- **Воспроизведение — один насос по часам AudioContext** вместо тысячи `setTimeout` (снят дрейф таймеров, фазовая когерентность с фоном). Планировщик `backing.js` НЕ переиспользован как есть: он привязан к такту и сетке 16-х, а свободное соло-воспроизведение ни к чему не привязано — переиспользован ПАТТЕРН (насос по AC-часам), не сетка.
- Живой ввод по-прежнему через `W*`, воспроизведение зовёт `ENG` НАПРЯМУЮ (мимо `W*`) → переигранное не пишется заново без участия `inPB()`. `inPB()` теперь гейтит ровно одно — живой ввод во время плейбэка; это тот гейт, что §3.5 снимет для овердаба.

**Что §3.5 берёт поверх:** длина петли + заворот (modulo + `softAllOff` зависших голосов на границе); count-in (одна доля метронома через планировщик фона); мягкая квантизация аккордов к долям (соло — никогда); снятие живого гейта `inPB()` + арбитраж голосов (живой овердаб vs переигранный слой за один envelope/`'latch'` — настоящая сложность овердаба, НЕ проблема гейта записи); инкремент `layer` + отмена последнего слоя.

### ✅ 3.5. Лупер
Записали круг аккордов → играет по кольцу → поверх ведёте соло. Инструмент самодостаточен.

Сделано (`recorder.js` — транспорт петли, `audio.js` — `metroClick`, UI — 4 кнопки + степпер):
- **Петля фиксированной длины** `loop.bars` тактов (степпер 1–8 в панели, меняется только на пустой петле). Время события — доли ВНУТРИ петли `t ∈ [0, loopBeats)`; заворот по `((now−t0)·bpm/60) mod loopBeats`.
- **Один насос по AC-часам** крутит петлю: на завороте гасит зависшие голоса (`softAllOff`) и переигрывает события с начала; `fireWindow(a,b]` шлёт события окна в `ENG` напрямую (мимо `W*` → сами себя не пишут).
- **● запись:** пусто → отсчёт 1 такт (метроном) → пишет слой 0 за круг → авто-луп и разоружение. Играет петля → тумблер **овердаба** (новый слой поверх).
- **Овердаб без перезаписи плейбэка:** живой ввод идёт через `W*` (звук сразу + запись), переигровка — через `ENG`. Живой гейт `inPB()` снят (это и была засада §3.4): «не переписывать плейбэк» держится структурно (насос зовёт `ENG`, не `W*`), а не гейтом. Слой, что пишется ПРЯМО СЕЙЧАС, `fireWindow` НЕ переигрывает (`recording && layer===loop.layer`) — иначе двойной триггер.
- **Разделение каналов = основной сценарий:** аккорды (`'latch'`) слоем 0, соло (lead) слоем 1 — не конфликтуют. Соло-поверх-соло / аккорд-поверх-аккорда делят один голос: побеждает последний (ограничение движка, не бага).
- **⤺ отмена** последнего слоя (по `layer`), **✕ очистка**, **⟳ луп** играть/пауза.
- **Count-in** через тот же насос (щелчки с опережением по AC-часам); отсчётный такт ничего не пишет (`push` при `e<0` выходит).
- **Мягкая квантизация аккордов** к доле (снап, если ближе 10% доли); соло — никогда.
- Время в долях ⇒ петля переживает смену BPM на сетке (позиция считается из живого `back.bpm`). Известное: смена BPM ПОСРЕДИ круга даёт разовый скачок позиции (t0 не переякоривается) — терпимо.

Открытое на потом: переякорь t0 при смене BPM во время игры.
✅ Полифонический овердаб одного канала — СДЕЛАН для СОЛО (пул голосов с ключами-владельцами
'lead:L/R' и 'leadloop:N:v', см. §3.38): соло-слой поверх соло-слоя и поверх живой руки звучит.
Аккорды это умели и раньше ('latch'/'loop:N'). Остался МОНО только БАС (один голос на слой).

### ⬜ 3.6. Режим урока (МУЗЫКАЛЬНАЯ теория) — механизм готов, содержание нет
Подсветить следующую ступень, задание «сыграй I–IV–V–I», проверка попадания.
> **Не путать с §3.20 (ОНБОРДИНГ по интерфейсу) — тот СДЕЛАН.** Общий «детектор выполнения», которого
> оба пункта ждали, построен там (один канал `hooks.tutor`, события из точек реального действия) и
> ПЕРЕИСПОЛЬЗУЕТСЯ: этому пункту остаётся МУЗЫКАЛЬНОЕ содержание (задания по ступеням/аккордам и их
> проверка), а не механизм. Урок добавляется записью в `LESSONS`, движок не трогается.

### ⬜ 3.7. Сравнение строёв
«Та же фигура в другом строе»: сыграть записанное в 12-TET, потом в 31-TET. Момент «ого», ради которого приложение существует. Зависит от 3.4.

### ⬜ 3.8. Показ отклонения в центах
`centsOf` уже написан, нужен вывод. Хорошо сочетается с §2 — таблица ошибок становится слышимой.

### ✅ 3.9. Заменить реверб — СДЕЛАНО (FDN)
Было: IR генерировался из случайного шума (`makeIR`) — самое слабое место звука, и хвост был ЗАПЕЧЁН в
буфер: живьём менялось только КОЛИЧЕСТВО посыла. **Заменён на FDN** (`buildFDN`+`REV_A` в `audio.js`):
четыре линии задержки, три входных аллпаса-диффузора, демпфирующий ФНЧ внутри петли, перемешивание
Хаусхолдера, стерео-отводы; `makeIR` и `ConvolverNode` удалены. Теперь ТРИ настоящих параметра реального
времени: подмес (посылы), длина (`setRevDecay`) и окраска (`setRevTone`). **Игроку пока отдан только
подмес** (глубина руки, как и раньше) — длина и окраска живут константами `REV_A` и ждут КОНСТРУКТОРА
ЭФФЕКТОВ, который их покажет; это был шаг 1 той работы. ⚠️ Q ФНЧ в петле обязан быть Баттервортом
(`REV_A.toneQ=−3.0103`): с дефолтным Q сеть самовозбуждалась свистом на ~3.9 кГц (разбор — в комментарии
у `setRevDecay` и в «Ключевых уроках» HANDOFF).

---

## 4. Баги

### ✅ Стартовый экран не исчезал
`#start{display:block}` (1-0-0) перебивал `.ov.on` (0-2-0). **Исправлено.**

### ✅ Подсветка при игре двумя руками в аккордах — ЗАКРЫТО ЗАЩЁЛКОЙ, как и планировалось
В `draw()` переменная `chAct` была скалярной, цикл по рукам её перезаписывал — побеждала последняя.
Закрылось само вместе с защёлкой (§3.1/§6): живой аккорд ровно один (владелец `'latch'`), подсветка
идёт от `latchDeg` (+ `loopChordDeg()` для аккорда петли). ⚠️ Переменной `chAct` в коде БОЛЬШЕ НЕТ —
искать её бесполезно.
Аргумент против двух аккордов сразу: лестница подписана `I`, `IV`, `Cmaj7` — два аккорда делают подпись ложью, назвать `I+V` одним именем нельзя. Учебный слой ломается. А защёлка освобождает вторую руку и делает вопрос беспредметным.

### ✅ `revDisp` не сбрасывается при отпускании соло-щипка — ИСПРАВЛЕНО
REV-бар замирал на последнем значении. Закрыто пер-кадровым сбросом: если НИ ОДНА рука не целится соло
(`soloNoteHand` — единый критерий и для прицела, и для сброса; в сплите знает роль-половину), `revDisp`
гасится в 0. Столбики FX при этом тоже не рисуются, так что стейла не остаётся.

### ✅ Сплит-экран: прицел `revDisp` ДО щипка — ВКЛЮЧЁН, развилка решена
Раньше предпросмотр был выключен под сплитом (обе ветки гейтнуты `!splitOn`), потому что было
неоднозначно, КАКАЯ рука целится в соло, когда ролей две. **Решено и выпущено:** критерий — `soloNoteHand`,
ЕДИНЫЙ и для прицела, и для сброса. Целится НОТНАЯ рука СОЛО-половины: половина берётся из
замороженного `S.role`, если рука щиплет, иначе — живая половина под указательным (`phoneHalves`), то есть
ровно так же, как её определяет отрисовка руки. Нет соло-половины (в паре нет `'ld'`) → никто не целится,
`revDisp` гасится в 0 и столбики FX не рисуются. ⚠️ Гейта `!splitOn` на этих ветках в коде БОЛЬШЕ НЕТ.

### ℹ️ Сплит-экран: ровно ДВЕ половины (не баг — область)
`phoneHalves(W)` и вся сплит-логика рассчитаны на пару ролей (левая|правая). Трёх- и более-ролевых
раскладок НЕТ. Если понадобится — отдельная задача (переработать `phoneHalves`, кнопки половин,
инвариант «разные роли», геометрию). Пока намеренно только 2.

### ✅ `clrBtn` / очистка во время записи — БОЛЬШЕ НЕ АКТУАЛЬНО
Пункт описывал модель записи ДО лупера (`recStart` и ручной отсчёт). Модель переписана
на лупер (`recorder.js`): `recStart` в коде НЕТ, а `clearRec()` уже зовёт
`setRecording(false)` (плюс `clearPump`, `softAllOff`, `droneOff`). Чистить нечего —
пункт снят, чтобы не сбивать с толку. (Имя `recStart` — в списке устаревших, см. CLAUDE.md.)

### ✅ `selTonic.onchange` не вызывает `softAllOff` — ИСПРАВЛЕНО
Чинилось вместе с защёлкой, как и намечалось. Сейчас: `selTonic.onchange` зовёт `softAllOff()`
(`ui.js`), звучащий аккорд глохнет и переатакуется в новой тональности.

### ✅ `qTriad`/`qSev` не вызывают `softAllOff` — ИСПРАВЛЕНО
То же: обе кнопки (`qTriad`/`qSev`) зовут `softAllOff()`, звучащее трезвучие не остаётся трезвучием
после переключения на септаккорды.

### ⬜ Идентификация рук: `key+=i`
При двух руках одной handedness ключ зависит от порядка в массиве результатов и может меняться между кадрами → редкая повисшая нота до watchdog.

### ✅ `refreshStyle(back.styleSel!=='auto')` затирает пользовательский BPM — НЕАКТУАЛЬНО
⚠️ Ни `refreshStyle`, ни `back.styleSel`, ни `back.bpm` в коде НЕ СУЩЕСТВУЮТ: это генеративный фон
`backing.js`, удалённый вместе со стилями (см. §3.3). Темп теперь один — `loop.bpm`, и его ставит
ТОЛЬКО пользователь (`setLoopBpm`), никакой автоподбор его не трогает. Бага не может быть.

### ✅ `chordGlide` при смене длины аккорда — ИСПРАВЛЕНО
Голосов было 3 (трезвучие), стало 4 (септаккорд) — четвёртый молча не звучал, т.к. `chordGlide` ведёт
лишь УЖЕ звучащие голоса. Сейчас смена ЧИСЛА НОТ форсирует ПЕРЕАТАКУ: в `gestures.js` и на свежем
щипке (`latchDeg<0 || ty.length!==latchLen → WchOn`), и в ведении (`dty.length!==latchLen → WchOn`),
иначе `WchSet` (глиссандо без переатаки). Комментарий у этого места прямо ссылается на этот пункт.

### ℹ️ Косметика: вводящий `tag` у части цент-ладов (НЕ баг, не срочно)
Мезотон, Натуральный ×2, Пифагоров, патеты, гамелан несут `tag:'penta'` (или иной) ЧИСТО потому,
что теги для этих ладов ИНЕРТНЫ — поведение гейтится свойствами (`cents`/`typedChords`/`gridChords`/
`period`/`noChords`), а `tag` ни один читатель на них не смотрит (`isTert`/`chordSteps`/draw проверяют
`'dia'/'ethnic'/'maqam'/'edo'`). Имя врёт глазу («пентатоника»?), но НИЧЕГО не ломает. Переименовать
когда-нибудь в честный инертный тег (напр. `'cents'`/`'temperament'`) — косметика, не поведение.

### ℹ️ Косметика: подписи корней аккордов = 12-TET имена нот у `edo:12` цент-ладов (НЕ баг)
`rootName()` для любого `edo===12` лада возвращает `NOTE_NAMES` (C/C#/D…). У цент-ладов
(Пифагоров/Натуральный/Мезотон) корень аккорда на РЕАЛЬНОЙ высоте строя, не 12-TET, поэтому имя
ноты — лишь ПРИБЛИЖЁННЫЙ якорь (какая ступень), а истинная высота видна в Гц ярлыка. Мелодическая
сетка у этих ладов уже показывает порядковый `Т,2,3…` (`gridNoteLbl`), а корни аккордов — нет
(общий `rootName` с chrom12). Разошлось намеренно: правка `rootName` рискует задеть chrom12.
Сделать одинаково (порядковый и для корней цент-ладов) — отдельный гейт `rootName` по `s.cents`.

---

## 5. Решено НЕ делать

- **Новые лады и тембры** ради количества. Их уже с избытком. (§2.2 — исключение: там лады решают конкретную задачу.)
- **MIDI-экспорт как ранняя фича.** Микротональность в обычный MIDI не влезает (только MPE).
- **React / Vue.** Рендерить нечего, всё на canvas.
- **Tone.js.** Движок написан и работает. Замена = месяц регрессий ради того же.
- **Сборщик.** Зависимостей нет, собирать нечего.

---

## 6. Спецификация: защёлка аккордов (LATCH) — ✅ ВЫПУЩЕНА, оставлена как история

⚠️ **Эта спецификация РЕАЛИЗОВАНА (см. §3.1) и местами УСТАРЕЛА — не читать как инструкцию:**
- «Правки по файлам» описывают `chAct` в `draw.js` и `softAllOff`-фиксы в `ui.js` — всё сделано, а
  переменной `chAct` в коде больше НЕТ (подсветка идёт от `latchDeg`);
- тождество защёлки в типизированных ладах теперь **(ступень + ТИП)**, а не только ступень, и тип
  морозится на атаке (`S.ty`) — таблицы «Поведение» это не учитывают;
- защёлка стала одной из ДВУХ функций аккордовой руки (`handFn.ch`: защёлка / удержание);
- п. 5 проверки («убрать руку — звучит») дополнен более сильным правилом: живой аккорд переживает и
  ЗАВОРОТ ПЕТЛИ (hard rule 20 в CLAUDE.md).

Ценность файла — «Ключевое решение: инертность» и «Известный риск» (дребезг на границе при стоп-щипке,
связанный с §2.3): рассуждение по-прежнему верно.

**Область: только колонка АККОРДОВ.** Соло и эффекты не трогаем.

### Поведение

| Событие | Что происходит |
|---|---|
| Соединил пальцы на ступени, ничего не защёлкнуто | Аккорд звучит. Рука ведёт: Y = ступень (глиссандо), X = громкость |
| Разъединил пальцы | **Аккорд продолжает звучать.** Ступень и громкость замирают |
| Соединил пальцы на **той же** ступени | **Аккорд выключается.** Рука инертна до размыкания |
| Соединил пальцы на **другой** ступени | Плавный переход (`chordGlide`, без переатаки). Старый снят |

**Защёлкнутый аккорд ровно один.** Любая рука им управляет; кто последним сделал щипок — тот и рулит.

### Ключевое решение: инертность

III защёлкнут, делаете стоп-щипок на III → звук выключился, но **пальцы всё ещё соединены**. Рука дрожит, палец уезжает на IV, аккорд следует за рукой (`chordGlide` каждый кадр) → IV включился. Выключить невозможно в принципе.

Поэтому: **после стоп-щипка рука ничего не играет, пока пальцы не разомкнуты.**

Глиссандо для обычного щипка сохраняется: соединил на III → звучит III → ведёшь к V не размыкая → плавно переходит → разомкнул → V защёлкнут.

### Состояние

- `latchDeg` — номер защёлкнутой ступени, `-1` если тишина
- `chOwner` — ключ руки, которая рулит (или `null`)
- `S.inert` — флаг на руке, ставится при стоп-щипке, снимается при размыкании
- **Владелец голосов — постоянный ключ `'latch'`**, не ключ руки

Последнее бесплатно решает watchdog: рука ушла из кадра → `endPinch` → голоса принадлежат `'latch'`, аккорд жив.

### Правки по файлам

| Файл | Что |
|---|---|
| `gestures.js` | Логика захвата: сравнение `deg` с `latchDeg`, ветка стоп / ветка переключения. Флаг `S.inert`. `endPinch` для зоны `'ch'` больше **не** зовёт `WchOff` |
| `audio.js` | Ничего. `chordOn/chordGlide/chordOff` уже принимают произвольный ключ |
| `draw.js` | `chAct` из `latchDeg`, а не из состояния рук. Подсветка горит всегда |
| `ui.js` | `selTonic.onchange`, `qTriad`/`qSev` → `softAllOff()` + сброс `latchDeg` |
| `recorder.js` | `softAllOff` обходит `Object.keys(chordHold)` → `'latch'` попадёт сам. Добавить `latchDeg = -1`. То же в `panic` |

### Проверка

1. Щипок → отпустил → **аккорд продолжает звучать**.
2. Щипок на той же ступени → **выключился**.
3. Щипок на другой → плавно переключился, наложения нет.
4. Стоп-щипок → не размыкая, поводить рукой → **тишина**.
5. Аккорд защёлкнут → **убрать руку из кадра** → продолжает звучать.
6. Защёлкнут → сменить тонику → **заглох** (не остался в старой тональности).
7. Защёлкнут → сменить лад → заглох.
8. Защёлкнут → panic → заглох, подсветка погасла.
9. Подсветка горит на защёлкнутом аккорде и без щипка.
10. Рука A защёлкнула III, рука B щипает V → V звучит, III снят.

### Известный риск

**Дребезг на границе при стоп-щипке.** III защёлкнут, щипаете у границы III/IV, шум даёт IV → вместо выключения переключились. `degHyst` при захвате не работает: там `S.deg = -1`, возвращается сырое значение.

Если промахи частые — применить `degHyst` при захвате с `prev = latchDeg` (уклон в пользу защёлкнутой ступени). Заранее не делать. **Связано с §2.3** — пиксельный пол гистерезиса поможет и здесь.
### ✅ Переработать меню — перегружено — СДЕЛАНО (поглощено §3.19 + §3.22)

⚠️ Пункт устарел ДВАЖДЫ и оставлен только как история. Во-первых, он уже выполнен: меню разнесено на
две панели по смыслу — **«Звукоряд»** (лад/тоника/эталон A4/септаккорды/тембры/«Функции рук») и
**«Лупер»** (такты/размер/BPM/квантизация/дробление доли/аранжировка), обе светлые и непрозрачные, в
одном месте, открытие одной прячет другую (§3.19 выплывающее меню, §3.22 компоновка — оба ✅).
Во-вторых, половина перечисленного больше не существует: **раздела «Фон» (стиль/BPM/громкость) нет**
вместе с `backing.js` (см. §3.3), **«смены рук» нет** — её заменила секция «Функции рук» (функция НА
РУКУ в пределах роли), тумблера ПК/Смартфон нет вместе с ПК-режимом.

Найти нужное было невозможно, а на телефоне список не помещался на экран.

Нужны разделы или вкладки. Черновая группировка:
- **Музыка**: лад, тоника, септаккорды
- **Звук**: тембры соло / аккордов / баса
- **Луп**: такты, квантизация, аранжировка (гармония / ритм / бас)
- **Фон**: стиль, BPM, громкость  ← вероятно исчезнет вместе с генеративным фоном
- **Устройство**: смена рук (режим ПК/Смартфон убран)

Делать ПОСЛЕ решения по генеративному фону (§3.3) — оно уберёт целый раздел.
### ✅ 3.10. Микрохроматика: богатая палитра аккордов — СДЕЛАНО (каталог ниже выпущен целиком)

**СДЕЛАНО.** Диагноз был верен («на всех ступенях одинаковый аккорд» — потому что тип был ЗАШИТ), и
предложенное решение выпущено ровно как написано: тип выбирает РУКА, палитрой по положению
(`typedChords:'edo19'`/`'edo31'` → наборы в `CHORD_FAM_SETS`), а не ладовая функция, которой у палитры
нет. Зашитые `chord`/`chord7` остались запасным путём для нетипизированного пути.

**Каталог ниже — не план, а ОПИСЬ ВЫПУЩЕННОГО:** в наборе `edo31` реально лежат «Мажор чистый»
`[0,10,18]`, «Домин.7 · 4:5:6:7» `[0,10,18,25]`, «Нейтральное · 11/9» `[0,9,18]`, «Субминор · 7/6»
`[0,7,18]` и ещё полтора десятка типов тремя колонками-семействами (мажор / минор / нейтр.-ум.),
у 19-TET — свой набор с чистейшим минором. Сверять при правках можно прямо по таблице.

Каталог типов, доступных в 31-TET и НЕВОЗМОЖНЫХ в 12-TET:
| Тип | Шаги (31-TET) | Центы | Чем уникален |
|---|---|---|---|
| Мажор чистый | 0·10·18 | 0·387·697 | терция +0.8¢, без биений |
| Минор чистый | 0·8·18 | 0·310·697 | |
| **Нейтральный** | 0·9·18 | 0·348·697 | ни мажор ни минор — звук вне оппозиции |
| **Домин.7 (4:5:6:7)** | 0·10·18·25 | 0·387·697·968 | натур. септима −1¢, НЕ тянет разрешаться |
| Мажор-maj7 | 0·10·18·28 | 0·387·697·1084 | |
| Минор7 чистый | 0·8·18·26 | 0·310·697·1006 | |
| **Субминор (7/6)** | 0·7·18·25 | 0·271·697·968 | терция между минором и «ещё меньше» |
| Увеличенный чистый | 0·10·20 | 0·387·774 | |

19-TET: чистейший минор (терция +0.2¢, 0·316·695) — лучший минор среди равномерных строёв.

Смысл: типы аккордов — это не просто джаз в хроматике, это ЕДИНСТВЕННЫЙ способ
услышать то, ради чего микротональные строи придуманы. Поднимает приоритет дизайна
жеста (палец выбирает семейство типа). Список типов зависит от строя: 12-TET —
привычные, 31-TET — плюс нейтральные/натуральные-септимы/субминор.

Зависит от: решения по жесту выбора типа (см. UI-MAP, роль «Аккорды» в хроматике).

### ✅ 3.11. Запись видео и звука — поделиться музыкой

**СДЕЛАНО** (`clip.js`, один движок на два вида): видео = `canvas.captureStream(30)` (кадр ХОЛСТА,
без HTML-накладок) + параллельный отвод звука с лимитера (`createRecordingTap`) в ОДИН `MediaStream`
→ один WebM с картинкой И звуком; аудио = только отвод. Развилки решены: (1) оба режима — 🎥 клип и
🎙 аудио, взаимоисключаемы; (2) захват ХОЛСТА (руки/сетка есть — это игра; HTML-хром исключён);
(3) запись в реальном времени (не рендер). Формат честный WebM/Opus (Safari может дать mp4); MP4 не
обещаем. Кнопки ведёт реальное состояние рекордера (`onClipChange`) — не залипают. Полоса лупера
прячется из кадра видео через `videoRec`.

<details><summary>Исходная спецификация (история)</summary>

Дать пользователю снять то, что он играет, и поделиться.

⚠️ Подводный камень, знать заранее: экран (canvas + камера) и звук (Web Audio) —
ДВА РАЗНЫХ потока, их надо СВЕСТИ в один файл. Технически:
- звук: MediaStreamDestination из AudioContext (уже есть контекст — подключить к нему)
- видео: canvas.captureStream()
- свести оба в MediaRecorder → webm
Три развилки, решить при подходе к задаче:
1. Только звук (проще, аудио-клип) или звук+видео (нагляднее, но тяжелее)?
2. Видео с камерой (видно руки — красиво для соцсетей) или только canvas (чисто)?
3. Запись в реальном времени (пишем пока играешь) или рендер лупа в файл
   (чище, но сложнее — надо «проиграть» события в буфер)?
На слабом телефоне (196мс инференс) одновременная запись видео может добить
производительность — проверить, не роняет ли захват экрана и без того низкий fps.

Формат: webm везде кроме iOS Safari (там нужен mp4/H.264 — отдельная головная боль).
</details>

### ⬜ 3.12. Больше строёв и больше тембров инструментов

Расширить, когда фундамент устоится. НЕ раньше перестройки меню в традиции (UI-MAP),
иначе новые строи некуда класть по-человечески.
- Строи: **турецкие макамы + прочие EDO — см. консолидированный §3.25** (последнее белое пятно карты).
  (Исторические темперации СДЕЛАНЫ ПОЛНОСТЬЮ: Пифагоров, Натуральный подвижный/фиксированный, Мезотон
  1/4 коммы + **велл-темперации Веркмайстер III / Валлотти / Кирнбергер III** — полка «Исторические
  темперации», все `fixedKey` кроме подвижного Натурального. Неоктавные Болен–Пирс/Карлос — сделаны.
  **Индийские раги — СДЕЛАНЫ как звукоряды**: традиция `indian`, сетка 22 шрути + 10 раг, цент-механизм,
  ярлыки сваром/шрути. Осталась только возможная ТЕМБРАЛЬНАЯ сторона ситара.)
- Тембры: расширить LEAD_INSTR / CHORD_INSTR / BASS_INSTR / DRUM_KITS. (Сильно расширены с прошлого
  снимка: **три пути синтеза** — субтрактив + FM + Карплюс–Стронг ворклет; наборы табла/дарбука/гамелан/
  тайко. Дальше — по конкретной культурной потребности, не ради количества.)

⚠️ Из раздела «решено НЕ делать»: добавлять строи и тембры РАДИ КОЛИЧЕСТВА —
самая приятная и бесполезная работа. Каждый новый — только если закрывает
конкретную музыкальную потребность, а не «пусть будет больше». Добавлять по одному
и слушать, а не списком из головы.

### ❓ 3.12a. Карплюс–Стронг в АККОРДАХ — отложено (нужен замер на телефоне)

**Что есть.** KS-щипок (`ks-worklet.js`) работает в СОЛО: банки Струна, Ситар, Уд (струна), Кото,
Сантур, Гитара, Пиццикато — параметрические вариации одной струны (`KS_BANKS` в `audio.js`).

**Почему не в аккордах СЕЙЧАС.** Аккордовый пул — 24 всегда-запущенных субтрактивных голоса
(`buildChordPool`). KS-голос — это `AudioWorkletProcessor` (посэмпловый JS в аудиопотоке). Перевести
аккорды на KS = держать несколько всегда-живых ворклет-узлов. Стоимость на слабых телефонах НЕИЗВЕСТНА,
а гадать нельзя (правило: неясная цена → отложить и записать, а не пихать наугад).

**Как делать, когда дойдём (вариант «а» — маленький выделенный KS-пул):**
- Отдельный пул на 4–6 KS-голосов (не 24): аккорды обычно 3–4 ноты, полифония триад покрывается.
- Аллокация/кража голосов как `cvAlloc` (по `tOn`), свой `chordHold`-словарь.
- Каждый голос — свой worklet-узел (freq/pluck), гейт громкости на выходе (как `g` у `cv`).
- НЕ трогать существующий субтрактивный пул: KS-аккорды — параллельная, отдельная ветка, выбираемая
  по `chIdx` (нужен флаг «это KS-инструмент» в `CHORD_INSTR`).
- Строй: та же дробно-задержечная точность, что в соло (аккорды по всем цент-строям обязаны звучать чисто).
- **Первый шаг перед реализацией — ЗАМЕРИТЬ** N всегда-живых ворклет-голосов на реальном телефоне
  (сколько тянет до заиканий), только потом выбирать размер пула.

**FM в аккордах — тот же подход, но дешевле.** FM-голос (`buildFMBank`) = 2 осц + gain (нативные узлы,
не ворклет), поэтому маленький выделенный FM-аккордовый пул (несущий+модулятор+modGain на голос, ~6
голосов) стоит меньше KS. Но 24-голосный пул на FM = 48+ осцилляторов — недёшево, поэтому тоже ОТДЕЛЬНЫЙ
маленький пул, не перевод общего. Даст аккорды-колокола/негармоничные грозди (гамелан). Огибающая индекса
печётся на атаке (как в соло). Порядок: KS-пул сначала (нужнее для щипковых культур), FM-пул — при
спросе на негармоничные аккорды. Оба — после замера стоимости ворклета/пулов на телефоне.

### ⬜ 3.13. Микшер слоёв лупера — громкость на канал

Отдельная громкость для каждого инструмента лупа: аккорды / бас / барабаны / дрон.

> **Предпосылка ВЫПОЛНЕНА:** гейн-стейджинг сделан (и урок из него записан в HANDOFF: заработал только
> после правки уровня ГОЛОСА, не одних шин). Значит запрет ниже снят — но снят и главный ПОВОД: чинить
> лимитером задушенный микс уже не надо. Прежде чем делать микшер, ПОИГРАТЬ и решить, нужен ли он ещё,
> и в каком виде (одна «громкость лупа» vs полные 4 канала) — вопрос ниже ровно об этом.

⚠️ Исходное условие (уже выполнено): ДЕЛАТЬ ТОЛЬКО ПОСЛЕ гейнстейджинга (правки уровней шин под лимитер).
Причина: жалоба «новый слой глушит старые» — это НЕ баланс, это лимитер прижимает
весь микс при перегрузе (bassBus=1.0 толкает сумму за threshold −6, ratio 20).
Сначала чиним лимитер уровнями шин — возможно, после этого слои перестанут душить
друг друга и микшер станет нужен только для художественного баланса, а не для
починки. Каким он должен быть — решать ПОСЛЕ, играя с исправленным звуком:
- может хватить одного «громкость лупа» (луп vs живое соло)
- может понадобиться полный микшер 4 канала
- зависит от того, что почувствуешь после гейнстейджинга

Дизайн (когда дойдём): ползунки в меню лупера, значения в state, применяются к
loopBus/шинам после initAudio (узлы существуют только после старта — hard rule).
Учесть перегруз меню — 4 ползунка это много, может вкладка «микс».

### ⬜ 3.14. A/B сравнение строёв (сравнение ВО ВРЕМЕНИ) — FUTURE, не начато

**Идея.** Два слота строя (A и B) + кнопка на экране; тап переключает ТЕКУЩИЙ строй между
слотами. Играешь один и тот же аккорд/фразу и слышишь разницу ПОДРЯД (пифагор ↔ натуральный:
большая терция 408¢ vs 386¢, разрыв 21.5¢).

**Зачем.** Главное открытие приложения — «один аккорд, разные миры» — сейчас требует лезть в
меню и искать лад руками. A/B делает его доступным за ОДИН ТАП; именно это стоит показывать
новым людям первым делом.

**Цена — небольшая.** `scaleIdx` уже глобальный; нужны два слота (два индекса), кнопка и
переключатель между ними. Работает в ОДНОРОЛЕВОМ режиме (он основной), ядро не трогает.

**Тонкость (записать явно):** сравнивать надо на ВЫДЕРЖАННОМ звуке — биение проявляется не
мгновенно, короткий щипок разницы не покажет — и на парах с ЗАМЕТНОЙ разницей. Пифагор ↔ 12-TET
= 8¢, почти неразличимо; пифагор ↔ натуральный = 21.5¢, слышно ясно. Плохая пара обесценит
фичу, поэтому в UI стоит подсказывать «слышимые» пары.

Родственно §3.7 (та же цель — сравнить строи ПОСЛЕДОВАТЕЛЬНО), но §3.7 переигрывает ЗАПИСАННУЮ
фигуру в другом строе, а это — переключение строя под ЖИВОЙ игрой. **НЕ путать с §3.15**
(одновременное звучание двух строёв — это про другое и это грязь, см. предупреждение там).

### ⬜ 3.15. Свой строй на каждую роль (полимодальная ЖИВАЯ игра) — FUTURE, не начато

**Идея.** В сплите у каждой половины СВОЙ строй; два инструмента звучат ОДНОВРЕМЕННО в разных
строях.

**Зачем.** Художественный эксперимент — живая полимодальность. Сейчас полимодальность есть
только в ЛУПЕРЕ (слой морозит свой `sc` и переигрывается в нём), живьём двух строёв сразу нет.

**Цена — ПРАВКА ЯДРА (большая).** `scaleIdx` перестаёт быть глобальным и становится «на роль»;
всё, что зовёт `CUR()`, должно узнать, ЧЕЙ строй считать. Это ровно та работа, что была с
резолвером регистров (`rectOctReg(role)`): один явный аргумент роли протаскивается через всех
читателей. Затрагивает `scales.js` / `gestures.js` / `draw.js` / `recorder.js`.

**ЧЕСТНОЕ ПРЕДУПРЕЖДЕНИЕ (не выкидывать при рефакторинге):** это **НЕ способ сравнить строи**.
Одновременное звучание двух разных строёв в БОЛЬШИНСТВЕ пар даёт грязь, а не разницу — высоты не
совпадают, ухо слышит один расстроенный инструмент, а не два строя. Сравнение — это §3.14
(ПОСЛЕДОВАТЕЛЬНО, один за другим), а не это. Ценность здесь чисто художественная (нарочные
битональные фактуры), а не учебная.

Не путать с полимодальностью ЛУПЕРА, которая уже работает и осмысленна именно потому, что слои
записаны как НАМЕРЕНИЕ (deg/oct + замороженный `sc`) и пере-настраиваются при воспроизведении —
там строи не «дерутся» вживую, а разнесены во времени петли.

### 🔵 3.16. Джем — музыкальное наполнение — МЕХАНИЗМ СДЕЛАН, наполнение — первый проход

**Механизм + первый проход СДЕЛАНЫ** (`🎵`, `ui.js`/`recorder.js`): цикл вариантов по свойствам лада,
метка `ev.jam`, слои лупера. Остаётся КУРАТОРСКОЕ наполнение (богаче варианты/ритмы/тембры по культуре) —
ниже. Оно во многом ждёт тембровую работу (§3.12).

> **Кнопка стала «Подложка» и раздвоилась:** тап с покоя открывает выбор пути — «Джем (вся подложка)» или
> «Только ударные»; дальше тап переключает вариант ВНУТРИ пути, как раньше. Список ударных фильтруется по
> РАЗМЕРУ петли, звучащий вариант НАЗВАН (кнопка + подсказка + тост — номер «4/10» из восемнадцати
> паттернов не говорит ничего). Обе ветки идут через ту же метку `ev.jam` — ОДНА метка, одна очистка,
> инвариант «записи игрока неприкосновенны» цел. **Материал для п.4 ниже теперь ЕСТЬ** (18 паттернов),
> но сам джем по-прежнему берёт из них два — кураторская привязка их пока не использует.

**Только про то, ЧТО джем ИГРАЕТ, не про механизм.** Механизм готов и под вопросом НЕ стоит:
цикл вариантов по СВОЙСТВАМ лада, метка слоёв `e.jam` (снять ровно джем, записи игрока целы),
джем — не режим, а обычные слои лупера. Всё это уже сделано и задокументировано — не переписывать.

**Честно:** выпущенный джем — ПЕРВЫЙ ПРОХОД, подобранный под «звучит приемлемо СРАЗУ», а не
кураторский музыкальный дизайн. Что требует работы:

1. **МАЛО ВАРИАНТОВ.** У большинства классов ладов всего два варианта плюс выкл (дрон с ударными /
   дрон без) — это тумблер, а не выбор. Семиступенчатые получают две прогрессии. Каждому классу
   нужен более богатый цикл. Растёт по мере появления материала (см. порядок ниже).

2. **ТЕМБРЫ НЕ УЧАСТВУЮТ.** Джем НИКОГДА не трогает выбор инструмента — играет тем, что стоит в
   меню строя. Поэтому макам может подпираться синтетическим лидом, а гамелан — обычным басом;
   культурного характера НЕТ. Будущий проход должен подбирать тембры на вариант/культуру. **Зависит
   от тембровой работы** (ситарный джавари, культурно-характерные банки и FM-цепь для негармоничных
   звуков — гамелан/колокол; см. §3.12 и связанные пункты о тембрах). Пока таких банков нет,
   подбирать нечего.

3. **ГРУБЫЙ ПОДБОР ПО ЛАДАМ.** Ветвление идёт по трём классам-свойствам, но ВНУТРИ класса каждый лад
   получает ОДИНАКОВУЮ подложку. Гамелан и рага Бхайрав сейчас получают один и тот же дрон+педаль;
   японские пентатоники — тот же бэкбит, что блюз. Это РАЗНЫЕ музыкальные миры, им нужны разные
   ритмический пульс и фактура. Нужна более тонкая привязка — но по-прежнему на СВОЙСТВАХ лада (или
   на новых свойствах, добавленных ради этого), НИКОГДА на именах/индексах (правило проекта). Скорее
   всего это значит ВВЕСТИ НОВОЕ СВОЙСТВО (напр. подсказку «культура/пульс» в записи лада), а не
   спецкейсить по scaleIdx.

4. **РИТМЫ — уже 18, но джем берёт из них ДВА.** В `RHYTHMS` теперь: прежние Рок (прямой)/Маqсум,
   паттерны нечётных размеров (Вальс/Дадра/Балканский 7/Кехерва/Балканский 9/Тинтал) и десять ходовых
   на 4/4 (Рок с подхватом, Поп, Фанк, Диско, Хип-хоп, Босса-нова, Самба, Регги, Марш, Шаффл — последний
   на ТРИОЛЬНОЙ сетке, `sub:3`). Их видно в «Только ударные» (кнопка 🎵 → выбор пути) и в списке ритмов
   панели, но **сам джем по-прежнему выбирает лишь `JR_BACK`/`JR_MAQSUM`** — материал появился, кураторская
   привязка (п. 3) его ещё не использует. Ближайший дешёвый шаг: развести варианты джема по культурам
   на уже существующих паттернах. Гамелану (котекан) и японским пентатоникам своих паттернов пока нет.

**Порядок зависимостей:** богатые ритмы (4) и тембры (2) должны появиться ПРЕЖДЕ, чем пер-ладовая
кураторская привязка (3) станет осмысленной; варианты (1) можно наращивать, как только есть из чего
выбирать. Итог: сначала материал (ритмы+тембры), потом тонкая привязка, а число вариантов растёт
параллельно материалу.

### ✅ 3.17. Запас кадра камеры (считываем шире, чем показываем) — СДЕЛАНО

**СДЕЛАНО** (`CAM_MARGIN=0.12` в config; `remapAxis`/`sx`/`sy` в state). Играем/показываем внутренние
~76% кадра, MediaPipe получает ПОЛНЫЙ кадр (`detectForVideo` не кропается) — рука на крайних
прямоугольниках ещё видна трекеру. Инвариант соблюдён: СЫРЫЕ координаты (0..1) ведут ВСЮ гест-математику
(щипок/размер ладони/Z-реверб/пороги), РЕМАПНУТЫЕ (`sx`/`sy`, единый источник для gestures и draw) —
позицию и попадание. Координаты и рисунок НЕ клампим — сатурируется только выбор. Видеофон кадрируется
тем же `CAM_MARGIN`, попадание и картинка из одной геометрии.

<details><summary>Исходная спецификация (история)</summary>

**Проблема.** Игровая сетка сейчас упирается в самые края кадра камеры. Поэтому в КРАЙНИХ
прямоугольниках рука уходит за границу кадра РАНЬШЕ, чем доходит до края экрана — MediaPipe теряет
пальцы, нота срывается там, где визуально ещё есть куда вести.

**Идея.** Развести ДВЕ области: ОБЛАСТЬ СЧИТЫВАНИЯ = весь кадр камеры; ОБЛАСТЬ ОТОБРАЖЕНИЯ/ИГРЫ =
внутренние ~70–80% кадра. Крайние ~10–15% по каждому краю остаются ЗА пределами показываемого:
визуально рука уже вышла за экран, но камера её ещё видит и трекинг не рвётся — есть «поле выбега».

**Цена.** Затронет отрисовку видеофона (рисуем/кропаем внутреннюю область, а не весь кадр) и
нормировку координат рук (нормировать x/y на область ОТОБРАЖЕНИЯ, а не на весь кадр).
⚠️ ГЛАВНОЕ — попадание (hit-test) и картинка обязаны считаться по ОДНОЙ И ТОЙ ЖЕ области, иначе
палец возьмёт не ту ячейку, что видит (тот же инвариант «вход и картинка из одной геометрии», что
проходит через весь проект — см. `flipX`/`phoneHalves`/`rectBandY`).
</details>

### 🔵 3.18. Локализация (i18n) — СДЕЛАНО для en+ru; es/de ждут проверяющего

**СДЕЛАНО** (`i18n.js`, 15-й модуль). Интерфейс по умолчанию АНГЛИЙСКИЙ + русский, переключатель на
стартовой карточке (§3.27 закрыт этим же), выбор персистится (`handsong.lang`) и переживает перезагрузку.
Зависимость от UI-волны соблюдена: переводили ПОСЛЕ §3.19/§3.21/§3.22, а не до.

**Как устроено (два разных механизма — не путать):**
- **Интерфейсный хром** (кнопки/подписи/подсказки/тосты/статус/ярлыки холста) — ключи в `DICTS.en`/
  `DICTS.ru`, через `t(key,params)`; статическая разметка через `data-i18n`/`data-i18n-title` + `applyI18n()`;
  всё, что строит JS, перерисовывается подписчиком `onLangChange`. Фолбэк: язык → en → умолчание вызова →
  **САМ КЛЮЧ** (пропущенный перевод ВИДЕН, а не молчит).
- **Музыкальные данные** (лады/традиции/группы/аккорды/тембры/аранжировка) — ВНУТРИ данных, через `L()`,
  ТРЕМЯ формами, и форма кодирует редакторское решение: голая строка = международная запись (`'SuperSaw'`,
  `'II–V–I'`), `{default, ru}` = ИМЯ СОБСТВЕННОЕ (транслитерация: `{default:'Tabla', ru:'Табла'}`), полный
  объект = ПЕРЕВОДИМОЕ слово (`{en:'Bell', ru:'Колокол'}`).

⚠️ **ОДНА ОПАСНОСТЬ, найденная и закрытая: отображаемое имя как ИДЕНТИФИКАТОР.** `demo.js` сопоставлял
сцены с ладами по `s.name`, а `fillScales` бакетил группы меню по ПОКАЗЫВАЕМОЙ подписи `grp` — оба тихо
сломались бы (сцены не нашли бы лад; одна группа развалилась бы на четыре по языку). Теперь стабильный
индекс (`SCENES[].idx`) и стабильный ключ (`grpKeyOf`). **Больше ничто не смеет опираться на отображаемое
имя** — это hard rule 25 в `CLAUDE.md`.

⚠️ **§3.2 «Английский интерфейс» ПОГЛОЩЁН этим пунктом** (ответ был: не замена, а i18n с en-дефолтом).

**ОСТАЛОСЬ: испанский и немецкий.** Механизм и формы имён рассчитаны на четыре языка, `LANGS` дописать —
одна строка. **Не включаем, пока терминологию некому проверить**: неверный музыкальный термин носитель
прочтёт как ошибку ПРИЛОЖЕНИЯ, а не как неточность перевода. Ждём ЧЕЛОВЕКА, а не времени.

### ✅ 3.19. Выплывающее меню вместо постоянной панели — СДЕЛАНО

**СДЕЛАНО** (`ui.js`: `revealBar`/`armBarHide`/`.min`). Верхняя полоска АВТО-СКРЫВАЕТСЯ через
`BAR_HIDE_MS`(3500мс) и раскрывается по тапу/движению мыши (порог `MOUSE_EPS`=8px, чтобы дрожание не
мигало); не прячется, пока открыта панель или зажат палец. Свёрнутая держит РОВНО то, что в спеке ниже:
индикатор роли(ей) + 🎥/🎙/● — **те же DOM-элементы** (не зеркальная полоса, единый источник состояния).

**Идея.** Панель разрослась (десять кнопок и растёт). Решение — не переупаковывать тесноту, а
СНЯТЬ её: меню ПРЯЧЕТСЯ и выплывает ПО ДЕЙСТВИЮ — тап по экрану на телефоне, движение мыши на
компьютере. Спряталось — и снова ушло.

**Что остаётся на экране, когда меню спрятано** (только это):
- значок текущего инструмента (или ДВУХ — если экран разделён сплитом);
- кнопка записи видео (🎥);
- кнопка записи аудио (🎙);
- кнопка записи лупа (●).

Всё прочее (лад/тоника/тембры/джем/камера/сплит/терменвокс/аранжировка…) — в выплывающем меню.

⚠️ **Конфликт-флаг: заменяет старый пункт «❓ Переработать меню — перегружено».** Тот предлагал
разделы/вкладки — то есть ПЕРЕУПАКОВКУ той же постоянной панели. Этот пункт решает тесноту В
ПРИНЦИПЕ (панели по умолчанию нет). Старый пункт считать поглощённым парой §3.19+§3.22; его
черновую группировку (Музыка/Звук/Луп/…) можно взять как содержимое выплывающего меню.

**Часть UI-волны** (§3.19 + §3.21 + §3.22 — один заход по интерфейсу).

### ✅ 3.20. Обучающий режим (пошаговый, с детектором выполнения) — СДЕЛАНО

**СДЕЛАНО** (`tutor.js`). Шесть уроков: Основы → Аккорды → Строи и тембры → Лупер → Функции рук → Две роли
сразу. Урок идёт ВНУТРИ живого приложения (камера, звук, сетка) — учатся, ДЕЛАЯ.

**«Детектор выполнения», которого ждал этот пункт, построен как ОДИН канал `hooks.tutor(kind,payload)`**,
и события шлются ИЗ ТОЧЕК РЕАЛЬНОГО ДЕЙСТВИЯ (метки `ЗАЦЕПКА ОБУЧЕНИЯ` в `gestures.js`/`ui.js`/
`recorder.js`), а не пересчитываются параллельно: событие возникает ⇔ действие произошло, поэтому
«сработало, а шаг не засчитался» невозможно по построению. ⚠️ Зацепки учат ТЕКУЩЕЙ жест-модели — правишь
жест, правь зацепку вместе с ним.

**Уроки и шаги — ДАННЫЕ** (`LESSONS`, `{key,gate,enter,done,final}`), цепочка полем `next`, финал — карточка
ВЫБОРА, не автопрыжок; `gate()` роняет шаги, бессмысленные в текущем ладу. Прогресс — тихая ✓ на урок
(localStorage), НЕ гейт. Добавить урок = добавить запись, движок не трогаем.

⚠️ **ГЛАВНЫЙ УРОК, оплаченный багами:** setup урока обязан привести приложение в состояние, где ЕГО шаги
выполнимы, ЧТО БЫ ни делал предыдущий урок. Закрытые протечки: функции рук текли в «Основы» и «Аккорды»
(аккорд-рука на «удержании» — шаг защёлки не мог сработать); джем/петля текли в следующий урок цепочки;
«Лупер» после «Строёв» попадал на бесаккордовый Пелог и просил роль, которая молчит.

**Не переехало (осталось):** демо строёв (`demo.js`) и мини-учебник (`#helpOv`) — оба ЖИВЫ в коде, но из
UI недостижимы; им место внутри обучения. §3.6 «Режим урока» (МУЗЫКАЛЬНАЯ теория — «сыграй I–IV–V–I»)
остаётся открытым: механизм-детектор для него теперь ГОТОВ и переиспользуется.

<details><summary>Исходная спецификация (история)</summary>

**Идея.** Отдельная кнопка «Обучение» на начальном экране (§3.21). Проводит человека по всем
основным функциям ПОШАГОВО, дожидаясь ВЫПОЛНЕНИЯ каждого действия перед переходом к следующему:
- соедини такие-то пальцы → зазвучит нота;
- веди вверх/вниз → выше/ниже по ступеням;
- смени пальцы → октава;
- затем эффекты (левая рука);
- затем аккорды и выбор ТИПА аккорда по палитре;
- и так по всем ключевым возможностям.

**Ключевое требование — «детектор выполнения».** Обучение должно ВИДЕТЬ, что человек сделал жест
(подписка на жестовый слой — щипок/ступень/зона), а не просто показывать текст со счётчиком и
таймером. Без этого детектора это не обучение, а слайд-шоу.

⚠️ **Связь с §3.6 «Режим урока».** §3.6 — про МУЗЫКАЛЬНУЮ теорию (подсветить следующую ступень,
задание «сыграй I–IV–V–I», проверка попадания). Этот пункт — про ОНБОРДИНГ по интерфейсу
(«как вообще играть»). Разные задачи, но обе требуют ОДНОГО механизма — детектора выполнения жеста;
делать детектор один раз, общим. Сюда же переезжает весь объясняющий текст со старого стартового
экрана (§3.21).

⚠️ **ЗАВИСИМОСТЬ:** нужен «детектор выполнения» (хуки завершения жеста из жестового слоя). Идёт
после UI-волны, но до i18n (§3.18) — иначе обучающий текст переведём до того, как он устоялся.

</details>

### ✅ 3.21. Переработка стартового экрана — СДЕЛАНО

**СДЕЛАНО.** Светлая карточка, две кнопки: «Играть» (прежний поток) и «Обучение» (→ §3.20, пока
показывает «скоро»). Подвал: заметка о первой версии, ссылка отзыва, слот доната (скрыт, пока URL
пуст), e-mail собран в JS (не `mailto` в исходнике). Длинный объясняющий текст уехал в `#helpOv`
(ВРЕМЕННО недостижим из UI — вернётся внутри обучения, §3.20).

<details><summary>Исходная спецификация (история)</summary>

**Сейчас.** Отдельный чёрный экран с длинным объясняющим текстом до входа в приложение.

**Стало.** Человек сразу попадает на НАЧАЛЬНЫЙ экран приложения с кнопками:
- «Обучение» (→ §3.20);
- «Играть»;
- внизу — ссылка на донаты и обратную связь + пометка: «первая версия, возможны ошибки, будем
  благодарны за сообщения».

Весь нынешний объясняющий текст переезжает в раздел обучения (§3.20), на стартовом экране его нет.

**Часть UI-волны** (§3.19 + §3.21 + §3.22).
</details>

### ✅ 3.22. Переработка меню — компоновка и подача — СДЕЛАНО (первый заход)

**СДЕЛАНО** (светлая тема панелей + чистка): обе панели (звукоряд, лупер) стали СВЕТЛЫМИ и
НЕПРОЗРАЧНЫМИ (лежат поверх яркой камеры — прозрачность размывала текст), в одном месте сверху
(взаимоисключаемы). Панель звукоряда урезана (убраны абзац-пояснение и раздел справки — осталась одна
⇄); панель лупера переехала на место звукоряда, влезает без прокрутки, подсказки срезаны до одной
строки. Функции не тронуты (все id/поведение целы). Дальнейшая тонкая компоновка — по мере надобности.

<details><summary>Исходная спецификация (история)</summary>

**Идея.** Сохранить ВСЕ нынешние функции меню, но сделать структуру и вид удобнее: сейчас меню
громоздкое и неудачное по дизайну. Это задача про КОМПОНОВКУ и ПОДАЧУ, НЕ про функции — ничего не
выкидываем, ничего не добавляем по возможностям.

⚠️ **Конфликт-флаг:** вместе с §3.19 заменяет старый «❓ Переработать меню — перегружено»
(см. флаг в §3.19). §3.19 решает, ЧТО на экране и как меню вызывается; этот пункт — как устроено
СОДЕРЖИМОЕ меню внутри. Делать в связке.

**Часть UI-волны** (§3.19 + §3.21 + §3.22 — один заход). Порядок всей группы задач:
**сначала UI-волна (3.19/3.21/3.22) → потом обучение (3.20, нужен детектор жеста) → потом i18n
(3.18, переводим уже устоявшиеся тексты).**
</details>

### ✅ 3.23. Размеры такта кроме 4/4 — СДЕЛАНО (переменный размер + акценты по группам + паттерны)

**Идея.** Сейчас такт жёстко 4/4: `BEATS_PER_BAR=4` (константа в `config.js`), читается в нескольких
местах — сетка записи и квантизации (`recorder.js`), рисунок полосы лупера (`draw.js`), генерация
аранжировки (`arrange.js`). Поддержать 3/4, 5/8, 7/8 и т.п. РАСШИРИЛО бы ритмическую палитру —
индийские циклы (тала), балканские размеры, вальс.

**Цена/область.** `BEATS_PER_BAR` придётся сделать переменной (состояние + сеттер, правило #6), а не
константой; свериться со ВСЕМИ читателями (квантизация к долям/восьмым/шестнадцатым, заворот петли,
count-in, сетка тактов в `drawLooper`, шаг ритма в `buildArrangement`). Скорее всего нужна и доля-
знаменатель (5/**8**), не только число долей. Хорошо ложится к §3.16 (джем: талы/котекан требуют не-4/4)
и к индийской традиции. Спецификация — при подходе (какие размеры, где выбор в UI, как это дружит с
существующими прогрессиями на 4 доли).

**РЕАЛИЗОВАНО (эта сессия), НЕ проверено в браузере.** `BEATS_PER_BAR` остался ДЕФОЛТОМ (сеет
`loop.metre`), живой размер — переменная `loop.metre` (recorder); сеттер `setLoopMetre(n)` гейтит
пустую петлю (как `setLoopBars`), клампит к набору `METRES=[2,3,4,5,6,7,8,9,10,12,16]`. Все четыре
чтения переведены на `loop.metre` (`loopBeats`, акцент метронома, count-in ×2 — count-in = ОДИН такт
при любом размере). `arrange`: каждый `RHYTHMS`-паттерн объявляет `beats`, `stepBeat` считается из
паттерна, несовместимый размер пропускается. `draw`: `total=bars*loop.metre`, граница `b%loop.metre`.
UI: дропдаун «Размер (долей)» в `#panelLoop`, сереет на непустой/играющей петле. **Знаменатель НЕ
нужен** (вопреки заметке выше): движок считает в ДОЛЯХ при BPM, 7/8 vs 7/4 — только скорость доли,
её задаёт BPM; для тал/балкана хватает числителя.

**ДОДЕЛАНО (следующие раунды):** (а) **акценты по группам** — `METRE_GROUPS` (7=3+2+2, 9=2+2+2+3, 16=4×4)
+ `METRE_KHALI` (16→3-й вибхаг) → `beatLevel` даёт 4 уровня метронома (2/1/0/−1 khali) И сетку тактов;
khali = ОСЛАБЛЕННАЯ голова, не тишина. (б) **паттерны нечётных размеров** в `RHYTHMS`: Вальс(3)/Дадра(6)/
Балканский 7/Кехерва(8)/Балканский 9/Тинтал(16), каждый со своим `beats`; несовместимый размер
пропускается. **Осталось только:** пользовательский выбор группировки (3+2+2 vs 2+2+3) — сейчас один
разумный вариант на размер (мелочь). Полиритмия (размер НА СЛОЙ) — отдельная фича §3.24.

### ⬜ 3.24. Полиритмия — свой размер у каждого слоя (future, не начато)

**ЭТО ДРУГАЯ ФИЧА, НЕ переменный размер (§3.23).** §3.23 даёт ОДИН размер на всю петлю — все слои в
общей сетке из N долей. Полиритм требует размер НА СЛОЙ. Здесь записан разбор, чтобы будущая сессия
не выводила его заново.

**Суть.** Африканская (и не только) полиритмия — это когда два и более цикла РАЗНОЙ ДЛИНЫ идут
ОДНОВРЕМЕННО и совпадают лишь через несколько оборотов. Классика: 3 против 2, или 12/8 против 4/4 —
одна партия делит цикл на три, другая на четыре, сходятся раз в двенадцать долей. Общий пульс рождается
из НАЛОЖЕНИЯ, а не задан сверху.

**Чем отличается от уже сделанного.** Переменный размер (`loop.metre`) — один размер на всю петлю, все
слои живут в общей сетке из N долей. Полиритм требует размер НА СЛОЙ (`layer.metre`).

**Что придётся тронуть** (то же ядро, что правил переменный размер):
- **Длина петли.** Сейчас `loopBeats = bars × metre`, одна на всех. При полиритме круг петли —
  НАИМЕНЬШЕЕ ОБЩЕЕ КРАТНОЕ длин слоёв (слой в 3 доли + слой в 4 доли → общий круг 12).
- **Размер как свойство слоя.** Там, где сейчас `loop.metre`, появится `layer.metre` со значением по
  умолчанию из `loop.metre`.
- **Квантизация.** Каждый слой равняется к СВОЕЙ сетке, а не к общей.
- **Отрисовка.** Как показать две разные сетки одновременно — открытый вопрос дизайна (наложить?
  рисовать сетку активного слоя? отдельные дорожки?).
- **Обёртка по времени** и вся арифметика позиции события.

**Открытый вопрос дизайна (решить ДО реализации).** Как человек это ИГРАЕТ? Записал слой в 4, переключил
размер на 3, записал второй — и они пошли параллельно? Тогда смену размера придётся разрешить МЕЖДУ
слоями, а не только на пустой петле — то есть правило, заложенное сейчас в §3.23 («длину и размер меняют
только на пустой петле»), придётся пересмотреть именно для размера.

**Почему стоит делать.** Африканская полиритмия — целый музыкальный мир, которого нет ни в одном
браузерном инструменте; для проекта, который уже даёт 75 строёв, это ритмический аналог той же идеи
(показать, что музыка устроена по-разному в разных культурах). Плюс питает джем (§3.16) и барабанные
рисунки.

**Порядок.** Делать ПОСЛЕ того, как переменный размер (одна сетка на петлю, §3.23) будет проверен и
устоится — полиритм ложится на него естественно, а не вместо него.

### ⬜ 3.25. Строи, которые ещё хотим добавить — консолидированный список (future, не начато)

Сводка того, что ещё осмысленно доложить в карту строёв, с честной приоритизацией. Отменяет
разрозненные упоминания «других EDO / других макамов» в §3.12 — они собраны и оценены здесь.
Правило проекта в силе: лад добавляем, только если он закрывает КОНКРЕТНУЮ музыкальную потребность,
а не ради количества (см. §5), и только по надёжным источникам.

**1. ТУРЕЦКИЕ МАКАМЫ — главный оставшийся пробел.** Турецкая традиция делит целый тон на ДЕВЯТЬ
комм и строится ИНАЧЕ, чем арабская четвертитоновая (24-TET), которая у нас уже есть. Ушшак, Раст,
Хиджазкар, Сегях, Хюсейни и другие звучат НЕ так, как их арабские тёзки — это разные интонации, а не
переименование. Обычно моделируется через **53-EDO** (комма ≈ 22.6¢). Нужно **8–12 макамов + базовая
сетка**. ⚠️ ВАЖНО при реализации: проверить по источникам, расходится ли теория
Арел-Эзгы-Уздилмена (учебный канон) с ЖИВОЙ исполнительской практикой (реальные интонации перде часто
отклоняются от 53-EDO модели), и пометить ЧЕСТНО — ровно как мы сделали со шрути (две конвенции
привязки имён, выбранная зафиксирована в исходнике, отвергнутая описана). Это заполняет
последнее крупное белое пятно микротональной карты.

**2. ПРОЧИЕ EDO (34, 41, 53).** Дёшево (только данные — `edo`/`iv`), но музыкального открытия мало:
это КОЛЛЕКЦИОНИРОВАНИЕ, а не новая потребность. Исключение — **53-EDO осмысленен В СВЯЗКЕ с
турецкими макамами** (п. 1): там он несёт традицию, а не сам себя. Отдельно от макамов брать 53/41/34
незачем.

**3. РАСШИРЕНИЕ СУЩЕСТВУЮЩИХ ТРАДИЦИЙ** (больше раг, больше арабских макамов, больше японских ладов).
Данные, быстро, ложится в готовые традиции — но ГЛУБИНЫ не добавляет: механизмы уже есть, это ещё
записи того же рода. Делать по запросу/по одному, слушая, а не списком.

**4. НЕ БРАТЬ без надёжных источников.** Тайская 7-EDO, бирманские, африканские настройки — данные
СПОРНЫЕ или плохо документированы, есть реальный риск выдать ДОМЫСЕЛ за традицию (для проекта с
учебным слоём это хуже, чем пробел). Спектральные строи (по обертонам конкретного инструмента) — это
скорее задача ТЕМБРА, чем лада, и живёт в §3.12/§3.12a, а не здесь.

**Карта после турецких макамов — по сути ПОЛНАЯ.** Равномерные EDO, арабские И турецкие
микротональные макамы, исторические темперации от Пифагора до велл-темпераций, индийские шрути и
раги, гамелан, японские лады, неоктавные (Болен–Пирс/Карлос), Партч. Дальше — не белые пятна, а
углубление уже покрытого (тембры §3.12, кураторский джем §3.16), не новые семейства строёв.

### 🔵 3.26. Фиксированный строй: тоника = КЛЮЧ (§1–5 СДЕЛАНО, хвосты — ниже)

**СДЕЛАНО (эта сессия), НЕ проверено в браузере.** Свойство `fixedKey:true` на шести исторических
строях (Пифагоров, Натуральный-фиксированный, Мезотон, Веркмайстер, Валлотти, Кирнбергер): сетка
приколочена к C (якорь `cFix()` от ЕДИНОЙ опоры, что и `baseF()` — **регулируемая опора 380–480 с тех пор
ВЫШЛА** (`aRef`, hard rule 17), и подвижные с фиксированными действительно едут вместе), тоника — КЛЮЧ-индекс в сетку, а не
множитель. Правки: `leadFreq`/`bassFreq` (fixed-ветка `cFix·2^(cents[tonic+шаг])`), `chordFreqs`
ветка 1 (индекс `tonic+deg+off`) и ветка 2 (корень Пифагора на сетке, гейт на `fixedKey`, НЕ на
`cents` — подвижный Натуральный цел), `centsOf` (интервал над КЛЮЧОМ), дрон через `tonicFreq()`,
`gridNoteLbl` → имена нот у fixed-строёв. Разметки «волк» в UI НЕТ (учит ухо). Подвижный Натуральный
и все мировые/EDO/BP/Карлос/Партч/раги — не тронуты.

**ХВОСТЫ (осознанно отложены):**
1. **Заморозка тоники per-event (§6 плана).** Сейчас тоника ЖИВАЯ: смена ключа в меню перекрашивает
   играющий fixed-слой лупера (и нельзя держать слой в C, играя вживую в F#). Чтобы слой хранил СВОЙ
   ключ (полимодальность окраски), нужно морозить тонику в событии — меняет схему события и три
   сигнатуры (`leadFreq`/`bassFreq`/`chordFreqs`), как заморозка `sc`. **Решение — ПОСЛЕ теста ядра.**
2. **Аккорды Пифагора — грид-версия (опционально).** Сейчас Пифагор держит ЧИСТО-отношенческие аккорды
   (ветка 2): мелодический урок (резкая терция 81/64 в дальних ключах) слышен, но в САМИХ аккордах
   окраски ключа нет (аккорд чист от любого корня). Перевести на `gridChords`+`natfix` (как прочие
   клавиши) → окраска ключа и в аккордах, ЦЕНОЙ документированной чисто-пифагоровой лексики и осиротев-
   шего набора `'pyth'`. Развилка, не срочно.
3. **Квинта дрона `1.498`** у fixed-строёв держится ~чистой, а не берётся из грид-сетки строя (могла
   бы быть волк-квинтой). Косметика дрона, помечено в `audio.js`.
4. **Симпатические струны (KS)** следуют `baseF()` (12-TET тоника), не `tonicFreq()` — низкий
   приоритет, тембровый резонатор.

### ✅ 3.27. Кнопка ЯЗЫКА на стартовом экране — СДЕЛАНО

**СДЕЛАНО** вместе с §3.18: `#langSel` — тихая строка ВВЕРХУ подвала стартовой карточки (не спорит с двумя
главными кнопками), опции — ЭНДОНИМЫ (самоназвания: English · Русский). Дефолт en; на свежем профиле язык
УГАДЫВАЕТСЯ из браузера, дальше — сохранённый выбор (`handsong.lang`, переживает перезагрузку). Смена
работает БЕЗ перезагрузки: `applyI18n()` перерисовывает разметку, подписчики — динамику, холст локализуется
следующим кадром сам (`t()`/`L()` читаются на кадр). Испанский/немецкий появятся в списке, когда войдут
в `LANGS` (см. §3.18).

### ⬜ 3.28. Справка по ладу за «?» рядом с выбором лада — ПО ГРУППАМ, после i18n (future, не начато)

**Идея.** Значок «?» рядом с селектором лада открывает короткую статью: что это за строй/лад, откуда,
чем звучит, на что обратить внимание. Учебный слой прямо у выбора, без отдельного экрана.

**ЧЕСТНЫЙ ОБЪЁМ — писать ПО ГРУППАМ, не по ладу.** 75 ладов × 4 языка = 300 текстов, если писать на
КАЖДЫЙ лад — это невыполнимо и избыточно. Решение: одна статья на ГРУППУ/семейство. Одна статья
покрывает шесть исторических темпераций (в чём разница строёв тех же 12 нот), одна — все раги, одна —
макамы, одна — гамелан/патеты, и т.д. Привязка к группе — по `grp`/`trad` (свойство, не индекс). Лады
одной группы делят статью; где нужна деталь на лад (напр. «чем Кафи ≠ Мальхар») — короткая приписка.

⚠️ **ЗАВИСИМОСТЬ (жёсткая): ПОСЛЕ i18n (§3.18).** Тексты справки — самый большой прирост user-facing
контента после обучающего оверлея; написать их ДО i18n = написать один раз и перевести дважды. Сначала
устоявшийся словарь (en-база), потом статьи по группам сразу в словарь. До тех пор — не начинать текст.
Родственно обучению (§3.20), но это СПРАВКА по требованию у селектора, а не пошаговый туториал.

### ⬜ 3.29. Конструктор жестов — свободное назначение параметров звука (future, не начато)

**Суть.** Сейчас рука выразительности (функция `'expr'`) имеет ОДНУ раскладку, заданную НАМИ: скорость
движения ведёт живость, раскрытость ладони — вау-фильтр, веер пальцев — характер искажения, наклон —
эхо. Человек изменить её не может. Конструктор даёт полную свободу: пользователь САМ решает, какое
положение руки каким параметром звука управляет.

⚠️ **НЕ ПУТАТЬ С §3.39 «Конструктор ЭФФЕКТОВ» и НЕ СЛИВАТЬ ИХ В ОДИН.** Это ДВА РАЗНЫХ СПОСОБА ИГРАТЬ,
а не два интерфейса к одному механизму:
- **ЭТОТ пункт (жесты/выразительность) — НЕПРЕРЫВНОЕ управление, «смычок».** Ничего не выбираешь и не
  включаешь: рука просто живёт в кадре, а звук откликается на ЕЁ СОСТОЯНИЕ (скорость, раскрытость,
  наклон, размах). Нет ни щипка, ни «взял ручку». Для ИГРЫ — во время фразы.
- **§3.39 (эффекты) — ДИСКРЕТНОЕ управление, «пульт».** ПАЛЕЦ выбирает, ЧТО менять, КООРДИНАТА задаёт,
  НАСКОЛЬКО. Действие явное и с границами: соединил пальцы — взял ручку, развёл — отпустил. Для точной
  НАСТРОЙКИ — между фразами.
- Музыкант делает и то и другое, но в РАЗНЫЕ моменты: крутит ручки между фразами и ведёт смычком во
  время неё. Нужны ОБА, и объединение их в «один конструктор» разрушит именно эту разницу.
- ⛔ **ОБЩЕЕ ОГРАНИЧЕНИЕ, из которого не выпрыгнуть: РУК ДВЕ.** Если одна играет ноты, вторая может быть
  ЛИБО на эффектах, ЛИБО на выразительности — но не на обоих сразу (`handFn` — одна функция на руку).
  Значит человек ВЫБИРАЕТ способ, и оба конструктора обязаны быть хороши поодиночке, а не в паре.
- Полезное следствие для обоих: п.1 «что важно учесть» ниже (отдельные пальцы годятся как ПЕРЕКЛЮЧАТЕЛЬ,
  но не как плавная величина) — это ровно та причина, по которой в §3.39 палец ВЫБИРАЕТ, а величину
  ведёт координата. Одно наблюдение, два разных вывода.

**Где живёт.** Подменю в разделе «Функции рук» — кнопка, открывающая конструктор (сам раздел уже есть,
`renderHandFn`).

**ОСИ (то, чем управляют).** Контрастные пары «максимум-минимум», надёжно читаемые камерой: раскрытость
ладони (кулак ↔ раскрыта), разведение пальцев (щепоть ↔ веер), наклон кисти, поворот ладони (к себе ↔
от себя), скорость движения, удаление от камеры, высота руки. Примерно шесть-семь осей. ⚠️ Часть из них
УЖЕ занята нотой и ревербом — при реализации решить, освобождаются ли они в этом режиме.

**ПАРАМЕТРЫ (то, чем управляется).** Всё доступное: эффекты (реверб, делей, вибрато, драйв, тремоло),
свойства синтеза (яркость фильтра, добротность, атака, расстройка, индекс FM, затухание петли
Карплюса), громкость, пространство. Список обязан РАСШИРЯТЬСЯ: параметры, которых ещё нет, должны
добавляться в конструктор без переписывания механизма.

**Что важно учесть при реализации:**

1. **ОТДЕЛЬНЫЕ ПАЛЬЦЫ НЕ ГОДЯТСЯ КАК ПЛАВНАЯ ВЕЛИЧИНА.** MediaPipe надёжно различает только явно
   согнутый и явно разогнутый палец, промежуточные положения шумят. На палец можно повесить
   ПЕРЕКЛЮЧАТЕЛЬ, но не непрерывный параметр.
2. **НЕ ВСЯКАЯ ОСЬ ГОДИТСЯ ПОД ЛЮБОЙ ПАРАМЕТР.** Медленные устойчивые положения (раскрытость, наклон,
   поворот) хороши для плавного — тембр, пространство, ширина. Быстрые (скорость движения) — для
   мгновенного: громкость, акценты. Повесить громкость на наклон ладони — инструмент станет вялым, и
   человек решит, что приложение плохое. Конструктор должен ПОДСКАЗЫВАТЬ пригодность (какие оси
   быстрые, какие плавные), а не молча позволять любую комбинацию.
3. **ВСЕ ОСИ НОРМИРУЮТСЯ НА РАЗМЕР КИСТИ** (иначе всё зависит от расстояния до камеры) и проходят
   МЁРТВУЮ ЗОНУ — иначе шум распознавания читается как управление. Для руки выразительности это уже
   сделано (`exprFeatures`) — механизм переиспользуется, а не пишется заново.
4. **ИНТЕРФЕЙС БУДЕТ БОЛЬШИМ:** матрица «семь осей × десяток параметров». Панели мы сознательно
   ужимали (§3.22), так что компоновку думать ОТДЕЛЬНО — возможно, готовые раскладки как отправная
   точка плюс правка отдельных назначений, а не пустая матрица.
5. **ЖИВОСТЬ ДАЁТ НЕ ЧИСЛО ПАРАМЕТРОВ, А ВНУТРЕННЯЯ ДИНАМИКА** (подкачка с затуханием, инерция,
   пружина). Конструктор назначает ВХОДЫ; средний слой с его физикой должен ОСТАТЬСЯ — иначе выйдет
   пульт с ручками, а не инструмент.

**ПОРЯДОК.** Делать ПОСЛЕ того, как нынешняя рука выразительности устоится и обрастёт отложенным
(акценты от рывка, микро-движение, выход на параметры FM и Карплюса): конструктор ляжет на ГОТОВЫЙ
набор осей и параметров, а не будет строиться в пустоте.
### ⬜ 3.30. Свинг как ОТНОШЕНИЕ (не сетка) — follow-up к триольному дроблению (future, не начато)

**Предпосылка СДЕЛАНА.** Паттерн ритма теперь объявляет `sub` (ячеек на долю: 4 = шестнадцатые, 3 =
триоли), `stepBeat = 1/sub` — единственный источник шага; квантизация живых ударов идёт по `loop.sub`
(явная настройка в панели лупера, не вывод из последнего загруженного паттерна). На триольной сетке
написан «Шаффл». `sub` — ПРЕДПОСЫЛКА свинга: без дробления доли о нём говорить нечего.

**ЭТО ДРУГОЙ МЕХАНИЗМ, НЕ ТРИОЛЬНАЯ СЕТКА.** Триольная сетка — это СЕТКА: ячейки стоят на 0, ⅓, ⅔
доли, и удар пишется прямо на них. Свинг — это ПРЕОБРАЗОВАНИЕ: сетка остаётся четырёхдольной, а каждая
вторая восьмая СДВИГАЕТСЯ на долю расстояния (50% — ровно, 66.7% — ровно триоль, и то и другое сетка
уже умеет).

**Зачем тогда он нужен.** Полезный диапазон джазового и поп-свинга — **55–60%**, и он не выражается
НИКАКОЙ сеткой: это не 1/2 и не 2/3, а число между ними. Именно там живёт разница между «ровно»,
«чуть качает» и «шаффл». Сеткой мы честно закрыли крайнюю точку (66.7%), середину — нет.

**Условие корректности (главное).** Свинг обязан лечь СРАЗУ на три места, иначе получится рассинхрон,
который слышно немедленно:
1. **ГЕНЕРАЦИЯ** — `buildArrangement` сдвигает времена ячеек паттерна;
2. **КВАНТИЗАЦИЯ** — `push`/`gridFor` снапят живой удар к СВИНГОВАННОЙ, а не ровной сетке;
3. **ЖИВАЯ ИГРА/переигровка** — уже записанные события тоже читаются со свингом (или свинг
   замораживается в событии, как лад).
Если сделать только (1) — ударные закачают, а руки останутся ровными, и человек услышит не свинг, а
расхождение. Это и есть причина не делать свинг «по-быстрому» одной строкой в генерации.

**Открытые вопросы к спецификации.** Свинг — свойство ПЕТЛИ (как темп) или ПАТТЕРНА (как `beats`/`sub`)?
Скорее петли: он про исполнение, а не про рисунок. На каком уровне дробления он действует (восьмые или
шестнадцатые)? Замораживать ли значение в событии ради полимодальности (§3.4)? Как показывать
свингованную сетку в полосе лупера (сейчас `drawLooper` рисует дробление ровными штрихами при
`loop.sub!==4`).

### ⬜ 3.31. Промо-ролик — приложение опубликовано, но в ДВИЖЕНИИ его никто не видел (не начато)

**Суть.** Handsong выложен и умеет объяснить себя сам (§3.20 обучение) — но **поделиться нечем**. Текстом
«жестовый синтезатор с 75 строями» никого не убедить: весь смысл в том, КАК рука ведёт звук, а это видно
только в движении. Нужна короткая запись реальной игры.

**Инструменты УЖЕ в приложении, новых фич не требуется:** `🎥` пишет ХОЛСТ + звук в один файл (HTML-хром
в кадр не попадает — панели/кнопки исключены по построению), `⛶` убирает полосы браузера, полоса лупера
прячется из кадра сама во время видеозаписи (`videoRec`). То есть материал снимается изнутри приложения.

**Что показать (черновой список, не сценарий):** рука ведёт ноту и видно, как она ведёт → тот же жест в
ДРУГОМ строе (гамелан/Веркмайстер — момент «ого», ради которого проект существует) → лупер: слой за слоем
из ничего → сплит, две роли сразу. Формат честный: WebM (Safari может дать mp4), не обещаем MP4/MP3.

⚠️ **Это работа про ДОВЕДЕНИЕ ДО ЛЮДЕЙ, а не про код.** Не превращать в задачу «сначала допилим ещё фич».

### ⬜ 3.32. Замерить задержку входа (рука → звук) — ЧИСЛОМ, не на слух (не начато)

**Суть.** Путь «палец сомкнулся → нота зазвучала» ни разу не измеряли числом. Известный подозреваемый —
**синхронный MediaPipe в ГЛАВНОМ потоке** (там же три пути синтеза): `main.loop` зовёт `detectForVideo`
прямо в кадре, и на время детекции поток стоит. Кадры при этом идут НЕРАВНОМЕРНО — важна не только
средняя задержка, но и РАЗБРОС: ухо прощает стабильные 40 мс и не прощает пляшущие 20–90.

⚠️ **ЭТО НЕ УДОБСТВО, А ГОДНОСТЬ ИНСТРУМЕНТА.** От этих чисел зависит, можно ли вообще играть В РИТМ.
Всё остальное (строи, тембры, лупер) не имеет значения, если рука и звук расходятся.

⚠️ **МЫ УЖЕ ОДИН РАЗ РЕШАЛИ ЭТО ВСЛЕПУЮ.** Вынос зрения в Worker отложили, ОПАСАЯСЬ, что он добавит
задержку самого распознавания, — то есть отказались от лечения, не измерив ни болезнь, ни цену лекарства.
Больше так не делать: сначала числа, потом решение про Worker.

**ЧТО ИМЕННО ПОКАЗАТЬ — временный счётчик на экране** (отладочный, за флагом; убрать или спрятать после):
- РЕАЛЬНАЯ частота отрисовки (не «60», а сколько есть) и разброс интервалов между кадрами;
- время одного `detectForVideo` — оно же длительность блокировки потока — среднее и худшее;
- по возможности путь ЦЕЛИКОМ: «движение → звук», от кадра, в котором щипок распознан, до `AC.currentTime`
  атаки. Это единственное число, которое чувствует играющий; остальные — его слагаемые.

**Что уже сделано и что это НЕ решает.** Планировщик с опережением (`SCHED_AHEAD≈300мс`, hard rule 15)
МАСКИРУЕТ зависания для СЛОЁВ лупера — они планируются на точное время и не спотыкаются. Живой игре он не
помогает вовсе: живой путь по определению «сейчас». И корень он не убирает — это записано прямо в
`config.js` рядом с константой.

**Порядок (правило проекта — диагностика до постройки):**
1. ИЗМЕРИТЬ. Метка времени в кадре детекции vs `AC.currentTime` атаки; отдельно — длительность
   `detectForVideo` (она же и есть блокировка) и её разброс. Телефон и десктоп отдельно: там разные fps.
2. Понять, ЧТО именно доминирует — детекция, ожидание кадра, или сама атака.
3. Только потом решать. Кандидаты (НЕ выбирать заранее): вынести vision в Worker/OffscreenCanvas,
   вернуться к вопросу GPU-делегата, снизить частоту детекции с интерполяцией между кадрами.

⚠️ Пока нет ЧИСЛА, любые «оптимизации» — угадывание. В прошлый раз задержку так и нашли: сначала
измерили, и корнем оказался делегат, а не то, на что думали.

**Побочная выгода замера:** он же даст цифру для суб-шага выразительности (`EXPR_CFG.subDt`, hard rule 22)
— сейчас известно лишь, что ниже ~13 fps пружина самовозбуждалась.

### ⬜ 3.33. Барабанные ритмы как фон — наполнение и КУЛЬТУРНАЯ уместность (future, не начато)

**Суть.** Человек включает готовый ритм как аккомпанемент под свободную игру, не собирая аранжировку
вручную. ⚠️ **Часть этого пункта УЖЕ ВЫШЛА** — ниже честно разделено, что есть и что осталось, иначе
будущая сессия примется делать сделанное.

**ЧТО УЖЕ ЕСТЬ (не переделывать):**
- **Механизм.** `RHYTHMS` в `arrange.js`: каждый рисунок объявляет свой размер (`beats`) и дробление доли
  (`sub`: 4 = шестнадцатые, 3 = триоли), `stepBeat=1/sub`, сетка обязана быть ровно `beats*sub`
  (`gridOk` — защита от опечатки). Переменный размер петли 2–16 (`loop.metre`), акцентная группировка
  (`METRE_GROUPS`: 7=3+2+2, 9=2+2+2+3, 16=4×4 с khali в 3-м вибхаге) — её слышно метрономом и видно
  в сетке лупера. **ПЯТЬ** наборов ударных (Стандарт · Дарбука · Табла · Гамелан · Тайко), и рисунки
  пишутся **В РЯДАХ** (0 кик · 1 снейр · 2 клэп · 3 хэт), а не в звуках, поэтому любой играет на любом
  наборе: ряд 0 на дарбуке — дум, на табле — баян, на тайко — большой барабан.
- **ВОСЕМНАДЦАТЬ рисунков.** 4/4: Рок (прямой) · Рок (с подхватом) · Поп · Фанк · Диско · Хип-хоп ·
  Босса-нова · Самба · Регги (уан-дроп) · Марш · Шаффл (единственный триольный) · Маqсум. Прочие размеры:
  Вальс (3) · Дадра (6) · Балканский 7 · Кехерва (8) · Балканский 9 · Тинтал (16).
- **Вход «только ритм» СДЕЛАН.** Кнопка **🎵 Подложка** раздвоена: тап с покоя спрашивает путь — «Джем
  (вся подложка)» или «🥁 Только ударные»; дальше тап переключает вариант ВНУТРИ пути. Дублирования с
  джемом нет по построению: джем кладёт гармонию+бас+ударные, «только ударные» — один ритмический слой,
  берущий длину у ПЕТЛИ (поэтому проверка длины ему отказать не может). Обе ветки помечены одной меткой
  `ev.jam` — одна очистка, записи игрока неприкосновенны.

**ЧТО ОСТАЛОСЬ — ТРИ РАЗНЫЕ ЗАДАЧИ, не путать:**

1. **КУЛЬТУРНОГО ГЕЙТА У «ТОЛЬКО УДАРНЫХ» НЕТ, А У ДЖЕМА ЕСТЬ — это НЕСОГЛАСОВАННОСТЬ ДВУХ ПОЛОВИН
   ОДНОЙ КНОПКИ, а не пожелание.** Проверено по коду: `jamVariants()` выбирает вариант ПО СВОЙСТВАМ лада
   (`supportsChords`, `supportsProgressions`, `edo===24` для макама) и потому не подсунет бэкбит под рагу;
   `drumVariants()` — это `rhythmsForMetre(loop.metre)`, то есть **фильтр ТОЛЬКО по размеру**. Итог:
   **под рагой в 4/4 «Только ударные» честно предлагает рок, диско и хип-хоп.** Одна кнопка, два пути,
   разные правила приличия — так оставлять нельзя: либо гейт есть у обоих, либо его нет ни у кого.
   ⚠️ **Только НА СВОЙСТВАХ, никогда на именах/индексах** (правило проекта; см. §3.16 п.3, где тот же
   гейт нужен джему для более тонкой привязки). Скорее всего это значит ВВЕСТИ НОВОЕ СВОЙСТВО: подсказку
   культуры/пульса у лада и парную ей у рисунка, а не спецкейсить по `scaleIdx`.
   **Открытый вопрос спецификации (решить ДО реализации):** фильтровать ЖЁСТКО (чужие рисунки не
   показывать) или мягко (уместные первыми, прочие ниже)? Жёстко честнее по смыслу проекта, мягко
   оставляет право на осознанную дичь. У джема сейчас де-факто жёстко — это аргумент за жёстко, ради
   единообразия той же кнопки.
2. **ДЫРЫ ПО РАЗМЕРАМ.** `METRES` допускает 2, 5, 10, 12 — а рисунков там **НЕТ НИ ОДНОГО**, и пункт
   «Только ударные» в этих размерах честно гаснет с причиной. 5 (аксак) и 12 (компас, африканский 12/8)
   — самые обидные пробелы.
3. **НАПОЛНЕНИЕ.** Чего действительно не хватает: **полька** (2), **африканские** циклы (нет ни одного —
   и это самый большой пробел карты ритмов), **индийские талы сверх трёх** (есть дадра/кехерва/тинтал;
   нет джхаптал 10, рупак 7, эктал 12), **латинские сверх боссы/самбы** (сон-клаве, румба, ча-ча),
   **арабские сверх маqсума** (баляди, саиди, чифтетелли). ⚠️ Африканская полиритмия ЧАСТИЧНО упирается
   в §3.24 (свой размер на слой): рисунок в ОДНОМ размере её не выражает — браться стоит после того, как
   решено, что именно моделируем.

**Мельче, но реально:** рисунок не подсказывает НАБОР (тинтал на «Стандарте» звучит не таблой, хотя
задуман ею) — можно дать рисунку необязательную подсказку кита, применяемую лишь когда человек кит сам
не выбирал; и рисунок не несёт ТЕМП (диско хочет ~118, хип-хоп ~85), а BPM глобальный — та же оговорка,
что в §3.30.

### ⬜ 3.34. Конструктор собственного строя — человек задаёт ступени сам (future, не начато)

**Суть.** Дать человеку задать СВОЙ строй: сколько ступеней и на какой высоте каждая. Это превращает
приложение из КОЛЛЕКЦИИ строёв в ИНСТРУМЕНТ ДЛЯ ИХ ИССЛЕДОВАНИЯ — можно собрать строй из статьи, из
собственной идеи или из замеров реального инструмента (расстроенное пианино, конкретный гамелан,
народная флейта). Ради этого проект и существует, так что пункт крупный.

**ЧТО УЖЕ ЕСТЬ — движок умеет ВСЁ нужное, не хватает только РЕДАКТОРА:**
- **Произвольные высоты** — свойство `cents:[...]` (длина = `iv.length`): каждая ступень задана точными
  центами над тоникой, шаг какой угодно неравный. Так живут гамелан, Партч, шрути, все исторические
  темперации.
- **Неоктавный круг** — свойство `period` (`periodOf(s)=s.period||2`): тритава у Болен–Пирса, квинта у
  Карлоса. Регистр сдвигает на ПЕРИОД, метки и `rectRows` уже это учитывают.
- **Привязка к абсолютной высоте** — `fixedKey` (сетка от абсолютного C, якорь `cFix()`), если строй
  задуман как настройка клавиатуры, а не как подвижный лад.
- **Эталон** — `aRef` (380–480), единый источник для подвижных и фиксированных.
- **Перевод цент↔герц уже написан** — `centsOf` и `leadFreq`/`bassFreq` (через `1200·log2`); выводить
  формулы заново не нужно, обе стороны уже есть.

**ЧТО СДЕЛАТЬ.** Редактор: число ступеней; высота каждой — **в центах от тоники ИЛИ прямо в герцах**, с
живым пересчётом в обе стороны (герцы нужны, когда строй снимают с реального инструмента тюнером);
период круга (2 = октава, 3 = тритава, 3/2 = квинта, произвольное число). Плюс прослушивание ступени по
тапу прямо в редакторе — иначе набор чисел не проверить на слух, а слух здесь главнее математики.

**ХРАНЕНИЕ.** Пользовательские строи обязаны переживать перезагрузку → `localStorage`. **Механизм уже
есть и работает** — `i18n.store` (обёртка с try/catch и падением в ПАМЯТЬ, чтобы приватный режим не ронял
старт); сейчас через него живут язык и прогресс уроков, это просто ещё один ключ. Желателен **экспорт/
импорт СТРОКОЙ** (компактный текст, а не файл), чтобы строем можно было поделиться в сообщении: тогда
строй из статьи расходится между людьми, а это половина ценности фичи.

**ЧТО УЧЕСТЬ (каждый пункт — реальная ловушка):**
- ⛔ **АДРЕСАЦИЯ — ЖЁСТКАЯ ПРЕДПОСЫЛКА, а не «что учесть». НЕ НАЧИНАТЬ редактор, пока её нет.**
  Полностью описана в §1 → **«СТАБИЛЬНЫЙ ИДЕНТИФИКАТОР ЛАДА»**. Коротко: у ладов НЕТ поля `id` (0 из 75),
  адрес — позиция в массиве, а слой петли морозит ССЫЛКУ на объект лада, которую нельзя сериализовать.
  Пользовательский строй по определению создают, редактируют и УДАЛЯЮТ — в append-only массив по индексу
  он не ложится, и без id нет ответа даже на простой вопрос «что со слоем, который играет удалённый
  строй». Сначала id и адресация по нему, потом UI редактора.
- **ПОДПИСИ СТУПЕНЕЙ.** У цент-лада имена нот 12-TET ВРУТ (высоты другие), поэтому `noteLbl` (единая
  подпись для обеих раскладок) показывает порядковые `Т,2,3…`. Пользовательскому строю по умолчанию — то же самое; хорошее развитие —
  дать задать СВОИ имена ступеням (человек, собравший строй по статье, знает, как они называются).
- **АККОРДЫ.** У произвольного строя нет типизированного набора (`typedChords` — ключ в
  `CHORD_FAM_SETS`), поэтому по умолчанию **`noChords`**, как гамелан и раги: роль аккордов молчит, играют
  соло/бас/дрон. Это честно, а не ограничение. Позже можно дать выбор набора (шаговый — если строй
  равномерный; отношениями — если задан отношениями).
- **ПРЯМОУГОЛЬНИКИ НЕ ПРИМЕНИМЫ автоматически, но проверка УЖЕ ГОТОВА.** Годность считает
  `scales.rectPad` (кратность 4 закрытой формой `iv.length+1` — только у октавных — или открытой
  `iv.length`), доступность — `rectEligible`, а контрол «Раскладка нот» сам блокируется С ПРИЧИНОЙ.
  Пользовательскому строю не нужно ничего решать: не сошлось — узкие ряды и внятное объяснение.
- **ПРОВЕРКА ВВОДА.** Ступени обязаны идти **строго по возрастанию** и умещаться **внутри периода**
  (0 ≤ центы < период в центах). Иначе строй звучит бессмыслицей, а ступенная арифметика (`degHyst`,
  гистерезис, модуло в `leadFreq`) ведёт себя непредсказуемо. Валидировать при вводе и говорить, ЧТО
  не так, а не молча чинить за человека.

**ЗАВИСИМОСТИ и порядок.** Редактор — не первый шаг: сначала **адресация** (стабильный id + хранилище),
потом UI. Хорошо ложится к §1 (localStorage настроек) и к §3.28 (справка по ладу — своему строю тоже
нужно описание). С §3.15 (свой строй на роль) не конфликтует и его не требует.

### ⬜ 3.35. Сохранение петли — записанное не должно умирать вместе со вкладкой (future, не начато)

**Суть.** Сейчас петля живёт ТОЛЬКО в сессии: закрыл вкладку — и четыре слоя, которые человек выстраивал
двадцать минут, исчезли. Это худший вид потери: не «функции не хватило», а «сделал и лишился». Для
инструмента, где вещь собирается слоями, сохранение — не удобство, а условие того, что собирать имеет смысл.

**ЧТО УЖЕ ЕСТЬ:**
- **Хранилище.** `i18n.store` — обёртка над localStorage с try/catch и падением в ПАМЯТЬ (приватный режим
  не роняет старт). Через неё уже живут язык и прогресс уроков; петле нужен просто ещё один ключ.
- **Формат.** Событие `{t, layer, fn, a, sc, sev}` (+ `jam`) — плоские данные, сериализуемые как есть:
  `t` в ДОЛЯХ (не в секундах — переживает смену BPM), высота как `deg/oct` (не Гц — переживает смену
  тоники), `a` — обычный объект. Ничего не надо переизобретать, кроме одного поля — см. ниже.

⛔ **БЛОКЕР — ТОТ ЖЕ, ЧТО У §3.34: у лада НЕТ идентификатора.** См. §1 «СТАБИЛЬНЫЙ ИДЕНТИФИКАТОР ЛАДА».
Поле `sc` — это **ССЫЛКА НА ОБЪЕКТ** лада (`sc:CUR()`), и в этом весь смысл полимодальности: слой играет
в строе записи, пока рука играет в текущем (hard rule 7). Но ссылку **нельзя записать на диск**. Если
сохранить петлю без строя, при загрузке `sc` подставится текущим — и **петля перестроится**: записанный
в Партче слой зазвучит в пентатонике. Это не «мелкая недоделка», это разрушение главного свойства лупера.

**РАЗВИЛКА ХРАНЕНИЯ СТРОЯ (решить до реализации):**
- **По ССЫЛКЕ (id):** в событии лежит `scaleId`, при загрузке ищем лад по нему. Компактно, сохраняет
  ИМЯ строя («это Веркмайстер»), но требует предпосылки из §1 и не переживает удаление/правку строя
  пользователем (§3.34).
- **КОПИЕЙ:** в файл кладём сам объект лада (или его существенную часть — `edo`/`iv`/`cents`/`period`/
  `fixedKey`). Самодостаточно: переживёт и удаление строя, и перенос на другое устройство, и версию,
  где такого лада нет. Дороже по объёму и теряет тождество («какой-то 12-нотный цент-строй» вместо имени).
- Скорее всего нужно **и то и другое**: id для узнавания, копия как страховка. Но это решение, а не
  умолчание — записать явно.

⚠️ **ВЕРСИОНИРОВАНИЕ — сразу, а не потом.** В сохранение кладём номер версии формата. Формат события уже
менялся на нашей памяти (появились `a.ty`, `a.bri`, `a.bend`, `ev.jam`, `sub`/`loop.metre` у петли), и
будет меняться дальше. Без версии старое сохранение однажды загрузится МОЛЧА НЕПРАВИЛЬНО — это хуже
честной ошибки «файл от старой версии». Правило: неизвестная версия → внятный отказ, известная старая →
миграция или явное «часть данных потеряна».

**ЧТО ЕЩЁ УЧЕСТЬ.** Сохранять надо не только `events`, но и геометрию петли (`bars`, `metre`, `bpm`,
`quant`) и тембры слоёв (`a.inst`/`a.kit` уже в событиях). Дрон — событие, попадёт сам. Имя/дата
сохранения, чтобы список был читаемым. Объём localStorage конечен — считать, сколько петель влезает,
и что делать при переполнении (не молча терять).

**Соседняя, но ДРУГАЯ задача:** экспорт петли ФАЙЛОМ или ссылкой (поделиться). Ложится сюда же по формату,
но это про обмен, а не про «не потерять»; см. §3.37 (поделиться настройкой) и §3.36 (совместная игра).

### ⬜ 3.36. Совместная игра — НАПРАВЛЕНИЕ, не ближайшая задача (future, не начато)

**Суть.** Всё построенное рассчитано на ОДНОГО человека перед камерой. А музыка — занятие совместное, и
приложение расположено для этого идеально: оно живёт **по ссылке в браузере**, без установки, без
регистрации, на любом устройстве с камерой. Отправил ссылку — и играете вдвоём. Ни один шаг установки
между «захотел» и «играем» — редкое преимущество, которое стоит когда-нибудь потратить.

⚠️ **Записано КАК НАПРАВЛЕНИЕ.** Это не «фича на пару заходов», а отдельная работа другого класса, и
браться за неё раньше §3.31/§3.32 (ролик, задержка) незачем: сначала должно быть хорошо ОДНОМУ.

**Что здесь по-настоящему трудно (чтобы будущая сессия не недооценила):**
1. **СЕРВЕР.** Сейчас проект — чистая статика (GitHub Pages, ни строки бэкенда, ни зависимостей). Любая
   совместность добавляет то, чего нет вообще: живой сервер (или p2p-сигналинг), его хостинг, доступность,
   стоимость. Это меняет природу проекта, а не только код.
2. **СИНХРОНИЗАЦИЯ ВРЕМЕНИ.** Наш лупер стоит на `AudioContext.currentTime` — на ЛОКАЛЬНЫХ часах. У двух
   участников это разные часы. Нужна общая временная база и оценка задержки канала; иначе доли разъедутся,
   а именно доли — то, ради чего играют вместе. Планировщик с опережением (hard rule 15) тут союзник:
   он уже умеет играть события в ЯВНОЕ будущее время — механизм, на который можно опереться.
3. **РАЗРЕШЕНИЕ КОНФЛИКТОВ.** Двое пишут в одну петлю: кто владеет слоем, что при одновременной правке,
   что при отвале связи посреди круга. Это вопрос МОДЕЛИ, а не транспорта: решать до кода.

**Дешёвая ступенька перед этим (стоит рассмотреть отдельно):** асинхронная совместность — поделиться
сохранённой петлёй (§3.35), другой человек догружает её и пишет свой слой. Никакого сервера, никакой
синхронизации времени, а «сыграли вместе» уже случается. Возможно, 80% ценности за 5% работы.

### ⬜ 3.37. Мелкое, но заметное — три отдельные вещи (future, не начато)

Три независимые задачи, объединённые только размером. Делать по одной, не пакетом.

**1. МЕТРОНОМ ОТДЕЛЬНО ОТ ЛУПЕРА.** Сейчас щелчки звучат ТОЛЬКО во время записи и отсчёта
(`schedClicks` гейтит `inCount || recording`). Играть под метроном просто так — нельзя, а это обычная
потребность: разыграться, проверить темп, поучить пассаж. Нужен независимый тумблер «метроном вкл»,
не связанный с записью. **Механизм готов целиком:** `metroClick(t, level)` с четырьмя уровнями акцента,
`beatLevel(metre,b)` с группировкой и khali, планирование по AC-часам с опережением. Работы — тумблер,
состояние и снятие гейта; всё остальное уже играет.

**2. ТРАНСПОНИРОВАНИЕ НА ЛЕТУ.** Сменить тонику, не заходя в меню, — кнопками ±, жестом или парой
клавиш. Сейчас тоника меняется только выпадающим списком в панели «Звукоряд». ⚠️ Учесть: смена тоники
зовёт `softAllOff()` (звучащее глохнет и переатакуется — это правильно), а у **`fixedKey`-строёв тоника
не транспонирует, а МЕНЯЕТ КЛЮЧ** внутри неподвижной сетки (hard rule 16) — то есть на Веркмайстере
«транспонирование» меняет ХАРАКТЕР, а не только высоту. Это не баг, это урок строя; но контрол обязан
не врать, что просто «сдвигает вверх».

**3. ПОДЕЛИТЬСЯ НАСТРОЙКОЙ ССЫЛКОЙ.** Человек нашёл красивое сочетание — строй, тоника, эталон A4,
тембры, эффекты, функции рук — и передать его может ТОЛЬКО СЛОВАМИ («возьми пелог, сантур и вот столько
реверба»). Ссылка с параметрами (`?scale=…&tonic=…&aref=…&lead=…`) решает это на статике, без сервера:
открыл — и ты в том же звуке. Ложится к §1 (localStorage настроек): одно и то же состояние, два способа
его записать. ⛔ **Для СТРОЯ в ссылке нужен тот же стабильный id, что и везде** (§1): индекс в массиве
для ссылки не годится — он поедет при следующем добавлении лада, и старая ссылка молча откроет ДРУГОЙ
строй. Пользовательские строи (§3.34) тем более требуют не индекса, а самоописания.

### 🟨 3.38. Прямоугольники по четыре ноты для ОБЫЧНЫХ строёв — и двухголосие (РАСКЛАДКА СДЕЛАНА, полифония нет)

> **СТАТУС: сделан ШАГ «РАСКЛАДКА» (пункт 3 из «ПОРЯДКА» ниже), в ДВА захода и раньше пунктов 1–2
> НАМЕРЕННО.** Смысл — выяснить НА ОЩУПЬ, лучше ли целиться в прямоугольники, ЧЕМ в узкие ряды, ДО того
> как вкладываться в полифонию соло (пункт 1 — правка аудио-движка). Соло по-прежнему МОНОФОНИЧНО: в
> прямоугольнике звучит одна нота за раз, «аккорды пальцами» из раздела ниже НЕ сделаны и в силе.
>
> **ЗАХОД 1 — раскладка стала переключаемой.** `rectGrid` стал СОСТОЯНИЕМ (`state.rectPref`:
> 'auto'|'rect'|'rows', дефолт 'auto'), свойство лада — его УМОЛЧАНИЕМ; контрол «Раскладка нот» в меню
> «Звукоряд» рядом с ладом. Тогда же закрыты три дефекта, видимые только на новых ладах: рисунок
> нетипизированных аккордов брал узкие ряды при rect-вводе; подписи слиты в ОДНУ `draw.noteLbl`
> (подпись зависит от ЛАДА, а не от раскладки); старый off-by-one терменвокса (делил на `IVX().length`).
>
> **ЗАХОД 2 — ПЕРЕМЕННОЕ k И НЕСКОЛЬКО ОКТАВ СРАЗУ.** Вся арифметика — ОДНА функция `scales.rectLayout`:
> нот в прямоугольнике k = 4→3→2 (первое подходящее), повтор тоники dup = 0/1 РАДИ ВЫРАВНИВАНИЯ (период
> занимает целое число прямоугольников), затем показываем сколько влезет периодов при потолках REG_N=4
> и 12 прямоугольников.
> - **Раскладываются ВСЕ 75 ладов**: чётные при k=2, нечётные при k=2+dup. Пентатоники, блюз,
>   **сетка 22 шрути** и **Болен–Пирс** были невозможны — теперь нет. Ветка «недоступно с причиной»
>   и две её строки словаря УДАЛЕНЫ: причины больше не существует, а мёртвое объяснение хуже пустого.
> - **Регистровая полоса ушла у большинства** — она есть ровно там, где показан НЕ ВЕСЬ диапазон
>   (`hasReg = octaves < REG_N`, ОДИН предикат на жест/рисунок/терменвокс). 7-нотный лад: 8
>   прямоугольников на 4 октавы БЕЗ полосы (треть экрана, что раньше занимал регистр, стала нотами);
>   хроматика — 12. Где полоса осталась (19-TET и гамма — 2 октавы; 31-TET, Парч, шрути, BP — 1), она
>   двигает ОКНО целиком, а не одну октаву, и выбираемых баз соответственно меньше четырёх.
> - ⛔ **Период-гейт у dup СНЯТ НАМЕРЕННО** — без этого BP (13) и Карлос-бета (11), оба простые, не
>   раскладываются никак. На высоту не влияет. Оговорка записана в коде: подпись «Т» у повторённого
>   слота честна для BP (тритава — его эквивалентность) и преувеличена для Карлос (квинта —
>   генератор, эквивалентности нет); если начнёт мешать — свойство `equiv:false`.
> - ⛔ **Байт-в-байт остались 31-TET и Парч** (и новые однопериодные шрути/BP ведут себя так же).
>   **19-TET и гамма ИЗМЕНИЛИСЬ НАМЕРЕННО** (2 октавы, 10+1 полос, полоса-окно) — прежняя гарантия
>   «все четыре старых rect-лада не тронуты» теперь держится ДЛЯ ДВУХ.
> - Ещё два дефекта того же рода закрыты здесь же: подсветка искала прямоугольник по ОДНОЙ ступени
>   (на многопериодной сетке всегда падала бы в нижний блок — теперь носим пару ступень+регистр и ищем
>   обратной формулой `rectSlotOf`), и тождество защёлки не включало регистр (тот же аккорд октавой
>   выше ГЛУШИЛ бы предыдущий вместо перехода октавой выше).
>
> **ЧТО СУДИТЬ НА ЖИВОМ ПРОГОНЕ (ради этого шаг и делался):** удобнее ли целиться в ЧЕТЫРЕ блока, чем в
> один; хватает ли маркера регистра, чтобы понимать, где ты; цена промаха по Y — теперь ЧЕТЫРЕ ноты (шаг
> прямоугольника), а не одна; плотность 12 полос в ЛАНДШАФТЕ (~30px на полосу). Гистерезис из прошлого
> захода перестал «липнуть» сам собой: полосы стали тоньше (~14px у 8 полос, ~9px у 12 — как у узких
> рядов). ⛔ Ни коэффициенты, ни потолки заранее НЕ подкручивать — это ушные решения. Так же НЕ смягчать
> заранее грубый терменвокс на многооктавных ладах (4 октавы на высоту экрана), низкий басовый блок на
> регистре 0 и тонкие аккордовые корни наверху: единое правило дороже трёх спец-случаев.

**ЗАМЫСЕЛ.** Сейчас rect-раскладка включена только там, где ступеней СЛИШКОМ МНОГО для узких рядов
(19-TET, 31-TET, Партч 43, гамма Карлоса) — она решала проблему попадания. Но у неё есть ВТОРАЯ ценность,
которую мы не использовали: **двенадцать нот превращаются в ТРИ ТОЛСТЫХ прямоугольника**, руки стоят в
разных, и под пальцами сразу ВОСЕМЬ нот. Это уже похоже на ИГРУ, а не на попадание в полоски. То есть
rect — не костыль для микротоники, а полноценный способ играть, который мы почему-то держим только там,
где без него нельзя.

✅ **ГЛАВНОЕ ПРЕПЯТСТВИЕ СНЯТО: СОЛО ПОЛИФОНИЧНО.** Было: общая цепь `banks → envGain → volGain` делила
ЖИВУЮ игру и переигровку, вторая рука не добавляла голос, а ПЕРЕХВАТЫВАЛА первый (`leadOwner` =
последний щипок). Сделан ПУЛ С КЛЮЧАМИ-ВЛАДЕЛЬЦАМИ, как у аккордов: живая рука — 'lead:L'/'lead:R'
(ключ строит `gestures.soloKey`, палец в него заложен заранее — многоголосие одной руки включится
ОДНИМ аргументом), слой петли — 'leadloop:N:v'. Две руки звучат вместе; живая игра и переигранный
соло-слой больше не дерутся; `leadOwner` и моно-передача удалены (у баса остались — он ещё моно).
⚠️ КЛЮЧЕВОЕ ПРОЕКТНОЕ РЕШЕНИЕ: голос строит ТОЛЬКО СВОЙ банк, лениво. Прежний движок держал ВСЕ 23
банка вечно (40 осцилляторов + 7 KS-ворклетов на любом телефоне), и «размножить это на голос» было бы
6× — вот это и казалось непроходимой ценой. С одним банком на голос пул вышел ДЕШЕВЛЕ сегодняшнего.
⛔ Не возвращать построение всех банков — ни глобально, ни в голосе.
Формат события получил ОДНО поле `a.v` (номер одновременной ноты в слое, чтобы спарить leadOn/leadOff);
старое событие без `v` читается как 0, поэтому прежние петли играют бит-в-бит.
Попутно закрыты три дефекта, невидимые при моно: общий ConstantSource гуманизации (вторая атака
перестраивала бы первую ноту), одиночное состояние записи (две пишущие руки слились бы в кашу) и
`setLeadInstr(a.inst)` в переигровке (слой уводил живой инструмент).
Два принятых изменения поведения: смена инструмента больше НЕ переливает звучащую ноту (правило
аккордов/баса) и моно-передачи у соло нет.
ОСТАЛОСЬ для «аккордов пальцами»: многопальцевый ввод (замер надёжности — пункт 2 «ПОРЯДКА»).

**АРИФМЕТИКА — ✅ РЕШЕНА (историческая формулировка задачи, оставлена для контекста).** Была: `rectGrid`
законен при `(iv.length + (periodOf===2?1:0)) % 4 === 0`, и хроматика (12+1=13) не раскладывалась.
Предлагались варианты (а) разрешить форму без верхней тоники и (б) допускать неполный верхний
прямоугольник. Сделано (а) — и сверх того ПЕРЕМЕННОЕ k (4/3/2), после чего вариант (б) не понадобился:
неполных прямоугольников нет ни у одного лада. Три обязательных случая (19→5, 31→8, гамма→5 на период)
сохранены; период-гейт у dup снят осознанно (см. СТАТУС выше).

**ГДЕ ЭТО ЖИВЁТ — РЕШЕНО (записать как решение, а не как вариант):**
- ⛔ **НЕ в «Функциях рук».** Функция руки говорит, что рука ДЕЛАЕТ (ноты, эффекты, терменвокс, лупер,
  выразительность). Раскладка — свойство ПОЛЯ, общее для обеих рук: невозможно, чтобы одна рука играла
  прямоугольники, а другая узкие ряды в том же поле. Положить туда — сломать смысл раздела.
- ⛔ **НЕ отдельным ладом.** «Хроматика прямоугольниками» дублировала бы ТЕ ЖЕ двенадцать высот ради
  режима ОТОБРАЖЕНИЯ, и не масштабировалась бы: то же желание придёт для мажора, минора и всех прочих —
  расплодятся клоны, а `SCALES` перестанет быть списком СТРОЁВ.
- ✅ **ПРАВИЛЬНАЯ ФОРМА: `rectGrid` становится СОСТОЯНИЕМ, которое человек переключает, а нынешнее
  свойство лада — ЕГО УМОЛЧАНИЕМ.** Партч и 31-TET по умолчанию в прямоугольниках (сорок четыре ряда
  иначе неиграбельны), хроматика по умолчанию узкими рядами — и то и другое ПЕРЕКЛЮЧАЕТСЯ. Одно решение
  закрывает ВСЕ лады сразу, а не только хроматику, и ни одного клона в `SCALES` не появляется.
- **Место контрола — меню «Звукоряд»**, рядом с самим ладом и «Функциями рук»: это про то, КАК ты
  играешь, а не про то, что звучит.
- **Контрол ОБЯЗАН быть НЕДОСТУПЕН там, где арифметика не позволяет** (22 шрути прямоугольниками не
  раскладываются — `(22+1)%4=3`): гасить или не показывать, как кнопка сплита существует только в
  ландшафте. Мёртвый переключатель хуже отсутствующего.

**НЕСКОЛЬКО ПАЛЬЦЕВ РАЗОМ — это сильно расширяет фичу.** Внутри одного прямоугольника человек может
прижать к большому СРАЗУ НЕСКОЛЬКО пальцев, и каждый прижатый звучит своей нотой. Тогда один
прямоугольник даёт аккорд до четырёх нот, а две руки — восемь: **аккорды строятся буквально ПАЛЬЦАМИ,
а не выбираются из палитры**. Соло из мелодической линии превращается в полифонический инструмент —
это, возможно, ценнее самой раскладки.

**ЧТО ЭТО ТРЕБУЕТ:**
- **ПОЛНАЯ полифония соло** — уже не «второй голос», а ПУЛ с ключами-владельцами на РУКУ и на ПАЛЕЦ,
  ровно как устроены аккорды. Та же предпосылка, что выше, но в сильной форме: восемь одновременных
  соло-голосов со своими огибающими.
- **ЖЕСТОВЫЙ СЛОЙ СЕЙЧАС ВИДИТ ОДИН ПАЛЕЦ.** `minFinger(r)` возвращает БЛИЖАЙШИЙ к большому — один на
  руку, и вся модель щипка (`S.oct`, захват/удержание/размыкание, заморозка пальца у hold/loop) построена
  вокруг одного. Понадобится сообщать ВСЕ прижатые пальцы, каждый со своим вкл/выкл, независимо друг от
  друга — это не «добавить поле», а пересмотр состояния руки.
- ⚠️ **ЧЕСТНЫЙ РИСК, измерить ДО проектирования:** распознать ТРИ пальца, прижатых к большому
  одновременно, ЗАМЕТНО труднее, чем один — пальцы перекрывают друг друга от камеры, и часть точек
  MediaPipe не видит, а ДОСТРАИВАЕТ. Вполне возможно, что на двух пальцах работает отлично, а на четырёх
  разваливается. **Сначала замер надёжности (сколько пальцев различается уверенно и при каком повороте
  кисти), потом дизайн.** Строить восьмиголосие в расчёте на четыре пальца, не проверив четыре пальца, —
  повторить ошибку «решали вслепую» из §3.32.

**ЗАМЕЧАНИЕ (обязательное).** Узкие ряды ОСТАЮТСЯ: они удобны для быстрых пассажей одной рукой, где
двенадцать нот подряд нужнее восьми под пальцами. Это должен быть ВЫБОР, а не замена — потому фича и
оформлена переключаемым состоянием с умолчанием, а не переделкой раскладки.

**ПОРЯДОК.** (1) полифония соло в аудио-движке — она полезна сама по себе; (2) замер надёжности
нескольких пальцев; (3) ✅ **СДЕЛАНО** — арифметика и переключатель `rectGrid` как состояние (вынесен
вперёд намеренно: он дёшев и даёт ответ «стоит ли вообще вкладываться в полифонию», см. статус вверху);
(4) многопальцевый ввод — только если (2) дал добро.

### 🔵 3.39. Конструктор ЭФФЕКТОВ — назначение параметров на пальцы и координаты (В РАБОТЕ)

**СТАТУС: идёт пластами, по одному за раз с обкаткой на превью между ними. СДЕЛАНЫ ПЛАСТЫ 1, 2 и 3.1;
ОСТАЛОСЬ 2.6 → 3.2 → 3.3 → 3.4 (порядок ПЕРЕСМОТРЕН, см. ниже).**
ПОЛНЫЙ замысел, пересмотр модели, разбор по слайсам и хвосты Пласта 2 (два закрыты — реверб в списке
меню и рассинхрон после перезагрузки; открыт один — висячая ссылка `A.k` в `gestures.js`) — в
`HANDOFF.md`, раздел «КОНСТРУКТОР ЭФФЕКТОВ». **HANDOFF — единственный источник правды; здесь ТОЛЬКО
СТАТУС**, чтобы не держать две расходящиеся копии одного плана.
- **Пласт 1 — ✅ СДЕЛАН.** Абстракция «модуль эффекта» `{id, labelKey, in, out, params[]}` и реверб как
  первый экземпляр (`makeReverbFx` в `audio.js`), вариант B — настоящий экземпляр, свои линии внутри
  объекта. Звук не изменился (критерий был «байт-в-байт как продакшен»).
- **Пласт 2 — ✅ СДЕЛАН, пять слайсов, все в `dev`.** Конструктор у fx-руки: раскладка «палец→эффект»
  (`state.fxLayout`), три оси X/Y/Z от ТОЧКИ ЗАХВАТА ЩИПКА, ось и инверсия на каждый параметр, реверб
  назначается на палец (длина+окраска), снятый эффект замолкает. Разбор по слайсам — в HANDOFF.
- **Пласт 3.1 — ✅ СДЕЛАН, в `dev`.** Реверб УШЁЛ с играющей руки (запись `revLead` убрана из `applyFx`);
  fx-рука получила ТРЕТИЙ параметр реверба — ПОДМЕС, и он пишет СОЛО-ПОСЫЛ `revLead`, а не общий wet
  (аккордовый посыл `revCh` не тронут). Реверб стал ЖИВЫМ: ключ `rev` в событии остался (формат цел,
  старые петли разбираются), новые события пишут ноль, переигровка его игнорирует. Раскладка по
  умолчанию теперь ЯВНАЯ: указательный — реверб, средний — делей, безымянный — драйв, мизинец — вибрато,
  тремоло не назначено. Плата принята: старая петля теряет ревербный свип правой руки.
- ⚠️ **ПОРЯДОК ОСТАВШЕГОСЯ ПЕРЕСМОТРЕН.** Прежнее деление 3.2 на «глубина + громкость одним заходом»
  (a/b) **ОТМЕНЕНО**: это разные пласты с разным риском, и перед ними встал фундамент, которого в модели
  не было. Новая суть: параметр эффекта — либо ПАЛЬЦЕВЫЙ (ось+инверсия), либо ЗАФИКСИРОВАННЫЙ значением
  из меню; эффект может стоять в цепи вообще без пальцевых параметров; отсюда — один эффект, разделённый
  между двумя руками. Порядок ниже — ПО ВОЗРАСТАНИЮ РИСКА.
- **Пласт 2.6 — ⬜ СЛЕДУЮЩИЙ, ФУНДАМЕНТ.** Параметр получает РЕЖИМ («пальцевый или фиксированный») +
  фиксированное значение 0..100 из меню. Достройка модели Пласта 2, **лупер не трогает вовсе**; обязана
  лечь ДО играющей руки — без «адреса управления» ни разделение эффекта между руками, ни двойной щипок
  не выражаются.
- **Пласт 3.2 — ⬜ ИГРАЮЩАЯ РУКА, ТОЛЬКО ГЛУБИНА.** Освободившаяся в 3.1 глубина (Z) становится адресом
  управления для параметра. Поверх 2.6 попутно даёт разделение одного эффекта между двумя руками. Громкость
  здесь НЕ трогаем.
- **Пласт 3.3 — ⬜ ДВОЙНОЙ ЩИПОК** (вторая привязка на палец: «следующие три параметра» — до 6 у эффекта —
  или «другой эффект» — до 8 на руке). НОВЫЙ жест, которого в коде нет; риск в горячем пути распознавания
  щипка (отличить двойной от двух одиночных) плюс умножение раскладки.
- **Пласт 3.4 — ⬜ ПЕРЕНАЗНАЧАЕМАЯ ГРОМКОСТЬ (X), ПОСЛЕДНИМ.** X по умолчанию громкость, но переназначаема
  на параметр эффекта; отданный эффекту X делает громкость фиксированной. ⛔ **Самое глубокое вмешательство
  в лупер:** `S.vol` кормит громкость ВСЕХ ЧЕТЫРЁХ ролей и пишется в событие каждой, плюс дельта-сжатие
  через `REC_VOL_EPS`. Сюда же переезжает нерешённая **судьба поля `rev`**. Мягко (поле остаётся,
  значение фиксированное) против жёстко (меняем формат) — решать в ОТДЕЛЬНОМ ПЛАНЕ (правила #7/#11),
  а не по ходу реализации.

⚠️ **ГЛАВНОЕ, ЧТО ВЫЯСНИЛ ПЛАСТ 1** (иначе Пласт 2 ляжет на неверную посылку): абстракция вышла
**ПОСЫЛ-ОБРАЗНОЙ**, а виды крепления у эффектов РАЗНЫЕ — ПОСЫЛ (реверб, делей) · ВСТАВКА (тремоло,
`in===out`) · МОДУЛЯЦИЯ (вибрато, в `.detune`, аудио-входа/выхода нет) · ПО ГОЛОСАМ (драйв, до
огибающей, нелинейность); и ПОДМЕС — свойство ПРИЦЕПКИ, а не эффекта (у реверба два посыла с разной
величиной, одним узлом на выходе их не выразить).

✅ **Развилка (а)/(б) ниже РЕШЕНА в пользу (б)** — три параметра ОДНОГО эффекта. Оговорка при (б)
(«требует, чтобы у эффектов вообще были эти параметры, а это работа в аудио-движке») подтвердилась и
частично закрыта: у реверба такие параметры теперь есть — длина и окраска настоящими `AudioParam`'ами.

**Суть.** Сейчас у fx-руки эффекты ЖЁСТКО привязаны к пальцам (`FX_META`: указательный→делей,
средний→вибрато, безымянный→драйв, мизинец→тремоло), и используется РОВНО ОДНА ось: вертикальный сдвиг
задаёт величину (относительно, от текущего значения, размах 70% высоты экрана, значение защёлкивается
после отпускания). Переназначить эффекты нельзя, воспользоваться остальными осями — тоже.

**ЧТО ПРОПАДАЕТ ЗРЯ.** У fx-руки свободны ВСЕ ТРИ оси — вертикаль, горизонталь и глубина. Возражение
«горизонталь занята громкостью, глубина ревербом» относится к ИГРАЮЩЕЙ руке — а это ДРУГАЯ рука со
своим набором координат; у fx-руки они не заняты ничем. Считаем честно: щипок берёт большой + один из
ЧЕТЫРЁХ пальцев (большой — партнёр по щипку, своего эффекта нести не может), то есть **4 пальца × 3 оси
= 12 управляемых величин, из которых используются 4.** Две трети выразительных возможностей руки просто
не подключены.

**ЧТО СДЕЛАТЬ:**
- **Раздел в меню «Звукоряд»**, где человек сам назначает, какой палец какой эффект берёт (рядом с
  «Функциями рук» — это тоже про то, КАК играешь).
- **И что несёт КАЖДАЯ ОСЬ. Две развилки, обе осмысленны — решить при реализации, не сейчас:**
  - **(а) НЕСКОЛЬКО ЭФФЕКТОВ на один палец**, по одному на ось: один палец управляет тремя эффектами
    сразу. Даёт ШИРИНУ — до 12 эффектов под одной рукой.
  - **(б) ТРИ ПАРАМЕТРА ОДНОГО эффекта:** у задержки — время, обратная связь, подмес; у реверба —
    размер, подмес, срез. Даёт ГЛУБИНУ: сейчас у эффекта ОДНА величина, а у настоящей педали три-четыре
    ручки, и именно поэтому она звучит как инструмент, а не как переключатель.
  - ⚠️ Вариант (б) требует, чтобы у эффектов ВООБЩЕ БЫЛИ эти параметры: сегодня `applyParams` принимает
    по одному числу на эффект (`dly`/`vib`/`drv`/`trm`), а узлы (задержка, тремоло-LFO, шейпер) держат
    остальное на фиксированных значениях. Значит (б) — это ещё и работа в аудио-движке, а не только UI.
    Оценивать соответственно.
- **РАСПРОСТРАНИТЬ НА ИГРАЮЩУЮ РУКУ.** Её оси сейчас зашиты жёстко — вертикаль высота, горизонталь
  громкость, глубина реверб, — но это НАШЕ решение, а не закон природы. Сделать их назначаемыми, а
  нынешние значения — УМОЛЧАНИЯМИ. ⚠️ С оговоркой: вертикаль у нотной руки несёт ВЫСОТУ, и это не просто
  «одно из назначений» — на ней стоят ступенная сетка, гистерезис (`degHyst`), rect-прямоугольники и
  подписи. Освобождать её можно, но это не то же самое, что переназначить громкость.
- **РАСПРОСТРАНИТЬ НА БАС, АККОРДЫ И БАРАБАНЫ.** У аккордов глубина сейчас несёт ЯРКОСТЬ (`a.bri`,
  пер-голосовой фильтр `fb`), у баса — ничего, у барабанов эффектов НЕТ ВОВСЕ. То есть свободного места
  там ещё больше, чем у соло.

**ЧТО УЧЕСТЬ:**
- ⚠️ **ГЛУБИНА ШУМНЕЕ ПЛОСКИХ КООРДИНАТ.** Она вычисляется из РАЗМЕРА КИСТИ (`dist(lm[0],lm[9])`), и
  поворот кисти сдвигает её, не двигая руку. Это ось для ПЛАВНОГО И ГРУБОГО (пространство, тембр,
  подмес), а не для точного. Не вешать на неё то, где промах слышен как ошибка.
- **МЁРТВЫЕ ЗОНЫ И НОРМИРОВКА НА РАЗМЕР КИСТИ ОБЯЗАТЕЛЬНЫ**, как везде (иначе всё зависит от расстояния
  до камеры, а шум распознавания читается как управление). **Механизм уже есть** и переиспользуется:
  `emaS` (сглаживание), `smoothstep` с мёртвой зоной и нормировка на `hs` — всё это работает у руки
  выразительности (`exprFeatures`) и у Z-реверба.
- ⚠️ **ГЕСТ-МАТЕМАТИКА — НА СЫРЫХ КООРДИНАТАХ** (hard rule 12). Нынешний fx-сдвиг читает `lm[4].y`
  напрямую, а не через `sy()`, и это правильно: рука работает и наполовину за кадром. Любые новые оси
  обязаны соблюдать то же разделение — сырые для управления, `sx`/`sy` только для позиции и попадания.
- ⛔ **УМОЛЧАНИЯ ОБЯЗАНЫ ВОСПРОИЗВОДИТЬ НЫНЕШНЕЕ ПОВЕДЕНИЕ ТОЧЬ-В-ТОЧЬ.** Иначе человек откроет
  приложение после обновления и НЕ УЗНАЕТ инструмент — а он к нему привык руками, а не глазами. Это то же
  правило, по которому `handFn` завёлся с дефолтами «левая=эффекты, правая=ноты», и оно ни разу не
  подвело.

**ОТНОШЕНИЕ К §3.29 «КОНСТРУКТОР ЖЕСТОВ» — ДВА РАЗНЫХ СПОСОБА ИГРАТЬ, не два интерфейса к одному.**
Записано с обеих сторон (см. ⚠️ в начале §3.29), чтобы их не слили при планировании:
- **ЭТОТ пункт — ДИСКРЕТНОЕ управление, «ПУЛЬТ».** Палец выбирает, ЧТО менять; координата — НАСКОЛЬКО.
  Действие ЯВНОЕ и с границами: соединил пальцы — взял ручку, развёл — отпустил.
- **§3.29 — НЕПРЕРЫВНОЕ, «СМЫЧОК».** Ничего не выбираешь и не включаешь: рука живёт в кадре, звук
  откликается на её состояние.
- **Первое — для точной НАСТРОЙКИ, второе — для ИГРЫ.** Музыкант крутит ручки МЕЖДУ фразами, а ВО ВРЕМЯ
  фразы ведёт смычком. Нужны оба, в разные моменты.
- ✅ **Полезное совпадение:** §3.29 отдельно предупреждает, что ОТДЕЛЬНЫЕ ПАЛЬЦЫ не годятся как ПЛАВНАЯ
  величина (MediaPipe уверенно различает лишь явно согнутый и явно разогнутый), но годятся как
  ПЕРЕКЛЮЧАТЕЛЬ. Здесь ровно так и устроено — палец ВЫБИРАЕТ, величину ведёт КООРДИНАТА. Одно наблюдение,
  два разных вывода; это и есть довод, что пункты не дублируют друг друга.
- ⛔ **ОБЩЕЕ ОГРАНИЧЕНИЕ: РУК ДВЕ.** Одна играет ноты → вторая либо на эффектах, либо на выразительности,
  но НЕ на обоих сразу (`handFn` даёт руке одну функцию). Человек ВЫБИРАЕТ способ, поэтому каждый
  конструктор обязан быть хорош ПООДИНОЧКЕ, а не в паре с другим.

**ПОРЯДОК.** Начинать с fx-руки (там оси реально свободны и цена ошибки мала), и только потом трогать
играющую руку и прочие роли. Развилку (а)/(б) решать НА СЛУХ, собрав один эффект по варианту (б):
три ручки у задержки сразу покажут, даёт ли глубина больше, чем ширина.
