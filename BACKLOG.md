# BACKLOG — Air Synth 3

Общий список задач. Обновляется по ходу работы.

**Статусы:**
`🔵` — в работе · `⬜` — готово к работе, спецификация есть · `❓` — нужна спецификация · `✅` — сделано

---

## 0. Сейчас в работе

### 🔵 Рефакторинг: разбиение на модули
Полный план — `PLAN.md`, 14 шагов. Правило: один шаг = одна проверка в браузере = один коммит.

| Шаг | Что | Статус |
|---|---|---|
| 1 | `style.css` | ✅ |
| 2 | Весь скрипт → `src/main.js` одним куском | ✅ |
| 3 | `src/config.js` — константы, имена магическим числам | 🔵 следующий |
| 4 | `src/state.js` + сеттеры | ⬜ |
| 5 | `src/scales.js` | ⬜ |
| 6 | `src/hooks.js` + разрезание DOM-записей | ⬜ |
| 7 | `src/audio.js` | ⬜ |
| 8 | `src/backing.js` | ⬜ |
| 9 | `src/recorder.js` | ⬜ |
| 10 | `src/vision.js` | ⬜ |
| 11 | `src/gestures.js` | ⬜ |
| 12 | `src/draw.js` | ⬜ |
| 13 | `src/ui.js` + финальный `main.js` | ⬜ |
| 14 | Обновить `CLAUDE.md` под новую структуру | ⬜ |

**Ничего из списков ниже не делаем, пока идут шаги 3–14.** Причина: успех рефакторинга измеряется тем, что поведение НЕ изменилось. Любая фича, вклинившаяся в середину, лишает нас возможности отличить «сломал при переносе» от «изменилось намеренно».

---

## 1. Фундамент (делать сразу после шага 14, до любых фич)

### ⬜ localStorage для настроек
Сейчас каждый запуск — минорная пентатоника от A. Раздражает с первой минуты.
Сохранять: `scaleIdx`, `tonic`, `seventh`, `leadIdx`, `chIdx`, `back.styleSel`, `back.bpm`, громкость подложки.

### ⬜ Service Worker: кэш модели
Модель `hand_landmarker.task` + WASM качаются с CDN при каждом запуске. Плохой интернет = приложение не стартует вообще.
Даёт: офлайн-работу и быстрый старт. Побочно чинит то, что отмечено в `PLAN.md` §4.1 — «кнопка Запустить мертва, пока jsdelivr не отдаст модуль».

---

## 2. Фичи

### ⬜ Защёлка аккордов (LATCH) — спецификация готова, см. §5
Убирает главную боль жестового интерфейса: рука перестаёт висеть в воздухе. Поставил гармонию — обе руки свободны.

### ❓ Английский интерфейс
**Запрошено. Требует решения до начала.**

Вопрос: заменить русский или добавить переключатель языка?
- **Замена** — проще, но убивает учебный слой: тексты про лады и строи по-русски написаны хорошо.
- **Переключатель (i18n)** — все строки в словарь `{ru: {...}, en: {...}}`, работы примерно вдвое больше, но обе версии живут.

Объём больше, чем кажется. Перевести придётся:
- названия ладов в `SCALES` («Мажор (ионийский)», «Макам Раст», …)
- подписи кнопок и панели настроек
- строку статуса
- **оверлей-учебник** — это самый большой кусок текста в проекте
- подписи зон на canvas (ЭФФЕКТЫ / АККОРДЫ / СОЛО)
- стартовый экран

⚠️ **До шага 14 правило `CLAUDE.md` «UI strings stay Russian» остаётся в силе.** Иначе модель начнёт переводить строки попутно при переносе кода, и инвариант «поведение не изменилось» рухнет.

### ❓ Переделать подложки
**Запрошено, но не специфицировано.** Нужно понять, что именно не так:
- бедные паттерны, мало вариаций?
- звук барабанов (сейчас всё синтезируется на лету из шума и осцилляторов, сэмплов нет)?
- мало стилей (сейчас 4: lofi / synthwave / ethnic / ambient)?
- плохо ложится на микротональные лады?
- не хватает управления (свой рисунок, отключение отдельных инструментов)?

Пока не ответим — задачи нет.

### ⬜ Запись → события намерения вместо кадров
Сейчас `WleadOn` вызывается каждый кадр (~60/с) — минута игры даёт порядка 7000 объектов, воспроизведение вешает столько же `setTimeout`. Это лог кадров, а не партитура: нельзя транспонировать, квантовать, редактировать, экспортировать.
Переделать на `{t, тип, ступень, октава, громкость}`. Разблокирует всё, что ниже.

### ⬜ Лупер
**Самая важная фича в списке.** Записали круг аккордов → играет по кольцу → поверх ведёте соло. Инструмент становится самодостаточным.
Всё необходимое уже есть: точный планировщик, две независимые руки, раздельные каналы аккордов и соло.
Зависит от: «запись → события намерения».

### ⬜ Режим урока
Подсветить следующую ступень, задание «сыграй I–IV–V–I», проверка попадания.

### ⬜ Сравнение строёв
Кнопка «та же фигура в другом строе»: сыграть записанное в 12-TET, потом в 19-TET подряд. Момент «ого», ради которого приложение существует.
Зависит от: «запись → события намерения».

### ⬜ Показ отклонения от натурального строя в центах
`centsOf` уже написан, нужен только вывод.

### ⬜ Заменить реверб
Сейчас IR генерируется из случайного шума (`makeIR`) — самое слабое место звука.

---

## 3. Баги

### ✅ Стартовый экран не исчезал
`#start{display:block}` (приоритет 1-0-0) перебивал `.ov.on` (0-2-0). Класс снимался, `display` оставался. Приходилось убирать через F12.
**Исправлено:** убрано `display:block` из `#start`.

### ⬜ `revDisp` не сбрасывается при отпускании соло-щипка
REV-бар замирает на последнем значении.

### ⬜ `clrBtn` не выключает `recording` и не сбрасывает `recStart`
Очистка во время записи → следующие события пишутся со старым отсчётом → воспроизведение начнётся с длинной паузы.

### ⬜ `selTonic.onchange` не вызывает `softAllOff`
Смена тоники не глушит звучащий аккорд — он остаётся в старой тональности. `selScale.onchange` вызывает, `selTonic` — нет.
Сейчас почти незаметно (надо держать щипок и лезть в меню другой рукой). **С защёлкой станет постоянным.** Чинить вместе с LATCH.

### ⬜ `qTriad`/`qSev` не вызывают `softAllOff`
То же самое: переключили на септаккорды, а звучащее трезвучие осталось трезвучием. **С защёлкой станет заметным.**

### ⬜ Идентификация рук: `key+=i`
При двух руках одной handedness ключ зависит от порядка в массиве результатов и может меняться между кадрами → редкая повисшая нота до срабатывания watchdog.

### ⬜ `refreshStyle(back.styleSel!=='auto')` затирает пользовательский BPM
При ручном выборе стиля BPM сбрасывается в дефолт стиля. Возможно задумано, но выглядит спорно.

### ⬜ `chordGlide` при смене длины аккорда
Если голосов было 3 (трезвучие), а стало 4 (септаккорд) — четвёртый не зазвучит до переатаки. `chordGlide` защищён `if(fr==null)return;`, так что молча.

---

## 4. Решено НЕ делать

- **Новые лады и тембры.** Их уже с избытком. Самая приятная и самая бесполезная работа.
- **MIDI-экспорт как ранняя фича.** Микротональность в обычный MIDI не влезает (только MPE — отдельная боль).
- **React / Vue.** Рендерить нечего, всё на canvas.
- **Tone.js.** Движок уже написан и работает. Замена = месяц регрессий ради того же результата.
- **Сборщик (Vite/webpack).** Зависимостей нет, собирать нечего.

---

## 5. Спецификация: защёлка аккордов (LATCH)

**Область: только колонка АККОРДОВ.** Соло и эффекты не трогаем.

### Поведение

| Событие | Что происходит |
|---|---|
| Соединил пальцы на ступени, ничего не защёлкнуто | Аккорд звучит. Рука ведёт: Y = ступень (глиссандо), X = громкость |
| Разъединил пальцы | **Аккорд продолжает звучать.** Ступень и громкость замирают на последних значениях |
| Соединил пальцы на **той же** ступени | **Аккорд выключается.** Рука становится инертной до размыкания |
| Соединил пальцы на **другой** ступени | Плавный переход в новый аккорд (`chordGlide`, без переатаки). Старый снят |

**Защёлкнутый аккорд ровно один.** Любая рука им управляет. Кто последним сделал щипок в колонке аккордов — тот и рулит; предыдущая рука теряет управление до своего следующего щипка.

### Ключевое решение: инертность

Без неё фича не работает. Разбор: III защёлкнут, вы делаете стоп-щипок на III → звук выключился, но **пальцы всё ещё соединены**. Рука дрожит, палец уезжает на IV, аккорд следует за рукой (`chordGlide` каждый кадр) → IV включился. Выключить невозможно в принципе.

Поэтому: **после стоп-щипка рука ничего не играет, пока пальцы не разомкнуты.** Разомкнул, соединил заново — жест начинается с нуля.

При этом глиссандо для обычного щипка сохраняется полностью: соединил на III → звучит III → ведёшь руку к V не размыкая → плавно переходит в V → разомкнул → V защёлкнут.

### Состояние

- `latchDeg` — номер защёлкнутой ступени, `-1` если тишина
- `chOwner` — ключ руки, которая сейчас рулит (или `null`)
- `S.inert` — флаг на состоянии руки, ставится при стоп-щипке, снимается при размыкании
- **Владелец голосов — постоянный ключ `'latch'`**, не ключ руки

Последнее решает проблему watchdog автоматически: рука ушла из кадра → `endPinch` → но голоса принадлежат `'latch'`, а не руке → защёлкнутый аккорд не умирает.

### Правки по файлам (после шага 14)

| Файл | Что |
|---|---|
| `gestures.js` | Логика захвата: сравнение `deg` с `latchDeg`, ветка стоп / ветка переключения. Флаг `S.inert`. `endPinch` для зоны `'ch'` больше **не** вызывает `WchOff` |
| `audio.js` | Ничего. `chordOn/chordGlide/chordOff` уже принимают произвольный ключ владельца |
| `draw.js` | `chAct` берётся из `latchDeg`, а не из состояния рук. Подсветка горит на защёлкнутом аккорде всегда |
| `ui.js` | `selTonic.onchange` и `qTriad`/`qSev` → добавить `softAllOff()` + сброс `latchDeg` |
| `recorder.js` | `softAllOff` уже обходит `Object.keys(chordHold)` → `'latch'` попадёт сам. Добавить сброс `latchDeg = -1`. То же для `panic` |

### Что проверять руками

1. Щипок → отпустил → **аккорд продолжает звучать**.
2. Щипок на той же ступени → **выключился**.
3. Щипок на другой → плавно переключился, старый снят, наложения нет.
4. Стоп-щипок → не размыкая, поводить рукой по колонке → **тишина**, ничего не включается.
5. Аккорд защёлкнут → **убрать руку из кадра совсем** → продолжает звучать (watchdog не трогает).
6. Аккорд защёлкнут → сменить тонику в меню → **аккорд заглох** (а не остался в старой тональности).
7. Аккорд защёлкнут → сменить лад → заглох.
8. Аккорд защёлкнут → panic → заглох, подсветка погасла.
9. Подсветка ряда горит на защёлкнутом аккорде и без щипка.
10. Обе руки: рука A защёлкнула III, рука B щипает V → V звучит, III снят.

### Известный риск (проверить руками)

**Дребезг на границе при стоп-щипке.** III защёлкнут, вы щипаете у границы III/IV, шум MediaPipe даёт IV → вместо выключения переключились на IV. Гистерезис `degHyst` при захвате не работает: там `S.deg = -1`, и функция возвращает сырое значение.

Если промахи окажутся частыми — применить `degHyst` в момент захвата с `prev = latchDeg`, то есть с уклоном в пользу защёлкнутой ступени. Заранее не делать: возможно, попадания и так хватит, а лишний код = лишний риск.
