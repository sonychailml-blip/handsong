# HANDOFF — снимок проекта Handsong (бывш. Air Synth 3) для нового чата

Этот файл — передача контекста в новую сессию. Прочитать ВМЕСТЕ с
`CLAUDE.md`, `BACKLOG.md`, `UI-MAP.md` (все в корне проекта). `PLAN.md` и
`MENU-PLAN.md` — АРХИВ завершённых работ (рефакторинг и перестройка меню), их
читать только как историю, под текущий код они не переписаны и не должны быть.
Пользователь также пришлёт все файлы `src/` — это актуальный код, опирайся на него,
а НЕ на память.

> `CLAUDE.md` переписан под текущий код и ему можно ДОВЕРЯТЬ. В нём, помимо прежнего (лупер, типы
> аккордов, свойства ладов, rect-модель, Парч, цент-аккорды, неоктавный период, слияние ПК/телефон,
> индийская традиция, камера/поля кадра, клип/UI-волна, три пути синтеза, переменный размер такта,
> планировщик с опережением, `fixedKey`+велл-темперации, `aRef`, разбор аккорда, функции рук у аккордов,
> `a.bri`+два фильтра, рука-лупер, разделы меню по звучанию, COVER-видео): **рука-ВЫРАЗИТЕЛЬНОСТЬ,
> ОБУЧЕНИЕ (шесть уроков), ЛОКАЛИЗАЦИЯ (en+ru), ПУБЛИКАЦИЯ/PWA, дробление доли `sub` + 18 ритмов,
> кнопка «Подложка»**. Hard rules — 25 штук; новые: **22–23 выразительность, 24 обучение, 25 «имя ≠
> идентификатор»**.
> Устаревшие имена, если встретишь: `backing.js`, `playRec`, `toggleRec`, `stopRec`, `recStart`,
> `sectHyst`, `TYPED_CH_VOL`, `drawChordSectors`, `RECT_OCT`, `uiMode`, `drawPC`, `drawBar`,
> `zoneAt`/`zoneX`, `FXW`/`ZB`, `modeBtn`, класс `.phone`, `#modeHint`, `#arrHint`, `#helpBtn`,
> `swapHands`, глобальный `theremin`-тумблер, `thereminBtn`/`applyTheremin`, `swapBtn`/`applySwap`,
> `drawThereminGrid`, `BEATS_PER_BAR` как жёсткая константа, **`handRole` (удалён — привязки руки к роли
> НЕТ НИГДЕ), `jamStep`/`applyJam`/`jamTo` (стали `backing*`), ключи словаря `jam.label`/`jam.title.*`**.
> ⚠️ Шапка-комментарий `main.js` всё ещё описывает УДАЛЁННУЮ трёхколоночную ПК-раскладку — устарела,
> не верить (правка кода, не документации).

---

## ПОСЛЕДНИЕ СЕССИИ — ЧТО ВЫШЛО (сверено с кодом)

Проект перешёл рубеж: он **опубликован**, **умеет объяснить себя сам** и **говорит на двух языках**.

1. **ПУБЛИКАЦИЯ. Живёт здесь: https://sonychailml-blip.github.io/handsong/** (GitHub Pages).
   ⚠️ Свой домен **`handsong.app` ЗАДУМАН, но ещё НЕ подключён** — адрес может смениться. В коде его
   нет нигде (и не должно быть: базовый путь относительный), поэтому переезд = правка ДОКОВ, не `src/`.
   Переименован в **Handsong**. Выложен статикой; базовый путь ОТНОСИТЕЛЬНЫЙ (`.`) —
   работает из подпапки GitHub Pages, а HTTPS там нужен камере/звуку/ворклету. **Устанавливается**:
   `manifest.json` + `sw.js`. SW **намеренно БЕЗ кэша** (пустой `fetch`) — единственная его задача в том,
   что Chromium не пришлёт `beforeinstallprompt` без зарегистрированного SW с fetch-обработчиком, и кнопка
   установки не появится вовсе; кэша нет, значит ничто не застрянет устаревшим. Кнопка установки — ПО
   РЕАЛЬНОЙ СПОСОБНОСТИ, не по строке браузера (Chromium — придержанное событие; iOS Safari — инструкция
   вместо мёртвой кнопки; standalone — прячем всё). Отзыв и донат — ДВЕ константы в `ui.js`
   (`FEEDBACK_URL`/`DONATE_URL`), пустой донат прячет строку целиком.
   URL записан выше — в `src/` его нет и быть не должно.
2. **ОБУЧЕНИЕ — шесть уроков** (`tutor.js`): Основы → Аккорды → Строи и тембры → Лупер → Функции рук →
   Две роли. Уроки и шаги — ДАННЫЕ, цепочка через поле `next`, всё ведётся событиями через ОДИН канал
   `hooks.tutor`, и события шлются ИЗ ТОЧЕК реального действия (событие ⇔ действие), а не пересчитываются
   параллельно. Урок идёт ВНУТРИ живого приложения. **Правило, ради которого чинили:** setup урока обязан
   привести приложение в состояние, где ЕГО шаги выполнимы, что бы ни делал предыдущий. Протечки, что
   нашли и закрыли: функции рук текли в «Основы» (левая уже не на эффектах, аккорд-рука на «удержании» —
   шаг защёлки не срабатывал), джем/петля текли в следующий урок, «Лупер» попадал на бесаккордовый Пелог
   после «Строёв» и просил роль, которая молчит. Подсказка прячется под работающим пальцем **по ФОКУСУ**,
   а не по pointer-событиям: нативный пикер `<select>` съедает `pointerup`.
3. **РУКА-ВЫРАЗИТЕЛЬНОСТЬ** — целый новый механизм. Рука нот не играет, а кормит систему СО СВОЕЙ
   ДИНАМИКОЙ. Инварианты (hard rules 22–23): живость даётся МАЛЫМ числом входов и БОГАТЫМ средним слоем
   (резонатор накачки-и-спада, дрейф тембра, инерц-пружина), а не числом параметров; четыре канала ведут
   ЧЕТЫРЕ РАЗНЫЕ оси (энергия→живость/вибрато, раскрытость→вау, размах пальцев→характер искажения,
   наклон→эхо) — раньше три из них толкали ЯРКОСТЬ, и всё ощущалось как одна ось, поделённая на четыре
   способа; **суб-шаговый интегратор (фикс. ~8 мс) существует потому, что пружина самовозбуждалась ниже
   ~13 fps — это и была загадочная дрожь 5–8 Гц**; мёртвая зона НЕСУЩАЯ (покоящаяся рука обязана давать
   РОВНО ноль, иначе шум распознавания читается как звук); из высоты трогает только живой шиммер в пределах
   пары центов и **никогда не пишется**.
4. **ЛОКАЛИЗАЦИЯ** (en по умолчанию + ru; es/de готовы ПО ФОРМЕ, но выключены — терминологию проверить
   некому). Интерфейс — в словарях; музыкальные данные — ВНУТРИ данных, тремя формами, и форма кодирует
   решение: голая строка = международная запись, `{default, ru}` = имя собственное (транслитерация),
   полный объект = переводимое слово. **Опасность идентификатора, закрытая: `demo.js` сопоставлял лады
   ПО ИМЕНИ, а меню бакетило группы по ПОКАЗЫВАЕМОЙ подписи** — оба сломались бы; теперь стабильный индекс
   и стабильный ключ. Больше ничто не смеет опираться на отображаемое имя (hard rule 25).
5. **РИТМ**: паттерн объявляет `sub` (ячеек на долю: 4 = 16-е, 3 = ТРИОЛИ), `stepBeat=1/sub`; сетка обязана
   быть ровно `beats*sub`, иначе паттерн пропускается (защита от опечатки в данных). Квантизация ЖИВЫХ
   ударов — по `loop.sub`, ЯВНОЙ настройке панели. Восемнадцать паттернов (десять ходовых 4/4, шаффл —
   единственный триольный). Кнопка **«Подложка»** — два пути (джем целиком / только ударные), список
   ударных ФИЛЬТРУЕТСЯ ПО РАЗМЕРУ, звучащий вариант НАЗВАН (кнопка + подсказка + тост). Обе ветки — та же
   метка `ev.jam`, одна очистка. **Две ошибки того же захода:** в аранжировку не передавался выбранный
   набор ударных (любая подложка играла «Стандартом»), и правило свёрнутой подсказки тура несло свой
   `display`, перебивая правило скрытия — пустая полоса вылезала БЕЗ урока.
6. **Мелочи, но настоящие:** палитра типов аккордов — ПО ПОЛОЖЕНИЮ и в single-role, и в сплите (`handRole`
   удалён целиком); тап по кнопке бара НАЖИМАЕТ её, а не разворачивает бар (иначе кнопка уезжала из-под
   пальца во вторую строку); видео кадрируется COVER (`coverView`/`viewRect`) — картинка не растягивается,
   и ОДНА область кормит и рисование, и попадание.

---

## ЧТО СДЕЛАНО С ПРОШЛОГО СНИМКА (~15 раундов, сверено с кодом)

Большая волна поверх слияния ПК/телефон и индийской традиции. Каждая фича — отдельный заход, single-role
путь цел. Часть внесла АРХИТЕКТУРНЫЕ инварианты — они выписаны в `CLAUDE.md` (hard rules 15–21).

**Звук — три пути синтеза (а не один):**
1. **FM** (`buildFMBank`): несущий через `mkOsc` (в общую цепь), модулятор только в `car.frequency`;
   огибающая ИНДЕКСА спадает быстрее амплитуды → «удар по металлу». Нецелый ratio даёт ингармонический
   спектр, недостижимый субтрактивно. Банки «Колокол (глубокий)» (1.41), «Металлофон» (3.51).
2. **Карплюс–Стронг** (`ks-worklet.js`, AudioWorklet — 15-й модуль): ворклет ОБЯЗАТЕЛЕН, не выбор —
   нативная петля DelayNode заперта одним квантом (128 сэмплов ≈ 344 Гц, выше недостижимо; сэмпл-
   квантованный delay детонирует). Строй через аллпас дробной задержки 1-го порядка + компенсацию фазы
   петлевого фильтра → <1 цента. Банки Струна/Ситар (джавари=`nl`, тараб=`symp`)/Уд/Кото/Сантур/Гитара/
   Пиццикато. Провал `addModule` → `buildKSFallback` (субтрактивные щипки, индексы не съезжают).
3. **Субтрактив расширен** (пады/органы/щипковые/духовые). Пады/органы питч-ЧИСТЫЕ (`hj:0`/`det:0`) —
   биения = СТРОЙ, не тембр. Гуманизация свежая на атаке, НЕ записывается; бас не гуманизируем по высоте;
   скорость→яркость на атаке, не пофреймово. Наборы ударных: Табла/Дарбука/Гамелан(FM)/Тайко.

**Время и лупер:**
4. **Переменный размер** (`loop.metre`, `METRES=[2..16]`, только на пустой петле) заменил константу
   `BEATS_PER_BAR`. Знаменателя НЕТ по замыслу (движок считает доли при BPM). Квантизация уже была по долям.
5. **Акценты по группам** (`METRE_GROUPS`: 7=3+2+2, 9=2+2+2+3, 16=4×4 с khali в 3-м вибхаге; `beatLevel`)
   — 4 уровня метронома И сетка тактов. khali = ОСЛАБЛЕННАЯ голова, не тишина. Ритмы объявляют `beats`.
6. **Планировщик с опережением** (hard rule): слои планируются на ТОЧНОЕ время (`loop.t0+ev.t*spb`) в
   окне `SCHED_AHEAD≈300мс`, живые вызовы — `AC.currentTime`. Голосовые функции берут опц. `when`. Убрал
   дрейф от блокировок main-thread (синхронный MediaPipe). Окно МАСКИРУЕТ зависания, корень — vision в
   main-thread. Ограниченные цены: хвост барабана до окна на стопе; ≤окно провал слоёв после undo/clearJam.

**Строй:**
7. **`fixedKey`** (hard rule): у шести исторических строёв тоника — КЛЮЧ, а не множитель (сетка от
   абсолютного C, якорь `cFix()`). Веркмайстер: терция ≈390¢ в C, ≈408¢ в F#. Ряды — РЕАЛЬНЫЕ имена нот.
   Натуральный подвижный намеренно НЕ `fixedKey` (контраст пары — урок). `fixedKey` ≠ `gridChords`.
8. **Регулируемый эталон A4** (`aRef`, hard rule «один источник»): `baseF()` И `cFix()` берут `a3=aRef/2`.
   Пресеты 415…444 + свободный ввод (кламп 380–480). Двинул эталон — уехало ВСЁ вместе.
9. **Велл-темперации** Веркмайстер III/Валлотти/Кирнбергер III (индексы 72/73/74) с рецептами.

**Дисплей:**
10. **Разбор аккорда**: пока аккорд звучит — Гц каждой ноты + интервал над корнем в центах, из ЗВУЧАЩИХ
    голосов (`chordHold`), не пересчёт. Только живой защёлкнутый аккорд.
11. **Центы в ярлыке соло/баса на КАЖДОМ ладу** (сняли гейт `edo!==12`, прятавший их у edo:12 темпераций).

**UI/управление:**
12. **Полный экран** (`⛶` на старте и в баре, одно состояние через `fullscreenchange`; спрятан без API).
13. **Функции рук** (заменили `〰` терменвокс и `⇄` смену рук): функция на руку в пределах роли (`handFn`,
    соло/бас — своя пара): Эффекты(соло)/Ноты/Ноты-с-удержанием/Терменвокс. Удержание морозит высоту,
    громкость по X живая. Терменвокс — теперь функция, соло И бас (Гц баса = соло/4, не записывается),
    АДДИТИВНЫЙ оверлей линий поверх сетки. Моно-передача: последний щипок берёт голос, отпустил →
    возвращается зажатой руке. **В сплите — ПО ПОЛОВИНЕ по её роли; прежний хардкод «эффекты у левой в
    соло-половине» УБРАН (дериватив дефолта `handFn.ld.L='fx'`) — один источник правды (hard rule 18).**

`SCALES` теперь **0..74 (75 ладов)**, хвост: 50 Болен–Пирс · 51/52/53 Карлос α/β/γ · 54/55/56 патеты
пелога · 57 Пифагоров · 58/59 Натуральный подвижный/фиксированный · 60 Мезотон · 61 сетка 22 шрути ·
62..71 раги · **72/73/74 Веркмайстер III/Валлотти/Кирнбергер III**. Порядок веток `chordFreqs`
(ФИКСИРОВАН, +`fixedKey`-ветка в каждой pitch-функции): (1) `s.cents && ty && s.gridChords` из сетки;
(2) `s.cents && ty` чистое отношение от корня; (3) `periodOf!==2 && ty` период-корень·ratio; (4) иначе
`chordSteps`.

---

## КАК МЫ РАБОТАЕМ (метод — не менять)

- Пользователь запускает **Claude Code в VS Code** (отдельно от этого чата).
- Ассистент (ты) — стратег: пишешь английские промты, пользователь вставляет их
  в Claude Code, приносит план/дифф обратно, ты сверяешь с РЕАЛЬНЫМ кодом и правишь.
- **Правило: после одобрения плана следующий промт должен явно говорить
  «implement now, don't stop at plan».** Иначе Claude Code останавливается на плане
  (мы его так приучили), и пользователь думает, что сделано, а кода нет. Теряли круги.
- **Один шаг = одна правка = одна проверка в браузере = один коммит.** Не сваливать
  несколько задач в один заход — иначе при поломке не понять, что виновато.
  (Это правило про КОД. Документы можно править без браузерной проверки.)
- Коммитит ТОЛЬКО пользователь. Ты git не запускаешь.
- **ВСЕГДА сверяйся с реальным кодом через чтение файлов перед промтом.** Память врёт;
  главная ошибка прошлых сессий — смотреть на старый файл вместо текущего `src/`.
  Проси свежий код, если сомневаешься.
- Промты — на английском (Claude Code), общение в чате — на русском.
- Тестирование на телефоне: `npx cloudflared tunnel --url http://localhost:5500`
  (Live Server на 5500). Ссылка каждый раз новая, живёт недолго. Кэш — через `?v=N`.

---

## ЧТО ЗА ПРОЕКТ

Браузерный жестовый синтезатор + учебник по строям/ладам. Вебкамера → MediaPipe
(отслеживание рук) → щипки пальцами играют ноты/аккорды/бас/ударные/эффекты через
Web Audio. Поверх живой игры — лупер (запись намерения, овердаб, undo по слою) и
аранжировка (гармония/ритм/бас как замороженные слои). UI и комментарии — русские.
Без сборщика, без зависимостей кроме MediaPipe с CDN. **Основная цель — телефон
вертикально.** ПК-режим УДАЛЁН (не заморожен — вырезан целиком; его «две роли сразу» закрыл
сплит-экран). Никакая будущая работа больше НЕ должна «беречь ПК».
15 ES-модулей в `src/` (config/state/hooks/scales/audio/**ks-worklet**/arrange/recorder/vision/**clip**/
**demo**/gestures/draw/ui/main) + `index.html` + `style.css`. Git на ветке master.

Философия (из слов пользователя): «ясная база, ясный фундамент, потом наращивать».
Программа даёт разные музыкальные традиции/строи, всё сочетается со всем через
слои лупера. Учит тому, как устроена музыка. Не гнаться за количеством ради количества.

---

## ТЕКУЩЕЕ СОСТОЯНИЕ (что готово, сверено с кодом)

### Архитектура
- Рефакторинг из 1 файла в модули — ЗАВЕРШЁН. Граф ацикличен. Аудио отвязано от DOM
  через `hooks.js`. `backing.js` УДАЛЁН; его дрон и дарбука переехали в `audio.js`,
  его роль источника времени заменил лупер в `recorder.js`.
- `CLAUDE.md` актуален (переписан эту сессию).

### Звук/движок (`audio.js` + `ks-worklet.js`)
- Пять сигнальных цепей в master→лимитер: соло (банки → драйв → огибающая → громкость
  → тремоло → делей/реверб), аккорды (пул `CHORD_POOL_N=24`), бас (пул `BASS_POOL_N=4`,
  моно на слой), ударные (синтез на лету, кит из `a.kit`), дрон (следует за `tonicFreq()`).
  Общий реверб — **FDN (сеть задержек с обратной связью), `buildFDN`+`REV_A`**; конвольвер и `makeIR`
  УДАЛЕНЫ. Четыре линии задержки, три входных аллпаса-диффузора, демпфирующий ФНЧ ВНУТРИ петли,
  перемешивание Хаусхолдера, стерео-отводы; ~49 узлов, без осцилляторов и ворклета. Смысл замены: у
  свёртки хвост ЗАПЕЧЁН в буфер, живьём менялось только КОЛИЧЕСТВО посыла. Теперь ТРИ настоящих
  параметра — подмес (посылы), длина (`setRevDecay`, RT60→g каждой линии) и окраска (`setRevTone`,
  срез ФНЧ в петле); ИГРОКУ пока отдан только подмес: Z-рукой рулит ТОЛЬКО send соло, как и раньше.
  ⚠️ Q ФНЧ в петле обязан быть Баттервортом (`REV_A.toneQ=−3.0103`) — иначе сеть самовозбуждается
  (см. «Ключевые уроки»). Оба посыла (соло `revLead`, аккорды `revCh`) приходят на узел `verb`, и их
  проводка при замене НЕ ТРОГАЛАСЬ — общий реверб работает по построению.
- **ТРИ пути синтеза лид-банков** (банк = `{gain,setFreq,cancel,strike?}`, все на одной цепи):
  субтрактив, FM (`buildFMBank` — ингармония), Карплюс–Стронг (`buildKSBank`/ворклет — строй <1¢,
  fallback `buildKSFallback`). Пады/органы питч-чистые (биения = строй). Наборы ударных: Стандарт/
  Дарбука/Табла/Гамелан(FM)/Тайко, те же 6 рядов.
- Гейн-стейджинг починен: бас выровнен под аккорд (`lvl`), не давит через лимитер.
- Латентность: MediaPipe на CPU (~196 мс телефон, ~57 мс десктоп) — баг MediaPipe web
  (delegate GPU молча = CPU), не наш; потолок платформы. Порядок цикла: видео и точки
  из одного кадра (когерентность, hard-rule #8 в CLAUDE.md).

### Лупер (`recorder.js`, готов)
- Запись хранит НАМЕРЕНИЕ: `{t в долях, layer, fn, a:{deg,oct,vol,inst,ty,kit…}, sc, sev}`.
  Транспорт по тактам (насос по часам AC), овердаб, undo-по-слою, счёт-вступление, квантизация.
  Темп в `loop.bpm`. **Размер такта переменный** (`loop.metre`, 2..16, без знаменателя, только на
  пустой петле); акценты по группам (`METRE_GROUPS`/`beatLevel`). **Слои планируются с опережением** на
  точное время (окно ~300мс); живые вызовы — «сейчас» (архитектурный инвариант, hard rule 15).
- **Заморозка контекста per-event = полимодальность.** Каждое событие помнит свой
  строй/тонику/септаккорд/тембр/ТИП. Слой играет в строе записи, живая рука — в текущем.
  Replay резолвит высоту через замороженный `sc`. ЭТО НЕСУЩЕЕ ПРАВИЛО — не сломать.
- Аранжировка (`arrange.js`): PROGRESSIONS (II-V-I, I-vi-ii-V, I-IV-V, блюз), RHYTHMS
  (18 паттернов: прямой рок и маqсум, нечётные размеры, десять ходовых на 4/4 включая
  ШАФФЛ на триольной сетке), BASS_MODES (none/roots/pedal), дрон как вариант гармонии.
  Грузятся как замороженные слои. Паттерн объявляет `beats` + `sub` (ячеек на долю,
  4 по умолчанию, 3 = триоли); `stepBeat=1/sub`, годность — `rhythmFits` (размер + сетка).

### Защёлка аккордов (готова)
- Щипок переключает аккорд; держится без руки. Owner `'latch'`. В типизированных ладах
  тождество защёлки = (ступень + ТИП), компаньон `latchTy` рядом с `latchDeg`.

### Микротоника
- Аккорды в 19/31-TET строятся ПО ИНТЕРВАЛАМ от корня (`chord`/`chord7` на ладу),
  не пауэр-аккордами. 31-TET септаккорд = 4:5:6:7.
- Макам: свойство `noChords` — аккорды отключены. Только соло + дрон. Хочешь аккорды
  под макам — запиши в другом ладу, переключись, играй соло (полимодальность).

### Обучение (`tutor.js`, готово)
- Шесть уроков ДАННЫМИ, цепочка полем `next`, финал — карточка ВЫБОРА (не автопрыжок). Шаги ждут реального
  действия, `done(acc)` — чистые предикаты, `gate()` роняет шаги, бессмысленные в текущем ладу.
- Всё через ОДИН канал `hooks.tutor` из точек действия (метки `ЗАЦЕПКА ОБУЧЕНИЯ` в `gestures/ui/recorder`).
  ⚠️ Уроки учат ТЕКУЩЕЙ жест-модели — правишь жест, правь его зацепку.
- `teardown` — ЕДИНСТВЕННЫЙ выход (финал/«Готово»/Пропустить/переход по цепочке/защитный автопроход).
- Прогресс — тихая ✓ на урок в localStorage, НЕ гейт: любой урок доступен в любом порядке.

### UI (перестройка меню + UI-волна — сделаны)
- **Старт — светлая карточка**: «Играть» и **«Обучение» (работает — открывает список уроков; выбор урока
  сам поднимает приложение тем же кликом)**; ниже ⛶ во весь экран и ⬇ установка (по реальной способности).
  Подвал: 🌐 язык / первая-версия / отзыв / донат (скрыт пустым) / e-mail собран в JS. Инструктаж — в
  `#helpOv` (пока недостижим из UI; ему место внутри обучения).
- Два меню: `#panelScale` (звукоряд: лад/тоника/**эталон A4**/раскладка/пальцы/септаккорды/тембры/
  **Функции рук**/**Конструктор эффектов**) и `#panelLoop` (такты/**размер**/BPM/квантизация/аранжировка).
  Обе панели **светлые, непрозрачные** (поверх яркой камеры), в одном месте (открытие одной прячет
  другую). ⇄ и `〰` УБРАНЫ — их роль забрала секция «Функции рук» (функция на руку в пределах роли:
  Эффекты/Ноты/Ноты-с-удержанием/Терменвокс).
  ⚠️ **`#panelScale` ПЕРЕГРУЖЕНА ПО СМЫСЛУ, и это признано:** половина её содержимого (тембры, функции
  рук, весь конструктор эффектов) к звукоряду отношения не имеет — панель зовётся «Звукоряд», пока в
  ней крутят реверб. Разрез на «Звукоряд» и «Звук и управление» описан отдельным пунктом ниже, в конце
  раздела «КОНСТРУКТОР ЭФФЕКТОВ» («РАЗДЕЛИТЬ ПАНЕЛЬ»).
- **Верхняя полоска авто-скрывается** (`.min`) через ~3.5с; тап/движение мыши раскрывает. Свёрнутая
  держит только индикатор роли(ей) + три кнопки записи (🎥/🎙/●) — ТЕ ЖЕ DOM-элементы. Не прячется при
  открытой панели. Раскрытая: `[Лад·Тоника]`·роль(и)·`◨`·`🔄`·`⛶`·`🎥`·`🎙`·`🎵`·`⚙`·`●`.
- Транспорт лупера — HTML-полоса под холстовой полосой тактов (независим от `#panelLoop`).
- Эффекты соло: вертикальные столбики СЛЕВА поверх сетки (в сплите — у соло-половины).
- Одна раскладка (ПК удалён). Кнопки режима 💻/📱 нет.

### Типы аккордов — ПАЛИТРА (СДЕЛАНО; 8 наборов в `CHORD_FAM_SETS`)
> Старая модель «секторы в ряду» (`sectHyst`, `TYPED_CH_VOL`, X=сектор) УДАЛЕНА. Ниже — актуальное.
- Экран аккорд-роли делится: СЛЕВА палитра типов (колонка=семейство, ряд=вариант, из данных
  `chordFams()`/`types.length`), СПРАВА ряды/прямоугольники КОРНЕЙ. Ячейка выбирается ПОЛОЖЕНИЕМ
  (большой+указательный, `S.oct===0 && S.x<palSplitX`, липко): вне сплита — левая (fx) рука; в
  СПЛИТЕ по положению любой рукой (привязки к руке нет). Игровая рука: Y=корень, X=ГРОМКОСТЬ, октаву —
  палец (нерект) ИЛИ октавный прямоугольник (19/31/Парч). `cellHyst` двухосный.
- Свойство `typedChords:'<ключ>'` → набор в `CHORD_FAM_SETS` (8 ключей): `chrom12`/`edo19`/`edo31`/
  `natfix` — целые шаги; `partch`/`bp`/`pyth`/`nat` — отношения. Пряности по ветке `chordFreqs`
  (см. порядок веток выше): grid для `natfix`+`gridChords`, чистое отношение для `partch`/`pyth`/`nat`,
  период для `bp`, целочисленный `chordSteps` для `chrom12`/`edo19`/`edo31`.
- Тип замораживается в событии (`a.ty` — ссылка на массив из таблицы), защёлка по (ступень+тип).
- Витрины: 31-TET Дом7 [0,10,18,25]=4:5:6:7 · Парч О7 чистый 4:5:6:7 · BP 3:5:7 без биений ·
  Натуральный подвижный vs фиксированный — тот же аккорд, второй волчит.

---

## ЧТО ДАЛЬШЕ (ближайшие задачи)

### КОНСТРУКТОР ЭФФЕКТОВ (BACKLOG §3.39) — СДЕЛАНЫ 1, 2, 2.6, 3.1—3.7; ОСТАЛИСЬ 3.8, 3.9 и 3.6 (независим)
> **ЦЕЛИ (отдельно от пластов, см. «ЧЕГО ЕЩЁ НЕТ»): (а) ✅ · (б) ✅ кроме ударных (остался б.4) · (в) ⬜ ·
> (г) ⬜ отложена пользователем.** Плюс внеплановый **слайс «д» ✅** — дискретные адреса (`handActs`).
> ⚠️ **НУМЕРАЦИЯ ОСТАВШИХСЯ ПЛАСТОВ ПЕРЕСМОТРЕНА ЕЩЁ РАЗ.** Прежний список («3.3 — двойной щипок,
> 3.4 — переназначаемая громкость») **ОТМЕНЁН**: между ними встал АРХИТЕКТУРНЫЙ ПОВОРОТ — эффекты
> принадлежат РОЛИ, а не руке (см. блок ниже), и он меняет и состав, и порядок. Канонический список —
> в «ОСТАВШИЕСЯ ПЛАСТЫ». Переназначаемая громкость (X) НЕ потеряна: она переехала в 3.7, к единому
> заходу по формату события (см. там).
> ⚠️ НЕ ПУТАТЬ с §3.29 «Конструктор ЖЕСТОВ» — это разные способы играть, см. предупреждение в BACKLOG.

**ЗАМЫСЕЛ.** Эффекты становятся УНИВЕРСАЛЬНЫМИ: любой эффект на любой роли (соло / аккорды / бас /
ударные), до ТРЁХ параметров у каждого, всё пишется в лупер, новый эффект добавляется МОДУЛЕМ, а не
правкой пяти мест. Сегодня эффектов ровно пять, они прибиты к соло и к руке-эффектов, и добавить шестой
означает трогать `applyFx`, столбики в `draw`, событие лупера и жест разом.

**МЕТОД — ПЛАСТАМИ, ПО ОДНОМУ ЗА РАЗ, каждый обкатывается на превью до следующего.** Это не осторожность
ради осторожности: критерий Пласта 1 — «звучит БАЙТ-В-БАЙТ как продакшен», и такой критерий проверяем
только когда за раз меняется одно. ⚠️ Пластов изначально задумывалось ТРИ; по ходу их стало больше —
Пласт 2 разошёлся на пять слайсов, Пласт 3 — на 3.1…3.7, и перед ними встал фундаментный 2.6. Дробить
дальше, когда заход оказывается крупнее, чем виделся, — норма этой работы, а не срыв плана: каждое
дробление здесь случалось потому, что ЧЕСТНЫЙ размер захода выяснялся при планировании, а не после.
- **ПЛАСТ 1 — АБСТРАКЦИЯ + РЕВЕРБ. СДЕЛАН.** Модуль эффекта `{id, labelKey, in, out, params[]}`
  (`makeReverbFx` в `audio.js`), вариант **B — настоящий ЭКЗЕМПЛЯР**: `buildFDN` ВОЗВРАЩАЕТ свои линии,
  они живут в объекте, сеттеры берут их аргументом. Реверб въехал БЕЗ единой правки аудио-графа:
  `verb`/`verbOut` — просто псевдонимы `FX_REV.in`/`.out`, поэтому строки посылов и запись `revLead` в
  `applyFx` НЕ менялись (тот же приём, что уберёг аккордовый посыл при замене конвольвера на FDN).
  Ничего не экспортируется — потребителя ещё нет.
- **ПЛАСТ 2 — КОНСТРУКТОР У РУКИ-ЭФФЕКТОВ. СДЕЛАН, пять слайсов, все в `dev`.** Три оси **X/Y/Z,
  отсчитываемые ОТ ТОЧКИ ЗАХВАТА ЩИПКА** (не от края экрана — рука начинает «отсюда», куда бы ни
  пришла), на СЫРЫХ координатах (правило #12), X через `flipX` (правило #13: «вправо = больше» обязано
  значить одно и то же на обеих камерах). Эффект жив, только пока БОЛЬШОЙ КАСАЕТСЯ ПАЛЬЦА; на
  отпускании параметры ЗАМИРАЮТ на последнем значении (латч — величина живёт в `state.fx`/в модуле, а
  `S.adj` гасит `endPinch`).
  - **2.1 — механизм.** Захардкоженная связка «палец→эффект→вертикаль» заменена на РАСКЛАДКУ
    (`state.fxLayout`, слот = палец, у слота `{fxId, params:[{axis,inv}]}`). Дефолт ВЫВЕДЕН из
    `FX_META`, поэтому «как раньше» верно ПО ПОСТРОЕНИЮ. Выражение по Y оставлено ДОСЛОВНО
    (`·H … /(H*0.7)`): `H` сокращается алгебраически, но не в плавающей точке. Байт-в-байт.
  - **2.2 — отрисовка.** Столбики строятся из раскладки, а не из `FX_META`; отступ под легенду считается
    по МАКСИМУМУ, иначе разворот активного слота двигал бы легенду под рукой. Временное поле `S.adj.k`
    СНЯТО: подсветка ключуется на СЛОТ, а не на имя эффекта — слот уникален, а один эффект с 2.3 может
    лежать на двух пальцах, и подсветились бы оба. Картинка байт-в-байт.
  - **2.3 — ПЛАТА ЗА ВСЁ ПРЕДЫДУЩЕЕ: реверб назначается на палец.** `FX_MODULES` (реестр модулей)
    экспортирован из `audio` — у абстракции появился первый потребитель. Два параметра на две оси:
    **Y → длина (RT60), X → окраска (срез ФНЧ в петле)**, перевод 0..1 → секунды/герцы по ЛОГ-шкале
    (`setNorm`), латч на отпускании, база щипка = текущее значение (продолжаем оттуда, где комната
    стоит). Меню `renderFxCtl` в `#panelScale` после «Функций рук»; зажатый слот РАЗВОРАЧИВАЕТСЯ в
    столбики по параметрам. ⚠️ **ПОДМЕС НЕДОСТУПЕН**, и это не забывчивость: `applyFx` пишет
    `revLead.gain` из `p.rev` НА КАЖДОМ кадре звучащей ноты, поэтому играющая рука перетёрла бы любое
    значение fx-руки шестьдесят раз в секунду. **Разблокировано в Пласте 3.1** — см. ниже.
  - **2.4 — ось и инверсия НА КАЖДЫЙ параметр** (Горизонталь/Вертикаль/Глубина + галочка). Данные жили
    в раскладке с 2.1, жест их читал; 2.4 лишь открыл их человеку — жест-математику не трогали.
    ⚠️ **КОНФЛИКТ ОСЕЙ РАЗРЕШЁН НАМЕРЕННО**: два параметра на одной оси едут вместе, это естественный
    результат жеста, а не ошибка; проверок не городим.
  - **2.5 — снятый эффект ЗАМОЛКАЕТ.** Эффект, убранный со ВСЕХ пальцев, гасится в нейтраль (0 у всех
    четырёх старых: в `applyFx` каждый входит МНОЖИТЕЛЕМ) — «звучит то, чем не управляешь» читается как
    поломка. Гасим ЧЕРЕЗ `state.fx[k]`, а не записью в узел: это тот же store, что уезжает в событие
    ноты, значит ноль означает разом «тихо живьём» и «записано нулём в НОВЫХ событиях»; старые события
    держат свои значения. Повторное назначение начинается С НУЛЯ само собой. ⚠️ **РЕВЕРБ сюда НЕ входит
    намеренно**: длина/окраска — свойство КОМНАТЫ, в которую посылаются и аккорды, а не свойство пальца.
- **ПЛАСТ 3.1 — РЕВЕРБ УШЁЛ С ИГРАЮЩЕЙ РУКИ. СДЕЛАН, в `dev`.** Пласт 3 разбит: 3.1 — передача реверба;
  что дальше — см. «ПЕРЕСМОТР МОДЕЛИ» ниже (порядок с тех пор пересмотрен).
  - **Играющая (правая) рука реверб больше НЕ ведёт.** Строка `revLead.gain.setTargetAtTime(p.rev*0.85,…)`
    удалена из `applyFx`; глубина у играющей руки больше не считается. Параллельного реверба на двух
    руках нет.
  - **fx-рука получила ТРЕТИЙ параметр реверба — ПОДМЕС.** ⚠️ Он пишет **СОЛО-ПОСЫЛ `revLead`**, а НЕ
    общий wet модуля: открытие Пласта 1 в силе — посылов два с разной величиной, и общий узел двинул бы
    заодно хвост АККОРДОВ. Прицепка аккордов (`revCh=0.12`) не тронута. `max 0.85 · curve 'lin' ·
    постоянная 0.08` дословно воспроизводят прежнее `p.rev*0.85`, поэтому подмес=1 звучит как прежняя
    полная глубина. **`revLead.gain` теперь имеет РОВНО ОДНОГО писателя** — это и есть гарантия, что
    руки не дерутся.
  - **РЕВЕРБ СТАЛ ЖИВЫМ (в запись не идёт), а ФОРМАТ СОБЫТИЯ НЕ ТРОНУТ.** Ключ `rev` остался везде
    (полезная нагрузка, запись открытой ноты, вызовы переигровки) — старые петли разбираются как прежде
    (правило #7). Новые события пишут в него НОЛЬ, а `applyFx` его больше не читает. Тот же закон, что
    у Гц терменвокса (#11). **Принятая плата:** старая петля с ревербным свипом правой руки переиграется
    БЕЗ этого свипа. Петля играет, свип потерян. Судьба поля `rev` — открытый вопрос, решать в плане того
    пласта, который первым тронет формат события (по канону — 3.7).
  - **РАСКЛАДКА ПО УМОЛЧАНИЮ ТЕПЕРЬ ЯВНАЯ, а не выведенная из `FX_META`** — вынужденно: реверб МОДУЛЬ,
    его в `FX_META` нет, а «из коробки» он обязан стоять на пальце, иначе комната, которой всегда
    звучало приложение, стала бы недостижимой. Указательный — **РЕВЕРБ** (длина→Y, окраска→X,
    подмес→Z, подмес засеян ≈0.25 прямо в узел рядом с созданием `revLead`: модуль строится РАНЬШЕ узла,
    и без явного внесения показ и звук разошлись бы). Средний — делей · безымянный — драйв · мизинец —
    вибрато. **ТРЕМОЛО по умолчанию НЕ НАЗНАЧЕНО**: пальцев четыре, эффектов пять; достаётся из меню.
    ⚠️ Подмес на ГЛУБИНЕ не случайность: глубиной реверб рулился всю жизнь приложения — мышечная память
    переезжает целиком, только на ДРУГУЮ руку.
  - **УБРАНА ВСЯ МАШИНЕРИЯ ПРИЦЕЛА:** столбик `REV`, `revDisp`/`setRevDisp`, `soloNoteHand`, пер-кадровый
    прицел до щипка и его сброс. Реверб показывают обычные столбики его слота (`TAIL`/`TONE`/`MIX`).
    ⚠️ `soloHalfRole` ОСТАВЛЕН — у него второй потребитель: проверка руки-ВЫРАЗИТЕЛЬНОСТИ в сплите.
    Удалить его вместе с прицелом значило бы сломать «смычок» на сплит-экране.
**ПЕРЕСМОТР МОДЕЛИ (замысел пользователя) — МЕНЯЕТ ПОРЯДОК ОСТАВШИХСЯ ПЛАСТОВ.**
⚠️ **ПРЕЖНИЙ ПЛАН 3.2 ОТМЕНЁН.** Он собирался за один заход отдать играющей руке И глубину, И громкость
(«3.2 a/b»). Теперь это РАЗНЫЕ пласты с РАЗНЫМ риском, а перед ними встаёт фундамент, которого в модели
не было. Ничего из сделанного (1, 2, 3.1) пересмотр не отменяет — он надстраивает.

**1. ПАРАМЕТР БОЛЬШЕ НЕ ОБЯЗАН БЫТЬ «ПАЛЬЦЕВЫМ».** Сегодня всякий параметр слота ведётся осью. В новой
модели каждый параметр эффекта — ОДНО ИЗ ДВУХ:
- **ВЕДЁТСЯ ПАЛЬЦЕМ по оси** (как сейчас: ось X/Y/Z + инверсия), ЛИБО
- **ЗАФИКСИРОВАН значением 0..100, выставленным В МЕНЮ** — пальца на нём нет вовсе, человек выкручивает
  его руками один раз.

И следствие, ради которого всё: **эффект может стоять в цепи, НЕ имея НИ ОДНОГО пальцевого параметра** —
просто включён с фиксированными настройками. Цепь перестаёт быть «эффекты, которые обязаны сидеть на
пальцах», и становится «эффекты, часть из которых пальцевые, часть — чисто меню».

**2. ОДИН ЭФФЕКТ МОЖЕТ БЫТЬ РАЗДЕЛЁН МЕЖДУ ДВУМЯ РУКАМИ** — и это почти бесплатно, ЕСЛИ модель выше
сделана правильно. Пример: реверб, у которого два параметра ведёт fx-рука, а третий — ГЛУБИНА играющей.
Работает это ровно при одном условии: параметр должен знать свой **АДРЕС УПРАВЛЕНИЯ** (какая рука / какой
палец / какая ось — ИЛИ «фиксированное значение»). Поэтому модель идёт ПЕРВОЙ, а руки — после неё.

**3. ДВОЙНОЙ ЩИПОК — НОВЫЙ ЖЕСТ, которого в коде НЕТ.** Быстрое двойное смыкание большого с пальцем,
назначаемое в меню ПО ПАЛЬЦУ, открывает ВТОРУЮ привязку. Два применения:
- **(A) БОЛЬШЕ ПАРАМЕТРОВ:** двойной щипок переводит с первых трёх параметров эффекта на следующие
  до трёх — то есть до ШЕСТИ параметров у одного эффекта.
- **(B) БОЛЬШЕ ЭФФЕКТОВ:** двойной щипок ведёт ДРУГОЙ эффект — до ВОСЬМИ эффектов на руке вместо четырёх.
Значит меню назначает эффекты не только на одиночный щипок, но и на двойной, для каждого пальца.
⚠️ **Это самая дорогая и рискованная часть замысла:** новый жест в ГОРЯЧЕМ пути распознавания щипка
(надо надёжно отличать двойной от двух одиночных — тайминги и пороги), и он же УМНОЖАЕТ раскладку.

**4. АРХИТЕКТУРНЫЙ ПОВОРОТ: ЭФФЕКТЫ ПРИНАДЛЕЖАТ РОЛИ, А НЕ РУКЕ.** Самый крупный сдвиг всего пересмотра
— и он меняет не меню, а то, ЧТО ТАКОЕ эффект в этой программе.

- **Эффект — ПРОЦЕССОР В ЦЕПИ, а цепью ВЛАДЕЕТ РОЛЬ** (соло · аккорды · бас · ударные). У каждой из
  ЧЕТЫРЁХ ролей — своя НЕЗАВИСИМАЯ цепь эффектов.
- **Пальцевое управление — НЕОБЯЗАТЕЛЬНЫЙ СЛОЙ СВЕРХУ.** Привязки к пальцам ДЕЙСТВУЮТ, только если
  функция руки — «управление эффектами»; иначе цепь всё равно СУЩЕСТВУЕТ и ЗВУЧИТ, со значениями,
  выставленными в меню. **Цепь — общий случай, живое пальцевое управление — частный.** Это прямое
  продолжение пункта 1 (эффект может стоять в цепи вообще без пальцевых параметров): там мы разрешили
  эффекту жить без пальца, здесь признаём, что и ВЛАДЕЛЕЦ у него не палец и не рука, а РОЛЬ.
- **Для СОЛО это почти ЧИСТЫЙ РЕФАКТОРИНГ.** Сегодняшний `fxLayout` и есть цепь роли «соло», просто
  названная по руке; обработка уже висит на соло-шине. Поэтому «отцепить эффект от руки, прицепить к
  роли» делается ПЕРВЫМ (пласт 3.3) — это безопасное основание, на которое встаёт всё остальное.
- ⛔ **А вот цепи АККОРДОВ / БАСА / УДАРНЫХ — это открытие Пласта 1, кусающее в полную силу.** Обработка
  сегодня подключена ТОЛЬКО к соло-шине; прочие роли идут в мастер МИМО эффектов. «Цепь у каждой роли»
  означает СТРОИТЬ ОБРАБОТКУ НА ЧЕТЫРЁХ ШИНАХ — со своими посылами, вставками, уровнями и своей ценой
  по процессору. Это громадно и РИСКОВАННО ДЛЯ ЗВУКА, поэтому идёт ПОСЛЕДНИМ из содержательных пластов
  и строго **ПО ОДНОЙ ШИНЕ ЗА ЗАХОД** (пласт 3.5).

**5. МЕНЮ «ОТ ПАРАМЕТРА», А НЕ «ОТ ПАЛЬЦА» — уточнение под пласт выделенного меню конструктора.**

**ПЕРЕВОРОТ.** Сегодняшнее меню устроено ОТ ПАЛЬЦА: строка на палец, и в ней выбираешь, какой ОДИН эффект
на этом пальце сидит. Новая модель — ОТ ПАРАМЕТРА: ЛЮБОЙ параметр ЛЮБОГО эффекта назначается на ЛЮБОЙ
**АДРЕС УПРАВЛЕНИЯ**. Адрес — это либо (fx-рука: КАКОЙ ПАЛЕЦ + КАКАЯ ОСЬ X/Y/Z), либо (играющая рука:
ГЛУБИНА). Палец перестаёт «владеть эффектом»; он становится координатой, на которую что-то подписано.

**СЛЕДСТВИЯ (подтверждены пользователем):**
- **ОДИН АДРЕС МОЖЕТ ВЕСТИ МНОГО ПАРАМЕТРОВ — И У РАЗНЫХ ЭФФЕКТОВ.** Двинул адрес (скажем, указательный
  по вертикали) — и всё, что на него подписано, поехало ВМЕСТЕ, сразу через несколько эффектов. Это не
  выдумка на будущее: у ГЛУБИНЫ играющей руки так работает УЖЕ СЕЙЧАС (на неё можно подписать параметры
  всех четырёх эффектов, и они пойдут разом). Пласт меню делает это ПРАВИЛОМ ДЛЯ КАЖДОГО адреса, а не
  привилегией одной глубины.
- **ОДИН ПАРАМЕТР — РОВНО ОДИН АДРЕС.** У параметра одна пара `hand`+`axis`, модель
  `{mode:'drive',hand,axis}` этого и не позволяет иначе. Значит связь МНОГИЕ-ПАРАМЕТРЫ-К-ОДНОМУ-АДРЕСУ и
  НИКОГДА наоборот. ⚠️ Заодно это и есть та гарантия, из-за которой два писателя на один параметр
  невозможны ПО ПОСТРОЕНИЮ (см. фильтр по руке в 2.6).
- **ПРОСТРАНСТВО АДРЕСОВ.** С двойным щипком у fx-руки становится ВОСЕМЬ «пальцев» — 4 одиночных + 4
  двойных, — и у каждого ТРИ оси: до **24 fx-адресов**, плюс глубина играющей руки. Любой параметр
  подписывается на любой из них.

**ПОЧЕМУ ЭТО СОГЛАСУЕТСЯ С УЖЕ НАПИСАННЫМ, а не ломает его:** параметр УЖЕ носит свой адрес
(`{mode:'drive',hand,axis}` с 2.6.1, `hand:'play'` с 3.2). Не хватает только меню: нынешнее, построенное
ОТ ПАЛЬЦА, физически не даёт ни выразить «много параметров на одном адресе», ни выбрать адрес свободно.
Само по себе это правка МЕНЮ, а не модели. ⚠️ И код уже дрейфует в ту же сторону: в 3.2.2 `draw.js`
пришлось перейти на обход параметров ПО ИНДЕКСУ — именно потому, что адрес живёт у параметра, и узнать
его иначе нельзя.

**МЕСТО В ПЛАНЕ.** Это уточняет пласт, который выносит конструктор в СВОЮ секцию меню (с выбором роли):
к прежней формулировке добавляется — **и меню это устроено ОТ ПАРАМЕТРА**: для каждого эффекта в цепи
роли каждый его параметр выбирает свой адрес (палец fx-руки + ось · глубина играющей · фиксированное
значение). Пласт ДВОЙНОГО ЩИПКА при этом УМНОЖАЕТ пространство адресов, которое меню обязано показать.

**ОТКРЫТЫЕ ВОПРОСЫ (ответить в плане пласта меню, не здесь):**
- ⬜ **Если один адрес ведёт много параметров — меню показывает это ОТ ПАРАМЕТРА (каждый параметр выбирает
  адрес) или ОТ АДРЕСА (каждый адрес перечисляет свои параметры)?** «От параметра» совпадает с формой
  данных (адрес принадлежит параметру), но «от адреса» нагляднее показывает, что поедет вместе. Решить.
- ⬜ **До 24 fx-адресов плюс глубина — как меню остаётся ЧИТАЕМЫМ?** Стена контролов недопустима;
  нужен способ (постепенное раскрытие, группировка по эффектам) — тоже в план пласта меню.

**⛳ ПРИНЦИП ЛУПЕРА (решает ВСЕ будущие развилки формата) — читать ДО любого плана, трогающего событие.**

**СЛОЙ — ЭТО ЗАМОРОЖЕННОЕ МГНОВЕНИЕ.** Записанный слой забирает параметры эффектов ТАКИМИ, КАКИМИ ОНИ
БЫЛИ В МОМЕНТ ЗАПИСИ, и переигрывается ИМЕННО С НИМИ — всегда. Покрутил параметры, записал новый слой →
новый слой взял НОВЫЕ значения. Играешь живьём с третьими → уже записанные слои этого НЕ ЗАМЕЧАЮТ, они
звучат как записаны. Ровно та же логика, что у заморозки лада (`sc`/`sev`, правило #7): слой помнит свой
контекст и не переезжает вслед за живыми настройками. Это и есть полимодальность, но по эффектам.

**СЛЕДСТВИЕ — ПРАВИЛО, КОТОРОЕ ЗАКРЫВАЕТ РАЗВИЛКИ `rev`/`vol`/и все будущие:** ЛЮБОЙ параметр, влияющий
на ЗВУК ноты, ОБЯЗАН ехать В СОБЫТИИ — иначе слой переиграется НЕВЕРНО. Значит **КОНЕЧНАЯ ЦЕЛЬ: ВСЕ
звуковые параметры эффектов попадают в событие лупера** — включая длину, окраску и подмес реверба.
⚠️ Отсюда прямо следует: **нынешний «живой» реверб (Пласт 3.1) — ВРЕМЕННЫЙ КОМПРОМИСС, а не конечное
состояние.** Он был правильной ценой за то, чтобы 3.1 прошёл без правки формата, и подлежит исправлению,
когда формат будут менять осознанно.

**⛳ УТОЧНЕНИЕ ПРИНЦИПА (планирование Пласта 3.7). ЦЕЛЬ НЕ УМЕНЬШИЛАСЬ — ОНА РАЗЛОЖИЛАСЬ НА ДВА ШАГА.**
Принцип выше писался ДО того, как эффекты стали модулями с цепью у каждой роли, и потому не различал
двух РАЗНЫХ по природе видов параметра. Планирование 3.7 это различие вскрыло:
- **ПРИЦЕПОЧНЫЕ параметры** (подмес/посыл, глубина драйва, глубина вибрато и тремоло, посыл в делей) —
  величины, которые МОГУТ ОТЛИЧАТЬСЯ ОТ НОТЫ К НОТЕ. Они становятся ПЕР-ГОЛОСОВЫМИ и едут в событии:
  пер-слойность получается ДАРОМ, без единого нового экземпляра сети.
  ⚠️ **Рабочий прототип этого УЖЕ БЫЛ В КОДЕ И РАБОТАЛ ГОДАМИ — яркость аккордов `bri`:** её значение
  едет в событии и садится в ПЕР-ГОЛОСОВОЙ фильтр `fb`, ключуемый владельцем, поэтому слой N звучит со
  своей яркостью. Мы не изобретаем механизм, а распространяем найденный.
- **СЕТЕВЫЕ параметры** (длина и окраска реверба; у будущего делея — время и обратная связь) описывают
  САМУ СЕТЬ ОБРАБОТКИ. Два слоя физически не могут иметь разную длину хвоста ВНУТРИ ОДНОЙ FDN: это одни
  и те же узлы с одними и теми же `AudioParam`.

**⚠️ РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ: ПЕР-СЛОЙНЫЕ СЕТИ НУЖНЫ. Это НЕ отброшено, а РАЗВЕДЕНО ПО ПЛАСТАМ.** Слой
должен уметь иметь СВОЮ КОМНАТУ, независимую от других слоёв. Порядок — по стоимости и по фундаменту:
- **Пласт 3.7 даёт пер-слойные ПРИЦЕПКИ.** И это не «половина цели»: сегодня эффекты НЕ ЗАПИСЫВАЮТСЯ
  ВООБЩЕ (см. находку ниже), так что 3.7 делает работающим то, чего никогда не было.
- **Пласт 3.8 «ПЕР-СЛОЙНЫЕ СЕТИ» даёт свою сеть слою** — отдельным заходом, на фундаменте 3.7, со своей
  ценой: **≈49 линейных узлов на FDN × слой × роль**, ПОТОЛОК на число слоёв, владеющих сетью, и
  ЖИЗНЕННЫЙ ЦИКЛ УДАЛЕНИЯ на undo/очистке, который не должен обрезать хвост слышно (сегодня экземпляры
  НЕ РАЗБИРАЮТСЯ ВООБЩЕ — и это осознанное решение Пласта 3.5.1, а не недосмотр).

**ИТОГОВАЯ ФОРМУЛИРОВКА ПРИНЦИПА, действующая с этого момента:** *всякий параметр, способный отличаться
ОТ НОТЫ К НОТЕ, едет в событии (это Пласт 3.7); параметры САМОЙ СЕТИ требуют ПЕР-СЛОЙНЫХ ЭКЗЕМПЛЯРОВ —
отдельный пласт со своей ценой по процессору, потолком и удалением (это Пласт 3.8).* Конечная цель
принципа — «слой звучит так, как записан» — не изменилась ни на йоту; изменилось лишь понимание, что
она берётся ДВУМЯ разными механизмами, а не одним.

**ПОЧЕМУ ФОРМАТ ТРОГАЕМ ПОСЛЕДНИМ И ОДНИМ ЗАХОДОМ.** Это САМОЕ КРУПНОЕ изменение события из всех: оно
должно нести ПРОИЗВОЛЬНЫЙ набор параметров ПРОИЗВОЛЬНЫХ эффектов (а не четыре фиксированных ключа
`vib/drv/trm/dly`), плюс дельта-сжатие на каждое новое поле. Делать это частями — значит менять формат
по разу на пласт. Поэтому **ВСЕ пласты до последнего (2.6, 3.2, 3.3, 3.4, 3.5, 3.6) формат НЕ ТРОГАЮТ
ВООБЩЕ**, а весь переход делается ОДНИМ сознательным заходом в **3.7**, когда модель эффектов уже
устоялась, цепи есть у всех ролей и ясно, что именно надо уметь записывать.
⚠️ **«МИГРАЦИЯ СТАРЫХ ПЕТЕЛЬ» ОТСЮДА УБРАНА — ЕЁ НЕ СУЩЕСТВУЕТ (сверено с кодом при планировании 3.7).**
Петли НЕ ПЕРСИСТЯТСЯ: `events` живёт в памяти, чистится `clearRec`, сериализации нет нигде (потому
стабильный id лада и назван ПРЕДПОСЫЛКОЙ сохранения петель — самого сохранения нет). После перезагрузки
`events` пуст, значит событие, записанное СТАРЫМ кодом, физически не может попасть в НОВЫЙ. Реальная
поверхность совместимости другая и уже: **события аранжировки** (`arrange.js` строит нагрузку вручную и
карты эффектов не несёт) — их и нельзя прочесть как «нейтраль». Не ищите миграцию, её нет.

⚠️ **ЭТО ПЕРЕФОРМУЛИРУЕТ ОТКРЫТЫЕ ВОПРОСЫ ПРО `rev` И `vol`.** Они больше НЕ «мягко против жёстко» сами
по себе: принцип говорит, что конечное состояние — «записано вместе со слоем», и **3.7** реализует это
для всех ПРИЦЕПОЧНЫХ параметров разом (сетевые — 3.8, см. уточнение принципа выше). Вопрос сузился с
«что делать с полем» до «как выглядит новый формат»: `rev` снимается как мёртвый груз, `vol` остаётся
именованным полем ноты (это не эффект), а всё прицепочное уезжает в карту `a.fx`.

**ПЛАСТЫ ПОСЛЕ ПЕРЕСМОТРА — ЧТО СДЕЛАНО И ЧТО ОСТАЛОСЬ (порядок по возрастанию риска).**
⚠️ Список КАНОНИЧЕСКИЙ. Прежняя нумерация («3.3 — двойной щипок, 3.4 — громкость») **ОТМЕНЕНА**:
архитектурный поворот вставил между ними два пласта, а громкость уехала в 3.7.
- **ПЛАСТ 2.6 — ✅ СДЕЛАН (2.6.1 модель + 2.6.2 меню). ПАРАМЕТР: «ПАЛЬЦЕВЫЙ ИЛИ ФИКСИРОВАННЫЙ».** Ретроактивное
  расширение модели данных Пласта 2: у параметра появляется РЕЖИМ и фиксированное значение, у меню —
  контрол этого значения на каждый параметр. **Лупер НЕ трогает вовсе.** Номер 2.6, а не 3.x, намеренно:
  это достройка Пласта 2, и она обязана лечь ДО играющей руки — без адреса управления ни разделение
  эффекта между руками, ни двойной щипок не выражаются.
  - ✅ **РЕШЕНО — ФОРМА ПАРАМЕТРА:**
    `{mode:'drive', hand:'fx'|'play', axis, inv}` **|** `{mode:'fixed', v01}`.
    **Различитель — «ведётся» против «зафиксирован», а РУКА — атрибут ведения**, и это не вкусовщина:
    палец и играющая рука — ОДИН И ТОТ ЖЕ механизм (смещение от точки захвата вдоль оси, с инверсией),
    различаются лишь тем, ЧЬЯ рука это смещение даёт. Фиксированный же параметр отличается по РОДУ:
    ни захвата, ни пофреймового пути. Отсюда две выгоды: **Пласт 3.2 становится ДОПОЛНЕНИЕМ**
    (`hand:'play'`), а не переписыванием, и **фильтр по руке в жест-цикле — сам механизм разделения
    эффекта между руками**: fx-рука ОБЯЗАНА пропускать параметр с `hand:'play'`, иначе они подерутся.
    Фильтр пишем СРАЗУ (в 2.6 он тривиально истинен) — тогда 3.2 добавляет второй цикл и не трогает
    ни данные, ни существующие ветки.
    **Дефолт — байт-в-байт:** каждый нынешний `{axis,inv}` становится `{mode:'drive',hand:'fx',axis,inv}`.
  - ✅ **РЕШЕНО — ФИКСАЦИЯ РАЗРЕШЕНА И СТАРЫМ СКАЛЯРНЫМ ЭФФЕКТАМ** (не только модулям/ревербу). Довод:
    значение старого параметра уже СЕГОДНЯ уезжает в событие ноты; фиксация лишь записывает РОВНОЕ
    значение вместо гуляющего за рукой — то же поле, тот же формат, изменений нет. Запрет же сделал бы
    меню непоследовательным (у одних параметров переключатель режима есть, у других нет) ради
    предотвращения того, что и не дефект. И это ПРЯМО СОГЛАСУЕТСЯ с принципом лупера выше: зафиксированный
    старый параметр, попавший в слой, — ровно то самое «слой забрал текущие параметры».
  - ⚠️ **ПРЕДСУЩЕСТВУЮЩАЯ ШЕРОХОВАТОСТЬ, КОТОРУЮ 2.6 СДЕЛАЕТ ЗАМЕТНОЙ** (чтобы её потом не приняли за
    новый баг): эффекты соло — ОБЩАЯ ШИНА, а `applyFx` зовётся на КАЖДОЙ переигранной ноте, то есть
    играющая петля постоянно перезаписывает живые `dly/vib/drv/trm` своими ЗАПИСАННЫМИ значениями.
    Сегодня это читается как «эффекты гуляют, пока играет петля»; как только человек ЗАФИКСИРУЕТ значение
    и будет ждать, что оно стоит, та же механика прочтётся как «мою фиксацию затирают». **Не 2.6 это
    внесла**, и починка требует трогать переигровку — вне рамок 2.6. Реверб к этому иммунен: его
    параметры в событие не попадают (пока — см. ПРИНЦИП ЛУПЕРА).
- **ПЛАСТ 3.2 — ✅ СДЕЛАН (3.2.1 механизм+меню, 3.2.2 показ). ИГРАЮЩАЯ РУКА, ТОЛЬКО ГЛУБИНА.**
  Глубина (Z), освободившаяся в 3.1 вместе с уходом реверба, стала АДРЕСОМ УПРАВЛЕНИЯ для параметра —
  ОТОБРАЖЕНИЕ АБСОЛЮТНОЕ (`clamp01((REV_NEAR-hs)/REV_RANGE)`, дословно прежняя ревербная формула), а не
  «от точки захвата»: играющая рука щиплет НА КАЖДУЮ НОТУ, и захват сбрасывал бы точку отсчёта десятки
  раз за фразу. Инверсия здесь ЗЕРКАЛИТ (`1-v`), а не меняет знак — величина уже абсолютная. Нет нотной
  руки в кадре → НИЧЕГО не пишем (параметр держит последнее): это ЗВУК, а не показ, щелчок в ноль был бы
  слышен. Предикат `soloNoteHand` ВОСКРЕШЁН (в 3.1 он умер вместе с показом, теперь у него настоящий
  потребитель — управление). Меню: ось параметра стала АДРЕСОМ (`fx:x|fx:y|fx:z|play:z`), режим
  переименован «Палец»→«Ведётся». В 3.2.2 play-параметр получил метку-точку в зазоре столбика.
  ✅ **И ЭТО ЖЕ ЗАКРЫЛО пункт 2 пересмотра** — разделение ОДНОГО эффекта между ДВУМЯ руками: у реверба
  длина и окраска остаются на пальце, подмес уходит глубине играющей. Эффект ОДИН, руки адресованы
  ПАРАМЕТРАМ. Громкость не тронута.
- **ПЛАСТ 3.3 — ✅ СДЕЛАН, в `dev`. РОЛЬ ВЛАДЕЕТ ЦЕПЬЮ ЭФФЕКТОВ. ФУНДАМЕНТ ПОВОРОТА.** `fxLayout` стал
  `fxChains = {ld:[…], ch:[], bs:[], dr:[]}` с доступом через `fxChainOf(role)`. Для соло это вышло
  ПОЧТИ ЧИСТЫМ РЕФАКТОРИНГОМ, как и предсказывалось: данные не переезжали (ld — прежний массив дословно),
  обработка и так висела на соло-шине, поведение байт-в-байт. Прочие роли — пустые `[]`: место в модели
  есть, звучать им негде до 3.5. ⚠️ Один межролевой читатель СОХРАНЁН НАМЕРЕННО: `draw.fxN()` считает
  резерв под легенду по СОЛО-цепи даже для аккордов/баса — иначе у них (цепи пусты) легенда уехала бы
  влево на всю ширину полосы столбиков.
  ⛳ **ЭТОГО ПРАВИЛА (R-1) БОЛЬШЕ НЕТ — оно не нарушено, а ПЕРЕСТАЛО СУЩЕСТВОВАТЬ (слайс б.2).** Столбики
  ушли к ПРАВОЙ кромке у ВСЕХ ролей, слева не осталось ничего, от чего надо отодвигаться, — значит и
  межролевому чтению нечего переносить. `fxBandR`/`fxBarsMaxN`/`fxN`/`soloBarsN`/`bandR` удалены, легенды
  считаются от собственного края (`x0+7` у сетки, `rx0+7` у подписей роли). Побочно: легенды НЕ-соло ролей
  сдвинулись ВЛЕВО — они отступали под ЧУЖИЕ столбики, которых у них не рисовалось никогда.
- **ПЛАСТ 3.4 — ✅ СДЕЛАН, четыре слайса, все в `dev`. ВЫДЕЛЕННАЯ СЕКЦИЯ: выбор РОЛИ + меню ОТ ПАРАМЕТРА.**
  - **3.4.1 — секция стала САМОСТОЯТЕЛЬНОЙ.** Конструктор перестал принадлежать «Функциям рук»:
    `renderHandFn` его больше не зовёт, `showScale`/`onLangChange` зовут обе секции ЯВНО, а ВИДИМОСТЬ
    больше не гейтится `roleHasFx` — цепь существует и звучит без руки на эффектах (фиксированные
    значения применяются), и старый гейт был к тому же ДЫРОЙ: зафиксировал значение, снял руку — оно
    действует, а меню исчезло. Появился ВЫБОР РОЛИ (состояние МЕНЮ, `fxCtlRole` в ui, НЕ в state и не
    следует за `phoneInstr`). У роли без цепи — объяснение и НИ ОДНОГО контрола: собрать цепь, которая
    не может зазвучать, хуже, чем честно сказать «пока нет».
    ⚠️ Один вызов `renderFxCtl` из «Функций рук» ОСТАВЛЕН — но как ЗАВИСИМОСТЬ ПО ДАННЫМ (подсказка
    «нет руки на эффектах» читает `roleHasFx`, а меняется он ровно там), а не как владение.
  - **3.4.2 — ЦЕПЬ СТАЛА СПИСКОМ ЭФФЕКТОВ, ПАЛЕЦ ПЕРЕЕХАЛ В АДРЕС ПАРАМЕТРА.** Форма:
    `{mode:'drive', hand:'fx', finger:0..3, axis, inv}` | `{mode:'drive', hand:'play', axis:'z', inv}` |
    `{mode:'fixed', v01}`. Индекс массива — МЕСТО В ЦЕПИ, а не палец; отсюда то, ради чего всё: один
    адрес может вести параметры РАЗНЫХ эффектов. Байт-в-байт по построению — пальцы засеяны из прежних
    слотов, порядок цепи прежний. Меню перестроено ОТ ПАРАМЕТРА, «режим» и «ось» слиты в ОДИН список
    адресов с группами по пальцам. Сеттеры получили РОЛЬ первым аргументом (обещание 3.3 исполнено),
    `setFxSlotId` исчез вместе со слотом.
    ⛔ **`S.adj` держит ЯВНЫЙ СПИСОК `{effIdx,pIdx,pa,set,base}`, а не позиционный массив** — прежний
    запрет «не фильтровать base» держался на позиционном соответствии `base[i]↔params[i]`, которого
    больше нет и быть не может (один палец собирает параметры вразбивку по цепи). Ошибка «палец поехал
    НЕ ТОТ параметр» стала НЕВЫРАЗИМОЙ: в пофреймовом цикле нет ни одного индекса.
  - **3.4.3 — АККОРДЕОН + ПРАВКА СОСТАВА ЦЕПИ.** Одна строка на эффект (`[▸][имя][сводка адресов][✕]`),
    развёрнут не больше одного; свёрнутое ≠ спрятанное — сводка несёт адреса всех параметров.
    «+ Добавить эффект» вернул правку состава (в 3.4.2 её не было вовсе, и тремоло было недостижимо);
    ✕ убирает, гася старый скалярный через `hushUnassignedFx` (правило 2.5). Новый эффект занимает
    СВОБОДНЫЙ палец, а если свободных нет (четыре пальца, пять эффектов) — приходит ФИКСИРОВАННЫМ:
    молчит предсказуемо, вместо того чтобы молча поехать вдвоём на чужом адресе.
  - **3.4.4 — ПОДСКАЗКА ОБЩЕГО АДРЕСА.** Чип «×N» рядом с адресом и на свёрнутом заголовке, в подсказке —
    что именно поедет вместе. Карта «адрес → параметры» ВЫВОДИТСЯ из цепи на каждую отрисовку (не
    состояние, обратного индекса не заводим); фиксированные параметры в неё не входят — они не адрес.
  - ✅ **ОБА ОТКРЫТЫХ ВОПРОСА ЗАКРЫТЫ.** *«От параметра или от адреса»* — **ОТ ПАРАМЕТРА**: так устроены
    данные (адрес принадлежит параметру — с 2.6.1, и в 3.2.2 `draw` уже пришлось обходить параметры по
    индексу именно поэтому), привязка — одна операция в одной строке, а «от адреса» потребовало бы
    обратного индекса и всё равно вложенного выбора параметра. Его единственное преимущество — видно,
    что едет вместе — добито чипом 3.4.4. *«Как остаться читаемым при 13 адресах (25 после 3.6)»* —
    тремя приёмами: аккордеон (цепь из четырёх эффектов = четыре строки), слияние «режим+ось» в ОДИН
    нативный список с `<optgroup>` по пальцам (один вопрос — один контрол; удвоение адресов в 3.6
    выдержит без перестройки) и сводка адресов на свёрнутом заголовке.
- **ПЛАСТ 3.5 — ✅ СДЕЛАН, четыре слайса, все в `dev`. ЦЕПИ У ВСЕХ ЧЕТЫРЁХ РОЛЕЙ.** Задумывался как самый
  рискованный ДЛЯ ЗВУКА кусок (обработка висела ТОЛЬКО на соло-шине, прочие роли шли в мастер мимо неё).
  Риск оказался не в проводке, а в МОДЕЛИ ЭКЗЕМПЛЯРА — её и решал первый слайс; остальные три вышли
  повторением одной формы.
  - **3.5.1 — МОДЕЛЬ ЭКЗЕМПЛЯРА (на одном соло, без единой новой шины).** Реестр раздвоен:
    **`FX_FACTORY`** — ЧТО СУЩЕСТВУЕТ (id → `{labelKey, params:СПЕЦИФИКАЦИЯ, make}`), заполняется НА
    ЗАГРУЗКЕ МОДУЛЯ и читается МЕНЮ — поэтому список эффектов больше не требует, чтобы экземпляр был
    построен (это же закрыло в источнике хвост «реверба нет в списке на чистой сессии»).
    **`FX_INST[role][fxId]`** — ЧТО ПОСТРОЕНО: ленивые, кэшируемые, **никогда не разбираемые** экземпляры
    (убрали эффект — уводим ПОСЫЛ В НОЛЬ, а не сносим узлы: снос посреди хвоста обрезал бы его слышно, а
    у FDN нет осцилляторов, так что простаивающая сеть стоит лишь линейной обработки — дух правила #3).
    `makeReverbFx` стала ФАБРИКОЙ НА (role,fxId) и **владеет своим посылом**: параметр «подмес» пишет
    `inst.send.gain` и больше ничего. ⛳ **Этим закрыта проблема, помеченная как нерешённая ещё в Пласте 1
    и переспрошенная трижды: «две роли назвали один эффект — получили ОДИН экземпляр».** Свойство «ровно
    один писатель посыла» (заведено в 3.1) стало держаться НА КАЖДЫЙ ЭКЗЕМПЛЯР. `fxParamsOf` получил
    роль первым аргументом; `revLead`/`FX_REV` исчезли.
  - **3.5.2 — АККОРДЫ.** Скрытый фиксированный посыл `revCh=0.12` СТАЛ ПЕРВОЙ ЗАПИСЬЮ ЦЕПИ РОЛИ: тот же
    узел, та же величина, но теперь его ВИДНО и можно править. Экземпляр аккордов строится с ТЕМИ ЖЕ
    `REV_A` (длина/окраска), что и соло, — на этом и держался довод, что замена неслышна: **FDN линейна,
    сумма двух одинаковых сетей равна одной, в которую слили оба источника.** Довод подтверждён на слух:
    хвост аккордов не изменился, а длина аккордовой комнаты больше НЕ ДВИГАЕТ соло-хвост — ради чего всё
    и делалось. Попутно `verb`/`verbOut` инлайнены: «общий реверб» перестал существовать как понятие,
    и переменная, в которую пишут, но которую никто не читает, была бы висячей ссылкой.
  - **3.5.3/3.5.4 — БАС и УДАРНЫЕ, одним заходом.** Оправданно: в отличие от аккордов, у них НЕЧЕГО
    СОХРАНЯТЬ — обе шины сухие сегодня и остаются сухими по умолчанию. Цепи пустые, поэтому **не
    строится и не цепляется ничего**: ни узла, ни процессорной цены, ни изменения звука. Прицепка стала
    ЛЕНИВОЙ (`fxRoleBus` запоминает шину, `fxAttach` соединяет, `inst.wired` страхует от повторной
    прицепки) — первый добавленный в меню эффект строит экземпляр и цепляется сам.
    ⛔ **СОЛО И АККОРДЫ НАМЕРЕННО НЕ ПРОПУЩЕНЫ ЧЕРЕЗ ЭТОТ МЕХАНИЗМ.** Их посылы прицеплены ЯВНЫМИ
    строками (у соло — к `leadOut`, который рождается позже реверба; у аккордов — рядом с `chordBus`).
    Зарегистрируешь их в `FX_ROLE_BUS`, не сняв те строки, — посыл прицепится ДВАЖДЫ, и роль поедет с
    удвоенным ревербом, тихо и правдоподобно. Флаг `inst.wired` страхует только путь через `fxAttach`.
    **ДРОН (`backBus`) — НЕ РОЛЬ и остаётся сухим.** Роль — то, ЧЕМ ИГРАЮТ: у неё есть кнопка выбора,
    функции рук и своя цепь в меню. Дрон включают тумблером, в `FX_ROLE_SEQ` его нет, править его цепь
    некому. Записано, чтобы не читалось как недосмотр.
  - **ЦЕНА ПО ПРОЦЕССОРУ, названная честно:** по умолчанию **ДВЕ сети FDN** (соло + аккорды), ~49
    линейных узлов каждая — задержки, фильтры, гейны; ни осцилляторов, ни воркле́та. Добавление реверба
    басу или ударным даёт третью и четвёртую. Для сравнения: восемь голосов «Ситар» — это восемь
    `AudioWorkletNode` (JS-обратный вызов каждые 128 сэмплов) плюс 40 биквадов, и это заметно тяжелее.
  - ⚠️ **ЧТО СЛАЙС НЕ ДАЛ (и почему это не недоделка, а граница):** цепи баса/ударных/аккордов сегодня
    могут держать ТОЛЬКО ФИКСИРОВАННЫЕ параметры — `roleHasFx` устроен «только соло», значит пальца,
    способного вести их параметр, не существует. И предлагать этим ролям можно ТОЛЬКО МОДУЛИ (сегодня
    это один реверб) — старые скалярные не переносимы. Оба ограничения — не проводка, а модель; они
    вынесены в «ЧЕГО ЕЩЁ НЕТ» ниже, пункты (б) и (в).
    ⚠️ **ПЕРВОЕ ОГРАНИЧЕНИЕ СНЯТО ЦЕЛЬЮ (б):** `roleHasFx` больше не «только соло», и цепи аккордов/баса
    держат пальцевые адреса наравне с соло. Второе (только модули) держится до цели (в).
- **ПЛАСТ 3.6 — ⬜ ДВОЙНОЙ ЩИПОК** (вторая привязка: «следующие три параметра» или «другой эффект»).
  Новый жест в ГОРЯЧЕМ пути распознавания, риск в отличении двойного от двух одиночных. Умножает
  пространство адресов до ВОСЬМИ «пальцев» × 3 оси, которое меню 3.4 обязано будет показать.
- **ПЛАСТ 3.7 — ✅ СДЕЛАН, три слайса + одна починка, всё в `dev`. ФОРМАТ СОБЫТИЯ ЛУПЕРА.**
  ⛳ **ЗДЕСЬ ЗАПИСЬ ЭФФЕКТОВ В СЛОЙ НАЧАЛА РАБОТАТЬ ВООБЩЕ** — и это не фигура речи. Планирование
  вскрыло (сверено с кодом): `vib/drv/trm/dly` ЛЕЖАЛИ в полезной нагрузке соло-события, но в сравнении
  дельта-сжатия ИХ НЕ БЫЛО, и состояние открытой ноты `r` их даже не хранило. Значит «покрутил ручку при
  зажатой ноте» не писало НИЧЕГО: слой уносил величину, случайно бывшую текущей, когда последний раз
  менялось какое-то ДРУГОЕ поле. 3.7 не «улучшил» запись эффектов и не «починил общую шину» — он
  заставил её работать ВПЕРВЫЕ.
  - **3.7.1 — ПЕР-ГОЛОСОВАЯ ПРИЦЕПКА (без единой правки формата).** Вибрато, тремоло и посыл в делей
    переехали с ОБЩИХ узлов шины В ГОЛОС (драйв там был всегда); рассылка драйва по всем живым голосам и
    костыль `lastFx.drv` удалены. `applyFx` больше не пишет узлы — он кладёт величины ЭТОЙ ноты в
    `pendFx`, а `leadOn`/`leadSet` применяют их к СВОЕМУ голосу.
    ⛳ **Поэтому `recorder.js` не потребовал НИ ОДНОЙ правки:** `applyFx` и так зовётся НЕПОСРЕДСТВЕННО
    ПЕРЕД голосовым вызовом той же ноты — и живьём, и на переигровке. ⚠️ Этот ПОРЯДОК ВЫЗОВА стал
    КОНТРАКТОМ (помечено в коде): позови `leadOn` без `applyFx` — голос возьмёт величины ЧУЖОЙ ноты.
    ⛳ Попутно закрыта «ШЕРОХОВАТОСТЬ ОБЩЕЙ ШИНЫ», записанная как предсуществующая ещё в 2.6: играющая
    петля больше НЕ перетирает живые `dly/vib/drv/trm` — общего писателя просто не осталось.
    ⚠️ **ОДНА ПРИНЯТАЯ АКУСТИЧЕСКАЯ ПЛАТА:** тремоло и точка посыла в делей оказались ПЕРЕД цепочкой
    ВЫРАЗИТЕЛЬНОСТИ (голос по определению предшествует `leadSum`). При нейтральной выразительности вся
    та цепочка — тождество, поэтому звук байт-в-байт; разница слышна ровно тогда, когда рука-«смычок»
    ведёт ТЕКСТУРУ (единственная нелинейность): тремоло модулирует вход шейпера, а эхо не несёт его окраски.
  - **3.7.2 — СОБСТВЕННО ФОРМАТ.** Карта `a.fx` в нагрузке: ключ — СТАБИЛЬНАЯ пара `fxId:paramKey`
    (**никогда индекс в цепи**, правило #25: перестановка или удаление не смеет переосмыслить записанное),
    значения НОРМИРОВАННЫЕ, и только ПРИЦЕПОЧНЫЕ параметры. Дельта-сжатие обобщено со списка полей на
    СРАВНЕНИЕ МНОЖЕСТВ с порогом **`REC_FX_EPS=0.03` в нормированных единицах** (`fxNorm`/`fxDenorm` для
    того и заведены, чтобы 0..1 были перцептивно ровными; совпадает с прежним поведением `bri`).
    `rev` снят как мёртвый груз (писался нулём с 3.1, читателей не было, его клауза `0>0.03` была ложью
    навсегда) — смысл вернулся ключом `reverb:mix`, и теперь он ПЕР-НОТНЫЙ.
    ⚠️ **ПОДМЕС РЕВЕРБА СТАЛ ПЕР-ГОЛОСОВЫМ ПОСЫЛОМ**, а у экземпляра появился **`gate`**: один узел не мог
    быть одновременно ВЕЛИЧИНОЙ подмеса шины, ВЫКЛЮЧАТЕЛЕМ эффекта в цепи и ПЕР-НОТНЫМ носителем. Гашение
    при снятии эффекта переехало на `gate` — иначе у соло (голоса идут мимо `send`) снятый реверб не
    замолчал бы вовсе.
    ⛔ **КАРТА КОПИРУЕТСЯ И ПРИ `push`, И В СОСТОЯНИИ СРАВНЕНИЯ.** Она первый МУТИРУЕМЫЙ объект в
    нагрузке (прежде были только примитивы, `ty` по ссылке и намеренно дописываемый `bend`); без копии
    один объект оказался бы и в записанном событии, и в живом состоянии — и следующее движение руки тихо
    переписало бы прошлое. Снаружи это читалось бы как «петля сама меняется».
    ⚠️ **ОТСУТСТВИЕ КАРТЫ = «ТЕКУЩИЕ ЗНАЧЕНИЯ ЦЕПИ РОЛИ», А НЕ НЕЙТРАЛЬ.** События аранжировки
    (`arrange.js` строит их вручную) карты не несут; прочти их нейтралью — и ВЕСЬ ДЖЕМ СТАНЕТ СУХИМ.
    Это и была настоящая поверхность совместимости пласта (миграции старых петель, напомним, не
    существует — петли не персистятся).
  - **ПОЧИНКА СРАЗУ ПОСЛЕ 3.7.2 — «УТЕЧКА ЭФФЕКТА НА АТАКЕ».** Симптом: записали слой, где начало сухое,
    а конец залит делеем, — и ПЕРВАЯ НОТА каждого повтора тоже звучала с делеем.
    **Причина не в записи, а в способе записи в узел:** прицепки СТАВИЛИСЬ через `setTargetAtTime`
    (экспоненциальное ПРИБЛИЖЕНИЕ: в момент атаки величина РАВНА СТАРОЙ, через 20 мс от неё остаётся ещё
    ~78% при τ=0.08), а голоса берутся ИЗ ПУЛА и при отпускании не сбрасываются — переиспользованный
    голос отдавал начало новой ноты на уровне предыдущей. На завороте петли это сходилось острее всего.
    **Стало:** на АТАКЕ величина СТАВИТСЯ (`cancelScheduledValues`+`setValueAtTime`), при ВЕДЕНИИ —
    подъезжает; границей служит УЖЕ СУЩЕСТВОВАВШИЙ гейт `v.on`, второй заводить не пришлось.
    ⛔ **Прицепки НЕ сбрасываются на отпускании** — отпущенная нота обязана кормить делей и реверб СВОИМ
    уровнем, пока звучит хвост; обнуление там обрезало бы эхо у каждой ноты.
    ⛔ **Хвост линии задержки через заворот НЕ промываем** — так делей и устроен, он не знает, где у петли
    шов; промывка дала бы слышный обрыв на каждом повторе. Отличать на слух: «дозвон хвоста» — это эхо
    ПОСЛЕДНИХ нот круга; исправленный баг давал эхо САМОЙ ПЕРВОЙ.
    🔁 **УРОК, ВТОРОЙ РАЗ ПОДРЯД: РЕШЕНИЕ УЖЕ ЛЕЖАЛО НА АККОРДОВОМ ПУТИ.** `chordOn` СТАВИТ яркость на
    атаке, `chordGlide` ВЕДЁТ её — ровно то разделение, которого не хватало соло. (Первый раз так же
    вышло с пер-голосовой прицепкой: `bri` был готовым прототипом.) **Следующий раз — сначала смотреть,
    как та же задача решена у аккордов.**
  - **3.7.3 — ПЕРЕНАЗНАЧАЕМАЯ ГРОМКОСТЬ (X).** У играющей руки появился ВТОРОЙ адрес — `play:x` рядом с
    `play:z`. Если параметр цепи РОЛИ подписан на него, громкость ЭТОЙ роли перестаёт следовать за рукой
    и берёт фиксированное значение из меню (0..100, дефолт 80, общий `buildStepper` — второго поля не
    заводили). Заморозка РОЛЕВАЯ: отданный X у соло не трогает аккорды, бас и ударные.
    ⚠️ Попутно вскрыты и починены две скрытые ошибки: блок ГЛУБИНЫ (с Пласта 3.2) фильтровал по
    `hand==='play'` **без проверки оси** — глубина писала бы и в параметр, отданный горизонтали; а
    `fxAddrOf` хардкодил `'play:z'` — X-адрес показывался бы как «глубина» и молча возвращался на Z.
- **ПЛАСТ 3.8 — ⬜ ПЕР-СЛОЙНЫЕ СЕТИ ЭФФЕКТОВ («своя комната у слоя»). РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ: НУЖНО.**
  3.7 даёт слою свои ПРИЦЕПКИ; 3.8 даёт ему СВОЮ СЕТЬ — длину и окраску реверба, время и обратную связь
  делея, то есть всё, что физически не может отличаться внутри ОДНОГО экземпляра. **Что обязан решить:**
  (1) **пер-слойные экземпляры** — кто их строит и по какому событию; (2) **ПОТОЛОК** на число слоёв,
  владеющих собственной сетью (≈49 линейных узлов на FDN × слой × роль — без потолка овердаб растёт
  неограниченно); (3) **ЖИЗНЕННЫЙ ЦИКЛ УДАЛЕНИЯ** на undo/очистке/смене джема, причём снос узлов посреди
  хвоста ОБРЕЗАЕТ ЕГО СЛЫШНО — а сегодня экземпляры не разбираются ВООБЩЕ, и это осознанное решение
  3.5.1, которое здесь придётся пересматривать явно; (4) **перенос сетевых параметров в событие** —
  поверх карты `a.fx`, которую заводит 3.7.
  ⚠️ **Становится посильным ТОЛЬКО ПОСЛЕ 3.7:** событие уже умеет нести параметры эффектов, а прицепки
  уже пер-голосовые — значит 3.8 занимается ровно сетями и ничем больше.
- **ПЛАСТ 3.9 — ⬜ ГРОМКОСТЬ КАК НЕСЪЁМНЫЙ ПАРАМЕТР ЦЕПОЧКИ. ПРЕДЛОЖЕНИЕ ПОЛЬЗОВАТЕЛЯ.**
  **ЗАМЫСЕЛ.** Сегодня меню говорит на ДВУХ языках: эффекты привязываются к адресам через общий
  механизм, а громкость — отдельная строка с особым правилом («X отдан эффекту ⇒ громкость
  фиксируется»). Предложение: сделать громкость ОБЫЧНЫМ ПАРАМЕТРОМ ЦЕПИ, назначаемым на ЛЮБОЙ адрес
  (любой палец, любая ось, либо играющая рука), но **НЕСЪЁМНЫМ** — его нельзя убрать из цепи.
  **ЧТО ЭТО ЧИНИТ — две вещи, и вторая важнее первой:**
  1. Сегодня громкость можно ПОТЕРЯТЬ, но нельзя ПЕРЕНАЗНАЧИТЬ: отдал X эффекту — она застыла числом.
     Как параметр цепи она просто ПЕРЕЕХАЛА БЫ на другой адрес (скажем, на палец fx-руки).
  2. **РАСТВОРЯЕТСЯ САМ ОСОБЫЙ СЛУЧАЙ**, который пользователю и не нравится. «X отдан эффекту» перестаёт
     быть ПРАВИЛОМ: громкость и эффект просто сидят на РАЗНЫХ адресах. А поставит человек их на ОДИН —
     поедут вместе, как любые два параметра на общем адресе (чип «×N» это и покажет). Никакой особой
     логики не остаётся вовсе.
  **ЧЕГО ЭТО СТОИТ — три цены, назвать честно ДО начала:**
  1. **ФОРМАТ.** `vol` — ИМЕНОВАННОЕ поле события во ВСЕХ ЧЕТЫРЁХ ролях со своим порогом
     (`REC_VOL_EPS`), а параметры эффектов живут в карте `a.fx`. Либо `vol` переезжает в карту — это
     правка формата через считаные дни после того, как его наконец стабилизировали в 3.7.2, — либо меню
     ДЕЛАЕТ ВИД, что громкость параметр цепи, не будучи им. Второе — та самая двуязычность, только
     спрятанная глубже.
  2. **ГРОМКОСТЬ НЕ ПРОХОДИТ ЧЕРЕЗ УЗЕЛ ЭФФЕКТА.** Она печётся в ОГИБАЮЩУЮ ГОЛОСА на атаке (`v.vol`), а
     не является посылом или вставкой. «Параметр цепи», который пишет огибающую, — исключение, которое
     придётся объяснять в каждом месте, где цепь обходят единообразно (снимок, дельта-сжатие, гашение
     снятого эффекта, пер-голосовая прицепка).
  3. **ОН ОБЯЗАН СУЩЕСТВОВАТЬ ВСЕГДА И ВО ВСЕХ ЧЕТЫРЁХ ЦЕПЯХ** — неудаляемая и недублируемая запись.
     То есть особый случай ВНУТРИ механизма, который строился ради единообразия: `fxChainRemove` должен
     её отказываться удалять, «+ Добавить эффект» — не предлагать, инвариант «одна запись на fxId»
     получает исключение.
  ⚠️ **ОТДЕЛЬНЫМ ПЛАСТОМ, НЕ ВНУТРЬ 3.7.** Естественный сосед — 3.8: оба доводят модель до настоящего
  единообразия (3.8 — по сетям, 3.9 — по громкости), и оба трогают то, что 3.7 только что закрепил.
- **БЮДЖЕТ ОСЕЙ правой руки — держать перед глазами всю дорогу:** щипок (нота вкл/выкл) · **Y — высота**
  (ступень, гистерезис, прямоугольники, подписи) · **X — громкость ИЛИ эффект** · **Z — эффект**.
  Щипок и Y заняты насмерть; назначаемых осей РОВНО ДВЕ, и X, отданный эффекту, стоит живой громкости.
  ⚠️ С Пласта 3.7.3 «X отдан эффекту» — уже РЕАЛЬНОСТЬ, а не план; Пласт 3.9 (если состоится) снимет с
  этого бюджета особый статус громкости: она станет таким же назначаемым параметром, как остальные.

**⛳ ПОРЯДОК ОСТАВШЕГОСЯ — ПЕРЕСЧИТАН ПОСЛЕ 3.7, ЧИТАТЬ ДО ВЫБОРА СЛЕДУЮЩЕГО ПЛАСТА.**
**3.7 БЫЛ РАЗБЛОКИРОВЩИКОМ, И ОН СРАБОТАЛ.** Событие теперь несёт ПРОИЗВОЛЬНЫЙ набор прицепочных
параметров (карта `a.fx`), прицепки пер-голосовые, громкость умеет уходить эффекту. Что это открыло:
- **(а) яркость аккордов — ✅ СДЕЛАНА** (прогноз подтвердился: механизм был готов, потребовался лишь
  перевод в цепь и адрес по умолчанию);
- **(в) делей и тремоло прочим ролям** — после 3.7.1 это прицепка ещё одного экземпляра к чужой шине;
  собственное поле в событии у них теперь есть (тот же ключ карты), отдельного store не требуется;
- **(б) рука-эффекты у прочих ролей — ✅ СДЕЛАНА** для соло/баса/аккордов (остались ударные, б.4 — и они
  идут ПЕРЕД (в), см. цель (б) ниже).
⚠️ **3.6 (двойной щипок) по-прежнему НЕЗАВИСИМ**: он про АДРЕСА (сколько привязок помещается на руку),
а не про запись. Идёт где удобно.
**ОСТАЛОСЬ ТРИ ПЛАСТА, и они РАЗНОГО РОДА** — выбирать по тому, чего хочется, а не по номеру:
- **3.8 — пер-слойные СЕТИ** (требование пользователя): «своя комната у слоя». Единственный, что
  упирается в тяжёлую цену — экземпляры на слой, потолок, удаление без обрыва хвоста.
- **3.9 — громкость как параметр цепи** (предложение пользователя): убирает последний особый случай в
  меню; цена — снова трогать формат, только что стабилизированный.
- **3.6 — двойной щипок**: чистое расширение пространства адресов, ни от чего не зависит.
⚠️ **А ЦЕЛИ (а)/(б)/(в) БОЛЬШЕ НЕ ЖДУТ ПЛАСТА** — они стали обычной работой поверх готовой модели и
могут делаться по одной, между пластами.
⛳ **ЗА ЭТИМ ПЛАНОМ — БУДУЩАЯ МОДЕЛЬ «ПАЛЕЦ КАК ЕДИНИЦА НАСТРОЙКИ»** (блок ниже, после «ЧЕГО ЕЩЁ НЕТ»).
Не запланирована, но решения по оставшимся пластам и целям НЕ ДОЛЖНЫ ей противоречить — там перила.

**ЧЕГО ЕЩЁ НЕТ — ЧЕТЫРЕ ЦЕЛИ, СОБРАННЫЕ В ОДНО МЕСТО.** Они были разбросаны по этому документу,
комментариям в коде и переписке; здесь — канон, чтобы не искать их в трёх местах.

- **(а) ✅ СДЕЛАНА — ЯРКОСТЬ АККОРДОВ (Z) СТАЛА ОБЫЧНЫМ ЭФФЕКТОМ ЦЕПИ** (коммит `3cc613d`, слайс а.1).
  Это был **ПЯТЫЙ непереносимый эффект**, и в разборе Пласта 1 его не было — он обнаружился позже.
  **ЧТО ЛЕГЛО:**
  - **Различитель `kind` у записи ФАБРИКИ: `'bus'` против `'voice'`.** Эффект-голос НЕ ВОЗВРАЩАЕТ УЗЛОВ
    вовсе — и оказалось, что контракт параметра узла НИКОГДА и не требовал: параметру нужен `set`, а не
    вход/выход. `makeBrightFx` отдаёт `{id,labelKey,kind:'voice',params}` без единого узла, величина
    живёт в `p.cur`, а голос забирает её НА АТАКЕ. Три места научились спрашивать `kind`: `fxAttach` и
    `fxSetActive` выходят сразу (цеплять нечего), обход цепи в меню/столбиках работает как со всеми.
  - **Параметр объявлен ЯРКОСТЬЮ (min<max, 100 = ярко), а переворот выражен `inv:true` В АДРЕСЕ ПО
    УМОЛЧАНИЮ** — не в данных параметра. Глубина руки растёт «от себя», поэтому её надо зеркалить, и это
    свойство АДРЕСА, а не величины: сняли инверсию — получили обратное поведение, не сломав параметр.
  - **Цепь аккордов засеяна записью `bright` на адресе `play:z` аккордовой руки** — прежнее поведение
    стало ДЕФОЛТНОЙ НАСТРОЙКОЙ, которую можно снять, зафиксировать числом или увести на горизонталь.
  - **Z-драйвер аккордов добавлен ВНУТРЬ аккордовой ветки**, переиспользуя УЖЕ ПОСЧИТАННУЮ в ней глубину.
    ⛔ **НЕ обобщать сюда соло-блок глубины:** он зовёт `emaS` САМ, и второй вызов за кадр ПРОДВИНУЛ БЫ
    фильтр сглаживания дважды (та же ловушка, о которой предупреждает `captureFx`).
  ⛳ **`kind:'voice'` — ГОТОВЫЙ ПРЕЦЕДЕНТ ДЛЯ ЦЕЛИ (в).** Драйв и вибрато у не-соло ролей упираются
  ровно в это: они не посыл и не вставка, у них нет аудио-входа/выхода на шине. Теперь есть форма, в
  которой такой эффект выражается честно, — и её не надо изобретать заново.
  **Два блокера, которые пришлось снять (разные по роду):**
  1. **Фильтр ПЕР-ГОЛОСОВОЙ, а не на шине** — и намеренно (правило #19): общий фильтр не смог бы дать
     разным слоям петли РАЗНУЮ яркость, а два фильтра на голос (`f` атака / `fb` яркость) нельзя
     сливать, иначе тембр навсегда сцепится с выразительностью.
  2. **`a.bri` ЕДЕТ В СОБЫТИИ АККОРДА** (как `a.rev` ехал у соло) — значит перевод яркости в параметр
     цепи это **ВОПРОС ФОРМАТА**, а не проводки. См. блок о порядке выше.
  **Замысел пользователя:** яркость становится НАЗНАЧАЕМЫМ эффектом в цепи аккордов, по умолчанию
  подписанным на адрес «ГЛУБИНА АККОРДОВОЙ РУКИ» — то есть сегодняшнее поведение перестаёт быть
  вшитым и становится ДЕФОЛТНОЙ НАСТРОЙКОЙ, которую можно снять, переназначить или заменить.

- **(б) ✅ СДЕЛАНА ДЛЯ СОЛО/БАСА/АККОРДОВ — ОСТАЛИСЬ УДАРНЫЕ (б.4).** Три слайса, все в `dev`.
  - **б.1 — МЕХАНИЗМ СТАЛ РОЛЕВЫМ, ПОВЕДЕНИЕ БАЙТ-В-БАЙТ** (`b4dcbef`). `roleHasFx` перестал быть
    «только соло»; оба гейта зоны берут роль ДЕЙСТВУЮЩЕЙ руки (сплит — `h.role`, single — `phoneInstr`);
    `captureFx` берёт роль с `S`, и ради этого `S.role` заморожена ТЕПЕРЬ И В SINGLE-ветке (прежде это
    делала только сплит-ветка — расхождение с CLAUDE.md, сходившее с рук лишь потому, что все читатели
    стояли под гейтом `splitOn`). `soloHalfRole` → `handHalfRole`. Байт-в-байт по построению: пока `'fx'`
    нет в `HANDFN_OPTS` прочих ролей, предикат ложен везде, кроме соло.
    ⚠️ При сверке нашлись ДВА потребителя сверх плановых, оба одного рода: цвет кисти fx-руки и подсветка
    столбика — оба перебирали ВСЕ руки без фильтра по роли (`fxParamOnFinger` сравнивает ТОЛЬКО НОМЕР
    ПАЛЬЦА), и в сплите чужая рука засветила бы чужие столбики.
  - **б.2 — БАС ПОЛУЧИЛ `'fx'`, А СТОЛБИКИ ВСЕХ РОЛЕЙ ПЕРЕЕХАЛИ К ПРАВОЙ КРОМКЕ** (`39ea068`). Сторона
    одна на все роли, потому что СЛЕВА ПАЛИТРА типов — ровно поэтому индикатор ЯРК с самого начала стоял
    справа. Выкладка СПРАВА НАЛЕВО с якорем у кромки: разворот эффекта под зажатым пальцем добавляет
    столбики ВЛЕВО, а стоящие правее не двигаются. Крайнее гнездо (`FX_EDGE`) отдано одиночным
    индикаторам роли — цепь начинается СЛЕДУЮЩИМ, и это спасло от прямого наложения на «ВЫР» (соло может
    держать руку-выразительность И руку-эффекты одновременно).
    ⛳ **ПОБОЧНЫЙ ГЛАВНЫЙ ИТОГ: R-1 ПЕРЕСТАЛО СУЩЕСТВОВАТЬ** — подробности в записи Пласта 3.3 выше.
    ⚠️ Цепь баса ПУСТА по умолчанию: рука-эффекты у баса ничего не ведёт, пока в цепь не добавят эффект.
    Засевать её ради красоты слайса нельзя — это была бы правка звука по умолчанию.
  - **б.3 — АККОРДЫ ПОЛУЧИЛИ `'fx'`.** Разбор Гц аккорда теперь УСТУПАЕТ дорогу полосе столбиков — но
    ТОЛЬКО при реальном пересечении по вертикали (оба прижаты к правой кромке, а `drawFxBars` идёт ПОЗЖЕ
    `drawRole` и лёг бы ПОВЕРХ цифр). Безусловный отступ съедал бы ~170px ширины разбора на КАЖДОМ
    аккорде, включая верхние. Попутно починен ЗАЛИПШИЙ `chOwner`: все его читатели сидят в ветке
    `S.zone==='ch'`, поэтому рука, переставленная на эффекты, туда больше не попадала и «руль защёлки»
    оставался за ней (звук при этом гасился корректно — `softAllOff` ставит `latchDeg=-1`; чинился
    ИНВАРИАНТ, а не звенящая нота).
  - **⬜ б.4 — УДАРНЫЕ, И ИМ ПРИДЁТСЯ ВЫДУМЫВАТЬ, А НЕ ДОБАВЛЯТЬ.** `handFn` имеет записи ТОЛЬКО для
    `ld`/`bs`/`ch`; у ударных записи НЕТ ВОВСЕ — обе руки просто бьют по рядам, выбирать было нечего
    (`handFnOf(key,'dr')` → `null`, `HAS_HANDFN` в `ui.js` ударных не знает, «Функции рук» их не
    показывают). У всех прочих ролей `'fx'` ДОПИСАЛИ в уже существующий список вариантов; ударным список
    нужно СОЗДАТЬ С НУЛЯ: дефолтная функция «удар» (`'hit'`, воспроизводит сегодняшнее поведение обеих
    рук) плюс `'fx'`. Что это потянет, названо заранее: запись `dr` в `handFn` и её дефолт;
    `HANDFN_OPTS.dr`; `'dr'` в `HAS_HANDFN` (секция «Функции рук» начнёт показывать ударные); ключ
    `handfn.hit` в ОБОИХ словарях; `tutorReset` обязан восстанавливать и `dr`, иначе рука-эффекты ударных
    протечёт в следующий урок (правило #24). `roleHasFx` и гейты зоны доработки НЕ требуют — они уже
    спрашивают `handFn[role]` и заработают сами, как только запись появится. ⚠️ `roleHasNotes('dr')`
    станет истинным (`playsNotes('hit')`) — при сверке у него не нашлось ни одного живого читателя, но
    перепроверить на месте, а не принять на веру. Цена по игре реальна: у ударных, в отличие от моно
    аккордов и баса, обе руки заняты по-настоящему (удар — независимо на каждый щипок), а цепь пуста.
    ⛳ **ИДЁТ ПЕРЕД ЦЕЛЬЮ (в) — решение пользователя.** Сначала механизм у ВСЕХ ЧЕТЫРЁХ ролей, потом цепи
    наполняются везде ОДНИМ заходом, — а не (в) на три роли с возвратом к ударным потом.
  **ЧТО ПОКАЗАЛ БЮДЖЕТ РУК ПРИ СВЕРКЕ — предупреждение в этом пункте было ПРЕУВЕЛИЧЕНО.** Говорилось:
  «у аккордов особенно дорого — вторая рука часто держит защёлку». Код говорит иначе: аккорды МОНО
  (`chOwner`), защёлка звучит и после размыкания, обе руки по умолчанию делают ОДНО И ТО ЖЕ (`latch`), —
  значит вторая рука для ИГРЫ не нужна. Настоящая цена у аккордов другая: на ладу с ПАЛИТРОЙ типов рука,
  ушедшая на эффекты, больше не выбирает тип (палитра берётся ПО ПОЛОЖЕНИЮ, годилась любая рука).
  Оставшаяся рука по-прежнему умеет и то и другое ПО ОЧЕРЕДИ; на ладах без палитры цена нулевая.
  Бас — самый дешёвый (тоже моно, `bassOwner`), ударные — единственные, где обе руки правда заняты.
  ⚠️ **ПОЗЖЕ ЧАСТЬ РЕШЕНИЯ б.3 БЫЛА СОЗНАТЕЛЬНО ОТМЕНЕНА** — см. следующий пункт.

- **⛳ ОТМЕНА ЧАСТИ б.3: СТОЛБИКИ СЛЕДУЮТ ЗА ЦЕПЬЮ, А НЕ ЗА РУКОЙ** (`b595ed1`, упрощение по требованию
  пользователя). **Довод, который перевесил:** столбик показывает состояние ЭФФЕКТА, а не РУКИ, — а с
  Пласта 3.4.1 цепь СУЩЕСТВУЕТ И ЗВУЧИТ без всякой руки на эффектах (фиксированные параметры работают).
  Значит и показывать её надо на тех же основаниях.
  **Что было не так у б.3:** показ зависел от руки, и это дало ЧЕТЫРЁХКЛЕТОЧНУЮ таблицу (яркость в цепи ×
  рука на эффектах), а сам столбик ЯРК КОЧЕВАЛ МЕЖДУ ДВУМЯ РИСОВАЛЬЩИКАМИ — с рукой его рисовал общий
  обход цепи, без руки отдельная функция. Один и тот же эффект выглядел по-разному в зависимости от того,
  чем занята рука, хотя звучал одинаково.
  **Что стало:** ОДНО правило — **есть в цепи, значит рисуется**. `roleHasFx` ушёл из пути отрисовки
  ЦЕЛИКОМ. Отдельного индикатора ЯРК больше НЕ СУЩЕСТВУЕТ: яркость — просто один из столбиков аккордовой
  цепи (`BRI`), всегда. Удалены `chBrightDisp`/`setChBrightDisp` (у зеркала не осталось читателей),
  `drawChordBright` и предикат `fxInChain` (он существовал ради единственного исключения, а исключения не
  стало). Четыре состояния схлопнулись в одно.
  ⚠️ Видимое следствие, названное честно: цепь с ОДНИМИ ФИКСИРОВАННЫМИ параметрами теперь тоже рисует
  столбики, которых не двигает ни один палец. Это верно и задумано — столбик сообщает величину эффекта,
  а её задали в меню.

- **«д» ✅ ДИСКРЕТНЫЕ АДРЕСА — ПАЛИТРА ТИПОВ СТАЛА НАЗНАЧАЕМЫМ ДЕЙСТВИЕМ ПАЛЬЦА** (`ca67816`, внеплановый
  слайс; д.1 и д.2 вышли ОДНИМ заходом). **Повод — баг, оказавшийся заготовкой:** рука на эффектах,
  щипнув большим+указательным ВНУТРИ палитры, выбирала тип аккорда — палитра гейтилась ПОЛОЖЕНИЕМ
  (`famHand`: роль + `typedChords` + `S.oct===0` + положение, ни слова о функции руки) и стояла в тернаре
  ВЫШЕ гейта эффектов. Прочие пальцы той же руки вели эффекты. «Указательный выбирает тип, остальные ведут
  эффекты» выходило СЛУЧАЙНО — раскладка полезная, но обязана быть НАСТРОЙКОЙ, а не совпадением геометрии.
  Случайности ровно один слайс: рука-эффекты у аккордов появилась только в б.3.
  **ЧТО ЛЕГЛО:**
  - **`handActs = {ld:[],ch:[],bs:[],dr:[]}`** (`state.js`), записи `{hand:'fx', finger, act}` — пара к
    `handFn`: тот говорит, ЧТО ТАКОЕ РУКА, этот — ЧТО ДЕЛАЮТ ЕЁ ПАЛЬЦЫ. ⛔ НЕ внутри `fxChains`: у действия
    нет параметра и нет эффекта (понадобился бы фиктивный эффект ради места под адрес), сломались бы
    «одна запись на fxId», обход `fxSnapshot`, пер-голосовая прицепка; ⛔ действие НИКОГДА не попадает в
    событие. `hand:'fx'` выписан, хотя других значений нет, — закон 2.6.
  - **Таблица `ACTIONS`** (`gestures.js`) `{id, labelKey, avail(), region, exclusive}` — движок не опознаёт
    действие по имени. Палитра: `avail:()=>typedChords()`, `region:'palette'`, `exclusive:false`.
  - **`paletteFinger(key,role,finger)` — ЕДИНСТВЕННЫЙ источник**, зовётся из обоих тернаров захвата И из
    пофреймовой перепроверки в зоне `'chFam'` (разойдись они — ведение оборвалось бы посреди жеста).
    `famHand` стал «роль аккордов + `typedChords()` + положение + `paletteFinger(…)`»; порядок тернара НЕ
    тронут. Роль — замороженная (`h.role` / `S.role`), ⛔ не глобальный `phoneInstr`.
  - **Секция «Действия пальцев»** в «🎛 Звук», сразу под «Функциями рук»: видна только у ролей с рукой на
    эффектах, четыре строки (по пальцу); недоступное действие показано ОТКЛЮЧЁННЫМ С ПРИЧИНОЙ; чип «×N» —
    когда подписанный палец ещё и ведёт параметры (для `exclusive:false` это законно и задумано).
  **РЕШЕНИЯ:**
  - **ПО УМОЛЧАНИЮ НЕ ПОДПИСАНО НИЧЕГО.** Случайность одного слайса не проектировалась и нигде не
    описывалась — мышечной памяти на неё нет ни у кого; выпустить её дефолтом значило бы узаконить порядок
    тернара как замысел. Нужна раскладка — её подписывают, в этом и суть настройки.
  - **СТАРШИНСТВО: сперва ФУНКЦИЯ РУКИ, потом ПОДПИСКА ПАЛЬЦА.** Рука не на эффектах — дословно как было
    (палец 0 + положение, одноручная модель аккордов); рука на эффектах делает только то, что назначено её
    пальцам. Два гейта стали взаимоисключающими ПО ПОСТРОЕНИЮ, а не по порядку в тернаре.
  - **Лад без палитры:** `avail()` ложен — подписка ИНЕРТНА, но НЕ стёрта; палец падает в обычное ведение
    параметров, а не повисает мёртвым.
  **ЧТО НАМЕРЕННО НЕ ТРОНУТО:**
  - **ЛУПЕР** (целая рука через `handFn 'loop'`) — естественный первый переезд и настоящая выплата по
    бюджету рук (четыре команды стоят руки, а стоили бы по пальцу), но он РАБОТАЕТ; переделывать работающее
    ради единообразия — способ получить регрессию. Записан как вмещающийся в модель, не запланирован.
    ⚠️ До переезда `fxFreeFinger` обязан научиться видеть `handActs` (у лупера было бы `exclusive:true`).
  - **ОКТАВНАЯ ПОЛОСА — разобрано и отклонено, четыре довода:** (1) её адрес — ОБЛАСТЬ ЭКРАНА, а не палец;
    (2) палец у неё — ЗНАЧЕНИЕ (палец 2 = регистр III), поэтому `{рука, палец, действие}` её не выражает;
    (3) руки она не стоит — гейтится положением, годится любая рука; (4) бага у неё нет. Понадобится
    адресация по области — это ДРУГАЯ ось (`{region:'…'}`), и она добавится дополнением.

- **(в) ⬜ ВСЕ ЭФФЕКТЫ ДОСТУПНЫ ВСЕМ РОЛЯМ.** Сегодня прочим ролям предлагаются ТОЛЬКО МОДУЛИ, а модулей
  ДВА — реверб и яркость. Старые четыре сопротивляются каждый по-своему, и это надо помнить, чтобы не
  пытаться «просто перенести»:
  ⛳ **ГЛАВНОЕ, ЧТО ИЗМЕНИЛА ЦЕЛЬ (а): появился `kind:'voice'`** — форма модуля БЕЗ УЗЛОВ, где величину
  забирает голос на атаке. Драйв и вибрато упираются ровно в отсутствие такой формы (см. ниже), и теперь
  она есть и обкатана на яркости. Это не отменяет работу по каждому из них, но снимает вопрос «а как
  вообще выразить эффект, который не посыл и не вставка».
  - **ДРАЙВ** — ПО ГОЛОСАМ, ДО огибающей. `env·tanh(x) ≠ tanh(env·x)`, поэтому вынос на шину изменил бы
    звук ОДНОЙ ноты (а дефолтный `drv` не ноль, а 0.12). Нужен шейпер в КАЖДОМ голосе чужого пула.
  - **ВИБРАТО** — вообще без аудио-входа/выхода: LFO в `.detune`, и цепляет его `mkOsc`. **Аккордовые
    голоса `mkOsc` не зовут вовсе** (создают осцилляторы напрямую) — переносить придётся в пул.
  - **ДЕЛЕЙ и ТРЕМОЛО** — СТРУКТУРНО ПЕРЕНОСИМЫ (посыл и вставка, обе формы шине понятны). Их держит не
    проводка, а то, что величины лежат в ОДНОМ глобальном `state.fx` и едут в СОЛО-событие ноты. Значит
    им нужны свой store на роль и своё место в событии — снова **3.7**.
  ⚠️ Отсюда же вытекает, что «модуляризовать делей» нельзя сделать втихую как рефакторинг: у соло его
  значение записывается, и превращение в модуль сделало бы его ЖИВЫМ — это повторило бы компромисс
  Пласта 3.1 (живой реверб) ещё четыре раза, на ролях, где так никто не договаривался.

- **(г) ⬜ УГЛУБИТЬ САМИ ЭФФЕКТЫ — ЯВНО ОТЛОЖЕНО ПОЛЬЗОВАТЕЛЕМ ДО КОНЦА РАБОТЫ НАД УПРАВЛЕНИЕМ.**
  У четырёх старых эффектов сегодня по ОДНОМУ параметру («величина»), а модель уже позволяет ТРИ на
  набор адресов (и ШЕСТЬ после двойного щипка). Реверб показал, куда это ведёт: из «одной ручки
  количества» он стал настоящей сетью с длиной, окраской и подмесом. Тот же путь открыт делею
  (время/обратная связь/фильтр в петле), драйву (тип кривой, тон после), тремоло (частота/форма/фаза).
  ✅ **СПОСОБ СОГЛАСОВАН: ПИШЕМ DSP САМИ, читая алгоритмы открытых синтезаторов как СПРАВОЧНИК** —
  Surge XT, ZynAddSubFX. Именно как справочник (какая топология, какие диапазоны звучат музыкально), а
  не как импорт. **Почему не библиотека — три отдельные причины, не одна:**
  фреймворки поверх Web Audio качества не добавляют (тот же граф, лишний слой);
  плагины (VST/AU) в браузере не живут — это не его формат;
  сэмплерные библиотеки ВОЮЮТ С МИКРОТОНИКОЙ — они записаны в 12-TET, а всё приложение построено на том,
  что высота берётся из строя (правило #2), и растягивание сэмплов ломает и тембр, и смысл.

**⛳ БУДУЩАЯ МОДЕЛЬ: ПАЛЕЦ КАК ЕДИНИЦА НАСТРОЙКИ (ПРОГРЕССИВНОЕ РАСКРЫТИЕ). ⬜ НЕ ЗАПЛАНИРОВАНО.**
> Замысел пользователя (записан 2026-09-10). Делать ПОСЛЕ текущего плана — пользователь решил это явно
> (порядок и почему он помогает — в конце блока). Записано СЕЙЧАС ради одного: чтобы ни одно решение,
> принятое до него, ему не противоречило. Здесь — форма модели, её цена и перила на промежуток; плана
> здесь нет. В BACKLOG пока не заведено.

**ПРИНЦИП, в формулировке пользователя.** Настройка РУКИ становится ЧАСТНЫМ СЛУЧАЕМ настройки ПАЛЬЦА.
«Вся рука на эффектах» — сокращение для «все четыре пальца на эффектах». Ничто построенное не
отменяется — оно ОБОБЩАЕТСЯ: сегодняшняя рука = четыре пальца с одинаковой настройкой.

**ПОЧЕМУ ЭТА ФОРМА, СКОРЕЕ ВСЕГО, ВЕРНА — ТОТ ЖЕ ХОД УЖЕ ДВАЖДЫ ОКУПИЛСЯ.** Оба раза особый случай
оказывался ЗНАЧЕНИЕМ более общего поля, и следующий шаг вставал ДОПОЛНЕНИЕМ, а не переписыванием:
1. **Пласт 2.6:** параметр получил `{mode:'drive', hand}`, а НЕ `{mode:'finger'}` — рука стала атрибутом
   ведения, а не видом параметра. Выплата — Пласт 3.2: играющая рука вошла ДОПОЛНЕНИЕМ (`hand:'play'`),
   без правки данных и существующих веток.
2. **Пласт 3.4.2:** цепь стала списком ЭФФЕКТОВ, а палец переехал в АДРЕС ПАРАМЕТРА (`finger:0..3`).
   Выплата — один адрес ведёт параметры РАЗНЫХ эффектов; палец перестал «владеть эффектом» и стал
   координатой, на которую подписываются.
3. **Третий шаг уже заложен** (слайс «д» ✅, коммит `ca67816`; запись слайса — в «ЧЕГО ЕЩЁ НЕТ» выше): `handActs`
   пишет `hand:'fx'`, хотя другого значения пока нет, — ровно затем, чтобы действие на пальце ИГРАЮЩЕЙ
   руки стало дополнением; словарь адресов `{рука, палец}` ОБЩИЙ у непрерывных и дискретных адресов.
   Туда же — `b595ed1`: столбики следуют за ЦЕПЬЮ, а не за рукой, то есть показ перестал спрашивать про руку.
Модель «палец» — следующая ступень той же лестницы: `handFn` (что такое РУКА) → `handActs` (что её
пальцы делают дискретно) → палец, настраиваемый целиком.

**ЧТО ПАЛЕЦ НЁС БЫ В СЕБЕ.**
- **ЧТО ИГРАЕТ.** Тембр — возможно, НЕСКОЛЬКО сразу. Октавы — возможно, НЕСКОЛЬКО сразу, звучащие
  вместе. (Указательный — тембр A; безымянный — тембр B и две октавы.)
- **КАК ИГРАЕТ.** То, что сегодня функция РУКИ — ноты / удержание / терменвокс / защёлка, — назначаемо
  по пальцу. Пример пользователя: терменвокс только на указательном, а мизинец играет защёлкнутые ноты.
  ⚠️ У соло защёлки сегодня НЕТ (`'latch'` есть лишь у аккордов, см. `HANDFN_OPTS`) — для соло-пальца это
  НОВОЕ поведение, а не переезд.
- **ЧЕМ УПРАВЛЯЕТ.** На КАЖДУЮ ОСЬ этого пальца — сколько угодно назначений: громкость, яркость тембра,
  параметр эффекта, у каждого своя инверсия. Несколько параметров на одной оси законно УЖЕ сегодня и
  уже показано чипом «×N» (3.4.4).
- **ДИСКРЕТНЫЕ ДЕЙСТВИЯ.** То, что держит `handActs`: сегодня палитра типов аккорда, естественный
  следующий — команды лупера (разбор переезда и его ⛔ «не трогать сейчас» — в шапке `handActs`, `state.js`).
- **ПОЗЖЕ — ЦЕПЬ У ТЕМБРА, а не у роли:** разные тембры на разных пальцах несут разные эффекты.

**⚠️ ТРИ ЦЕНЫ — назвать ДО плана. Сверено с кодом.**
1. **НЕСКОЛЬКО ТЕМБРОВ/ОКТАВ НА ПАЛЬЦЕ УМНОЖАЮТ ГОЛОСА.** Сегодня касающийся палец = ОДИН голос
   (`soloKey(key,finger)` → один владелец → один голос; при «Пальцев в руке» = 1 — ровно один голос на
   щипок). Голос звучит ОДНИМ тембром за раз (`leadVoiceBank` переключает банки гейтом, `v.ins` один),
   значит два тембра — это ДВА голоса, а не один с двумя банками. Два тембра × две октавы = ЧЕТЫРЕ голоса
   на щипок; четыре пальца — до ШЕСТНАДЦАТИ при пуле `LEAD_POOL_N=12`. С Карплусом–Стронгом теснее:
   `leadCap` не даёт KS-запросу растить пул сверх `LEAD_POOL_KS=8` голосов ВСЕГО — дальше КРАЖА, а
   KS-голос и так самое тяжёлое, что делает приложение (ворклет на голос, у Ситара ещё 5 полосовых).
   Аккорды умножают иначе, но тоже: один щипок — уже 3–5 голосов из `CHORD_POOL_N=24`. **Бас МОНО по
   построению** (`bassOwner`, пул «моно на слой») — «две октавы на бас-пальце» ломает сам принцип баса, а
   не только бюджет. **Потолок надо СПРОЕКТИРОВАТЬ** (на палец? на руку? какой голос отдавать первым?),
   а не обнаружить кражей на слух.
   Попутно: ключ владельца обязан получить новое измерение (тембр/октава внутри пальца) — и расти ему
   в ОДНОМ месте, `soloKey`, где уже живёт палец. Событие, похоже, выдержит без нового формата (у ноты
   уже есть `a.inst`, `oct` и слот `a.v`) — проверить в плане, НЕ принимать на веру.
2. **ПОПАЛЬЦЕВЫЕ ФУНКЦИИ РАСТВОРЯЮТ «РУКУ-ЭФФЕКТОВ» КАК ПОНЯТИЕ** — а на нём стоят зоны, захват,
   отрисовка и весь конструктор. В рамке пользователя это обобщение, а не отмена, но КАЖДОЕ из мест ниже
   обязано принять «этот палец — эффекты, тот — ноты» на ОДНОЙ руке:
   - **зона — ОДНА на руку, решается НА ЗАХВАТЕ** из функции руки (`handFnOf(key,role)==='fx'` → `'fx'`,
     две ветки тернара в `gestures.js` — сплит и single); положение руки при мультищипке читается с
     ГЛАВНОГО пальца (`S.order[0]`);
   - **захват и ведение:** `captureFx` и пофреймовая ветка `S.zone==='fx'` — параметры ведутся ТОЛЬКО
     внутри fx-зоны;
   - **адрес параметра** читает `finger` лишь при `hand==='fx'` (шапка адреса, `state.js`): у играющей
     руки палец сегодня не выбирается вовсе;
   - **показ:** цвет кисти (`handFnOf==='fx'` в `draw.js`), `fxActiveFinger` (ищет руку в fx-зоне);
   - **предикаты:** `roleHasFx` (гейт секции «Действия пальцев» и подсказки `fx.noHand`), `playsNotes`
     (рука ЛИБО играет, ЛИБО нет), `soloNoteHand`, `fxFreeFinger` (свободные пальцы — это пальцы fx-руки);
   - **старшинство** «сперва функция руки, потом подписка пальца» (шапка `ACTIONS`) — в модели «палец»
     двух ступеней больше нет.
   ⚠️ **ОГРАНИЧЕНИЕ, КОТОРОЕ ОБОБЩЕНИЕ НЕ СНИМАЕТ: у руки ОДНО ПОЛОЖЕНИЕ.** Все её одновременно зажатые
   пальцы читают одни X/Y/Z. Палец-ноты берёт из Y высоту, fx-палец — параметр: зажми их вместе, и одно
   движение поведёт ОБА. Смешанная рука связна, пока из одновременно зажатых пальцев НЕПРЕРЫВНО ведётся
   положением не более одного, а прочие ЗАМОРОЖЕНЫ на захвате (удержание, защёлка) или сработали ФРОНТОМ
   (дискретное действие). Пример пользователя проходит именно поэтому: защёлкнутый мизинец замер на
   захвате, дальше Y ведёт только терменвокс. Меню обязано это правило либо охранять, либо честно
   показывать сцепку (как «×N» показывает общий адрес) — решить в плане.
3. **ЦЕПЬ У ТЕМБРА — ЧЕТВЁРТАЯ СМЕНА ВЛАДЕЛЬЦА ЭФФЕКТОВ** (рука → роль → тембр). Экземпляры сегодня
   ключуются парой (роль, эффект) — `FX_INST[role][fxId]` — и НЕ РАЗБИРАЮТСЯ никогда (решение 3.5.1). По
   тембру владельцев становится 23 у одного соло (`LEAD_INSTR`), плюс тембры аккордов и баса, — поэтому
   ленивое создание ТОЛЬКО для звучащих тембров из оптимизации становится ОБЯЗАТЕЛЬНЫМ, а вместе с ним
   возвращается вопрос разборки, который 3.5.1 закрыл словом «никогда».
   ⚠️ Смягчение, найденное при сверке: ПРИЦЕПКИ (вибрато, тремоло, посыл в делей, драйв, подмес реверба)
   с 3.7.1 уже ПЕР-ГОЛОСОВЫЕ, а голос знает свой тембр, — для них «цепь у тембра» почти даром. Цена
   ложится на СЕТИ (FDN реверба, линия делея) — ровно на территорию Пласта 3.8.

**⚠️ ЧЕТВЁРТАЯ ЦЕНА — НАЙДЕНА ПРИ СВЕРКЕ, в замысле не звучала: У ПАЛЬЦА УЖЕ ЕСТЬ ВЫСОТНЫЙ СМЫСЛ.**
В узких рядах палец = РЕГИСТР (`S.oct` = номер пальца: указательный I … мизинец IV); в прямоугольниках
палец = НОТА ВНУТРИ ПРЯМОУГОЛЬНИКА (слот `g = прямоуг·k + палец` → `rectNoteAt`). «Октавы на пальце»
сталкиваются с первым, «свой тембр на пальце» — со вторым (перенастроил мизинец — а в прямоугольнике он
ещё и последняя нота блока). Значит в модели «палец» высотный смысл пальца — сам одна из настраиваемых
граней (или смещение поверх неё), а не данность. Пока это не решено, «две октавы на безымянном» не имеют
однозначного смысла.

**МЕНЮ: ОДИН ЭКРАН ВМЕСТО ТРЁХ.** Сегодня один и тот же предмет — палец — настраивается на ТРЁХ разных
языках: «Функции рук» (`renderHandFn`: чем РУКА является), «Действия пальцев» (`renderHandActs`: что палец
делает ДИСКРЕТНО) и конструктор (`renderFxCtl`: что палец ВЕДЁТ). Пользователь хочет ОДНО место с
ПОСТЕПЕННЫМ РАСКРЫТИЕМ:
- **простой уровень** выглядит как сегодня: выбрал роль, задал функции рук, добавил эффекты;
- **подробный уровень** раскрывает ПАЛЕЦ и настраивает его целиком — что играет, как, чем управляет,
  какие действия.
- ⚠️ **Объединяется ЭКРАН, не обязательно ДАННЫЕ.** Доводы, по которым `handActs` — отдельная таблица,
  а не запись в цепи (у действия нет параметра; сломались бы «одна запись на fxId», обход `fxSnapshot`,
  пер-голосовая прицепка; ⛔ дискретное действие не смеет попасть в событие), от единого экрана НЕ
  слабеют. Экран «палец» — ПРЕДСТАВЛЕНИЕ, собираемое по адресу из `handFn` + `handActs` + цепей, так же
  как карта «адрес → параметры» для чипа «×N» ВЫВОДИТСЯ на каждую отрисовку (3.4.4), без обратного
  индекса в состоянии.
- ⚠️ **Решение 3.4 «меню ОТ ПАРАМЕТРА» этим НЕ отменяется.** Подробный уровень — вид ОТ АДРЕСА (от
  пальца), а правка параметра остаётся одной операцией в одной строке. Это тот самый вид «от адреса», чьё
  единственное преимущество («видно, что едет вместе») 3.4 признал, — только выведенный, а не хранимый.
- ⬜ **ОТКРЫТО: нужен ли ВИЗУАЛЬНЫЙ элемент** (схема кисти, пальцы — кнопки?), чтобы экран оставался
  читаемым. Четыре пальца × пять граней × (после 3.6) одиночный/двойной щипок — это уже не список, и
  приёма 3.4 (нативный `<select>` с `<optgroup>`) может не хватить.
- ⬜ **ОТКРЫТО: что показывает простой уровень, когда пальцы РАЗОШЛИСЬ?** Рамка пользователя («рука =
  все четыре пальца») читается как РАЗВЁРТКА: контрол руки пишет в четыре пальца, а значение руки
  ВЫВОДИТСЯ (все четыре равны — показываем его, иначе «по пальцам»). Альтернатива — значение руки плюс
  переопределения пальцев — хранит меньше, но двусмысленна (правка руки затирает пальцы или нет?).

**⚠️ ЖЁСТКОЕ РАЗЛИЧЕНИЕ, КОТОРОЕ ДИЗАЙН ОБЯЗАН СОХРАНИТЬ (его выделил пользователь).** Одни действия
срабатывают НА СМЫКАНИИ (щипок выбирает тип аккорда; команда лупера), другие требуют УДЕРЖАТЬ-И-ВЕСТИ
(параметр едет от точки захвата). Меню пальца обязано выражать оба рода, не смешивая их. В коде различие
уже есть:
- **фронт** — дискретное действие исполняется в блоке ЗАХВАТА (`false→true`), единичное срабатывание ПО
  ПОСТРОЕНИЮ (`fireLooperCmd`, правило #21);
- **ведение** — `captureFx` запоминает базу на захвате, пофреймовый цикл ведёт от неё, на отпускании латч.
- **Таблица `ACTIONS` — ЗАРОДЫШ общей модели, а не разовая затычка для палитры.** `region` (где должен
  оказаться щипок) и `exclusive` (съедает ли действие палец) — это ровно те два вопроса, которые общая
  модель задаст КАЖДОМУ фронтовому действию. Палитра (`exclusive:false`, `region:'palette'`) уже
  показывает, как оба рода мирно живут на ОДНОМ пальце: внутри палитры палец выбирает тип, вне её —
  ведёт свои параметры, и разводит их ПОЛОЖЕНИЕ, без дополнительного правила. У команд лупера было бы
  `exclusive:true`.
- **Третий род, о котором легко забыть, — ИГРА НОТ.** Это тоже удержание-и-ведение (Y — высота, X —
  громкость), только ведёт оно ГОЛОС, а не параметр. Поэтому «ноты» и «эффекты» на одном пальце — не два
  рода, а два ПОТРЕБИТЕЛЯ одного ведения, и конфликтуют они по ОСЯМ (см. «бюджет осей» выше и
  ограничение «одно положение» в цене 2), а не по роду.

**ПОРЯДОК — РЕШЁН ПОЛЬЗОВАТЕЛЕМ: СНАЧАЛА ДОДЕЛАТЬ ТЕКУЩИЙ ПЛАН** — цель (б) (остались ударные), цель (в),
3.8, 3.9, 3.6 — и только потом модель «палец». Это не откладывание, а подготовка почвы:
- **(б)** доводит руку-эффектов до всех ролей: ОБЩИЙ случай «рука» становится полным ПРЕЖДЕ, чем его
  обобщать до пальца. ⚠️ **Внутри этого шага — решение пользователя: б.4 (ударные) ИДЁТ ПЕРЕД целью (в).**
  Сначала механизм существует у ВСЕХ ЧЕТЫРЁХ ролей, потом цепи наполняются эффектами везде ОДНИМ заходом, —
  а не (в) на три роли и возврат к ударным отдельно (что значило бы проходить по тем же местам дважды).
- **(в)** даёт меню пальца НАСТОЯЩИЕ эффекты: сегодня прочим ролям доступен один реверб, и попальцевая
  настройка аккордов/баса/ударных настраивала бы один эффект.
- **3.8** — репетиция цены №3: пер-слойные сети впервые заставят решить потолок экземпляров и их разборку
  без обрыва хвоста; цепь у тембра возьмёт готовый ответ, а не будет искать его сама.
- **3.9** делает громкость ОБЫЧНЫМ параметром — иначе грань «чем управляет» унаследует тот самый особый
  случай, от которого 3.9 избавляет, только размноженный на четыре пальца.
- **3.6** определяет, что такое «палец» как адрес (одиночный/двойной щипок — восемь «пальцев»): модель,
  спроектированная раньше, пришлось бы перекраивать под удвоение.
- Итог: попальцевая настройка ПОЧТИ ПУСТОЙ модели была бы настройкой НИЧЕГО.

**⛔ ПЕРИЛА НА ПРОМЕЖУТОК — чтобы решения до этой модели ей не противоречили.**
- Новое, что по природе может жить на пальце, ключуется АДРЕСОМ `{рука, палец}` (общий словарь `handActs`
  и параметров), а не одной рукой. `hand` выписывается, даже когда тривиально истинен (закон 2.6 и «д»).
- Новый код задаёт САМЫЙ УЗКИЙ вопрос: «ведёт ли этот ПАЛЕЦ параметр» (ответ — в адресе параметра), а не
  «на эффектах ли РУКА» (`S.zone==='fx'`/`roleHasFx`), там где узкого достаточно. Образец — `b595ed1`.
- `handActs` в цепь НЕ сливать — доводы в шапке `state.js` в силе; объединение — дело экрана (см. выше).
- Новое измерение владельца голоса — только через `soloKey`; второго построителя ключей не заводить.
- В плане 3.6 определить двойной щипок как расширение АДРЕСА (`{рука, палец, щипок:1|2}`) СРАЗУ в обеих
  таблицах, как обещано в шапке `handActs`, — модель «палец» его унаследует, а не будет вводить заново.
- `fxFreeFinger` научить видеть `handActs` ДО первого действия с `exclusive:true` (⛔ уже записано в `state.js`).
- Пока секций три — одни и те же подписи и порядок пальцев (`FINGER_KEYS`) во всех: при слиянии меньше
  переучивать.

**ВОПРОСЫ МОДЕЛИ: ДВА ЗАКРЫТЫ (Пласт 2.6), ОДИН ОТКРЫТ (Пласт 3.6).**
- ✅ **Одно представление или два? — ДВА, но не там, где казалось.** Различитель проходит между
  «ведётся» и «зафиксирован», а РУКА — атрибут ведения (`hand:'fx'|'play'`). Полная форма и доводы —
  в записи Пласта 2.6 выше.
- ✅ **Как меню сопоставляет 0..100 с настоящим min/max? — НИКАК, и в этом суть.** Ручка 0..100 —
  это просто `v01*100` для показа; в звук она уходит ЧЕРЕЗ ТОТ ЖЕ `setNorm`, что и палец. `min`/`max`/
  `curve` живут ИСКЛЮЧИТЕЛЬНО внутри `setNorm`/`fxDenorm` (`audio.js`), поэтому второго представления
  диапазона не возникает в принципе, а лог-шкала реверба (секунды и герцы) соблюдается сама собой.
  Кламп сеттера (`setRevDecay`/`setRevTone`) остаётся властью, как и был.
- ⬜ **ОТКРЫТ (решать в плане Пласта 3.6): двойной щипок — «следующие три параметра» против «другой
  эффект» — это выбор В МЕНЮ НА КАЖДЫЙ ПАЛЕЦ или ГЛОБАЛЬНЫЙ режим?** Разные ответы дают разную
  раскладку и разную нагрузку на меню.

**ГЛАВНОЕ ОТКРЫТИЕ ПЛАСТА 1 — ради него и планировали.** Абстракция вышла **ПОСЫЛ-ОБРАЗНОЙ, а не
универсальной**, и это надо знать ДО того, как на неё ляжет конструктор:
- **Виды крепления РАЗНЫЕ, и система обязана их различать, а не делать вид, что эффект — однородный
  кубик.** ПОСЫЛ (реверб, делей: свои `in`/`out`, влажный сигнал параллельно сухому) · ВСТАВКА (тремоло:
  `in===out`, врезается в разрыв цепи, «прицепить к шине» для неё — неверный глагол) · МОДУЛЯЦИЯ
  (вибрато: аудио-входа/выхода НЕТ ВООБЩЕ, LFO в `.detune` из `mkOsc`) · ПО ГОЛОСАМ (драйв: N
  экземпляров, ДО огибающей — `env·tanh(x) ≠ tanh(env·x)`, вынос на шину изменил бы звук ОДНОЙ ноты).
  Ждать, что Пласт 2 опишет все пять одним видом крепления, — значит переписывать его в Пласте 3.
- **ПОДМЕС (mix / wet) — свойство ПРИЦЕПКИ, а не эффекта.** У реверба ДВА посыла с РАЗНОЙ величиной:
  соло `p.rev*0.85` (живой, от глубины руки) и аккорды `revCh=0.12` (фиксированный). Один узел подмеса
  на выходе эффекта физически не выразил бы обе величины сразу — значит подмес обязан жить НА ПРИЦЕПКЕ,
  по одному на источник. Практический вывод для Пласта 2: палец, назначенный на «количество реверба»,
  крутит ПОСЫЛ СОЛО, а не реверб; крути он общий подмес — поехал бы и хвост аккордов. У делея подмес
  сегодня стоит наоборот, на ВЫХОДЕ (`dlyWet`) — источник один, поэтому можно; при переезде делея это
  расхождение придётся решить явно, а не «унифицировать» молча.
- Отсюда же: в Пласте 2 у реверба было СВОИХ параметров **ДВА** (длина, окраска), а не три — подмес лишь
  КАЗАЛСЯ третьим. ⚠️ В Пласте 3.1 их стало ТРИ, и это НЕ отмена наблюдения, а его следствие: подмес
  въехал как параметр, который пишет **именно ПОСЫЛ**, а не выход модуля.
- ✅ **ЧЕМ ЭТО КОНЧИЛОСЬ (Пласт 3.5) — наблюдение не отменено, а ИСЧЕРПАНО.** Узел посыла стал
  **СОБСТВЕННОСТЬЮ ЭКЗЕМПЛЯРА** (`inst.send`), а экземпляр — своим у каждой роли. «Одна прицепка — один
  узел — одна величина» перестало быть соглашением, которое можно нарушить, и стало свойством
  конструкции. Двух посылов в один реверб больше нет вовсе: есть две роли, две комнаты, два посыла.
  ⚠️ Оговорка про **делей остаётся в силе**: у него подмес до сих пор на ВЫХОДЕ (`dlyWet`), потому что
  источник один. При его переезде (цель (в)) это расхождение придётся решить ЯВНО, а не «унифицировать»
  молча — ровно как предупреждал Пласт 1.

**ХВОСТЫ ПЛАСТА 2 — ВСЕ ТРИ ЗАКРЫТЫ.** Оставлены записью: каждый из них стоил отдельной диагностики, и
причины стоит помнить — они повторимы.
- **✅ (а) РЕВЕРБА НЕ БЫЛО В СПИСКЕ меню на чистой сессии — ПОДТВЕРЖДЕНО И ИСПРАВЛЕНО.** Список вариантов
  строится из `FX_MODULES`, а тот заполняется ВНУТРИ `initAudio` (по клику ▶); строки же меню собирались
  на инициализации `ui` — РАНЬШЕ, — и `startApp` после `initAudio` ничего не перерисовывал. Панель при
  этом открывается только ПОСЛЕ старта (⚙ живёт в баре, а бар поднимает `startApp`), поэтому человек
  ГАРАНТИРОВАННО видел строки, построенные при ПУСТОМ реестре. ⚠️ Пласт 3.1 превратил это из спящего
  хвоста в ПЕРВОЕ ЖЕ ВПЕЧАТЛЕНИЕ: реверб стал эффектом указательного ПО УМОЛЧАНИЮ, `sel.value='reverb'`
  не находил опции, `selectedIndex` уходил в −1 — и меню показывало ПУСТОТУ на пальце, который звучит.
  Хуже: выбор любого другого пункта молча снимал реверб без пути назад.
- **✅ (б) РАССИНХРОН МЕНЮ И СОСТОЯНИЯ ПОСЛЕ ПЕРЕЗАГРУЗКИ — ИСПРАВЛЕН.** Код был не виноват: `renderFxCtl`
  всегда рисует ИЗ ДАННЫХ и никогда не читает DOM как истину, `fxLayout` не персистится — значит значение
  в `<select>` перетирал БРАУЗЕР (восстановление состояния форм) уже ПОСЛЕ отрисовки, а переутвердить
  данные было некому.
- **ОБА закрыты ОДНОЙ правкой:** `showScale` теперь зовёт `renderHandFn()` при ОТКРЫТИИ панели — зеркало
  того, что `showLoop` давно делает с `refreshMetreCtl()` («панель могла простоять закрытой»). Обе
  динамические секции пересобираются из `fxLayout` на каждое открытие: к этому моменту `initAudio` уже
  отработал (значит реверб в списке есть), и что бы браузер ни восстановил — оно отбрасывается. Зовём
  `renderHandFn`, а не `renderFxCtl`, потому что он сам дёргает второй: одна точка входа на обе секции.
  Плюс `autocomplete='off'` теперь на ВСЕХ динамических контролах панели (в 2.4 он стоял только у
  `renderFxCtl`).
- **✅ (в) ВИСЯЧАЯ ССЫЛКА — ЗАКРЫТА (в слайсе 3.2.2).** Зацепка обучения читала `tutorTap('fx',{k:A.k})`,
  но поле `k` было снято с `S.adj` ещё в слайсе 2.2 → уезжало `undefined`. Стало `{k:A.fxId}`; `A.k` в
  `src/` больше не встречается (проверено grep). Безвредна она была всегда (обучение поле не смотрит,
  ему хватает факта «эффект двинулся») — но ссылка на удалённое поле не должна жить в коде.

**ХВОСТЫ ПЛАСТОВ 3.4/3.5 — НИ ОДИН НЕ БЛОКИРУЕТ, но записаны, чтобы не приняли за баги.**
- 🐞 **НАСТОЯЩИЙ БАГ, НАЙДЕН ПРИ ЧТЕНИИ `recorder.js` ДЛЯ ПЛАНА 3.7. НЕ ЗАДАЧА 3.7 — отдельная правка
  в одну строку.** Остановка овердаба зовёт `recLeadOff()` **БЕЗ АРГУМЕНТА**, а функция ждёт ключ
  владельца: `recLead.get(undefined)` не находит ничего. Следствия: открытые в этот момент соло-ноты
  НЕ ЗАКРЫВАЮТСЯ — их `leadOff` в слой не пишется (в петле нота повиснет до заворота, где её снимет
  `releaseLoopLayersAt`), а записи `recLead`/`vSlots` этого владельца ТЕКУТ до ближайшего
  `recLeadReset` (паника/очистка/заворот). Появилось вместе с полифонией соло: до неё состояние записи
  было ОДНО на всех, и вызов без аргумента был осмысленным. Чинится обходом всех открытых владельцев
  вместо одного вызова. ⚠️ Записано отдельно от хвостов конструктора намеренно: это не «шероховатость
  по замыслу», а ошибка.
- ⬜ **ЯРЛЫК У КИСТИ fx-РУКИ показывает только СТАРЫЕ СКАЛЯРНЫЕ эффекты.** `drawHandsPhone` подписывает
  щипнувшую fx-руку («Делей 42%»), разрешая эффект через `FX_META`, — а модулей (реверб) там нет и быть
  не должно. Поэтому на пальце с ревербом подпись у кисти НЕ появляется, хотя столбики его показывают.
  Так было и ДО пласта (в 3.4.2 лишь уточнено: теперь берётся ПЕРВЫЙ скалярный из захваченных пальцем,
  раз их может быть несколько), и пользователь сознательно отложил это на потом.
  **Как чинить, когда дойдут руки:** `S.adj.ent` уже несёт ВСЕ параметры, привязанные к зажатому пальцу,
  — и скалярные, и модульные, каждый со своим `fxId` и живым значением. Ярлык обобщается из него, без
  обращения к `FX_META`; попутно решить, что показывать, когда палец ведёт НЕСКОЛЬКО эффектов сразу
  (сегодня показался бы один). Это `draw.js`, звука не касается.
- ✅ **ПОДСКАЗКА `fx.noHand` — ЗАКРЫТА ЦЕЛЬЮ (б), БЕЗ ЕДИНОЙ ПРАВКИ ТЕКСТА.** Она была странной у
  не-соло ролей: «сейчас ни одна рука не назначена на Эффекты…» писалось для соло, где руку МОЖНО
  назначить, а у аккордов/баса/ударных `roleHasFx` был ложен ПО УСТРОЙСТВУ — подсказка висела там всегда
  и намекала на действие, которого нет. Ровно как и предсказывала эта запись, чинить её отдельно не
  пришлось: `roleHasFx` стал ролевым (б.1), и текст снова стал осмысленным сам собой.
  ⬜ Остаётся у УДАРНЫХ — до б.4.
- ⬜ **ПОЛЗУНОК BPM — ТА ЖЕ ЛОВУШКА, что убила ползунок фиксированного значения.** В панели лупера темп
  правится `<input type=range>`, и он живёт в такой же тесной строке. Диагноз тот раз был поставлен
  НЕВЕРНО («виноват `touch-action:none`») и потом исправлен: ползунок **схлопывался в НУЛЕВУЮ ШИРИНУ**,
  а касание по ползунку нулевой ширины читается как его ЛЕВЫЙ КРАЙ, то есть минимум, — обработчик
  честно писал этот минимум в состояние. **Лечится тем же, чем вылечили тот:** заменить на числовое
  поле со ступеньками `−[поле]＋` (форма `.seg`, как у тактов лупера и эталона A4). ⚠️ Не гоняться за
  `touch-action` — дело в ШИРИНЕ.
- ⬜ **ИМПУЛЬСНЫЕ ОТКЛИКИ КАБИНЕТОВ — как БУДУЩИЙ ТИП ЭФФЕКТА (не как возврат реверба).** `ConvolverNode`
  в приложении уже был — под реверб, и был ОТТУДА УБРАН осознанно: у свёртки хвост ЗАПЕЧЁН в буфер, и
  живьём менялось только КОЛИЧЕСТВО посыла, а длина и окраска стояли намертво — для комнаты, которой
  управляет рука, это неприемлемо. **Для КАБИНЕТА всё наоборот:** кабинет — вещь СТАТИЧНАЯ по природе
  (динамик и корпус не «удлиняются»), поэтому запечённый отклик тут не ограничение, а точность, и
  свёртка даёт то, чего фильтрами честно не получить. Ложится в модель как обычный модуль
  (`{in,out,params}`, крепление — вставка или посыл), параметрами были бы выбор отклика и подмес.
  ⚠️ Не путать с целью (г): там мы пишем DSP сами, здесь — данные (файлы откликов), и это отдельный
  разговор про вес загрузки и лицензии на записи.

### РАЗДЕЛИТЬ ПАНЕЛЬ: «ЗВУКОРЯД» ↔ «ЗВУК И УПРАВЛЕНИЕ» (предложение пользователя) — ⬜ НЕ СДЕЛАНО

**ПРОБЛЕМА — СЕМАНТИЧЕСКАЯ, А НЕ КОСМЕТИЧЕСКАЯ.** `#panelScale` — одна длинная прокрутка с заголовком
«Звукоряд», но ПОЛОВИНА её к звукоряду отношения не имеет: тембры, функции рук и целиком конструктор
эффектов живут там же. Панель буквально называется «Звукоряд», пока в ней настраивают реверб. Секции
уже размечены разделителями `.psep` — не хватает ровно РАЗРЕЗА по ним.

**РАЗРЕЗ (решение пользователя) — по вопросу, на который отвечает настройка:**
- **ОСТАЁТСЯ в «Звукоряде» — КАКИЕ НОТЫ ЕСТЬ И ГДЕ ОНИ ЛЕЖАТ:** строй · лад · тоника · эталон A4 ·
  раскладка нот · пальцев в руке · трезвучия/септаккорды.
  ⚠️ **Раскладка и пальцы остаются здесь НАМЕРЕННО, и довод пользователя стоит записать:** раскладка
  СЛЕДУЕТ ЗА ЛАДОМ — вариант «По ладу» разрешается ПО КАЖДОМУ ЛАДУ отдельно (`scales.rectGrid` читает
  свойство лада), да и сама геометрия (сколько нот в прямоугольнике, сколько октав влезло) —
  арифметика лада. Значит её дом рядом с ладом, а не рядом с руками, хотя пальцами её и играют.
- **ПЕРЕЕЗЖАЕТ во вторую панель/вкладку — КАК ЭТО ЗВУЧИТ И ЧЕМ ЭТИМ УПРАВЛЯЮТ:** тембры
  (соло/аккорд/бас/ударные) · функции рук · конструктор эффектов.

**ПОЧЕМУ ЭТО ДЁШЕВО — ОБРАЗЕЦ УЖЕ В КОДЕ.** `#panelLoop` — это УЖЕ вторая панель: свой `.phead` со
своим «Свернуть ✕», свой пункт в баре, а `showScale`/`showLoop` уже ЗАКРЫВАЮТ ДРУГ ДРУГА при открытии
(они лежат в одном месте экрана и иначе перекрылись бы). Третья панель ПОВТОРЯЕТ образец, а не
изобретает механизм: разметка режется по существующим `.psep`, динамические секции (`renderHandFn`,
`renderFxCtl`) переезжают целиком вместе со своими контейнерами, точка пересборки при открытии — та же,
что у `showScale` сегодня. **Похоже на ОДИН СЛАЙС, а не на пласт.**
⚠️ Что проверить при исполнении, чтобы не разъехалось: взаимное закрытие теперь у ТРЁХ панелей (правило
«открыта одна» становится общим, а не парным); `syncTutorBarPos`/`hushStrip` считают положение полосы
обучения по ОТКРЫТОЙ панели — их селектор `#panelScale.on, #panelLoop.on` придётся дополнить третьей;
и `showScale` сегодня несёт пересборку динамических секций (реестр модулей заполняется в `initAudio`,
браузер восстанавливает формы после отрисовки) — эта обязанность обязана уехать ВМЕСТЕ с секциями.

**⛳ ЭТО ТРЕТЬЯ ИЗ ТРЁХ ИДЕЙ, ТОЛКАЮЩИХ В ОДНУ СТОРОНУ — К ЕДИНОЙ МОДЕЛИ ВМЕСТО НАКОПЛЕННЫХ ОСОБЫХ
СЛУЧАЕВ.** Их стоит держать как ОДНУ ЛИНИЮ МЫСЛИ, а не как три разрозненные задачи:
1. **Громкость как несъёмный параметр цепи (Пласт 3.9)** — убирает отдельную строку громкости и правило
   «X отдан эффекту ⇒ громкость фиксируется»: остаются просто параметры на адресах.
2. **✅ Яркость аккордов как эффект цепи (цель «а») — СДЕЛАНА.** Убрала ПОСЛЕДНИЙ параметр, вшитый в
   роль мимо цепи: глубина аккордовой руки стала обычным адресом обычного параметра.
   ⛳ И дала выплату, которой в замысле не было: показ столбиков перестал спрашивать про РУКУ (`b595ed1`).
3. **Этот разрез панели** — убирает смысловую путаницу в меню: «какие ноты» и «как звучит» перестают
   жить под одним заголовком.
Первые две наводят порядок в МОДЕЛИ, третья — в её ОТРАЖЕНИИ. Сделанные вместе, они дают то, к чему шёл
весь конструктор: одно правило вместо оговорок. ⚠️ Порядок между ними не критичен, но разрез панели
ЛОГИЧНО ДЕЛАТЬ ПОСЛЕДНИМ — он раскладывает по полкам то, что к тому моменту уже перестанет быть особым
случаем (иначе придётся перекладывать дважды).

### СТРОИ — по сути ПОЛНЫ, кроме турецких макамов
Карта строёв почти закрыта: равномерные EDO, арабские макамы (24-TET), микротоника (19/31-TET),
исторические темперации от Пифагора до велл-темпераций, индийские шрути+раги, гамелан, японские, Партч,
неоктавные (Болен–Пирс/Карлос). Последнее крупное белое пятно — **турецкие макамы** (целый тон на 9 комм,
обычно через 53-EDO; звучат ИНАЧЕ арабских тёзок). После них новые СЕМЕЙСТВА строёв не нужны — дальше
углубление (тембры, кураторский джем). Детали и «не брать без источников» — BACKLOG §3.25.

### СЛЕДУЮЩИЙ ШАГ — ПРОМО-РОЛИК
**Приложение опубликовано и умеет объяснить себя само (обучение) — но в ДВИЖЕНИИ его никто не видел.**
Ссылкой поделиться нечем: текстом «жестовый синтезатор с 75 строями» никого не убедить, а весь смысл —
в том, КАК рука ведёт звук. Нужна короткая запись игры. Инструменты для этого УЖЕ в приложении: `🎥`
пишет холст+звук в один файл (HTML-хром в кадр не попадает), `⛶` убирает полосы браузера. Это работа
про доведение до людей, а не про код — новых фич не требует.

### ПОТОМ — ЗАМЕРИТЬ ЗАДЕРЖКУ ВХОДА
Путь рука → звук ни разу не мерили ЧИСЛОМ. Известный корень — **синхронный MediaPipe в главном потоке**
(там же три пути синтеза); планировщик с опережением (`SCHED_AHEAD≈300мс`) его МАСКИРУЕТ для слоёв, но
живой игре не помогает и корень не убирает. Правило проекта: **сначала измерить, потом строить** —
именно так в прошлый раз нашли GPU-делегат. Пока нет числа, любые «оптимизации» — угадывание.

### ПОТОМ — ИСПАНСКИЙ И НЕМЕЦКИЙ
Механизм готов и рассчитан на четыре языка, формы имён (`{default, ru}` / полный объект) уже проставлены,
`LANGS` дописать — одна строка. **Не включаем, пока музыкальную терминологию некому проверить**: неверный
термин носитель прочтёт как ошибку приложения, а не как неточность перевода. Ждём человека, не времени.

### Мельче
- **Глиссандо-в-луп:** терменвокс сейчас пишет ближайшую ступень (живой звук непрерывен, запись
  квантована). Чтобы петля повторяла бенды — нужен непрерывный формат события (или дорожка
  контроллера высоты). Отдельная задача, ядро лупера.
- **Раги/ситар — звукоряды СДЕЛАНЫ** (традиция `indian`, 22 шрути + 10 раг как цент-лады). Осталась
  возможная ТЕМБРАЛЬНАЯ сторона (ситар: резонансные струны, джавари, бурдон-тампура) — отдельная задача
  в `LEAD_INSTR`, если решим, что нужна. Микротоника раг уже играется.
- **Подсказка аккорда лупа поверх поля соло** — мелкая визуальная задача.
- **Сплит поддерживает РОВНО две половины.** Трёх- и более-ролевых раскладок нет (и `phoneHalves`
  под это не рассчитан). Если понадобится — отдельная задача.
- **Прицел revDisp под сплитом — РЕШЁН** (нотная рука соло-половины; старый баг «висит на последнем
  значении» тоже закрыт пер-кадровым сбросом). Больше не задача.
- **Две косметики (НЕ чинить, в BACKLOG как пункты):** (а) у части цент-ладов вводящий `tag`
  (мезотон/натуральный/индийские — `tag:'penta'`), потому что теги для них инертны; (б) подписи корней
  аккордов через `rootName()` дают 12-TET имена нот у любого `edo:12` лада — у цент-ладов это
  приблизительный якорь, реальная высота видна в Гц.

---

## КРУПНОЕ В БЭКЛОГЕ (не срочно, см. BACKLOG.md)

- localStorage настроек (§1: `scaleIdx`/`tonic`/тембры/`aRef`/`handFn`/`loop.metre` — сейчас каждый запуск
  сбрасывается; язык и прогресс уроков УЖЕ персистятся через `i18n.store`, механизм готов).
- Service Worker **с кэшем модели** для офлайна — SW уже зарегистрирован, но НАМЕРЕННО пустой (он только
  ради устанавливаемости). Кэш добавляется в него, если решим, что офлайн нужен.
- ~~Запись видео+звука~~ — **СДЕЛАНО** (`clip.js`: клип 🎥 и аудио 🎙, взаимоисключаемы).
- ~~Размеры кроме 4/4~~ — **СДЕЛАНО** (`loop.metre`, 2..16, акценты по группам; §3.23).
- Турецкие макамы + прочие строи (§3.25 — последнее белое пятно карты, потом только углубление).
- Микшер слоёв лупера (§3.13 — только если после гейн-стейджинга реально нужен баланс).
- Полиритмия — свой размер у слоя (§3.24, ПОСЛЕ переменного размера).
- ⛔ **ПРЕДПОСЫЛКА: стабильный идентификатор лада** (§1). У ладов НЕТ поля `id` (0 из 75), адрес — позиция
  в массиве, а слой петли морозит **ССЫЛКУ** на объект лада (`sc:CUR()`). В памяти это верно, но
  сериализовать нельзя → **сохранение петель и конструктор строёв (§3.34) заблокированы, пока id нет.**
  Не браться ни за то, ни за другое раньше.
- ⚠️ **Несогласованность двух половин кнопки «Подложка»** (§3.33 п.1): у джема гейт ПО СВОЙСТВАМ лада,
  у «только ударных» — только по РАЗМЕРУ. Итог: под рагой предлагается рок. Дефект, не пожелание.
- Заморозка тоники per-event для `fixedKey`-слоёв (полимодальность окраски; §3.26 — отложена осознанно).
- Свинг как ОТНОШЕНИЕ, а не сетка (§3.30 — `sub` уже есть, это предпосылка; полезный диапазон 55–60%
  сеткой не выражается, и лечь обязан СРАЗУ на генерацию + квантизацию + живую игру).
- Конструктор жестов (§3.29) — ПОСЛЕ того, как рука-выразительность устоится: он ляжет на ГОТОВЫЙ набор
  осей и параметров, а не будет строиться в пустоте.

---

## КЛЮЧЕВЫЕ УРОКИ

- Всегда сверяйся с РЕАЛЬНЫМ кодом (читай файлы) перед промтом. Память врёт.
- Диагностируй до постройки (латентность: измерили → нашли GPU-делегат как корень).
- Ухо главнее математики в звуке (центы построили и откатили — не услышал разницы;
  гейн-стейджинг заработал только после правки уровня голоса, не только шин).
- Правки на СВОЙСТВАХ лада (`noChords`, `typedChords`) переживают перестройки; правки
  на тегах/индексах — ломаются. Всегда свойство.
- Начинай маленьким, играй, расширяй. Не строй списки из воображения.
- «Можем» ≠ «нужно». По секторам пользователь настоял на 6, подтвердив попадание.
- После одобрения плана — явно «делай код, не останавливайся на плане».
- **Живость даёт СРЕДНИЙ СЛОЙ, а не число параметров.** Рука-выразительность: четыре входа + резонатор,
  пружина, инерция звучат живо; десять входов, присвоенных напрямую, звучали бы как пульт с ручками.
- **Разные каналы обязаны вести РАЗНЫЕ оси восприятия.** Три канала, толкавшие яркость, ощущались как
  ОДНА ось, поделённая на четыре способа — жалоба была именно такой, хотя формально каналов было четыре.
- **Численная устойчивость — не «оптимизация», а слышимый баг.** Пружина 2.5 Гц самовозбуждалась ниже
  ~13 fps и давала дрожь 5–8 Гц, которую искали в жестах. Лечится суб-шагом, а не подбором коэффициентов.
- **Покой обязан давать РОВНО ноль.** Без мёртвой зоны шум распознавания читается как игра, и инструмент
  «играет сам». Это несущая часть, а не полировка.
- **Отображаемое имя — НЕ идентификатор.** Локализация мгновенно ломает всё, что сопоставлялось по имени
  (`demo.js` по `s.name`, бакеты меню по подписи `grp`). Стабильный индекс или стабильный ключ.
- **CSS-специфичность ломает инварианты не хуже кода.** `#tutorBar.mini` нёс свой `display` и перебивал
  `#tutorBar{display:none}` — пустая полоса вылезала без всякого урока. Видимость должна решать РОВНО
  один класс.
- **Спрашивай про хвост состояния.** Почти все баги обучения — протечки из предыдущего урока (функции рук,
  джем, лад), а не ошибки в шагах.
- **Дефолт короткого поля бывает МОЛЧА неверным и заводит петлю.** У Web Audio `Q` фильтров lowpass/highpass
  задаётся в ДЕЦИБЕЛАХ, и дефолт `Q=1` — это не «нейтрально», а резонансный пик **+1.96 дБ** (|LP|≈1.25).
  Вне петли — подкраска; в петле реверба `0.886·1.25 = 1.11 > 1` → самовозбуждение свистом на ~3.9 кГц.
  Урок шире одного бага: **считать усиление петли, а не доверять слову «фильтр гасит»**. И проверять, что
  защита стоит на ТОЙ величине: кламп `gMax` ограничивал `g`, а разгон давал фильтр, — защита была не там.
  Лимитер аварию поймал, но лимитер — последний рубеж, а не гарантия схемы.
