# HANDOFF — снимок проекта Air Synth 3 для нового чата

Этот файл — передача контекста в новую сессию. Прочитать ВМЕСТЕ с
`CLAUDE.md`, `BACKLOG.md`, `UI-MAP.md` (все в корне проекта). `PLAN.md` и
`MENU-PLAN.md` — АРХИВ завершённых работ (рефакторинг и перестройка меню), их
читать только как историю, под текущий код они не переписаны и не должны быть.
Пользователь также пришлёт все файлы `src/` — это актуальный код, опирайся на него,
а НЕ на память.

> `CLAUDE.md` переписан под текущий код: убран `backing.js`, описан лупер, типы аккордов,
> свойства ладов, phone-режим как ведущий, rect-модель, октавный прямоугольник, Парч + аккорды,
> цент-аккорды, терменвокс, **неоктавный период (`period`/`periodOf`), 4 ветки `chordFreqs`,
> исторические темперации, порядковые ярлыки**. Ему можно доверять. Устаревшие имена, если
> встретишь: `backing.js`, `playRec`, `toggleRec`, `stopRec`, `recStart`, `sectHyst`,
> `TYPED_CH_VOL`, `drawChordSectors`, `RECT_OCT`.

---

## ЧТО СДЕЛАНО В ЭТОЙ СЕССИИ (неоктавные строи + исторические темперации, сверено с кодом)

Всё — data/ядро-правки на СВОЙСТВАХ ладов, гейтнуты узко, старые пути байт-в-байт целы.
`SCALES` теперь **0..60 (61 лад)**. Хвост индексов: 50 Болен–Пирс · 51/52/53 Карлос α/β/γ ·
54/55/56 патеты пелога Лима/Нем/Баранг · 57 Пифагоров · 58 Натуральный подвижный · 59 Натуральный
фиксированный · 60 Мезотон 1/4 коммы. (Предыдущая сессия: rect-модель, октавный прямоугольник,
Парч + аккорды, цент-аккорды, терменвокс — см. историю ниже, всё цело.)

1. **Период как свойство лада** (`period`, `periodOf(s)=s.period||2`). Зашитая октавная двойка
   обобщена до `P` в `leadFreq`/`bassFreq`/`chordFreqs`(равная ветка)/`centsOf` (`pc=1200*log2(P)`).
   Дефолт 2 ⇒ ВСЕ октавные лады байт-в-байт. Регистр `P^oct` сдвигает на период.
2. **Болен–Пирс** (SCALES[50], `trad:'nonoct'`): период 3 (тритава), 13 равных шагов, `tag:'bp'`.
3. **Аккорды BP** — `CHORD_FAM_SETS['bp']` (15 аккордов подгруппы 3.5.7: 3:5:7 мажор / 5:7:9 минор /
   характерные) + ВЫДЕЛЕННАЯ период-ветка `chordFreqs`, гейт `(P!==2 && ty)`: корень `P^(iv/edo)`,
   нота = корень·ratio.
4. **Карлос α/β/γ** (SCALES[51..53], `trad:'nonoct'`): делят чистую квинту 3:2 на 9/11/20 равных
   (`period:3/2`), `noChords`. Регистр сдвигает на КВИНТУ.
5. **Rect для гаммы** — счётчик `rectRows` РАЗВЯЗАН: `+1` (верхняя тоника) гейтится периодом
   (`(iv.length + (periodOf===2?1:0))/4`), у неоктавного лада его нет → гамма 20/4=5. Только гамма
   квалифицируется (α 9, β 11, BP 13 не делятся). Октавные лады байт-в-байт.
6. **Ярлыки/слова под период**: `gridNoteLbl` даёт порядковый `Т,2,3…` у неоктавных И у цент-ладов
   (имя ноты соврало бы о 12-TET-высоте); `regWord` — окт/тритава/рег.
7. **Патеты пелога** (SCALES[54..56], цент-лады, `noChords`): Лима/Нем СПЕЦИАЛЬНО одни ноты
   (различие функциональное), Баранг — другой набор.
8. **Полка «Исторические темперации»** (grp в Ладовой) + **Пифагоров** (SCALES[57], цент-лад,
   `typedChords:'pyth'` — 24 типа chrom12, но ПИФАГОРОВЫ отношения, терция 81/64 резкая).
9. **Натуральный ×2** — ПОДВИЖНЫЙ (SCALES[58], `typedChords:'nat'`, чистые 5-предельные отношения
   ОТ КОРНЯ, аккорд чист на любом корне) и ФИКСИРОВАННЫЙ (SCALES[59], `typedChords:'natfix'` +
   `gridChords:true`). Для фиксированного — НОВАЯ grid-ветка `chordFreqs`, гейт `(s.cents && ty &&
   s.gridChords)`, СТОИТ ПЕРВОЙ: `ty` = целые полутоновые смещения, нота берётся ИЗ cents-сетки →
   часть аккордов чистые, часть волк (историческая проблема фикс. чистой интонации).
10. **Мезотон 1/4 коммы** (SCALES[60], цент-лад, переиспользует `natfix`+`gridChords`): квинты
    сужены до 696.58¢ ради чистых терций 386.31¢, вся ошибка собрана в ОДНУ волк-квинту G#–Eb.
    Замыкает линию Пифагор → Натуральный → Мезотон → 12-TET.

**Порядок веток `chordFreqs` (ФИКСИРОВАН, не переставлять):** (1) `s.cents && ty && s.gridChords`
→ из сетки; (2) `s.cents && ty` → чистое отношение от корня; (3) `periodOf!==2 && ty` → период-
корень·ratio; (4) иначе `chordSteps` (целочисленный путь, chrom12/edo19/edo31 и все нетипизированные —
байт-в-байт). Порядок решает, кто куда попадёт.

---

## КАК МЫ РАБОТАЕМ (метод — не менять)

- Пользователь запускает **Claude Code в VS Code** (отдельно от этого чата).
- Ассистент (ты) — стратег: пишешь английские промты, пользователь вставляет их
  в Claude Code, приносит план/дифф обратно, ты сверяешь с РЕАЛЬНЫМ кодом и правишь.
- **Правило: после одобрения плана следующий промт должен явно говорить
  «implement now, don't stop at plan».** Иначе Claude Code останавливается на плане
  (мы его так приучили), и пользователь думает, что сделано, а кода нет. Теряли круги.
- **Один шаг = одна правка = одна проверка в браузере = один коммит.** Не сваливать
  несколько задач в один заход — иначе при поломке не понять, что виновато.
  (Это правило про КОД. Документы можно править без браузерной проверки.)
- Коммитит ТОЛЬКО пользователь. Ты git не запускаешь.
- **ВСЕГДА сверяйся с реальным кодом через чтение файлов перед промтом.** Память врёт;
  главная ошибка прошлых сессий — смотреть на старый файл вместо текущего `src/`.
  Проси свежий код, если сомневаешься.
- Промты — на английском (Claude Code), общение в чате — на русском.
- Тестирование на телефоне: `npx cloudflared tunnel --url http://localhost:5500`
  (Live Server на 5500). Ссылка каждый раз новая, живёт недолго. Кэш — через `?v=N`.

---

## ЧТО ЗА ПРОЕКТ

Браузерный жестовый синтезатор + учебник по строям/ладам. Вебкамера → MediaPipe
(отслеживание рук) → щипки пальцами играют ноты/аккорды/бас/ударные/эффекты через
Web Audio. Поверх живой игры — лупер (запись намерения, овердаб, undo по слою) и
аранжировка (гармония/ритм/бас как замороженные слои). UI и комментарии — русские.
Без сборщика, без зависимостей кроме MediaPipe с CDN. **Основная цель — телефон
вертикально.** ПК-режим ЗАМОРОЖЕН (работает, не трогаем, но и не ломаем).
12 ES-модулей в `src/` (config/state/hooks/scales/audio/arrange/recorder/vision/gestures/draw/ui/main)
+ `index.html` + `style.css`. Git на ветке master.

Философия (из слов пользователя): «ясная база, ясный фундамент, потом наращивать».
Программа даёт разные музыкальные традиции/строи, всё сочетается со всем через
слои лупера. Учит тому, как устроена музыка. Не гнаться за количеством ради количества.

---

## ТЕКУЩЕЕ СОСТОЯНИЕ (что готово, сверено с кодом)

### Архитектура
- Рефакторинг из 1 файла в модули — ЗАВЕРШЁН. Граф ацикличен. Аудио отвязано от DOM
  через `hooks.js`. `backing.js` УДАЛЁН; его дрон и дарбука переехали в `audio.js`,
  его роль источника времени заменил лупер в `recorder.js`.
- `CLAUDE.md` актуален (переписан эту сессию).

### Звук/движок (`audio.js`)
- Пять сигнальных цепей в master→лимитер: соло (банки → драйв → огибающая → громкость
  → тремоло → делей/реверб), аккорды (пул `CHORD_POOL_N=24`), бас (пул `BASS_POOL_N=4`,
  моно на слой), ударные (синтез на лету, кит из `a.kit`), дрон (расстроенная пара пил,
  свип НЧ-фильтра 0.06 Гц, следует за тоникой). Общий конвольвер-реверб; Z-рукой рулит
  ТОЛЬКО send соло.
- Гейн-стейджинг починен: бас выровнен под аккорд (`lvl`), не давит через лимитер.
- Латентность: MediaPipe на CPU (~196 мс телефон, ~57 мс десктоп) — баг MediaPipe web
  (delegate GPU молча = CPU), не наш; потолок платформы. Порядок цикла: видео и точки
  из одного кадра (когерентность, hard-rule #8 в CLAUDE.md).

### Лупер (`recorder.js`, готов)
- Запись хранит НАМЕРЕНИЕ: `{t в долях, layer, fn, a:{deg,oct,vol,inst,ty,kit…}, sc, sev}`.
  Транспорт по тактам (насос по часам AC), овердаб, undo-по-слою, счёт-вступление,
  квантизация. Темп в `loop.bpm`.
- **Заморозка контекста per-event = полимодальность.** Каждое событие помнит свой
  строй/тонику/септаккорд/тембр/ТИП. Слой играет в строе записи, живая рука — в текущем.
  Replay резолвит высоту через замороженный `sc`. ЭТО НЕСУЩЕЕ ПРАВИЛО — не сломать.
- Аранжировка (`arrange.js`): PROGRESSIONS (II-V-I, I-vi-ii-V, I-IV-V, блюз), RHYTHMS
  (Бэкбит, Маqсум), BASS_MODES (none/roots/pedal), дрон как вариант гармонии. Грузятся
  как замороженные слои.

### Защёлка аккордов (готова)
- Щипок переключает аккорд; держится без руки. Owner `'latch'`. В типизированных ладах
  тождество защёлки = (ступень + ТИП), компаньон `latchTy` рядом с `latchDeg`.

### Микротоника
- Аккорды в 19/31-TET строятся ПО ИНТЕРВАЛАМ от корня (`chord`/`chord7` на ладу),
  не пауэр-аккордами. 31-TET септаккорд = 4:5:6:7.
- Макам: свойство `noChords` — аккорды отключены. Только соло + дрон. Хочешь аккорды
  под макам — запиши в другом ладу, переключись, играй соло (полимодальность).

### UI (перестройка меню — сделана)
- Два меню: `#panelScale` (оверлей звукоряда: лад/тоника/септаккорды/тембры) и
  `#panelLoop` (рабочая панель: такты/BPM/квантизация/аранжировка).
- Кнопка-индикатор «Лад·Тоника». Кнопка роли (соло/аккорды/бас/барабаны) — тик по кругу.
- Транспорт лупера — HTML-полоса под холстовой полосой тактов. В баре компактный ●.
- Эффекты соло: вертикальные столбики СЛЕВА поверх сетки.
- Режим ПК/📱 и смена рук ⇄ — иконки; ⇄ и «?» живут в меню звукоряда.

### Типы аккордов — ПАЛИТРА (СДЕЛАНО; 8 наборов в `CHORD_FAM_SETS`)
> Старая модель «секторы в ряду» (`sectHyst`, `TYPED_CH_VOL`, X=сектор) УДАЛЕНА. Ниже — актуальное.
- Экран аккорд-роли делится: СЛЕВА палитра типов (колонка=семейство, ряд=вариант, из данных
  `chordFams()`/`types.length`), СПРАВА ряды/прямоугольники КОРНЕЙ. ЛЕВАЯ рука выбирает ячейку
  ПОЛОЖЕНИЕМ (большой+указательный, `S.oct===0 && S.x<SPLIT`, липко). ПРАВАЯ играет Y=корень,
  X=ГРОМКОСТЬ, октаву — палец (нерект) ИЛИ октавный прямоугольник (19/31/Парч). `cellHyst` двухосный.
- Свойство `typedChords:'<ключ>'` → набор в `CHORD_FAM_SETS` (8 ключей): `chrom12`/`edo19`/`edo31`/
  `natfix` — целые шаги; `partch`/`bp`/`pyth`/`nat` — отношения. Пряности по ветке `chordFreqs`
  (см. порядок веток выше): grid для `natfix`+`gridChords`, чистое отношение для `partch`/`pyth`/`nat`,
  период для `bp`, целочисленный `chordSteps` для `chrom12`/`edo19`/`edo31`.
- Тип замораживается в событии (`a.ty` — ссылка на массив из таблицы), защёлка по (ступень+тип).
- Витрины: 31-TET Дом7 [0,10,18,25]=4:5:6:7 · Парч О7 чистый 4:5:6:7 · BP 3:5:7 без биений ·
  Натуральный подвижный vs фиксированный — тот же аккорд, второй волчит.

---

## ЧТО ДАЛЬШЕ (ближайшие задачи)

### ГЛАВНАЯ СЛЕДУЮЩАЯ ФИЧА — унификация ПК/телефон (ДИЗАЙН-задача)
Неоктавные строи и исторические темперации закрыты. Следующая крупная — не звук, а РАСКЛАДКА.
Проблема в том, что два режима вяжут роль руки ПО-РАЗНОМУ:
- **ПК** вяжет роль по МЕСТУ: экран делится на 3 X-колонки (эффекты · аккорды · соло), любая
  рука играет любую зону, роль от положения щипка (`FXW`/`ZB` в config).
- **Телефон** вяжет роль по РУКЕ: один инструмент на весь экран (`phoneInstr`), правая=ноты,
  левая=эффекты/палитра (`handRole`, `swapHands`).
- **ПК ЗАМОРОЖЕН и ОТСТАЁТ:** rect-модель, октавный прямоугольник, палитра типов, терменвокс,
  период — всё только в phone-ветке. ПК их не умеет (и по правилу «PC frozen» его не расширяли).

Это ДИЗАЙН-задача, не просто код. Развилки, решить ДО постройки:
- **Догнать ПК** (перенести rect/палитру/терменвокс в трёхколоночную модель) — много работы,
  вопрос как палитра/прямоугольники живут в X-колонке.
- ЛИБО **сделать раскладку адаптивной по ШИРИНЕ** вместо тумблера режима: широкий экран → колонки,
  узкий → phone-раскладка; одна модель ролей, а не две. Убирает тумблер ПК/📱 совсем.
- Что делает handedness-привязка на десктопе с мышью/одной «рукой»? Что с двумя игроками на ПК?
Сначала СПЕЦИФИКАЦИЯ (какая из моделей побеждает), потом код. Не начинать с кода.

### Мельче
- **Глиссандо-в-луп:** терменвокс сейчас пишет ближайшую ступень (живой звук непрерывен, запись
  квантована). Чтобы петля повторяла бенды — нужен непрерывный формат события (или дорожка
  контроллера высоты). Отдельная задача, ядро лупера.
- **Баг `revDisp` (реальный, открыт):** не сбрасывается при отпускании соло-щипка —
  `setRevDisp` зовётся только в ветке `'ld'` (`gestures.js`), `endPinch` реверб не
  обнуляет, столбик REV висит на последнем значении. Мелкая правка в `endPinch`.
- **Раги/ситар** — открытая задача: ЛАД со своей микротоникой vs ТЕМБР (раги требуют обоих).
  Специфицировать до старта.
- **Две косметики (НЕ чинить, в BACKLOG как пункты):** (а) у части цент-ладов вводящий `tag`
  (мезотон/натуральный — `tag:'penta'`), потому что теги для них инертны; (б) подписи корней
  аккордов через `rootName()` дают 12-TET имена нот у любого `edo:12` лада — у цент-ладов это
  приблизительный якорь, реальная высота видна в Гц.
- (ПК-режим больше не «мелочь» — вырос в ГЛАВНУЮ следующую фичу, см. выше.)

---

## КРУПНОЕ В БЭКЛОГЕ (не срочно, см. BACKLOG.md)

- localStorage настроек, Service Worker (кэш модели для офлайна).
- Запись видео+звука чтобы делиться (§3.11 — осторожно: 2 потока, может убить fps
  на телефоне; начинать со звука-only).
- Больше строёв/тембров (§3.12 — ТОЛЬКО после традиций, не ради количества).
- Микшер слоёв лупера (§3.13 — только если после гейн-стейджинга реально нужен баланс).
- Английский интерфейс / i18n (§3.2 — ТОЛЬКО когда UI застынет; сейчас правило
  «UI строки русские» в силе).

---

## КЛЮЧЕВЫЕ УРОКИ

- Всегда сверяйся с РЕАЛЬНЫМ кодом (читай файлы) перед промтом. Память врёт.
- Диагностируй до постройки (латентность: измерили → нашли GPU-делегат как корень).
- Ухо главнее математики в звуке (центы построили и откатили — не услышал разницы;
  гейн-стейджинг заработал только после правки уровня голоса, не только шин).
- Правки на СВОЙСТВАХ лада (`noChords`, `typedChords`) переживают перестройки; правки
  на тегах/индексах — ломаются. Всегда свойство.
- Начинай маленьким, играй, расширяй. Не строй списки из воображения.
- «Можем» ≠ «нужно». По секторам пользователь настоял на 6, подтвердив попадание.
- После одобрения плана — явно «делай код, не останавливайся на плане».
