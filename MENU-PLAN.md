# MENU-PLAN — перестройка интерфейса (смартфон)

Финальное ТЗ. Все решения приняты. Основано на реальном текущем коде.
Реализовывать ПО ШАГАМ, каждый — проверка в браузере, затем коммит.

---

## ГЛАВНЫЙ ПРИНЦИП

**Смартфон — ведущий режим. ПК — заморожен.**
Всё новое делаем ТОЛЬКО для смартфон-ветки. ПК-режим не трогаем — он работает.
Правило: каждая правка идёт под `if(S.phone)` / фазу смартфона и НЕ ЛОМАЕТ ПК-путь.
«Не делать для ПК» ≠ «сломать ПК». ПК-ветка остаётся как есть, проверять что цела.

Почему: ПК и смартфон — два разных способа связать руку и роль:
- ПК: роль по МЕСТУ (X-зона), любая рука в любой зоне, можно две роли сразу.
- Смартфон: роль по РУКЕ (левая/правая закреплены), одна активная роль, тик сверху.
Это не сводится одно к другому. Ведём смартфон, ПК догоним потом.

---

## ВЕРХНЯЯ ПАНЕЛЬ (смартфон)

```
┌──────────────────────────────────────────────┐
│ [💻][⇄]   [Мажор·A]   [🎸 Соло]   [⚙ Луп]   ? │
└──────────────────────────────────────────────┘
```

Слева — два ТЕХНИЧЕСКИХ тумблера-иконки (редко трогают):
- `[💻/📱]` — режим ПК/Смартфон (оставляем как есть, пока не решим судьбу ПК)
- `[⇄]` — смена рук (какая рука ведущая: ноты/эффекты)

По центру — три РАБОЧИЕ кнопки:
- `[Мароп·A]` — ЗВУКОРЯД + индикатор. Тап → оверлей-меню звукоряда.
  Текст = `${лад} · ${тоника}`, обновляется при смене. Всегда видно, где ты.
- `[🎸 Соло]` — РОЛЬ, тик по кругу (соло/аккорды/бас/барабаны). Уже есть (instrBtn).
- `[⚙ Луп]` — открывает/прячет РАБОЧУЮ панель лупера (не оверлей!).

Справа — `?` учебник. Транспорт (запись/undo/очистить/паника) — где сейчас.

---

## ДВА РАЗНЫХ ВИДА «ОТКРЫТОГО»

Ключевое различие, не перепутать:

**Меню ЗВУКОРЯДА — ОВЕРЛЕЙ.** Открыл → перекрывает игру → выбрал → закрыл.
Играть с открытым не нужно. Одна за раз.

**Панель ЛУПЕРА — РАБОЧАЯ панель.** Открыта ВО ВРЕМЯ игры: видишь такты, играешь,
наглядно. Повторный тап — прячет, освобождает место. Сейчас лупер визуально хорош —
СОХРАНИТЬ этот вид, просто сделать сворачиваемым по кнопке ⚙.

---

## РАСПРЕДЕЛЕНИЕ КОНТРОЛОВ

### Меню ЗВУКОРЯДА (оверлей) — «ноты и звук»
- Лад / строй (selScale)
- Тоника (selTonic)
- Трезвучия / Септаккорды (qTriad/qSev) ← сюда (свойство звучания аккордов)
- Соло-инструмент (selLead)
- Аккорд-инструмент (selChord)
- Бас-инструмент (selBass)
- Набор ударных (selDrumKit)

### Панель ЛУПЕРА (рабочая) — «время и слои»
- Длина в тактах (loopMinus/loopBarsV/loopPlus)
- Темп BPM (bpm slider)
- Квантизация (qOn/qOff)
- Гармония / Ритм / Бас-партия (selProg/selRhythm/selBassMode)
- + Добавить слои (addArrBtn)
- (позже: громкость лупа / микшер §3.13)

### Технические иконки (верх слева)
- Режим ПК/Смартфон (modePC/modePhone) → иконка [💻/📱]
- Смена рук (swapNotes/swapFx) → иконка [⇄]

---

## ПОРЯДОК РЕАЛИЗАЦИИ (по шагам)

### Шаг 1. Разнести #panel на две
`#panelScale` (оверлей) и `#panelLoop` (рабочая панель). Раскидать существующие
`prow` по списку выше. НИЧЕГО не переименовывать, логику НЕ менять — только
переместить строки. Каждый id и обработчик в ui.js сохраняется.
✅ Проверка: обе панели открываются, ВСЕ контролы работают как раньше, ПК-режим цел.

### Шаг 2. Кнопка-индикатор звукоряда
Объединить menuBtn+scaleBtn в один. Текст `${лад}·${тоника}`, обновлять в onchange
лада/тоники и при старте. Тап → #panelScale (оверлей).
✅ Надпись показывает и меняется, открывает правильную панель.

### Шаг 3. Кнопка лупера — сворачиваемая рабочая панель
⚙ показывает/прячет #panelLoop. НЕ оверлей — играбельно при открытой.
Сохранить текущий наглядный вид тактов.
✅ Открывается/прячется, играть можно с открытой.

### Шаг 4. Технические иконки слева
Режим и смену рук — в маленькие иконки [💻][⇄] сверху слева.
✅ Обе работают, ПК-режим переключается, смена рук действует.

### Шаг 5. Причесать под 380px
DevTools 380px: верхняя панель помещается, панели скроллятся осмысленно.
✅ На узком экране всё доступно.

---

## ОТДЕЛЬНО (НЕ в этой задаче)

Эффекты соло — столбики слева вместо нижней полосы. Это задача про `draw.js`,
НЕ про меню. Делать ОТДЕЛЬНЫМ заходом, не смешивать с перестройкой панелей.

---

## ГРАНИЦЫ

- Только РАЗМЕЩЕНИЕ контролов, НЕ логика.
- ПК-ветку НЕ трогать, проверять что цела на каждом шаге.
- Роль-тик (instrBtn) не переделывать, только соседство кнопок.
- Транспорт лупера не трогать.
- Новых функций не добавлять (громкость/микшер — потом).
- Русские подписи как есть.

---

## ОТКРЫТЫЙ ВОПРОС (не сейчас)

Судьба ПК-режима. Все новые фичи — смартфонные (индикатор, роль-тик, столбики,
луп-панель). ПК отстаёт по построению (роль по месту, не по руке). Когда-нибудь
решить: догонять ПК или сделать раскладку адаптивной по ширине экрана вместо
тумблера режима. Пока — заморожен, работает, не трогаем.
