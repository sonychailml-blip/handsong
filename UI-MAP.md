# UI-MAP — карта интерфейса Air Synth 3

Фундамент интерфейса. К этому документу возвращаемся при любой задаче про UI,
меню, роли, лупер. **Не реализация — карта.** Прежде чем строить кусок UI, свериться отсюда.

---

## Главный принцип

**Экран — почти целиком игровое поле.** Пикселей мало (телефон вертикально —
основной режим). Меню не воруют место у игры: тонкие полоски, спрятаны, или совмещены
с индикатором. Всё, что не игровое поле, ужато до предела.

---

## Три оси (из них растёт всё)

### Ось 1 — ЗВУКОРЯД (в каком музыкальном мире я нахожусь)
Глобальный, один активный за раз, меняется РЕДКО. Живёт в меню звукоряда.
Организован по традициям:
- **Ладовая** (12-TET): диатоника (мажоры/миноры), лады-моды, этнические,
  пентатоники/блюз, симметричные (целотон/октатоники/Мессиан), экзотические
  (фриг.доминантный, двойной гарм., Энигматическая, Прометей…), мировые пентатоники,
  японские (хирадзёси, кумои, ин, ивато).
- **Арабская** (24-TET): макамы (Раст, Баяти, Хиджаз, Саба, Сикях, Нахаванд, Курд,
  Аджам, Никриз, Нава Атар) — БЕЗ лестницы аккордов, подгруппа «Макамы».
- **Микротональная**: 19-TET и 31-TET (весь строй) — с палитрой типов аккордов.
- **Хроматика**: 12 нот, аккорд задаётся типом (палитра), а не ладом.
- **Мировые строи** (НЕравномерные, заданы центами): Слендро, Пелог (яван. гамелан).
  Строй по реальным центам, а не по равному edo — механизм `cents` в ядре.

Расти можно бесконечно: новый лад = строка в традицию, новая традиция = группа.
Фундамент при этом не трогается.

### Ось 2 — РОЛЬ (что рука делает с нотами)
Одна активна за раз. Меняется ЧАСТО — тик по кнопке сверху (соло → аккорды → бас →
барабаны). Соло = мелодия, Аккорды = гармония, Бас = низ, Барабаны = ритм.
(Дрон — тонический гул; живёт как слой аранжировки в лупере.)

### Ось 3 — ВРЕМЯ (живьём / в луп / слоями)
Живая игра · запись в луп · наслоение. Слой лупа ПОМНИТ свой звукоряд (`sc: CUR()`,
включая его `cents`) — отсюда полимодальность: записал роль в одном строе, переключился,
играешь другую роль в другом. Пользователь переключается между мирами, не держит два.

---

## Карта экрана (телефон вертикально)

Верхняя полоска — 3 кнопки по трём осям: **[Звукоряд·тоника]** (сама себе индикатор,
тап → меню звукоряда) · **[Роль]** (тик по кругу) · **[⚙ Луп]**. Подсказка лупа — поверх
поля (какой аккорд крутит петля, чтобы попадать в неё мелодией). Нижняя полоска —
транспорт лупера (REC · Play · Loop · такты).

Два меню, разделены по смыслу: **звукоряд = ноты и звук** (лад/тоника/тембры/тип
аккорда) · **лупер = время и слои** (BPM/такты/квантизация/аранжировка/дрон).

---

## Роль «Аккорды» — адаптивное поле

Одна роль, выглядит по-разному от звукоряда:
- **Диатоника**: ступени по вертикали, тип диктует лад.
- **Типизированные** (Хроматика, 31-TET, 19-TET — свойство `typedChords`): экран делится
  на ПАЛИТРУ типов слева (колонка=семейство, ряд=вариант, из данных) и ряды нот справа.
  Левая рука выбирает тип ПОЛОЖЕНИЕМ (большой+указательный, липко, по умолчанию мажор);
  правая играет Y=корень, палец=октава, X=громкость. Тип мёрзнет в лупе (`a.ty`).
  Дизайн-вопрос жеста РЕШЁН (палитра заменила прежние секторы в ряду).
  ПК: палитровая рука только на телефоне → в ПК тип не сменить, играет липкий выбор.
- **Макам** (`noChords`): роль отключена (арабская музыка монофонична) — только соло + дрон.

---

## Решённые вопросы

- **Жест выбора типа — палитра** (левая рука по положению; правой вернули громкость).
- **Макам без аккордов** (`noChords`); аккорды под макам — через полимодальность.
- **Хроматика/19-TET/31-TET — типизированные** через свойство `typedChords`.
- **Порядок меню независим от порядка массива**: лады добавляются В КОНЕЦ `SCALES`
  (индекс = `scaleIdx`), меню группирует по ключу `grp`.
- **Строй по центам** (Мировые строи): гамелан задан реальными центами, не равным edo.
  Октава остаётся 2:1 (растяжку/ombak не моделируем). Цент-лады — `noChords`.

---

## Будущая крупная фича — «4 ноты в прямоугольнике» (много-ступенчатые строи)

**Проблема:** в строях с большим числом ступеней (19-TET=19, 31-TET=31, Парч=43) слишком
много узких рядов, чтобы попадать по Y. 43 ряда на телефоне — ~18px, щипком не попасть.

**Решение (идея пользователя):** в этих строях СНЯТЬ с правого пальца функцию октавы и
отдать её под ДОВЫБОР ноты внутри прямоугольника. В каждом прямоугольнике — 4 ноты:
большой+указательный = нижняя, +средний = выше, +безымянный, +мизинец = верхняя.
Палец MediaPipe читает точно (в отличие от попадания в узкий ряд) — надёжнее.

Арифметика ложится ровно ((ступени+октава)/4): 19-TET → 20/4 = **5 прямоугольников**,
31-TET → 32/4 = **8**, Парч → 44/4 = **11**. В верхнем прямоугольнике мизинец = тоника
октавой выше. Прямоугольники ~в 4 раза выше — попадать легко. Парч из «нереально»
становится «11 крупных прямоугольников».

**ОТКРЫТЫЙ ВОПРОС (блокер выпуска, не полировка):** если палец занят довыбором ноты
внутри октавы, октавный регистр теряется → инструмент схлопнется до одной октавы, пока
не сделан ДРУГОЙ механизм переключения октав. Кандидаты: X = октава (вместо громкости),
или левая рука задаёт октавный регистр (как задаёт семейство в палитре). Решить в дизайне
ДО выпуска.

**Взаимодействия:** это правка ЯДРА жестовой модели (`gestures.js` + `draw.js`), гейтнутая
на эти строи, а не данные. И в 19/31-TET правая рука уже в палитровом аккорд-режиме
(Y=корень, палец=октава, X=громкость) — если палец меняет смысл, надо согласовать соло и
аккорды (они тут расходятся). Стадийно: дизайн октав → прототип на одном строе/одной
октаве → пощупать пальцами → октавы → раскатать на 19/31/43.

---

## Порядок работ

**Сделано:** макам без аккордов · два меню + индикатор строя · палитра типов (Хроматика,
31-TET, 19-TET) · аккорды 19/31-TET по интервалам · гарм./мел. мажор + группировка по
`grp` · волны экзотических/симметричных, мировых пентатоник, макамов, японских ·
механизм центов + слендро/пелог.

**Дальше:** «4 ноты в прямоугольнике» (крупная, с дизайна октав) · Парч (после неё) ·
Bohlen–Pierce / Карлос (неоктавный период — отдельная правка ядра, «что делает палец
без октав») · аккорды на цент-ладах · подсказка аккорда лупа поверх поля соло · мелкие
(баг revDisp; судьба ПК-режима).

Правило: не обустраивать комнаты, пока не согласован план дома. Карта — план дома.
Английский/локализацию — только когда UI застынет.
